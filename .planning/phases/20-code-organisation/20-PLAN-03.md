---
phase: 20-code-organisation
plan: 03
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - InsightFlow/Extensions/Logger+App.swift
  - InsightFlow/Services/UmamiAPI.swift
  - InsightFlow/Services/PlausibleAPI.swift
  - InsightFlow/Services/AnalyticsCacheService.swift
  - InsightFlow/Services/AccountManager.swift
  - InsightFlow/Services/SharedCredentials.swift
  - InsightFlow/Services/NotificationManager.swift
  - InsightFlow/Views/Detail/WebsiteDetailViewModel.swift
  - InsightFlow/Views/Reports/ReportsViewModel.swift
  - InsightFlow/Views/Dashboard/DashboardViewModel.swift
  - InsightFlow/Views/Sessions/SessionsViewModel.swift
  - InsightFlow/Views/Events/EventsViewModel.swift
  - InsightFlow/Views/Realtime/RealtimeViewModel.swift
  - InsightFlow/Views/Reports/PagesViewModel.swift
  - InsightFlow/Views/Admin/AdminViewModel.swift
  - InsightFlow/Views/Detail/CompareViewModel.swift
autonomous: true
requirements: [REFACTOR-06]

must_haves:
  truths:
    - "All 88 print() statements are replaced with os.Logger calls"
    - "No #if DEBUG print() blocks remain in the codebase"
    - "Logs are filterable in Console.app by subsystem (de.godsapp.statflow) and category (api, cache, auth, ui)"
    - "No sensitive data logged with .public privacy"
  artifacts:
    - path: "InsightFlow/Extensions/Logger+App.swift"
      provides: "Logger extension with 4 categories"
      contains: "static let api = Logger"
    - path: "InsightFlow/Services/UmamiAPI.swift"
      provides: "Logger.api usage instead of print()"
      contains: "Logger.api.debug"
    - path: "InsightFlow/Services/AnalyticsCacheService.swift"
      provides: "Logger.cache usage instead of print()"
      contains: "Logger.cache.debug"
    - path: "InsightFlow/Services/AccountManager.swift"
      provides: "Logger.auth usage instead of print()"
      contains: "Logger.auth.debug"
  key_links:
    - from: "All files with logging"
      to: "InsightFlow/Extensions/Logger+App.swift"
      via: "import os + Logger.category"
      pattern: "import os"
---

<objective>
Replace all 88 print() statements with structured os.Logger calls using 4 categories.

Purpose: REFACTOR-06 requires filterable structured logging. The current 88 `#if DEBUG print()` blocks are unstructured debug noise. os.Logger provides zero-cost debug logging in release builds, Console.app filtering, and privacy controls.

Output: Logger+App.swift extension with 4 categories, all print() calls migrated, zero #if DEBUG print() blocks remaining.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v2.7-ROADMAP.md
@.planning/milestones/v2.7-REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-code-organisation/20-RESEARCH.md
@.planning/phases/20-code-organisation/20-01-SUMMARY.md

Category mapping from research:
- **api** (7 prints): UmamiAPI.swift (5), PlausibleAPI.swift (2)
- **cache** (9 prints): AnalyticsCacheService.swift (9)
- **auth** (17 prints): AccountManager.swift (9), SharedCredentials.swift (8)
- **ui** (55 prints): WebsiteDetailViewModel (17), ReportsViewModel (7), DashboardView→DashboardViewModel (5), SessionsView→SessionsViewModel (4), EventsViewModel (3), RealtimeView→RealtimeViewModel (3), PagesView→PagesViewModel (2), AdminView→AdminViewModel (2), CompareViewModel (2), other Views (~10)

Migration pattern:
```swift
// BEFORE:
#if DEBUG
print("AnalyticsCacheService: Saved \(key)")
#endif

// AFTER:
Logger.cache.debug("Saved \(key)")
```

Note: `#if DEBUG` wrapper is removed because Logger.debug() is automatically stripped in release builds (zero overhead).

IMPORTANT: After Plan 01 extraction, the print() statements that were in View files (DashboardView, SessionsView, RealtimeView etc.) are now in the extracted ViewModel files. The research counts reference original locations — the prints moved with their ViewModel code.

Build command: `xcodebuild -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Logger extension and migrate Service files (33 prints)</name>
  <read_first>
    - InsightFlow/Services/UmamiAPI.swift (search for `#if DEBUG` blocks)
    - InsightFlow/Services/PlausibleAPI.swift (search for `#if DEBUG` blocks)
    - InsightFlow/Services/AnalyticsCacheService.swift (search for `#if DEBUG` blocks)
    - InsightFlow/Services/AccountManager.swift (search for `#if DEBUG` blocks)
    - InsightFlow/Services/SharedCredentials.swift (search for `#if DEBUG` blocks)
    - InsightFlow/Services/NotificationManager.swift (search for `#if DEBUG` blocks)
  </read_first>
  <files>
    InsightFlow/Extensions/Logger+App.swift (NEW)
    InsightFlow/Services/UmamiAPI.swift
    InsightFlow/Services/PlausibleAPI.swift
    InsightFlow/Services/AnalyticsCacheService.swift
    InsightFlow/Services/AccountManager.swift
    InsightFlow/Services/SharedCredentials.swift
    InsightFlow/Services/NotificationManager.swift
  </files>
  <action>
    1. **Create Logger+App.swift** in InsightFlow/Extensions/:
       ```swift
       import os

       extension Logger {
           private static let subsystem = Bundle.main.bundleIdentifier ?? "de.godsapp.statflow"

           /// API calls and responses (UmamiAPI, PlausibleAPI)
           static let api = Logger(subsystem: subsystem, category: "api")
           /// Cache operations (AnalyticsCacheService)
           static let cache = Logger(subsystem: subsystem, category: "cache")
           /// Authentication and account management
           static let auth = Logger(subsystem: subsystem, category: "auth")
           /// UI and ViewModel operations
           static let ui = Logger(subsystem: subsystem, category: "ui")
       }
       ```

    2. **For each Service file**, perform this transformation for every `#if DEBUG print(...) #endif` block:

       a. Add `import os` to the file's imports (once per file, after existing imports)
       b. Replace the entire 3-line block:
          ```swift
          #if DEBUG
          print("SomePrefix: message \(variable)")
          #endif
          ```
          with:
          ```swift
          Logger.{category}.debug("{cleaned message} \(variable)")
          ```
       c. Strip the prefix from the message (e.g., "AnalyticsCacheService: " becomes just the message part, since the category already identifies the source)
       d. For error descriptions, use `\(error.localizedDescription)` directly (no privacy concern for error strings)
       e. For URLs or tokens, use default privacy (auto-redacted in release): `\(url)` NOT `\(url, privacy: .public)`

    Category assignments:
    - UmamiAPI.swift (5 prints) → `Logger.api`
    - PlausibleAPI.swift (2 prints) → `Logger.api`
    - AnalyticsCacheService.swift (9 prints) → `Logger.cache`
    - AccountManager.swift (9 prints) → `Logger.auth`
    - SharedCredentials.swift (8 prints) → `Logger.auth`
    - NotificationManager.swift (prints if any) → `Logger.ui`

    3. Build to verify after completing all Service files.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `test -f InsightFlow/Extensions/Logger+App.swift` succeeds
    - `grep -c "static let api = Logger" InsightFlow/Extensions/Logger+App.swift` returns 1
    - `grep -c "#if DEBUG" InsightFlow/Services/UmamiAPI.swift InsightFlow/Services/PlausibleAPI.swift InsightFlow/Services/AnalyticsCacheService.swift InsightFlow/Services/AccountManager.swift InsightFlow/Services/SharedCredentials.swift` returns 0 for each
    - `grep -c "Logger\." InsightFlow/Services/UmamiAPI.swift` returns >= 5
    - `grep -c "Logger\." InsightFlow/Services/AnalyticsCacheService.swift` returns >= 9
    - `grep -c "Logger\." InsightFlow/Services/AccountManager.swift` returns >= 9
    - Build succeeds: `** BUILD SUCCEEDED **`
  </acceptance_criteria>
  <done>Logger+App.swift created with 4 categories. All 33 print() statements in Service files replaced with Logger calls. Zero #if DEBUG blocks in Services.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ViewModel print() statements (55 prints)</name>
  <read_first>
    - InsightFlow/Views/Detail/WebsiteDetailViewModel.swift (search for `#if DEBUG`)
    - InsightFlow/Views/Reports/ReportsViewModel.swift (search for `#if DEBUG`)
    - InsightFlow/Views/Dashboard/DashboardViewModel.swift (search for `#if DEBUG`)
    - InsightFlow/Views/Sessions/SessionsViewModel.swift (search for `#if DEBUG`)
    - InsightFlow/Views/Events/EventsViewModel.swift (search for `#if DEBUG`)
    - InsightFlow/Views/Realtime/RealtimeViewModel.swift (search for `#if DEBUG`)
    - InsightFlow/Views/Reports/PagesViewModel.swift (search for `#if DEBUG`)
    - InsightFlow/Views/Admin/AdminViewModel.swift (search for `#if DEBUG`)
    - InsightFlow/Views/Detail/CompareViewModel.swift (search for `#if DEBUG`)
  </read_first>
  <files>
    InsightFlow/Views/Detail/WebsiteDetailViewModel.swift
    InsightFlow/Views/Reports/ReportsViewModel.swift
    InsightFlow/Views/Dashboard/DashboardViewModel.swift
    InsightFlow/Views/Sessions/SessionsViewModel.swift
    InsightFlow/Views/Events/EventsViewModel.swift
    InsightFlow/Views/Realtime/RealtimeViewModel.swift
    InsightFlow/Views/Reports/PagesViewModel.swift
    InsightFlow/Views/Admin/AdminViewModel.swift
    InsightFlow/Views/Detail/CompareViewModel.swift
  </files>
  <action>
    Same migration pattern as Task 1, but all ViewModel prints use `Logger.ui`:

    For each ViewModel file:
    1. Add `import os` to imports
    2. Replace every `#if DEBUG print(...) #endif` block with `Logger.ui.debug("...")`
    3. Clean up message prefixes (the Logger category + subsystem provide context)

    Expected counts (from research, post-extraction):
    - WebsiteDetailViewModel.swift: ~17 prints → Logger.ui
    - ReportsViewModel.swift: ~7 prints → Logger.ui
    - DashboardViewModel.swift: ~5 prints → Logger.ui
    - SessionsViewModel.swift: ~4 prints → Logger.ui
    - EventsViewModel.swift: ~3 prints → Logger.ui
    - RealtimeViewModel.swift: ~3 prints → Logger.ui
    - PagesViewModel.swift: ~2 prints → Logger.ui
    - AdminViewModel.swift: ~2 prints → Logger.ui
    - CompareViewModel.swift: ~2 prints → Logger.ui
    - (Check remaining View files for any prints that stayed in Views rather than moving with VMs)

    After all migrations, do a final sweep:
    ```bash
    grep -rn "#if DEBUG" InsightFlow/ --include="*.swift" | grep -v ".claude/"
    ```
    This should return ZERO results. If any remain, migrate them too.

    Also check for any `print(` outside `#if DEBUG` (shouldn't exist per research, but verify):
    ```bash
    grep -rn "print(" InsightFlow/ --include="*.swift" | grep -v "#if DEBUG" | grep -v ".claude/" | grep -v "//.*print"
    ```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "#if DEBUG" InsightFlow/ --include="*.swift" | grep -vc ".claude/"` returns 0
    - `grep -rn "print(" InsightFlow/ --include="*.swift" | grep -v ".claude/" | grep -vc "//.*print"` returns 0
    - `grep -rc "Logger\." InsightFlow/Views/ --include="*.swift" | grep -v ".claude/" | grep -v ":0$"` shows Logger usage in all ViewModel files
    - `grep -c "import os" InsightFlow/Views/Detail/WebsiteDetailViewModel.swift` returns 1
    - Build succeeds: `** BUILD SUCCEEDED **`
    - `xcodebuild test` passes all existing tests
  </acceptance_criteria>
  <done>All 88 print() statements replaced with os.Logger calls. Zero #if DEBUG blocks remaining. Zero print() calls remaining. All logs use correct categories (api, cache, auth, ui). Project builds and tests pass.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `xcodebuild build` succeeds
2. `xcodebuild test` — all existing tests pass
3. `grep -rn "#if DEBUG" InsightFlow/ --include="*.swift" | grep -vc ".claude/"` returns 0
4. `grep -rn "print(" InsightFlow/ --include="*.swift" | grep -v ".claude/" | grep -vc "//.*print"` returns 0
5. Logger+App.swift exists with 4 categories
6. All files that had print() now have `import os` and `Logger.{category}` calls
</verification>

<success_criteria>
- Logger+App.swift exists with subsystem "de.godsapp.statflow" and categories: api, cache, auth, ui
- All 88 print() statements replaced with Logger.{category}.debug() calls
- Zero #if DEBUG blocks remain
- Zero print() calls remain in production code
- Logs filterable in Console.app by subsystem and category
- Project builds and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-code-organisation/20-03-SUMMARY.md`
</output>
