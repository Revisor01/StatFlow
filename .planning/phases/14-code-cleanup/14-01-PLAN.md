---
phase: 14-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - InsightFlow/Services/UmamiAPI.swift
  - InsightFlow/Models/Admin.swift
  - InsightFlow/Models/Share.swift
  - InsightFlow/Models/Reports.swift
autonomous: true
requirements:
  - CLEAN-01

must_haves:
  truths:
    - "UmamiAPI.swift enthaelt keine der 43 identifizierten ungenutzten Methoden mehr"
    - "Keine verwaisten Model-Typen in Admin.swift, Share.swift oder Reports.swift die nur von geloeschten Methoden genutzt wurden"
    - "Das Projekt kompiliert fehlerfrei nach allen Loeschungen"
  artifacts:
    - path: "InsightFlow/Services/UmamiAPI.swift"
      provides: "Bereinigte API-Klasse ohne toten Code"
      contains: "func getWebsites"
    - path: "InsightFlow/Models/Admin.swift"
      provides: "Bereinigte Admin-Models"
    - path: "InsightFlow/Models/Share.swift"
      provides: "Bereinigte Share-Models"
    - path: "InsightFlow/Models/Reports.swift"
      provides: "Bereinigte Report-Models"
  key_links:
    - from: "InsightFlow/Services/UmamiAPI.swift"
      to: "InsightFlow/Services/AnalyticsProvider.swift"
      via: "Protocol conformance — alle verwendeten Methoden muessen erhalten bleiben"
      pattern: "func getAnalytics"
---

<objective>
Alle 43 ungenutzten API-Methoden aus UmamiAPI.swift entfernen sowie verwaiste Model-Typen in Admin.swift, Share.swift und Reports.swift bereinigen.

Purpose: Toten Code entfernen — reduziert Wartungslast und Compile-Zeit. Die App nutzt nur ~40 der ~83 Methoden.
Output: Schlankere UmamiAPI.swift (~600 Zeilen weniger), bereinigte Model-Dateien.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-code-cleanup/14-RESEARCH.md

@InsightFlow/Services/UmamiAPI.swift
@InsightFlow/Models/Admin.swift
@InsightFlow/Models/Share.swift
@InsightFlow/Models/Reports.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ungenutzte Methoden aus UmamiAPI.swift loeschen</name>
  <files>InsightFlow/Services/UmamiAPI.swift</files>
  <read_first>
    - InsightFlow/Services/UmamiAPI.swift
    - .planning/phases/14-code-cleanup/14-RESEARCH.md (Abschnitt "Vollstaendige Liste ungenutzter Methoden" und "Welche Methoden BLEIBEN")
  </read_first>
  <action>
Die folgenden 43 Methoden aus UmamiAPI.swift loeschen (inklusive ihrer gesamten Methodenbodies):

MARK: - Me (3):
- func getMe() -> MeResponse
- func getMyTeams() -> [Team]
- func getMyWebsites(includeTeams:) -> [Website]

MARK: - Stats/Metrics (3):
- func getDateRange(websiteId:) -> DateRangeResponse
- func getEventsSeries(websiteId:dateRange:timezone:) -> [EventData]
- func getExpandedMetrics(websiteId:dateRange:type:limit:) -> [ExpandedMetricItem]

MARK: - Event Data (5):
- func getEventData(websiteId:dateRange:page:pageSize:) -> EventDataResponse
- func getEventDataById(websiteId:eventId:) -> EventDataItem
- func getEventDataFields(websiteId:dateRange:) -> [EventDataField]
- func getEventDataProperties(websiteId:dateRange:) -> [EventDataProperty]
- func getEventDataStats(websiteId:dateRange:) -> EventDataStats

MARK: - Sessions (5):
- func getSessionStats(websiteId:dateRange:) -> SessionStatsResponse
- func getSessionsWeekly(websiteId:dateRange:timezone:) -> [WeeklySessionPoint]
- func getSessionProperties(websiteId:sessionId:) -> [SessionPropertyItem]
- func getSessionDataProperties(websiteId:dateRange:) -> [SessionDataProperty]
- func getSessionDataValues(websiteId:dateRange:propertyName:) -> [SessionDataValue]

MARK: - Website Management (1):
- func resetWebsiteStats(websiteId:) -> Void

MARK: - Teams (7):
- func getUserTeams(page:pageSize:) -> [Team]
- func joinTeam(accessCode:) -> TeamMember
- func getTeam(teamId:) -> Team
- func updateTeam(teamId:name:accessCode:) -> Team
- func getTeamMember(teamId:userId:) -> TeamMember
- func updateTeamMemberRole(teamId:userId:role:) -> TeamMember
- func getTeamWebsites(teamId:page:pageSize:) -> [Website]

MARK: - Users (Admin) (4):
- func getUser(userId:) -> UmamiUser
- func updateUser(userId:username:password:role:) -> UmamiUser
- func getUserWebsites(userId:includeTeams:page:pageSize:) -> [Website]
- func getUserTeamsList(userId:page:pageSize:) -> [Team]

MARK: - Admin (1):
- func getAdminWebsites(page:pageSize:) -> [Website]

MARK: - Share (6):
- func createSharePage(entityId:shareType:name:slug:parameters:) -> SharePage
- func getSharePage(shareId:) -> SharePage
- func updateSharePage(shareId:name:slug:parameters:) -> SharePage
- func deleteSharePage(shareId:) -> Void
- func getWebsiteShares(websiteId:) -> [SharePage]
- func createWebsiteShare(websiteId:name:parameters:) -> SharePage

MARK: - Reports (4):
- func createReport(...) -> Report
- func getReport(reportId:) -> Report
- func updateReport(...) -> Report
- func deleteReport(reportId:) -> Report

MARK: - Report Types (3):
- func getPerformanceReport(websiteId:dateRange:) -> [PerformanceItem]
- func getBreakdownReport(websiteId:dateRange:property:) -> [BreakdownItem]
- func getRevenueReport(websiteId:dateRange:) -> [RevenueItem]

WICHTIG: Die folgenden Methoden NICHT loeschen (werden aktiv verwendet):
- getReports(websiteId:...), getUTMReport, getAttributionReport, getFunnelReport, getGoalReport, getJourneyReport
- getSession(websiteId:sessionId:), getSessions(...), getSessionActivity(...)
- Alle Methoden im "Welche Methoden BLEIBEN" Abschnitt der RESEARCH.md

Nach dem Loeschen: Leere MARK-Sektionen entfernen falls alle Methoden einer Sektion geloescht wurden.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && grep -c "func getMe\|func getMyTeams\|func getMyWebsites\|func getDateRange\|func getEventsSeries\|func getExpandedMetrics\|func resetWebsiteStats\|func getUserTeams\|func joinTeam\|func getTeamMember\|func updateTeamMemberRole\|func getTeamWebsites\|func getAdminWebsites\|func createSharePage\|func getSharePage\|func updateSharePage\|func deleteSharePage\|func getWebsiteShares\|func createWebsiteShare\|func createReport\|func getReport\|func updateReport\|func deleteReport\|func getPerformanceReport\|func getBreakdownReport\|func getRevenueReport" InsightFlow/Services/UmamiAPI.swift | grep -q "^0$" && echo "PASS: all dead methods removed" || echo "FAIL: dead methods still present"</automated>
  </verify>
  <acceptance_criteria>
    - grep "func getMe()" UmamiAPI.swift returns 0 matches
    - grep "func resetWebsiteStats" UmamiAPI.swift returns 0 matches
    - grep "func createSharePage" UmamiAPI.swift returns 0 matches
    - grep "func getPerformanceReport" UmamiAPI.swift returns 0 matches
    - grep "func getWebsites()" UmamiAPI.swift returns 1+ match (kept method)
    - grep "func getReports(" UmamiAPI.swift returns 1+ match (kept method)
    - grep "func getSession(" UmamiAPI.swift returns 1+ match (kept method)
  </acceptance_criteria>
  <done>Alle 43 ungenutzten Methoden aus UmamiAPI.swift entfernt. Keine verwendeten Methoden versehentlich geloescht.</done>
</task>

<task type="auto">
  <name>Task 2: Verwaiste Model-Typen bereinigen und Build verifizieren</name>
  <files>InsightFlow/Models/Admin.swift, InsightFlow/Models/Share.swift, InsightFlow/Models/Reports.swift</files>
  <read_first>
    - InsightFlow/Models/Admin.swift
    - InsightFlow/Models/Share.swift
    - InsightFlow/Models/Reports.swift
    - InsightFlow/Services/UmamiAPI.swift (nach Task 1 Aenderungen)
  </read_first>
  <action>
Nach dem Loeschen der API-Methoden in Task 1 pruefen welche Model-Typen verwaist sind.

Schritt 1: Fuer jeden Typ in Admin.swift, Share.swift und Reports.swift per grep pruefen ob er noch irgendwo ausserhalb seiner eigenen Datei verwendet wird:
```bash
grep -r "TypeName" InsightFlow/ --include="*.swift" -l
```

Schritt 2: Typen die NUR in ihrer eigenen Model-Datei vorkommen (oder nur in der jetzt bereinigten UmamiAPI.swift als Return-Typ standen) loeschen.

Erwartete Kandidaten fuer Loeschung (nach Research):
- Admin.swift: TeamWebsitesResponse, UserWebsitesResponse, UserTeamsResponse
- Share.swift: SharePage und alle Share-Typen (sofern nicht in Views referenziert)
- Reports.swift: PerformanceItem, BreakdownItem, RevenueItem (sofern nicht in Views referenziert)

WICHTIG: JourneyPath in Admin.swift NICHT loeschen — wird von SessionsView verwendet.
WICHTIG: Report, FunnelReport, GoalReport, UTMReport, AttributionReport in Reports.swift NICHT loeschen — werden von ReportsViewModel verwendet.

Schritt 3: xcodebuild build ausfuehren um sicherzustellen dass das Projekt kompiliert:
```bash
xcodebuild build -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -20
```

Falls Build-Fehler auftreten: Fehlende Typen wiederherstellen — Research-Empfehlung "erst loeschen, dann Build pruefen" befolgen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild build -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED" && echo "PASS: build succeeds" || echo "FAIL: build broken"</automated>
  </verify>
  <acceptance_criteria>
    - xcodebuild build exits with BUILD SUCCEEDED
    - grep "TeamWebsitesResponse" InsightFlow/Models/Admin.swift returns 0 matches
    - grep "UserWebsitesResponse" InsightFlow/Models/Admin.swift returns 0 matches
    - grep "JourneyPath" InsightFlow/Models/Admin.swift returns 1+ match (kept)
  </acceptance_criteria>
  <done>Alle verwaisten Model-Typen entfernt. Projekt kompiliert fehlerfrei. UmamiAPI.swift hat ~600 Zeilen weniger als vorher.</done>
</task>

</tasks>

<verification>
1. `grep -c "func getMe\|func createSharePage\|func getPerformanceReport" InsightFlow/Services/UmamiAPI.swift` ergibt 0
2. `xcodebuild build` ist erfolgreich (BUILD SUCCEEDED)
3. Alle verwendeten Methoden (getWebsites, getStats, getMetrics, getSessions, getReports etc.) sind weiterhin vorhanden
</verification>

<success_criteria>
- UmamiAPI.swift enthaelt keine der 43 identifizierten ungenutzten Methoden
- Verwaiste Model-Typen in Admin.swift, Share.swift, Reports.swift bereinigt
- Projekt kompiliert fehlerfrei
- Keine Regression — alle verwendeten Methoden intakt
</success_criteria>

<output>
After completion, create `.planning/phases/14-code-cleanup/14-01-SUMMARY.md`
</output>
