---
phase: 14-code-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - InsightFlow/Views/Detail/WebsiteDetailViewModel.swift
  - InsightFlow/Views/Detail/WebsiteDetailView.swift
  - InsightFlow/Views/Events/EventsViewModel.swift
  - InsightFlow/Views/Events/EventsView.swift
  - InsightFlow/Views/Sessions/SessionsView.swift
  - InsightFlow/Views/Reports/ReportsViewModel.swift
  - InsightFlow/Views/Reports/ReportsHubView.swift
  - InsightFlow/Resources/en.lproj/Localizable.strings
  - InsightFlow/Resources/de.lproj/Localizable.strings
autonomous: true
requirements:
  - CLEAN-02

must_haves:
  truths:
    - "WebsiteDetailView zeigt Offline-Banner wenn Netzwerk nicht erreichbar"
    - "EventsView zeigt Offline-Banner wenn Netzwerk nicht erreichbar"
    - "SessionsView zeigt Offline-Banner wenn Netzwerk nicht erreichbar"
    - "ReportsHubView zeigt Offline-Banner wenn Netzwerk nicht erreichbar"
    - "Offline-Banner verschwindet nach erfolgreichem Reload"
    - "Banner-Text unterscheidet sich von DashboardView (kein Cache-Bezug)"
  artifacts:
    - path: "InsightFlow/Views/Detail/WebsiteDetailViewModel.swift"
      provides: "isOffline Published property"
      contains: "@Published var isOffline"
    - path: "InsightFlow/Views/Detail/WebsiteDetailView.swift"
      provides: "Offline-Banner UI"
      contains: "offlineBanner"
    - path: "InsightFlow/Views/Events/EventsViewModel.swift"
      provides: "isOffline Published property"
      contains: "@Published var isOffline"
    - path: "InsightFlow/Views/Events/EventsView.swift"
      provides: "Offline-Banner UI"
      contains: "offlineBanner"
    - path: "InsightFlow/Views/Sessions/SessionsView.swift"
      provides: "isOffline property + Offline-Banner UI"
      contains: "isOffline"
    - path: "InsightFlow/Resources/en.lproj/Localizable.strings"
      provides: "detail.offline localization key"
      contains: "detail.offline"
    - path: "InsightFlow/Resources/de.lproj/Localizable.strings"
      provides: "detail.offline localization key"
      contains: "detail.offline"
  key_links:
    - from: "InsightFlow/Views/Detail/WebsiteDetailViewModel.swift"
      to: "InsightFlow/Views/Detail/WebsiteDetailView.swift"
      via: "@Published var isOffline wird im View abgefragt"
      pattern: "viewModel\\.isOffline"
    - from: "InsightFlow/Views/Events/EventsViewModel.swift"
      to: "InsightFlow/Views/Events/EventsView.swift"
      via: "@Published var isOffline wird im View abgefragt"
      pattern: "viewModel\\.isOffline"
---

<objective>
Offline-Indikator in WebsiteDetailView, EventsView, SessionsView und ReportsHubView ergaenzen — analog zum bestehenden Pattern in DashboardView.

Purpose: Nutzer sehen bei Netzwerkproblemen einen klaren "Offline"-Hinweis statt eines leeren Screens ohne Kontext.
Output: 4 Views mit Offline-Banner, passende Lokalisierung ohne Cache-Bezug.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-code-cleanup/14-RESEARCH.md

@InsightFlow/Views/Dashboard/DashboardView.swift
@InsightFlow/Views/Detail/WebsiteDetailView.swift
@InsightFlow/Views/Detail/WebsiteDetailViewModel.swift
@InsightFlow/Views/Events/EventsView.swift
@InsightFlow/Views/Events/EventsViewModel.swift
@InsightFlow/Views/Sessions/SessionsView.swift
@InsightFlow/Views/Reports/ReportsHubView.swift
@InsightFlow/Views/Reports/ReportsViewModel.swift
@InsightFlow/Resources/en.lproj/Localizable.strings
@InsightFlow/Resources/de.lproj/Localizable.strings

<interfaces>
<!-- Bestehendes Offline-Pattern aus DashboardView als Vorlage -->

From DashboardView.swift (ViewModel, ~Zeile 882):
```swift
@Published var isOffline = false

func loadWebsites() async {
    isOffline = false  // Reset bei jedem Load
    do {
        // ... API calls ...
    } catch {
        let isNetworkError = (error as? URLError)?.code == .notConnectedToInternet ||
                             (error as? URLError)?.code == .networkConnectionLost ||
                             (error as? URLError)?.code == .timedOut ||
                             (error as? URLError)?.code == .cannotFindHost ||
                             (error as? URLError)?.code == .cannotConnectToHost
        if isNetworkError {
            isOffline = true
        } else {
            self.error = error.localizedDescription
        }
    }
}
```

From DashboardView.swift (View, ~Zeile 306-319):
```swift
private var offlineBanner: some View {
    HStack(spacing: 8) {
        Image(systemName: "wifi.slash")
            .font(.subheadline)
        Text("dashboard.offline")
            .font(.subheadline)
        Spacer()
    }
    .padding(.horizontal, 12)
    .padding(.vertical, 10)
    .background(Color.orange.opacity(0.15))
    .foregroundStyle(.orange)
    .clipShape(RoundedRectangle(cornerRadius: 10))
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Offline-Erkennung in ViewModels + Lokalisierung</name>
  <files>
    InsightFlow/Views/Detail/WebsiteDetailViewModel.swift,
    InsightFlow/Views/Events/EventsViewModel.swift,
    InsightFlow/Views/Reports/ReportsViewModel.swift,
    InsightFlow/Resources/en.lproj/Localizable.strings,
    InsightFlow/Resources/de.lproj/Localizable.strings
  </files>
  <read_first>
    - InsightFlow/Views/Dashboard/DashboardView.swift (Zeilen 880-930 fuer isOffline-Pattern)
    - InsightFlow/Views/Detail/WebsiteDetailViewModel.swift
    - InsightFlow/Views/Events/EventsViewModel.swift
    - InsightFlow/Views/Reports/ReportsViewModel.swift
    - InsightFlow/Resources/en.lproj/Localizable.strings
    - InsightFlow/Resources/de.lproj/Localizable.strings
  </read_first>
  <action>
1. In WebsiteDetailViewModel.swift:
   - Property hinzufuegen: `@Published var isOffline = false`
   - In der Haupt-Load-Methode (loadData oder aehnlich) am Anfang: `isOffline = false`
   - Im catch-Block die gleiche URLError-Pruefung wie DashboardViewModel:
     ```swift
     let isNetworkError = (error as? URLError)?.code == .notConnectedToInternet ||
                          (error as? URLError)?.code == .networkConnectionLost ||
                          (error as? URLError)?.code == .timedOut ||
                          (error as? URLError)?.code == .cannotFindHost ||
                          (error as? URLError)?.code == .cannotConnectToHost
     if isNetworkError {
         isOffline = true
     }
     ```
   - Bestehende Error-Behandlung beibehalten (else-Branch fuer nicht-Netzwerk-Fehler)

2. In EventsViewModel.swift:
   - Gleiche Aenderungen wie oben: `@Published var isOffline = false`, Reset am Load-Start, URLError-Check im Catch

3. In ReportsViewModel.swift:
   - Gleiche Aenderungen wie oben: `@Published var isOffline = false`, Reset am Load-Start, URLError-Check im Catch

4. Lokalisierung — in en.lproj/Localizable.strings hinzufuegen:
   ```
   "detail.offline" = "Offline – data unavailable";
   ```

5. Lokalisierung — in de.lproj/Localizable.strings hinzufuegen:
   ```
   "detail.offline" = "Offline – Daten nicht verfuegbar";
   ```

WICHTIG: Text OHNE Cache-Bezug — diese Views haben keinen Cache-Fallback (anders als DashboardView).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && grep -l "isOffline" InsightFlow/Views/Detail/WebsiteDetailViewModel.swift InsightFlow/Views/Events/EventsViewModel.swift InsightFlow/Views/Reports/ReportsViewModel.swift | wc -l | grep -q "3" && grep -q "detail.offline" InsightFlow/Resources/en.lproj/Localizable.strings && grep -q "detail.offline" InsightFlow/Resources/de.lproj/Localizable.strings && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "@Published var isOffline" WebsiteDetailViewModel.swift returns 1 match
    - grep "@Published var isOffline" EventsViewModel.swift returns 1 match
    - grep "@Published var isOffline" ReportsViewModel.swift returns 1 match
    - grep "isOffline = false" WebsiteDetailViewModel.swift returns 1+ match (reset)
    - grep "detail.offline" en.lproj/Localizable.strings returns 1 match
    - grep "detail.offline" de.lproj/Localizable.strings returns 1 match
    - Lokalisierter Text enthaelt NICHT "cached" oder "gecachte"
  </acceptance_criteria>
  <done>Alle 3 ViewModels haben isOffline-Property mit URLError-Erkennung. Lokalisierungsschluessel ohne Cache-Bezug angelegt.</done>
</task>

<task type="auto">
  <name>Task 2: Offline-Banner in Views einbinden</name>
  <files>
    InsightFlow/Views/Detail/WebsiteDetailView.swift,
    InsightFlow/Views/Events/EventsView.swift,
    InsightFlow/Views/Sessions/SessionsView.swift,
    InsightFlow/Views/Reports/ReportsHubView.swift
  </files>
  <read_first>
    - InsightFlow/Views/Dashboard/DashboardView.swift (Zeilen 28-30 fuer Banner-Einbindung, Zeilen 306-319 fuer offlineBanner computed property)
    - InsightFlow/Views/Detail/WebsiteDetailView.swift
    - InsightFlow/Views/Events/EventsView.swift
    - InsightFlow/Views/Sessions/SessionsView.swift
    - InsightFlow/Views/Reports/ReportsHubView.swift
  </read_first>
  <action>
In jeder der 4 Views das gleiche Pattern anwenden:

1. Computed Property `offlineBanner` hinzufuegen (identisch zur DashboardView, aber mit Key "detail.offline"):
   ```swift
   private var offlineBanner: some View {
       HStack(spacing: 8) {
           Image(systemName: "wifi.slash")
               .font(.subheadline)
           Text("detail.offline")
               .font(.subheadline)
           Spacer()
       }
       .padding(.horizontal, 12)
       .padding(.vertical, 10)
       .background(Color.orange.opacity(0.15))
       .foregroundStyle(.orange)
       .clipShape(RoundedRectangle(cornerRadius: 10))
   }
   ```

2. Banner am Anfang des ScrollView-Contents einbinden:
   ```swift
   if viewModel.isOffline {
       offlineBanner
           .padding(.horizontal)
   }
   ```

Spezifisch pro View:

**WebsiteDetailView.swift**: Banner im ScrollView VStack oben einbinden. ViewModel-Referenz ist `viewModel`.

**EventsView.swift**: Banner im Haupt-Content-Bereich oben einbinden. ViewModel-Referenz pruefen (evtl. `viewModel` oder `eventsViewModel`).

**SessionsView.swift**: Diese View hat moeglicherweise KEIN separates ViewModel. Falls SessionsView die Daten inline laedt:
- `@State private var isOffline = false` als Property hinzufuegen
- URLError-Check direkt in der View-eigenen Load-Methode
- Banner-Referenz dann `isOffline` statt `viewModel.isOffline`

**ReportsHubView.swift**: Banner oben im Content einbinden. ViewModel-Referenz pruefen (evtl. `viewModel` oder `reportsViewModel`).

3. Build pruefen:
   ```bash
   xcodebuild build -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -20
   ```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && grep -l "offlineBanner" InsightFlow/Views/Detail/WebsiteDetailView.swift InsightFlow/Views/Events/EventsView.swift InsightFlow/Views/Sessions/SessionsView.swift InsightFlow/Views/Reports/ReportsHubView.swift | wc -l | grep -q "4" && echo "PASS: all 4 views have offline banner" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "offlineBanner" WebsiteDetailView.swift returns 2+ matches (declaration + usage)
    - grep "offlineBanner" EventsView.swift returns 2+ matches
    - grep "offlineBanner" SessionsView.swift returns 2+ matches
    - grep "offlineBanner" ReportsHubView.swift returns 2+ matches
    - grep "wifi.slash" in all 4 View files returns matches
    - grep "detail.offline" in all 4 View files returns matches
    - xcodebuild build ergibt BUILD SUCCEEDED
  </acceptance_criteria>
  <done>Alle 4 Views zeigen bei Netzwerkproblemen einen orangenen Offline-Banner mit wifi.slash-Icon und lokalisiertem Text. Projekt kompiliert fehlerfrei.</done>
</task>

</tasks>

<verification>
1. `grep -l "offlineBanner" InsightFlow/Views/Detail/WebsiteDetailView.swift InsightFlow/Views/Events/EventsView.swift InsightFlow/Views/Sessions/SessionsView.swift InsightFlow/Views/Reports/ReportsHubView.swift` listet alle 4 Dateien
2. `grep -l "@Published var isOffline" InsightFlow/Views/Detail/WebsiteDetailViewModel.swift InsightFlow/Views/Events/EventsViewModel.swift InsightFlow/Views/Reports/ReportsViewModel.swift` listet alle 3 Dateien
3. `xcodebuild build` ergibt BUILD SUCCEEDED
4. Lokalisierung: "detail.offline" in beiden .strings-Dateien vorhanden, ohne Cache-Bezug
</verification>

<success_criteria>
- 4 Views (WebsiteDetail, Events, Sessions, ReportsHub) zeigen Offline-Banner bei Netzwerkfehlern
- Banner verwendet konsistentes Design (orange, wifi.slash-Icon, RoundedRectangle)
- Text unterscheidet sich von DashboardView (kein "gecachte Daten" Bezug)
- isOffline wird bei erfolgreichem Reload zurueckgesetzt
- Projekt kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/14-code-cleanup/14-02-SUMMARY.md`
</output>
