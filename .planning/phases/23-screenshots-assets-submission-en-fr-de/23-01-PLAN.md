---
phase: 23-screenshots-assets-submission-en-fr-de
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app-store/validate.sh
  - app-store/screenshots/source/de/.gitkeep
  - app-store/screenshots/source/en/.gitkeep
  - app-store/screenshots/source/fr/.gitkeep
  - app-store/screenshots/export/de/.gitkeep
  - app-store/screenshots/export/en/.gitkeep
  - app-store/screenshots/export/fr/.gitkeep
  - app-store/submission/.gitkeep
  - .gitignore
autonomous: true
requirements:
  - SHOT-01
  - SHOT-05
must_haves:
  truths:
    - "app-store/validate.sh existiert und ist ausführbar"
    - "app-store/validate.sh quick gibt grünen Output für Verzeichnisstruktur"
    - "Alle Verzeichnisse source/{de,en,fr}/ und export/{de,en,fr}/ existieren"
    - ".gitignore enthält Einträge für generator/node_modules, .next, out, submission/node_modules, source/"
  artifacts:
    - path: "app-store/validate.sh"
      provides: "Validation runner (quick + full mode)"
    - path: "app-store/screenshots/export/de/.gitkeep"
      provides: "Export-Zielverzeichnis DE"
    - path: "app-store/screenshots/export/en/.gitkeep"
      provides: "Export-Zielverzeichnis EN"
    - path: "app-store/screenshots/export/fr/.gitkeep"
      provides: "Export-Zielverzeichnis FR"
  key_links:
    - from: "app-store/validate.sh"
      to: "app-store/screenshots/export/{de,en,fr}/"
      via: "assert_file_exists + assert_image_dim"
      pattern: "assert_image_dim"
---

<objective>
Infrastruktur für Phase 23 aufbauen: Verzeichnisstruktur anlegen, .gitignore ergänzen, validate.sh-Helper-Script erstellen. Alle nachfolgenden Pläne setzen diese Infrastruktur voraus.

Purpose: Wave-0-Abhängigkeit — ohne validate.sh und korrekte Ordnerstruktur können nachfolgende Tasks keine automatisierten Verifizierungen durchführen.
Output: app-store/validate.sh (ausführbar), 6 Zielverzeichnisse, korrekte .gitignore-Regeln.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-screenshots-assets-submission-en-fr-de/23-CONTEXT.md
@.planning/phases/23-screenshots-assets-submission-en-fr-de/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verzeichnisstruktur + .gitignore</name>
  <files>
    app-store/screenshots/source/de/.gitkeep
    app-store/screenshots/source/en/.gitkeep
    app-store/screenshots/source/fr/.gitkeep
    app-store/screenshots/export/de/.gitkeep
    app-store/screenshots/export/en/.gitkeep
    app-store/screenshots/export/fr/.gitkeep
    app-store/submission/.gitkeep
    .gitignore
  </files>
  <read_first>
    - .planning/phases/23-screenshots-assets-submission-en-fr-de/23-CONTEXT.md (D-20, D-21, D-22)
    - .gitignore (existierende Einträge nicht duplizieren)
  </read_first>
  <action>
Lege folgende Verzeichnisse mit .gitkeep-Dateien an (damit git sie trackt):
  - app-store/screenshots/source/de/.gitkeep
  - app-store/screenshots/source/en/.gitkeep
  - app-store/screenshots/source/fr/.gitkeep
  - app-store/screenshots/export/de/.gitkeep
  - app-store/screenshots/export/en/.gitkeep
  - app-store/screenshots/export/fr/.gitkeep
  - app-store/submission/.gitkeep

Ergänze .gitignore am Ende mit folgendem Block (per D-21 + D-22 — Source-PNGs nicht committen):
```
# Phase 23 — Screenshot Generator + Submission
app-store/screenshots/generator/node_modules/
app-store/screenshots/generator/.next/
app-store/screenshots/generator/out/
app-store/submission/node_modules/
# Source-Shots werden NICHT committet (D-22 — können jederzeit aus Simulator regeneriert werden)
app-store/screenshots/source/
```

WICHTIG: app-store/secrets.md ist bereits in .gitignore, nicht doppeln.
  </action>
  <verify>
    <automated>
test -d /Users/simonluthe/Documents/umami/app-store/screenshots/source/de && \
test -d /Users/simonluthe/Documents/umami/app-store/screenshots/source/en && \
test -d /Users/simonluthe/Documents/umami/app-store/screenshots/source/fr && \
test -d /Users/simonluthe/Documents/umami/app-store/screenshots/export/de && \
test -d /Users/simonluthe/Documents/umami/app-store/screenshots/export/en && \
test -d /Users/simonluthe/Documents/umami/app-store/screenshots/export/fr && \
test -d /Users/simonluthe/Documents/umami/app-store/submission && \
grep -q "generator/node_modules" /Users/simonluthe/Documents/umami/.gitignore && \
grep -q "screenshots/source/" /Users/simonluthe/Documents/umami/.gitignore && \
echo "OK: Struktur + .gitignore korrekt"
    </automated>
  </verify>
  <done>Alle 7 Verzeichnisse existieren; .gitignore enthält alle 5 Phase-23-Einträge.</done>
</task>

<task type="auto">
  <name>Task 2: validate.sh — Validation Helper Script</name>
  <files>
    app-store/validate.sh
  </files>
  <read_first>
    - .planning/phases/23-screenshots-assets-submission-en-fr-de/23-VALIDATION.md (Wave-0-Requirements)
    - .planning/phases/23-screenshots-assets-submission-en-fr-de/23-CONTEXT.md (D-20 — Zieldateinamen)
  </read_first>
  <action>
Erstelle app-store/validate.sh als ausführbares Shell-Script (chmod +x). Das Script hat zwei Modi: `quick` (Struktur + Zähler) und `full` (zusätzlich Dimensionencheck).

Exakter Inhalt:

```bash
#!/usr/bin/env bash
set -euo pipefail

MODE="${1:-quick}"
EXPORT_BASE="app-store/screenshots/export"
PASS=0
FAIL=0
ERRORS=()

# --- Assertion helpers ---

assert_file_exists() {
  local path="$1"
  if [ -f "$path" ]; then
    echo "  ✓ $path"
    ((PASS++))
  else
    echo "  ✗ MISSING: $path"
    ERRORS+=("MISSING: $path")
    ((FAIL++))
  fi
}

assert_dir_exists() {
  local path="$1"
  if [ -d "$path" ]; then
    echo "  ✓ dir: $path"
    ((PASS++))
  else
    echo "  ✗ MISSING dir: $path"
    ERRORS+=("MISSING dir: $path")
    ((FAIL++))
  fi
}

assert_image_dim() {
  local path="$1"
  local expected_w="$2"
  local expected_h="$3"
  if [ ! -f "$path" ]; then
    echo "  ✗ FILE NOT FOUND: $path"
    ERRORS+=("FILE NOT FOUND: $path")
    ((FAIL++))
    return
  fi
  local w h
  w=$(sips -g pixelWidth "$path" 2>/dev/null | awk '/pixelWidth/{print $2}')
  h=$(sips -g pixelHeight "$path" 2>/dev/null | awk '/pixelHeight/{print $2}')
  if [ "$w" = "$expected_w" ] && [ "$h" = "$expected_h" ]; then
    echo "  ✓ ${path}: ${w}×${h}"
    ((PASS++))
  else
    echo "  ✗ WRONG DIM: ${path} got ${w}×${h}, expected ${expected_w}×${expected_h}"
    ERRORS+=("WRONG DIM: $path got ${w}x${h}, expected ${expected_w}x${expected_h}")
    ((FAIL++))
  fi
}

assert_json_field() {
  local file="$1"
  local jq_path="$2"
  local expected="$3"
  if [ ! -f "$file" ]; then
    echo "  ✗ FILE NOT FOUND: $file"
    ERRORS+=("FILE NOT FOUND: $file")
    ((FAIL++))
    return
  fi
  local actual
  actual=$(jq -r "$jq_path" "$file" 2>/dev/null || echo "JQ_ERROR")
  if [ "$actual" = "$expected" ]; then
    echo "  ✓ ${file}[${jq_path}] = ${actual}"
    ((PASS++))
  else
    echo "  ✗ WRONG VALUE: ${file}[${jq_path}] = '${actual}', expected '${expected}'"
    ERRORS+=("WRONG VALUE: $file[$jq_path] = '$actual', expected '$expected'")
    ((FAIL++))
  fi
}

# --- Quick mode: structure checks ---

echo ""
echo "=== validate.sh — Phase 23 (mode: $MODE) ==="
echo ""
echo "--- Directory structure ---"
assert_dir_exists "app-store/screenshots/source/de"
assert_dir_exists "app-store/screenshots/source/en"
assert_dir_exists "app-store/screenshots/source/fr"
assert_dir_exists "app-store/screenshots/export/de"
assert_dir_exists "app-store/screenshots/export/en"
assert_dir_exists "app-store/screenshots/export/fr"
assert_dir_exists "app-store/submission"

echo ""
echo "--- Generator ---"
assert_file_exists "app-store/screenshots/generator/src/app/page.tsx"
assert_file_exists "app-store/screenshots/generator/src/app/layout.tsx"
assert_file_exists "app-store/screenshots/generator/public/mockup.png"

echo ""
echo "--- Export PNG count (expected 24: 8 slides × 3 locales) ---"
DE_COUNT=$(ls "${EXPORT_BASE}/de/"*.png 2>/dev/null | wc -l | tr -d ' ')
EN_COUNT=$(ls "${EXPORT_BASE}/en/"*.png 2>/dev/null | wc -l | tr -d ' ')
FR_COUNT=$(ls "${EXPORT_BASE}/fr/"*.png 2>/dev/null | wc -l | tr -d ' ')
echo "  DE: ${DE_COUNT}/8  EN: ${EN_COUNT}/8  FR: ${FR_COUNT}/8"
TOTAL=$((DE_COUNT + EN_COUNT + FR_COUNT))
if [ "$TOTAL" -eq 24 ]; then
  echo "  ✓ 24 PNGs gesamt vorhanden"
  ((PASS++))
else
  echo "  ⚠ Erst ${TOTAL}/24 PNGs vorhanden (OK bis Export abgeschlossen)"
fi

echo ""
echo "--- Submission script ---"
assert_file_exists "app-store/submission/submit.ts"
assert_file_exists "app-store/submission/lib/jwt.ts"
assert_file_exists "app-store/submission/lib/asc-api.ts"
assert_file_exists "app-store/submission/lib/config.ts"

if [ "$MODE" = "full" ]; then
  echo ""
  echo "--- Full mode: PNG dimension checks (1320×2868) ---"
  SLIDES=(
    "01-dashboard" "02-details" "03-realtime" "04-vergleich"
    "05-events" "06-widget" "07-account-switcher" "08-start"
  )
  for locale in de en fr; do
    for slide in "${SLIDES[@]}"; do
      PNG="${EXPORT_BASE}/${locale}/${slide}-${locale}.png"
      if [ -f "$PNG" ]; then
        assert_image_dim "$PNG" 1320 2868
      fi
    done
  done

  echo ""
  echo "--- TypeScript type checks ---"
  if [ -f "app-store/screenshots/generator/tsconfig.json" ]; then
    echo "  Running: npx tsc --noEmit (generator)"
    cd app-store/screenshots/generator && npx tsc --noEmit 2>&1 && echo "  ✓ generator: no TS errors" || { echo "  ✗ generator: TS errors"; ERRORS+=("generator: TS errors"); ((FAIL++)); }; cd ../../..
  fi
  if [ -f "app-store/submission/tsconfig.json" ]; then
    echo "  Running: npx tsc --noEmit (submission)"
    cd app-store/submission && npx tsc --noEmit 2>&1 && echo "  ✓ submission: no TS errors" || { echo "  ✗ submission: TS errors"; ERRORS+=("submission: TS errors"); ((FAIL++)); }; cd ../..
  fi
fi

echo ""
echo "=== Result: ${PASS} passed, ${FAIL} failed ==="
if [ "${#ERRORS[@]}" -gt 0 ]; then
  echo ""
  echo "Failures:"
  for e in "${ERRORS[@]}"; do echo "  - $e"; done
  exit 1
fi
echo "All checks passed."
```

Setze Executable-Bit: chmod +x app-store/validate.sh
  </action>
  <verify>
    <automated>
chmod +x /Users/simonluthe/Documents/umami/app-store/validate.sh && \
cd /Users/simonluthe/Documents/umami && \
bash app-store/validate.sh quick 2>&1 | grep -E "(PASS|passed|✓|assert)" | head -5 && \
echo "validate.sh ausführbar und startet korrekt"
    </automated>
  </verify>
  <done>validate.sh ist ausführbar; `bash app-store/validate.sh quick` läuft durch (Failures wegen noch fehlender Generator-Dateien sind erwartet — nur Crash/Syntax-Fehler sind problematisch).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo → .gitignore | Source-Shots und Secrets dürfen nicht in VCS landen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information Disclosure | .gitignore | mitigate | Source-PNGs und submission/node_modules explizit ausschließen per D-21/D-22 |
| T-23-02 | Information Disclosure | app-store/secrets.md | accept | Bereits vor Phase 23 in .gitignore; kein erneutes Committen möglich |
</threat_model>

<verification>
1. `bash app-store/validate.sh quick` — startet ohne Crash, gibt Verzeichnis-Checks aus
2. `ls app-store/screenshots/export/{de,en,fr}/` — alle drei Ordner vorhanden
3. `grep "screenshots/source/" .gitignore` — Eintrag vorhanden
</verification>

<success_criteria>
- validate.sh ist ausführbar und gibt strukturierten Output
- 7 Verzeichnisse existieren (source/de+en+fr, export/de+en+fr, submission)
- .gitignore hat alle 5 neuen Einträge aus D-21 + D-22
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/23-screenshots-assets-submission-en-fr-de/23-01-SUMMARY.md` erstellen.
</output>
