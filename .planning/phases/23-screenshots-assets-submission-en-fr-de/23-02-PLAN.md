---
phase: 23-screenshots-assets-submission-en-fr-de
plan: 02
type: execute
wave: 1
depends_on:
  - "23-01"
files_modified:
  - app-store/screenshots/generator/src/app/page.tsx
  - app-store/screenshots/generator/src/app/layout.tsx
  - app-store/screenshots/generator/public/mockup.png
  - app-store/screenshots/generator/package.json
  - app-store/screenshots/generator/tsconfig.json
  - app-store/screenshots/generator/next.config.ts
  - app-store/screenshots/generator/tailwind.config.ts
  - app-store/screenshots/generator/postcss.config.mjs
autonomous: true
requirements:
  - SHOT-01
  - SHOT-02
must_haves:
  truths:
    - "Generator-Projekt existiert unter app-store/screenshots/generator/"
    - "bun dev startet ohne Fehler auf Port 3333"
    - "page.tsx enthält THEME mit bgFrom=#FAFAF7, textPrimary=#000000"
    - "LAYOUT_ROTATION = ['headline-top','mock-left','mock-right','headline-top','mock-left','mock-right','headline-top','mock-left']"
    - "SCREEN_IDS enthält alle 8 Slide-IDs (D-04)"
    - "COPY enthält Platzhalter-Einträge für alle 8 Slides × DE/EN/FR (wird in Plan 03 befüllt)"
    - "BG_VARIANTS-Blobs haben opacity 0.12-0.20 (hell-tauglich)"
  artifacts:
    - path: "app-store/screenshots/generator/src/app/page.tsx"
      provides: "Screenshot-Generator mit StatFlow THEME + 3-Locale COPY-Map"
    - path: "app-store/screenshots/generator/src/app/layout.tsx"
      provides: "Next.js Layout Root"
    - path: "app-store/screenshots/generator/public/mockup.png"
      provides: "iPhone 16 Pro Max Kit-Mockup"
    - path: "app-store/screenshots/generator/package.json"
      provides: "bun-Projekt mit html-to-image Dependency"
  key_links:
    - from: "app-store/screenshots/generator/src/app/page.tsx"
      to: "public/screenshots/{de,en,fr}/"
      via: "sourcePath() Funktion"
      pattern: "sourcePath"
    - from: "SLIDES flatMap"
      to: "COPY Record"
      via: "COPY[id][locale].headline"
      pattern: "COPY\\[id\\]\\[locale\\]"
---

<objective>
Next.js-Screenshot-Generator scaffolden und mit StatFlow-spezifischem THEME + Gerüst für 3-Locale-COPY bestücken. Der Generator folgt 1:1 dem Playbook-Rezept — nur THEME, SCREEN_IDS, LAYOUT_ROTATION und COPY-Typ werden angepasst.

Purpose: Schafft die technische Basis für Screenshot-Export (SHOT-01/SHOT-02). Copy-Texte werden in Plan 03 eingetragen — dieser Plan stellt sicher, dass der Generator baut und alle Slots vorhanden sind.
Output: Lauffähiger Next.js-Generator mit StatFlow-Branding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-screenshots-assets-submission-en-fr-de/23-CONTEXT.md
@.planning/phases/23-screenshots-assets-submission-en-fr-de/23-RESEARCH.md
@app-store-screenshots-playbook/PLAYBOOK.md
@app-store-screenshots-playbook/reference/page.tsx
@app-store-screenshots-playbook/reference/layout.tsx
</context>

<interfaces>
<!-- Key patterns aus reference/page.tsx — Executor übernimmt diese 1:1, nur markierte Stellen anpassen -->

Unveränderter Kern (aus reference/page.tsx übernehmen):
- `MultilineText` Component (whiteSpace: "nowrap" per Zeile, \n → <br/>, Anti-Pattern #2)
- `Background` Component mit BG_VARIANTS (Blobs-Positionen — NUR opacity anpassen)
- `DeviceMockup` Component (mockupW=1022, mockupH=2082, screenInsets wie im Reference)
- `HeadlineBlock` Component (headlineSize/sublineSize als Props)
- `SlideCanvas` Component (headline-top/mock-left/mock-right Layouts — mock-podest/edge weglassen)
- Export-Logik: Hidden full-size nodes bei left:-100000px (Anti-Pattern #1 Fix)
- Visible scaled preview: CANVAS_W * 0.25 Scale

Angepasste Stellen (StatFlow-spezifisch):
```typescript
// 1. THEME — D-01/D-03
const THEME = {
  bgFrom: "#FAFAF7",
  bgTo: "#F0F0EC",
  blobLight: "#FFFFFF",
  blobMid: "#F5F5F0",
  blobDark: "#E8E8E2",
  textPrimary: "#000000",
  textSecondary: "rgba(0,0,0,0.65)",
} as const;

// 2. SCREEN_IDS — D-04
const SCREEN_IDS = [
  "01-dashboard", "02-details", "03-realtime", "04-vergleich",
  "05-events", "06-widget", "07-account-switcher", "08-start",
] as const;

// 3. LAYOUT_ROTATION — D-06
const LAYOUT_ROTATION: Layout[] = [
  "headline-top", // 01
  "mock-left",    // 02
  "mock-right",   // 03
  "headline-top", // 04
  "mock-left",    // 05
  "mock-right",   // 06
  "headline-top", // 07
  "mock-left",    // 08
];

// 4. COPY-Typ — 3 Locales statt 2
type Copy = {
  de: { headline: string; subline: string };
  en: { headline: string; subline: string };
  fr: { headline: string; subline: string };
};
type Locale = "de" | "en" | "fr";

// 5. SLIDES — FR hinzu
const SLIDES: Slide[] = SCREEN_IDS.flatMap((id, idx) =>
  (["de", "en", "fr"] as const).map((locale) => ({
    id,
    locale,
    headline: COPY[id][locale].headline,
    subline: COPY[id][locale].subline,
    screenshot: sourcePath(id, locale),
    layout: LAYOUT_ROTATION[idx],
    bgVariant: (idx % 3) as 0 | 1 | 2,
  }))
);

// 6. sourcePath — 3 Locales
function sourcePath(id: string, locale: Locale): string {
  const num = id.slice(0, 2);
  return `/screenshots/${locale}/${num}-${id.slice(3)}.png`;
}
```

BG_VARIANTS blob opacity auf 0.12-0.20 reduzieren (NICHT 0.35-0.55 wie Reference — heller Hintergrund, Anti-Pattern aus RESEARCH.md):
```typescript
// Alle blob opacity Werte: max 0.18, mindestens 0.10
// Beispiel Variant 0:
{ ...blobLight, blur: 140, opacity: 0.15 },
{ ...blobDark,  blur: 160, opacity: 0.12 },
{ ...blobMid,   blur: 120, opacity: 0.10 },
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Next.js-Projekt scaffolden + html-to-image installieren</name>
  <files>
    app-store/screenshots/generator/package.json
    app-store/screenshots/generator/next.config.ts
    app-store/screenshots/generator/tsconfig.json
    app-store/screenshots/generator/tailwind.config.ts
    app-store/screenshots/generator/postcss.config.mjs
    app-store/screenshots/generator/public/mockup.png
  </files>
  <read_first>
    - app-store-screenshots-playbook/PLAYBOOK.md (Setup-Sektion, Zeilen 18-37)
    - .planning/phases/23-screenshots-assets-submission-en-fr-de/23-RESEARCH.md (Standard Stack, Installation-Block)
  </read_first>
  <action>
Führe folgende Befehle aus (aus PLAYBOOK.md + RESEARCH.md kombiniert):

```bash
# 1. Scaffold (aus dem Repo-Root heraus — Zielordner wird erstellt)
cd /Users/simonluthe/Documents/umami
bunx --bun create-next-app@latest app-store/screenshots/generator \
  --typescript --tailwind --app --src-dir --no-eslint \
  --import-alias "@/*" --yes

# 2. html-to-image installieren
cd app-store/screenshots/generator
bun add html-to-image

# 3. Kit-Skill installieren und mockup.png kopieren
npx -y skills add ParthJadhav/app-store-screenshots -a claude-code -y -g 2>/dev/null || true
# Falls Skills-CLI nicht verfügbar: mockup.png aus Playbook-Verzeichnis suchen
# oder aus ~/.claude/skills/app-store-screenshots/mockup.png kopieren
cp ~/.claude/skills/app-store-screenshots/mockup.png public/mockup.png 2>/dev/null || \
  echo "MANUAL: Kopiere mockup.png nach app-store/screenshots/generator/public/mockup.png"

# 4. Screenshot-Source-Verzeichnisse unter public/ anlegen
mkdir -p public/screenshots/de public/screenshots/en public/screenshots/fr
```

Falls Skills-CLI fehlt oder mockup.png nicht gefunden: Placeholder-PNG als mockup.png anlegen (1022×2082 weißes PNG via sips):
```bash
# Fallback: transparentes 1022x2082 PNG als Platzhalter
python3 -c "
from PIL import Image
img = Image.new('RGBA', (1022, 2082), (255,255,255,0))
img.save('public/mockup.png')
" 2>/dev/null || \
  sips -s format png /System/Library/CoreServices/CoreTypes.bundle/Contents/Resources/GenericDocumentIcon.icns \
  --out public/mockup.png 2>/dev/null || \
  echo "MANUAL FALLBACK: mockup.png fehlt — aus Kit holen"
```

Dokumentiere Ergebnis: War mockup.png verfügbar?
  </action>
  <verify>
    <automated>
test -f /Users/simonluthe/Documents/umami/app-store/screenshots/generator/package.json && \
test -f /Users/simonluthe/Documents/umami/app-store/screenshots/generator/next.config.ts && \
grep -q "html-to-image" /Users/simonluthe/Documents/umami/app-store/screenshots/generator/package.json && \
test -d /Users/simonluthe/Documents/umami/app-store/screenshots/generator/public/screenshots/de && \
echo "OK: Generator-Scaffold + html-to-image vorhanden"
    </automated>
  </verify>
  <done>package.json existiert mit html-to-image in dependencies; public/screenshots/{de,en,fr}/ angelegt; public/mockup.png vorhanden (oder Fallback dokumentiert).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: page.tsx + layout.tsx — StatFlow-Generator</name>
  <files>
    app-store/screenshots/generator/src/app/page.tsx
    app-store/screenshots/generator/src/app/layout.tsx
  </files>
  <read_first>
    - app-store-screenshots-playbook/reference/page.tsx (VOLLSTÄNDIG — das ist das Template)
    - app-store-screenshots-playbook/reference/layout.tsx
    - .planning/phases/23-screenshots-assets-submission-en-fr-de/23-CONTEXT.md (D-01 bis D-12)
    - .planning/phases/23-screenshots-assets-submission-en-fr-de/23-RESEARCH.md (Pattern 1, Pattern 2, Anti-Patterns)
  </read_first>
  <behavior>
    - TypeScript kompiliert ohne Fehler (npx tsc --noEmit)
    - THEME.bgFrom = "#FAFAF7" (greifbar via grep)
    - THEME.textPrimary = "#000000"
    - SCREEN_IDS hat exakt 8 Einträge: 01-dashboard bis 08-start
    - LAYOUT_ROTATION hat exakt 8 Einträge: ['headline-top','mock-left','mock-right','headline-top','mock-left','mock-right','headline-top','mock-left']
    - Alle BG_VARIANTS blob opacity Werte sind ≤ 0.20
    - COPY enthält alle 8 SCREEN_IDS als Keys mit de/en/fr sub-objects
    - SLIDES = SCREEN_IDS.flatMap × ["de","en","fr"] → 24 Einträge
    - mock-podest und mock-edge Layouts sind NICHT enthalten (Playbook Anti-Pattern #3)
  </behavior>
  <action>
Erstelle app-store/screenshots/generator/src/app/page.tsx basierend auf reference/page.tsx mit diesen EXAKTEN Änderungen:

**1. THEME ersetzen (D-01, D-03):**
```typescript
const THEME = {
  bgFrom: "#FAFAF7",
  bgTo: "#F0F0EC",
  blobLight: "#FFFFFF",
  blobMid: "#F5F5F0",
  blobDark: "#E8E8E2",
  textPrimary: "#000000",
  textSecondary: "rgba(0,0,0,0.65)",
} as const;
```

**2. Copy-Typ auf 3 Locales erweitern:**
```typescript
type Copy = {
  de: { headline: string; subline: string };
  en: { headline: string; subline: string };
  fr: { headline: string; subline: string };
};
type Locale = "de" | "en" | "fr";
```

**3. SCREEN_IDS (D-04, D-05 — kein 5b):**
```typescript
const SCREEN_IDS = [
  "01-dashboard",
  "02-details",
  "03-realtime",
  "04-vergleich",
  "05-events",
  "06-widget",
  "07-account-switcher",
  "08-start",
] as const;
```

**4. LAYOUT_ROTATION (D-06):**
```typescript
const LAYOUT_ROTATION: Layout[] = [
  "headline-top", // 01 Dashboard
  "mock-left",    // 02 Details
  "mock-right",   // 03 Realtime
  "headline-top", // 04 Vergleich
  "mock-left",    // 05 Events
  "mock-right",   // 06 Widget
  "headline-top", // 07 Account Switcher
  "mock-left",    // 08 Start Screen
];
```

**5. COPY mit Platzhaltern (Plan 03 füllt echte Texte ein):**
```typescript
const COPY: Record<string, Copy> = {
  "01-dashboard": {
    de: { headline: "PLACEHOLDER_DE", subline: "PLACEHOLDER_DE_SUB" },
    en: { headline: "PLACEHOLDER_EN", subline: "PLACEHOLDER_EN_SUB" },
    fr: { headline: "PLACEHOLDER_FR", subline: "PLACEHOLDER_FR_SUB" },
  },
  // ... gleiches Muster für 02-details, 03-realtime, 04-vergleich,
  //     05-events, 06-widget, 07-account-switcher, 08-start
};
```

**6. sourcePath für 3 Locales:**
```typescript
function sourcePath(id: string, locale: Locale): string {
  const num = id.slice(0, 2);
  const name = id.slice(3); // "dashboard", "details", etc.
  return `/screenshots/${locale}/${num}-${name}.png`;
}
```

**7. SLIDES auf 3 Locales erweitern:**
```typescript
const SLIDES: Slide[] = SCREEN_IDS.flatMap((id, idx) =>
  (["de", "en", "fr"] as const).map((locale) => ({
    id,
    locale,
    headline: COPY[id][locale].headline,
    subline: COPY[id][locale].subline,
    screenshot: sourcePath(id, locale),
    layout: LAYOUT_ROTATION[idx],
    bgVariant: (idx % 3) as 0 | 1 | 2,
  }))
);
```

**8. BG_VARIANTS Blob-Opazitäten reduzieren (RESEARCH.md Anti-Pattern):**
Alle opacity-Werte in BG_VARIANTS auf max 0.18 setzen (Kontrast auf hellem BG sonst schlecht):
- Variant 0: opacity 0.15, 0.12, 0.10
- Variant 1: opacity 0.12, 0.15, 0.10
- Variant 2: opacity 0.13, 0.13, 0.10

**9. mock-podest und mock-edge Layout-Branches ENTFERNEN** (sind Anti-Pattern — Playbook §Anti-Pattern #3). Der Layout-Typ bleibt: `"headline-top" | "mock-left" | "mock-right"`.

**10. Slide-Typ — locale auf 3 Locales:**
```typescript
type Slide = {
  id: string;
  locale: "de" | "en" | "fr";
  headline: string;
  subline: string;
  screenshot: string;
  layout: Layout;
  bgVariant: 0 | 1 | 2;
};
```

**11. Page-Header aktualisieren:**
```typescript
<h1 style={{ color: "white", fontSize: 32, marginBottom: 24 }}>
  StatFlow — App Store Screenshots (8 Screens × DE/EN/FR)
</h1>
<p style={{ color: "#94a3b8", marginBottom: 32 }}>
  Chrome verwenden. Klicke „Export" unter einem Slide für 1320×2868 PNG.
  Source-Screenshots in public/screenshots/{'{de,en,fr}'}/ ablegen.
</p>
```

**Unveränderter Kern aus reference/page.tsx:** `MultilineText`, `Background`, `DeviceMockup`, `HeadlineBlock`, `SlideCanvas` (ohne mock-podest/edge), Export-Logik (hidden full-size nodes), Grid-Layout.

**layout.tsx:** Exakt wie reference/layout.tsx, nur title auf "StatFlow — App Store Screenshots" ändern.

Nach Erstellung: `cd app-store/screenshots/generator && npx tsc --noEmit` muss 0 zurückgeben.
  </action>
  <verify>
    <automated>
cd /Users/simonluthe/Documents/umami/app-store/screenshots/generator && \
npx tsc --noEmit 2>&1 && \
grep -q 'bgFrom.*#FAFAF7' src/app/page.tsx && \
grep -q 'textPrimary.*#000000' src/app/page.tsx && \
grep -q '01-dashboard' src/app/page.tsx && \
grep -q '08-start' src/app/page.tsx && \
grep -q '"fr"' src/app/page.tsx && \
python3 -c "
import re, sys
content = open('src/app/page.tsx').read()
opacities = [float(x) for x in re.findall(r'opacity: (0\.\d+)', content)]
bad = [o for o in opacities if o > 0.20]
if bad: sys.exit(f'FAIL: blob opacity > 0.20: {bad}')
print(f'OK: {len(opacities)} opacity values, max={max(opacities) if opacities else 0}')
" && \
echo "OK: page.tsx valide"
    </automated>
  </verify>
  <done>npx tsc --noEmit gibt Exit-Code 0; page.tsx enthält korrekte THEME-Werte, 8 SCREEN_IDS, 3 Locales im SLIDES-Array; alle Blob-Opazitäten ≤ 0.20.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generator (localhost) → Filesystem | Export-PNGs landen in Download-Ordner via Browser — kein API-Aufruf |
| public/screenshots → html-to-image | Screenshot-Pfade sind lokale Pfade, kein Remote-Fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Tampering | page.tsx COPY-Placeholder | accept | Placeholder werden in Plan 03 überschrieben; keine sicherheitsrelevante Daten |
| T-23-04 | Information Disclosure | generator/node_modules | mitigate | Bereits in .gitignore aus Plan 01 |
</threat_model>

<verification>
1. `cd app-store/screenshots/generator && npx tsc --noEmit` — Exit 0
2. `grep -c '"headline-top"\|"mock-left"\|"mock-right"' src/app/page.tsx` — mindestens 8 Treffer
3. `bash app-store/validate.sh quick` — generator-Checks grün
</verification>

<success_criteria>
- Generator scaffoldet und baut ohne TypeScript-Fehler
- StatFlow THEME ist korrekt eingesetzt (Off-White/Schwarz statt Türkis)
- 24 Slide-Slots (8 SCREEN_IDS × 3 Locales) sind im SLIDES-Array vorhanden
- Blob-Opazitäten sind hell-tauglich (≤ 0.20)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/23-screenshots-assets-submission-en-fr-de/23-02-SUMMARY.md` erstellen.
</output>
