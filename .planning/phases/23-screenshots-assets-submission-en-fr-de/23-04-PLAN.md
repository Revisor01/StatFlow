---
phase: 23-screenshots-assets-submission-en-fr-de
plan: 04
type: execute
wave: 2
depends_on:
  - "23-02"
  - "23-03"
autonomous: false
requirements:
  - SHOT-03
  - SHOT-05
files_modified:
  - app-store/screenshots/export/de/
  - app-store/screenshots/export/en/
must_haves:
  truths:
    - "Simulator iPhone 17 Pro Max (UDID A2FD53C9) läuft mit Statusbar-Override 9:41/WiFi/Akku-voll"
    - "16 Source-Shots in app-store/screenshots/generator/public/screenshots/{de,en}/ liegen vor"
    - "16 Export-PNGs in app-store/screenshots/export/{de,en}/ existieren (je 8 pro Locale)"
    - "Alle Export-PNGs haben Dimension 1320×2868"
    - "Export-PNGs sind committet (.gitignore schließt source-Verzeichnis aus)"
  artifacts:
    - path: "app-store/screenshots/export/de/01-dashboard-de.png"
      provides: "DE Screenshot Slide 1"
    - path: "app-store/screenshots/export/en/01-dashboard-en.png"
      provides: "EN Screenshot Slide 1"
  key_links:
    - from: "app-store/screenshots/export/{de,en}/"
      to: "app-store/submission/lib/config.ts"
      via: "SCREENSHOT_PATHS in Plan 06"
      pattern: "export.*de.*en"
---

<objective>
Source-Screenshots aus dem iPhone 17 Pro Max Simulator ziehen, in den Generator einlesen und 16 fertige Export-PNGs exportieren (8 Slides × DE/EN). Enthält einen manuellen Checkpoint: Der User muss alle Slides visuell abnicken bevor die PNGs committet werden.

Purpose: SHOT-03 + SHOT-05 — ohne echte Screenshots sind die Generator-Layouts nicht validierbar. Die Export-PNGs sind der Input für Plan 06 (ASC API Upload).
Output: 16 PNGs in app-store/screenshots/export/{de,en}/, Dimension 1320×2868, in git committet.

Scope-Change 2026-04-14: FR-Lokalisierung gestrichen (war irrtümlich aus anderem Projekt übernommen). Ursprünglich 24 PNGs geplant (DE/EN/FR) — jetzt 16 (DE/EN).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-screenshots-assets-submission-en-fr-de/23-CONTEXT.md
@app-store-screenshots-playbook/PLAYBOOK.md
@.planning/phases/23-screenshots-assets-submission-en-fr-de/23-02-SUMMARY.md
@.planning/phases/23-screenshots-assets-submission-en-fr-de/23-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simulator vorbereiten + Source-Shots ziehen (DE/EN)</name>
  <files>
    app-store/screenshots/generator/public/screenshots/de/
    app-store/screenshots/generator/public/screenshots/en/
  </files>
  <read_first>
    - app-store-screenshots-playbook/PLAYBOOK.md (§Simulator vorbereiten, §Workflow Schritt 3-5)
    - .planning/phases/23-screenshots-assets-submission-en-fr-de/23-CONTEXT.md (D-23, D-24, D-25)
  </read_first>
  <action>
**Statusbar-Override und Simulator-Start (per D-25, aus PLAYBOOK.md):**

```bash
UDID="A2FD53C9-DF1A-4BE4-A256-8C2F1B94303A"  # iPhone 17 Pro Max (verifiziert in RESEARCH.md)

# Simulator booten
xcrun simctl boot "$UDID" 2>/dev/null || true
open -a Simulator

# Statusbar-Override setzen (bleibt bis Simulator-Shutdown)
xcrun simctl status_bar "$UDID" override \
  --time "9:41" \
  --dataNetwork wifi --wifiMode active --wifiBars 3 \
  --cellularMode active --cellularBars 4 \
  --batteryState charged --batteryLevel 100
```

**Hinweis an User:** Öffne die StatFlow-App im Simulator, navigiere zu jedem Screen (Dashboard, Website-Details, Realtime, Vergleich, Events, Widget, Account-Switcher, Start/Accounts-Liste) und ziehe Screenshots mit:
```bash
# Beispiel Slide 1 Deutsch:
xcrun simctl io booted screenshot ~/Desktop/01-dashboard-de.png
# Wiederholen für alle 8 Screens × 2 Sprachen (App-Sprache wechseln für EN)
```

**Sprache wechseln für EN-Shots:**
```bash
# App in englischer Sprache starten
xcrun simctl launch "$UDID" de.godsapp.statflow -AppleLanguages "(en)" -AppleLocale "en_US"
```

**Source-Shots in Generator-Verzeichnis kopieren:**
Die 8 DE-Shots kommen in: `app-store/screenshots/generator/public/screenshots/de/`
Die 8 EN-Shots in: `app-store/screenshots/generator/public/screenshots/en/`

Dateinamen-Schema (muss sourcePath()-Funktion in page.tsx matchen):
```
01-dashboard.png
02-details.png
03-realtime.png
04-vergleich.png
05-events.png
06-widget.png
07-account-switcher.png
08-start.png
```

**WICHTIG (D-23, D-24):** Echte Daten aus den konfigurierten Test-Accounts (Umami: t.godsapp.de / Plausible: plausible.godsapp.de). Keine Fake-Daten. Diese Source-Shots sind Platzhalter für Layout-Review — finale Shots werden kurz vor Submission nochmal gezogen.

**Dimension validieren:**
```bash
for f in app-store/screenshots/generator/public/screenshots/de/*.png; do
  echo -n "$f: "
  sips -g pixelWidth -g pixelHeight "$f" 2>/dev/null | grep -E "Width|Height"
done
```
Erwartet: 1320 × 2868 pro Shot (aus iPhone 17 Pro Max Simulator).
  </action>
  <verify>
    <automated>
DE_COUNT=$(ls /Users/simonluthe/Documents/umami/app-store/screenshots/generator/public/screenshots/de/*.png 2>/dev/null | wc -l | tr -d ' ') && \
EN_COUNT=$(ls /Users/simonluthe/Documents/umami/app-store/screenshots/generator/public/screenshots/en/*.png 2>/dev/null | wc -l | tr -d ' ') && \
echo "Source-Shots: DE=${DE_COUNT}/8, EN=${EN_COUNT}/8" && \
if [ "$DE_COUNT" -ne 8 ] || [ "$EN_COUNT" -ne 8 ]; then
  echo "FAIL: nicht alle 16 Source-Shots vorhanden. Task unvollständig — Simulator-Shots fehlen."
  exit 1
fi
echo "OK: alle 16 Source-Shots vorhanden"
sips -g pixelWidth -g pixelHeight /Users/simonluthe/Documents/umami/app-store/screenshots/generator/public/screenshots/de/01-dashboard.png 2>/dev/null | grep -q "pixelWidth: 1320" || { echo "FAIL: Dimension !=1320px"; exit 1; }
sips -g pixelHeight /Users/simonluthe/Documents/umami/app-store/screenshots/generator/public/screenshots/de/01-dashboard.png 2>/dev/null | grep -q "pixelHeight: 2868" || { echo "FAIL: Höhe !=2868px"; exit 1; }
echo "OK: Dimension 1320×2868 korrekt"
    </automated>
  </verify>
  <done>16 Source-Shots (je 8 in de/en) in public/screenshots/ vorhanden; alle 1320px breit (≈2868px hoch).</done>
</task>

<task type="auto">
  <name>Task 2: Generator starten + 16 PNGs exportieren</name>
  <files>
    app-store/screenshots/export/de/
    app-store/screenshots/export/en/
  </files>
  <read_first>
    - app-store-screenshots-playbook/PLAYBOOK.md (§Workflow Schritt 6-7)
    - .planning/phases/23-screenshots-assets-submission-en-fr-de/23-CONTEXT.md (D-22 — Export committen)
  </read_first>
  <action>
**Generator starten:**
```bash
cd app-store/screenshots/generator
bun dev
```
(Port 3334 — Ports 3000 und 3333 sind lokal belegt, siehe package.json.)

Öffne http://localhost:3334 in Chrome (nicht Firefox — Font-Bugs, Anti-Pattern #2 im Playbook).

Du siehst 16 Slide-Vorschauen (8 Screens × 2 Locales: DE, EN) in einem Grid.

**Pro Slide: "Export PNG" klicken.** Der Browser lädt eine 1320×2868 PNG herunter.

**PNGs umbenennen und in export-Ordner verschieben:**
Download-Dateiname aus Generator: `01-dashboard-de.png` (entspricht `{id}-{locale}.png`)

Ziel-Ordner und Dateinamen-Schema:
```
app-store/screenshots/export/de/01-dashboard-de.png
app-store/screenshots/export/de/02-details-de.png
app-store/screenshots/export/de/03-realtime-de.png
app-store/screenshots/export/de/04-vergleich-de.png
app-store/screenshots/export/de/05-events-de.png
app-store/screenshots/export/de/06-widget-de.png
app-store/screenshots/export/de/07-account-switcher-de.png
app-store/screenshots/export/de/08-start-de.png
# (gleiches Schema für en/)
```

**Dimensionen validieren:**
```bash
bash app-store/validate.sh full 2>&1 | grep -E "✓|✗|WRONG"
```

Jedes PNG muss 1320×2868 zurückgeben. Falls ein PNG falsche Dimensionen hat: nochmal exportieren (Chrome-Reload, dann Export klicken).

**Source-Shots sind NICHT committet** (per D-22 in .gitignore). Nur export/ wird committet.
  </action>
  <verify>
    <automated>
DE_COUNT=$(ls /Users/simonluthe/Documents/umami/app-store/screenshots/export/de/*.png 2>/dev/null | wc -l | tr -d ' ') && \
EN_COUNT=$(ls /Users/simonluthe/Documents/umami/app-store/screenshots/export/en/*.png 2>/dev/null | wc -l | tr -d ' ') && \
echo "Export-PNGs: DE=${DE_COUNT}/8, EN=${EN_COUNT}/8" && \
bash /Users/simonluthe/Documents/umami/app-store/validate.sh full 2>&1 | tail -5
    </automated>
  </verify>
  <done>16 PNGs in export/{de,en}/; validate.sh full gibt "16 PNGs gesamt vorhanden" und alle Dimensionen 1320×2868.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visuelle Qualitätskontrolle aller 16 Screenshots</name>
  <what-built>
    16 App-Store-Screenshots für StatFlow (8 Slides × DE/EN), exportiert via Next.js-Generator mit Hybrid-hell-mit-Tinte-Theme (Off-White #FAFAF7, schwarze Typo #000000, iPhone-Mockup). Alle Slides sichtbar unter http://localhost:3334 oder als PNG in app-store/screenshots/export/
  </what-built>
  <how-to-verify>
Öffne alle 16 PNGs und prüfe gegen diese Checkliste:

**Layout-Prüfung (per Slide):**
- Slides 1, 4, 7 (Dashboard, Vergleich, Account Switcher): headline-top — Text oben zentriert, Mock unten
- Slides 2, 5, 8 (Details, Events, Start): mock-left — Mock links übergroß, Text rechts oben
- Slides 3, 6 (Realtime, Widget): mock-right — Mock rechts übergroß, Text links oben

**Inhaltliche Prüfung:**
- Claim-Text ist lesbar (schwarzer Text auf Off-White — genug Kontrast)
- Subline-Text ist lesbar (rgba(0,0,0,0.65) auf Off-White)
- Claim wiederholt sich NICHT in der Subline (Playbook Anti-Pattern #7)
- App-Screen im Mockup ist sichtbar und korrekt
- Keine weißen Flecken wo der Simulator-Shot fehlt
- DE/EN Copy klingt idiomatisch

**Technische Prüfung:**
- Kein "Export-Bug" (Bild klein oben links in großem Canvas) — sollte durch hidden full-size nodes behoben sein
- Keine abgeschnittenen Headlines (whiteSpace: nowrap sollte Umbrüche verhindern)
- Kein unerwünschter Auto-Wrap (Multi-line durch \n, nie durch Browser)

**Anti-Patterns-Check (Playbook):**
- Anti-Pattern #2: Headlines brechen nicht an falscher Stelle
- Anti-Pattern #7: Sublines haben keine Wurst-Aufzählung ohne Zeilenumbrüche
- Anti-Pattern #9: Layout-Komplexität — alle 3 Layouts klar erkennbar, nicht vermischt

Wenn etwas visuell nicht stimmt: Beschreibe das Problem (welcher Slide, was ist falsch). Claude kann die Copy oder Layout-Parameter in page.tsx / copy.ts anpassen.
  </how-to-verify>
  <resume-signal>Tippe "approved" wenn alle 16 Screenshots akzeptabel aussehen, oder beschreibe welche Anpassungen nötig sind.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Simulator → public/screenshots/ | Source-Shots bleiben lokal (nicht committet per D-22) |
| export/ → git | Nur finalisierte PNGs werden committet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07 | Information Disclosure | Source-Shots mit echten Account-Daten | mitigate | .gitignore schließt source/ aus; nur export/ in git (D-22) |
| T-23-08 | Tampering | PNG-Dimensionen | mitigate | validate.sh full prüft 1320×2868 für alle 16 PNGs |
</threat_model>

<verification>
1. `bash app-store/validate.sh full` — alle 16 Dimension-Checks grün
2. `git status app-store/screenshots/` — source/ nicht im Staged-Bereich
3. Checkpoint approved — User hat alle Slides visuell abgenickt
</verification>

<success_criteria>
- 16 PNGs in export/{de,en}/ mit korrekten Dimensionen 1320×2868
- User hat visuelle Qualität per Checkpoint abgenickt
- Source-Shots nicht in git
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/23-screenshots-assets-submission-en-fr-de/23-04-SUMMARY.md` erstellen.
</output>
