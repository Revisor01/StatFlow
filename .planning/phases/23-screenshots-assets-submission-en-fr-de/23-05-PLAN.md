---
phase: 23-screenshots-assets-submission-en-fr-de
plan: 05
type: execute
wave: 2
depends_on:
  - "23-01"
files_modified:
  - app-store/submission/package.json
  - app-store/submission/tsconfig.json
  - app-store/submission/lib/jwt.ts
  - app-store/submission/lib/asc-api.ts
  - app-store/submission/lib/config.ts
autonomous: true
requirements:
  - SUBMIT-01
must_haves:
  truths:
    - "app-store/submission/ enthält package.json mit jsonwebtoken + ts-node"
    - "lib/jwt.ts generiert gültiges ES256 JWT mit iss/aud/exp/kid Header"
    - "lib/asc-api.ts hat ascFetch() mit Auth-Header + Token-Refresh + Retry-Logik (429)"
    - "lib/config.ts enthält APP_ID, LOCALE_CODES, SCREENSHOT_PATHS, ASC-Metadaten für DE/EN/FR"
    - "npx tsc --noEmit in app-store/submission/ gibt Exit 0"
  artifacts:
    - path: "app-store/submission/lib/jwt.ts"
      provides: "generateASCToken() — ES256 JWT via jsonwebtoken"
      exports: ["generateASCToken"]
    - path: "app-store/submission/lib/asc-api.ts"
      provides: "ascFetch() wrapper mit Retry + Token-Refresh"
      exports: ["ascFetch", "withRetry"]
    - path: "app-store/submission/lib/config.ts"
      provides: "Alle Konstanten + Metadata für Submit-Script"
      exports: ["APP_BUNDLE_ID", "LOCALES", "METADATA", "SCREENSHOT_BASE_PATH"]
  key_links:
    - from: "lib/jwt.ts generateASCToken()"
      to: "lib/asc-api.ts ascFetch()"
      via: "Authorization: Bearer header"
      pattern: "generateASCToken"
    - from: "lib/config.ts METADATA"
      to: "submit.ts (Plan 06)"
      via: "frDescription, deDescription, enDescription etc."
      pattern: "METADATA"
---

<objective>
ASC-Submission-Infrastruktur aufbauen: JWT-Auth-Modul, ASC-API-Client mit Retry-Logik, und Config-Modul mit allen Metadaten (DE/EN/FR) für das Submit-Script. Das Submit-Script selbst (submit.ts) kommt in Plan 06.

Purpose: Trennung von Infrastruktur (jwt, asc-api, config) und Geschäftslogik (submit.ts). Diese Module sind unabhängig testbar. Plan 06 hat klare Schnittstellen zum Importieren.
Output: Typsichere, von Plan 06 importierbare Submission-Bibliothek.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-screenshots-assets-submission-en-fr-de/23-CONTEXT.md
@.planning/phases/23-screenshots-assets-submission-en-fr-de/23-RESEARCH.md
@app-store/description.md
@app-store/review-notes.md
@app-store/secrets.md
</context>

<interfaces>
<!-- Verwendete Patterns aus RESEARCH.md — exakt so implementieren -->

Pattern 3 (JWT-Generierung, verifiziert gegen Apple ASC OpenAPI-Spec):
```typescript
import * as jwt from "jsonwebtoken";
// Header: { alg: "ES256", kid: KEY_ID, typ: "JWT" }
// Payload: { iss: ISSUER_ID, aud: "appstoreconnect-v1", exp: now + 20*60 }
// Key: .p8-Datei via process.env.APP_STORE_CONNECT_KEY_PATH
```

Pattern 6 (Retry bei 429):
```typescript
async function withRetry<T>(fn: () => Promise<T>, maxAttempts = 5): Promise<T>
// exponential backoff: 2^attempt * 1000ms + random 0-500ms
```

Env-Vars (aus RESEARCH.md + secrets.md):
```
APP_STORE_CONNECT_KEY_ID=6JGT8ZLHRJ
APP_STORE_CONNECT_ISSUER_ID=(aus secrets.env)
APP_STORE_CONNECT_KEY_PATH=(Pfad zur .p8-Datei, aus secrets.env)
```

Pitfall 5 (Locale-Codes): Verwende "de-DE", "en-US", "fr-FR" — NICHT "de", "en", "fr".
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Submission-Projekt + lib/jwt.ts + lib/asc-api.ts</name>
  <files>
    app-store/submission/package.json
    app-store/submission/tsconfig.json
    app-store/submission/lib/jwt.ts
    app-store/submission/lib/asc-api.ts
  </files>
  <read_first>
    - .planning/phases/23-screenshots-assets-submission-en-fr-de/23-RESEARCH.md (Pattern 3, Pattern 6, Pitfall 3, Standard Stack)
    - .planning/phases/23-screenshots-assets-submission-en-fr-de/23-CONTEXT.md (D-17, D-19)
    - app-store/secrets.md (Env-Var-Namen verifizieren)
  </read_first>
  <behavior>
    - generateASCToken() gibt einen nicht-leeren String zurück (das JWT)
    - JWT hat Format "xxxxx.yyyyy.zzzzz" (drei Punkte-getrennte Base64-Teile)
    - ascFetch() wirft bei 4xx/5xx einen typisierten Fehler mit HTTP-Status
    - withRetry() wartet bei 429 und versucht erneut (maxAttempts=5)
    - Token-Refresh: ascFetch() prüft ob Token < 2 Minuten Restlaufzeit hat und regeneriert
    - npx tsc --noEmit gibt Exit 0
  </behavior>
  <action>
**1. Submission-Projekt initialisieren:**
```bash
cd /Users/simonluthe/Documents/umami/app-store/submission
npm init -y
npm install typescript @types/node jsonwebtoken @types/jsonwebtoken ts-node
```

**2. tsconfig.json:**
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "commonjs",
    "lib": ["ES2022"],
    "outDir": "./dist",
    "rootDir": "./",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true
  },
  "include": ["**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
```

**3. lib/jwt.ts:**
```typescript
import * as fs from "fs";
import * as jwt from "jsonwebtoken";

const TOKEN_LIFETIME_SECONDS = 20 * 60; // Apple-Maximum: 20 Minuten

let cachedToken: string | null = null;
let tokenExpiresAt = 0;

export function generateASCToken(): string {
  const now = Math.floor(Date.now() / 1000);

  // Token-Refresh: regeneriere wenn < 2 Minuten Restlaufzeit (Pitfall 3)
  if (cachedToken && tokenExpiresAt - now > 120) {
    return cachedToken;
  }

  const keyId = process.env.APP_STORE_CONNECT_KEY_ID;
  const issuerId = process.env.APP_STORE_CONNECT_ISSUER_ID;
  const keyPath = process.env.APP_STORE_CONNECT_KEY_PATH;

  if (!keyId || !issuerId || !keyPath) {
    throw new Error(
      "Missing ASC env vars: APP_STORE_CONNECT_KEY_ID, APP_STORE_CONNECT_ISSUER_ID, APP_STORE_CONNECT_KEY_PATH"
    );
  }

  const privateKey = fs.readFileSync(keyPath, "utf-8");
  const exp = now + TOKEN_LIFETIME_SECONDS;

  const token = jwt.sign(
    {
      iss: issuerId,
      aud: "appstoreconnect-v1",
      exp,
    },
    privateKey,
    {
      algorithm: "ES256",
      header: {
        alg: "ES256",
        kid: keyId,
        typ: "JWT",
      },
    }
  );

  cachedToken = token;
  tokenExpiresAt = exp;
  return token;
}
```

**4. lib/asc-api.ts:**
```typescript
import { generateASCToken } from "./jwt";

const ASC_BASE = "https://api.appstoreconnect.apple.com";

export class AscApiError extends Error {
  constructor(
    public readonly status: number,
    public readonly body: unknown,
    message: string
  ) {
    super(message);
    this.name = "AscApiError";
  }
}

export async function ascFetch<T = unknown>(
  path: string,
  options: RequestInit = {}
): Promise<T> {
  const token = generateASCToken();
  const url = path.startsWith("http") ? path : `${ASC_BASE}${path}`;

  const response = await fetch(url, {
    ...options,
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${token}`,
      ...(options.headers as Record<string, string> ?? {}),
    },
  });

  if (!response.ok) {
    const body = await response.json().catch(() => response.text());
    throw new AscApiError(
      response.status,
      body,
      `ASC API ${options.method ?? "GET"} ${path} → ${response.status}: ${JSON.stringify(body)}`
    );
  }

  if (response.status === 204) return {} as T;
  return response.json() as Promise<T>;
}

export async function withRetry<T>(
  fn: () => Promise<T>,
  maxAttempts = 5
): Promise<T> {
  for (let attempt = 0; attempt < maxAttempts; attempt++) {
    try {
      return await fn();
    } catch (err: unknown) {
      const isRateLimit = err instanceof AscApiError && err.status === 429;
      if (!isRateLimit || attempt === maxAttempts - 1) throw err;
      const delay = Math.pow(2, attempt) * 1000 + Math.random() * 500;
      console.log(`Rate limited (429), retrying in ${Math.round(delay)}ms... (attempt ${attempt + 1}/${maxAttempts})`);
      await new Promise((resolve) => setTimeout(resolve, delay));
    }
  }
  throw new Error("Max retries exceeded");
}
```

**5. Typen prüfen:**
```bash
cd app-store/submission && npx tsc --noEmit
```
  </action>
  <verify>
    <automated>
test -f /Users/simonluthe/Documents/umami/app-store/submission/lib/jwt.ts && \
test -f /Users/simonluthe/Documents/umami/app-store/submission/lib/asc-api.ts && \
grep -q "generateASCToken" /Users/simonluthe/Documents/umami/app-store/submission/lib/jwt.ts && \
grep -q "appstoreconnect-v1" /Users/simonluthe/Documents/umami/app-store/submission/lib/jwt.ts && \
grep -q "withRetry" /Users/simonluthe/Documents/umami/app-store/submission/lib/asc-api.ts && \
grep -q "429" /Users/simonluthe/Documents/umami/app-store/submission/lib/asc-api.ts && \
cd /Users/simonluthe/Documents/umami/app-store/submission && npx tsc --noEmit 2>&1 && \
echo "OK: jwt.ts + asc-api.ts valide"
    </automated>
  </verify>
  <done>jwt.ts und asc-api.ts erstellt; generateASCToken() mit Token-Cache und Refresh implementiert; withRetry() mit Exponential-Backoff; npx tsc --noEmit gibt Exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: lib/config.ts — Metadaten + Screenshot-Pfade</name>
  <files>
    app-store/submission/lib/config.ts
  </files>
  <read_first>
    - app-store/description.md (VOLLSTÄNDIG — DE/EN/FR Metadaten werden hier eingetragen)
    - app-store/review-notes.md (Review-Notes werden als String eingetragen)
    - .planning/phases/23-screenshots-assets-submission-en-fr-de/23-CONTEXT.md (D-15, D-17)
    - .planning/phases/23-screenshots-assets-submission-en-fr-de/23-RESEARCH.md (Pitfall 5 — Locale-Codes BCP-47)
  </read_first>
  <action>
Erstelle app-store/submission/lib/config.ts mit ALLEN Konstanten und Metadaten-Texten, die submit.ts (Plan 06) benötigt. Alle Texte werden direkt aus description.md und review-notes.md übernommen — copy-paste, keine Verkürzungen.

```typescript
import * as path from "path";

// --- App Identifiers ---
export const APP_BUNDLE_ID = "de.godsapp.statflow";
export const APP_VERSION_STRING = "1.0";

// --- Locale-Codes (BCP-47, Pitfall 5) ---
export const LOCALES = {
  de: "de-DE",
  en: "en-US",
  fr: "fr-FR",
} as const;

export type LocaleKey = keyof typeof LOCALES;

// --- Screenshot Paths ---
const REPO_ROOT = path.resolve(__dirname, "../../../..");
export const SCREENSHOT_BASE_PATH = path.join(
  REPO_ROOT,
  "app-store/screenshots/export"
);

export const SLIDE_IDS = [
  "01-dashboard",
  "02-details",
  "03-realtime",
  "04-vergleich",
  "05-events",
  "06-widget",
  "07-account-switcher",
  "08-start",
] as const;

export type SlideId = typeof SLIDE_IDS[number];

export function screenshotPath(locale: LocaleKey, slideId: SlideId): string {
  return path.join(SCREENSHOT_BASE_PATH, locale, `${slideId}-${locale}.png`);
}

// --- ASC Screenshot Display Type ---
// RESEARCH.md: APP_IPHONE_69 existiert NICHT im OpenAPI-Spec (Stand April 2026)
// Primär APP_IPHONE_67 verwenden; bei 422 → manueller Upload-Fallback
export const SCREENSHOT_DISPLAY_TYPE = "APP_IPHONE_67";

// --- App Store Metadata ---
// Alle Texte direkt aus app-store/description.md übernommen
export const METADATA: Record<LocaleKey, {
  name: string;
  subtitle: string;
  keywords: string;
  description: string;
  promotionalText: string;
  whatsNew: string;
}> = {
  de: {
    name: "StatFlow",
    subtitle: "Analytics Dashboard für iPhone",
    keywords: "analytics,umami,plausible,statistik,dashboard,website,tracking,besucher,self-hosted,datenschutz",
    description: `Deine Website-Zahlen. Immer dabei.

StatFlow bringt dein Analytics-Dashboard aufs iPhone — für Umami und Plausible Analytics. Schnell, sicher und ohne Umwege.

Wer eine Website betreibt und auf datenschutzfreundliche Analytics setzt, kennt das Problem: Um die Besucherzahlen zu checken, muss man sich jedes Mal am Desktop einloggen. StatFlow macht Schluss damit. Eine App, alle Accounts, alle Websites.

MULTI-ACCOUNT & MULTI-PROVIDER
Verwalte beliebig viele Umami- und Plausible-Accounts in einer App. Wechsle zwischen Accounts mit einem Tap oder sieh alle Websites aller Accounts in einer kombinierten Ansicht.

DASHBOARD MIT SPARKLINES
Die Übersicht zeigt dir sofort, was auf deinen Websites passiert: Besucher, Seitenaufrufe, Verweildauer und Absprungrate — alles mit kompakten Sparkline-Charts für den schnellen Trend-Check.

ECHTZEIT-ANSICHT
Wer ist gerade auf deiner Website? Die Realtime-View zeigt dir Live-Besucher, aktuelle Seiten und Referrer in Echtzeit.

ZEITRAUMVERGLEICH
Vergleiche beliebige Zeiträume miteinander: Diese Woche vs. letzte Woche, dieser Monat vs. Vormonat. So erkennst du Trends sofort.

EVENTS & REPORTS
Detaillierte Auswertungen für Custom Events, Funnel-Analysen, UTM-Tracking, Goals und Attribution — alles direkt in der App.

WIDGET FÜR DEN HOMESCREEN
Ein Blick auf den Homescreen genügt: Das StatFlow-Widget zeigt dir die wichtigsten Zahlen deiner Website, ohne die App zu öffnen.

OFFLINE-MODUS
Kein Netz? Kein Problem. StatFlow zeigt dir die zuletzt geladenen Daten als Fallback an, damit du auch unterwegs nicht im Dunkeln tappst.

DATENSCHUTZ
- Keine Tracking-SDKs — die App selbst enthält keinerlei Analytics oder Tracking
- Keine Werbung — komplett werbefrei
- Credentials im Keychain — deine Zugangsdaten werden sicher in der iOS Keychain gespeichert
- Keine Datensammlung — die App kommuniziert ausschließlich mit deinen eigenen Servern
- Keine externen Dependencies — komplett in SwiftUI gebaut, ohne Drittanbieter-Bibliotheken

TECHNIK
StatFlow ist eine native iOS-App, komplett in Swift und SwiftUI gebaut. Keine externen Abhängigkeiten, keine Drittanbieter-Frameworks. Deine Daten bleiben auf deinem Gerät und deinen Servern.

Für Website-Betreiber, die ihre Self-Hosted Analytics auch unterwegs im Blick behalten wollen.`,
    promotionalText: "Dein Analytics-Dashboard für unterwegs. Umami und Plausible Analytics sicher auf dem iPhone — mit Echtzeit-Daten, Widgets und Zeitraumvergleich.",
    whatsNew: "Erste Veröffentlichung im App Store! Dashboard, Echtzeit-Ansicht, Zeitraumvergleich, Events, Reports, Widget und Offline-Modus — alles für Umami und Plausible Analytics.",
  },

  en: {
    name: "StatFlow",
    subtitle: "Analytics Dashboard for iPhone",
    keywords: "analytics,umami,plausible,statistics,dashboard,website,tracking,visitors,self-hosted,privacy",
    description: `Your website stats. Always with you.

StatFlow brings your analytics dashboard to your iPhone — for Umami and Plausible Analytics. Fast, secure, and straightforward.

If you run a website with privacy-friendly analytics, you know the problem: checking your visitor stats means logging in on a desktop every time. StatFlow changes that. One app, all accounts, all websites.

MULTI-ACCOUNT & MULTI-PROVIDER
Manage any number of Umami and Plausible accounts in one app. Switch between accounts with a tap or view all websites across all accounts in a combined view.

DASHBOARD WITH SPARKLINES
The overview shows you instantly what's happening on your websites: visitors, pageviews, session duration, and bounce rate — all with compact sparkline charts for quick trend checks.

REALTIME VIEW
Who's on your website right now? The realtime view shows you live visitors, current pages, and referrers as they happen.

PERIOD COMPARISON
Compare any time periods: this week vs. last week, this month vs. last month. Spot trends immediately.

EVENTS & REPORTS
Detailed analytics for custom events, funnel analysis, UTM tracking, goals, and attribution — all right in the app.

HOMESCREEN WIDGET
One glance at your homescreen is all it takes: the StatFlow widget shows your website's key metrics without opening the app.

OFFLINE MODE
No connection? No problem. StatFlow shows your last loaded data as a fallback, so you're never in the dark while on the go.

PRIVACY
- No tracking SDKs — the app itself contains zero analytics or tracking
- No ads — completely ad-free
- Credentials in Keychain — your login data is securely stored in iOS Keychain
- No data collection — the app communicates exclusively with your own servers
- No external dependencies — built entirely in SwiftUI, no third-party libraries

TECHNOLOGY
StatFlow is a native iOS app built entirely in Swift and SwiftUI. No external dependencies, no third-party frameworks. Your data stays on your device and your servers.

For website owners who want to keep their self-hosted analytics in sight while on the go.`,
    promotionalText: "Your analytics dashboard on the go. Umami and Plausible Analytics on your iPhone — with realtime data, widgets, and period comparison.",
    whatsNew: "First App Store release! Dashboard, realtime view, period comparison, events, reports, widget, and offline mode — all for Umami and Plausible Analytics.",
  },

  fr: {
    name: "StatFlow",
    subtitle: "Tableau de bord analytics",
    keywords: "analytics,umami,plausible,statistiques,dashboard,site web,suivi,visiteurs,self-hosted,confidentialité",
    // FR Description: aus description.md FR-Block (Plan 03 hat ihn erstellt)
    description: `Tes statistiques de site web. Toujours avec toi.

StatFlow apporte ton tableau de bord analytics sur iPhone — pour Umami et Plausible Analytics. Rapide, sécurisé et sans détour.

Si tu gères un site web avec des analytics respectueux de la vie privée, tu connais le problème : vérifier tes statistiques signifie se connecter sur desktop à chaque fois. StatFlow change ça. Une app, tous tes comptes, tous tes sites.

MULTI-COMPTES & MULTI-FOURNISSEURS
Gère autant de comptes Umami et Plausible que tu veux dans une seule app. Passe d'un compte à l'autre d'un tap ou vois tous les sites de tous tes comptes dans une vue combinée.

TABLEAU DE BORD AVEC SPARKLINES
La vue d'ensemble te montre immédiatement ce qui se passe sur tes sites : visiteurs, pages vues, durée de session et taux de rebond — le tout avec des graphiques sparklines pour un aperçu rapide des tendances.

VUE EN TEMPS RÉEL
Qui est sur ton site en ce moment ? La vue temps réel te montre les visiteurs en direct, les pages actuelles et les référents à la seconde.

COMPARAISON DE PÉRIODES
Compare n'importe quelles périodes : cette semaine vs la semaine dernière, ce mois vs le mois précédent. Repère les tendances immédiatement.

ÉVÉNEMENTS & RAPPORTS
Analyses détaillées pour les événements personnalisés, les entonnoirs de conversion, le suivi UTM, les objectifs et l'attribution — directement dans l'app.

WIDGET POUR L'ÉCRAN D'ACCUEIL
Un coup d'œil sur l'écran d'accueil suffit : le widget StatFlow t'affiche les chiffres clés de ton site sans ouvrir l'app.

MODE HORS LIGNE
Pas de connexion ? Pas de problème. StatFlow affiche les dernières données chargées en fallback, pour que tu ne sois jamais dans le noir.

VIE PRIVÉE
- Aucun SDK de suivi — l'app elle-même ne contient aucun analytics ni tracking
- Sans publicité — entièrement sans pub
- Identifiants dans le Keychain — tes données de connexion sont stockées de manière sécurisée dans le Keychain iOS
- Aucune collecte de données — l'app communique exclusivement avec tes propres serveurs
- Aucune dépendance externe — entièrement développée en SwiftUI, sans bibliothèques tierces

TECHNOLOGIE
StatFlow est une app iOS native, entièrement développée en Swift et SwiftUI. Aucune dépendance externe, aucun framework tiers. Tes données restent sur ton appareil et tes serveurs.

Pour les propriétaires de sites web qui veulent garder un œil sur leurs analytics self-hosted, même en déplacement.`,
    promotionalText: "Ton tableau de bord analytics en poche. Umami et Plausible Analytics sur iPhone — avec données en temps réel, widgets et comparaison de périodes.",
    whatsNew: "Première publication sur l'App Store ! Tableau de bord, vue temps réel, comparaison de périodes, événements, rapports, widget et mode hors ligne — pour Umami et Plausible Analytics.",
  },
};

// --- App Review Notes ---
// Aus app-store/review-notes.md
export const REVIEW_NOTES = `Test Account 1 — Umami Analytics:
Server URL: https://t.godsapp.de
Username: admin
Password: rn%MWru13

Steps:
1. Open app, tap "Account hinzufuegen" (Add Account)
2. Select "Umami" as provider
3. Enter server URL, username, and password
4. Tap "Anmelden" (Sign In) — dashboard loads with test websites

Test Account 2 — Plausible Analytics:
Server URL: https://plausible.godsapp.de
API Key: 4uJEjvR5JiXZuQRm84fM4C3wMvQqVPgF2xBUiSlSCRcz1Vu4zt1qvEQ0BtDU5U2S

Steps:
1. Tap "Account hinzufuegen"
2. Select "Plausible" as provider
3. Enter server URL and API key
4. Tap "Anmelden" — dashboard loads with 3 test websites

Note: App requires a self-hosted Umami or Plausible server. No data collection. Credentials in iOS Keychain.`;

// --- Privacy Policy URL (alle Locales, D-15: bleibt EN) ---
export const PRIVACY_POLICY_URL = "https://simonluthe.de/apps/statflow/datenschutz/";
export const SUPPORT_URL = "https://simonluthe.de/apps/statflow/";
```

Nach Erstellung: `cd app-store/submission && npx tsc --noEmit`
  </action>
  <verify>
    <automated>
test -f /Users/simonluthe/Documents/umami/app-store/submission/lib/config.ts && \
grep -q "APP_BUNDLE_ID.*de.godsapp.statflow" /Users/simonluthe/Documents/umami/app-store/submission/lib/config.ts && \
grep -q "fr-FR" /Users/simonluthe/Documents/umami/app-store/submission/lib/config.ts && \
grep -q "de-DE" /Users/simonluthe/Documents/umami/app-store/submission/lib/config.ts && \
grep -q "APP_IPHONE_67" /Users/simonluthe/Documents/umami/app-store/submission/lib/config.ts && \
grep -q "tableau de bord" /Users/simonluthe/Documents/umami/app-store/submission/lib/config.ts && \
cd /Users/simonluthe/Documents/umami/app-store/submission && npx tsc --noEmit 2>&1 && \
echo "OK: config.ts valide"
    </automated>
  </verify>
  <done>config.ts enthält alle METADATA für DE/EN/FR, SCREENSHOT_DISPLAY_TYPE=APP_IPHONE_67, screenshotPath()-Funktion, REVIEW_NOTES; npx tsc --noEmit gibt Exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ~/.claude/secrets.env → lib/jwt.ts | API-Key und .p8-Dateipfad via Env-Vars — nie im Repo |
| lib/asc-api.ts → api.appstoreconnect.apple.com | Nur über HTTPS mit JWT-Auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-09 | Information Disclosure | ASC API Key in config.ts | mitigate | Alle Secrets via Env-Vars, nie hardcoded (D-19); config.ts enthält keine Keys |
| T-23-10 | Elevation of Privilege | JWT Token Scope | accept | ASC API Keys sind auf App-Management begrenzt; kein Developer Account Zugriff |
| T-23-11 | Denial of Service | Rate-Limit 429 | mitigate | withRetry() mit Exponential-Backoff (max 5 Versuche) |
| T-23-12 | Spoofing | JWT Token-Ablauf | mitigate | Token-Cache prüft Restlaufzeit < 2 min → Regenerierung (Pitfall 3) |
</threat_model>

<verification>
1. `cd app-store/submission && npx tsc --noEmit` — Exit 0
2. `grep -c "fr-FR\|de-DE\|en-US" app-store/submission/lib/config.ts` — mindestens 3 Treffer
3. `bash app-store/validate.sh quick` — submission-Checks werden grüner
</verification>

<success_criteria>
- lib/jwt.ts, lib/asc-api.ts, lib/config.ts existieren und kompilieren ohne Fehler
- JWT-Token-Refresh-Logik implementiert (< 2 min Restlaufzeit → regenerieren)
- Retry-Logik für 429-Responses mit Exponential-Backoff
- Alle Metadaten für DE/EN/FR in config.ts vorhanden
- Keine Secrets im Code — alles via Env-Vars
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/23-screenshots-assets-submission-en-fr-de/23-05-SUMMARY.md` erstellen.
</output>
