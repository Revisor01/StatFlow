---
phase: 17-modale-account-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - InsightFlow/Views/Auth/LoginView.swift
  - InsightFlow/Views/Dashboard/DashboardView.swift
  - InsightFlow/Views/Admin/AdminSheets.swift
autonomous: false
requirements: [ACCT-01, ACCT-02, ACCT-03, MODAL-01]

must_haves:
  truths:
    - "Im Onboarding-Flow zeigt der Self-Hosted-Badge den lokalisierten Text, nicht den String-Key"
    - "AddAccountView zeigt einen Cloud/Self-Hosted-Selektor mit ServerTypeButton-Komponenten"
    - "AddAccountView hat einen X-Button in der Toolbar zum Schließen"
    - "Alle Admin-Sheet-Toolbar-Buttons zeigen nur Icons (xmark/checkmark) statt Text-Labels"
  artifacts:
    - path: "InsightFlow/Views/Auth/LoginView.swift"
      provides: "ACCT-01 Fix — String(localized:) statt rohem Key"
      contains: "String(localized: \"login.selfhosted.title\")"
    - path: "InsightFlow/Views/Dashboard/DashboardView.swift"
      provides: "ACCT-02 + ACCT-03 — serverType-State, ServerTypeButton-Sektion, X-Toolbar"
      contains: "@State private var serverType: ServerType"
    - path: "InsightFlow/Views/Admin/AdminSheets.swift"
      provides: "MODAL-01 — Icon-only Toolbar-Buttons in allen 8 Sheets"
      contains: "Image(systemName: \"xmark\")"
  key_links:
    - from: "AddAccountView"
      to: "ServerTypeButton"
      via: "serverType @State + ForEach(ServerType.allCases)"
      pattern: "ServerTypeButton"
    - from: "AddAccountView.addAccount()"
      to: "normalizedURL"
      via: "serverType == .cloud ? cloudURL : serverURL"
      pattern: "serverType == .cloud"
---

<objective>
Vier UI-Lücken im Account-Flow und den Admin-Modalen beheben: lokalisierten String-Bug im Onboarding, Self-Hosted/Cloud-Auswahl im Account-hinzufügen-Flow, X-Schließen-Button im Account-Modal, und Icon-only Toolbar-Buttons in allen Admin-Sheets.

Purpose: Vollständig und konsistent bedienbare Modale und Account-Flows gemäß iOS-Konventionen.
Output: Drei Quelldateien mit chirurgisch präzisen Fixes ohne neue Abhängigkeiten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-modale-account-flow/17-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ACCT-01 — Lokalisierten String-Bug in LoginView beheben</name>
  <files>InsightFlow/Views/Auth/LoginView.swift</files>
  <read_first>
    InsightFlow/Views/Auth/LoginView.swift Zeile 238–248 (credentialsSection-Badge)
  </read_first>
  <action>
    Zeile 242 in LoginView.swift: Den rohen String-Key durch einen korrekt lokalisierten Aufruf ersetzen.

    VORHER (Zeile 242):
    ```swift
    Text(serverType == .cloud ? selectedProvider.cloudURL : "login.selfhosted.title")
    ```

    NACHHER:
    ```swift
    Text(serverType == .cloud ? selectedProvider.cloudURL : String(localized: "login.selfhosted.title"))
    ```

    Nur diese eine Zeile ändern. Keine weiteren Änderungen in LoginView.swift.
  </action>
  <verify>
    <automated>xcodebuild build -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"login.selfhosted.title"' InsightFlow/Views/Auth/LoginView.swift` gibt keine Treffer zurück (roher Key entfernt)
    - `grep -n 'String(localized: "login.selfhosted.title")' InsightFlow/Views/Auth/LoginView.swift` gibt genau einen Treffer auf Zeile ~242 zurück
    - Build kompiliert ohne Fehler
  </acceptance_criteria>
  <done>LoginView Zeile 242 verwendet String(localized:) — kein roher String-Key mehr im Badge.</done>
</task>

<task type="auto">
  <name>Task 2: ACCT-02 + ACCT-03 — ServerType-Selektor und X-Button in AddAccountView</name>
  <files>InsightFlow/Views/Dashboard/DashboardView.swift</files>
  <read_first>
    InsightFlow/Views/Dashboard/DashboardView.swift Zeilen 497–701 (AddAccountView komplett)
    InsightFlow/Views/Auth/LoginView.swift Zeilen 451–495 (ServerTypeButton Komponente)
    InsightFlow/Views/Auth/LoginView.swift Zeilen 54–78 (ServerType enum)
    InsightFlow/Views/Auth/LoginView.swift Zeilen 182–208 (providerSelectionSection als Vorlage)
  </read_first>
  <action>
    AddAccountView in DashboardView.swift erhält zwei Ergänzungen:

    **ACCT-03: X-Button (Toolbar)**
    Nach `.navigationBarTitleDisplayMode(.inline)` (Zeile 639) das folgende Modifier-Block einfügen:
    ```swift
    .toolbar {
        ToolbarItem(placement: .cancellationAction) {
            Button {
                dismiss()
            } label: {
                Image(systemName: "xmark")
            }
        }
    }
    ```
    ACHTUNG: AddAccountView wird in SettingsView bereits in einem externen NavigationStack präsentiert (SettingsView.swift:53). Keinen eigenen NavigationStack in AddAccountView einbauen.

    **ACCT-02: ServerType-Selektor**
    1. State-Variable hinzufügen (nach bestehenden @State-Deklarationen, Zeile ~512):
    ```swift
    @State private var serverType: ServerType = .cloud
    ```

    2. Eine neue Sektion im body nach der Provider-Auswahl (nach Zeile ~539, dem Ende des Provider-HStack) einfügen:
    ```swift
    // Server Type Selection
    VStack(alignment: .leading, spacing: 12) {
        Text("login.serverType")
            .font(.subheadline)
            .fontWeight(.medium)
            .foregroundStyle(.secondary)
            .padding(.horizontal, 4)

        HStack(spacing: 12) {
            ForEach(ServerType.allCases, id: \.self) { type in
                ServerTypeButton(
                    type: type,
                    isSelected: serverType == type,
                    providerColor: selectedProvider == .umami ? Color.orange : Color.blue
                ) {
                    withAnimation(.spring(duration: 0.3)) {
                        serverType = type
                        if type == .cloud {
                            serverURL = selectedProvider == .umami
                                ? "https://cloud.umami.is"
                                : "https://plausible.io"
                        } else {
                            serverURL = ""
                        }
                    }
                }
            }
        }
    }
    ```

    3. Das URL-TextField (Zeile ~555–562) so umhüllen, dass es nur bei `.selfHosted` erscheint:
    ```swift
    if serverType == .selfHosted {
        TextField("account.add.serverURL", text: $serverURL)
            .textContentType(.URL)
            .textInputAutocapitalization(.never)
            .keyboardType(.URL)
            .padding()
            .background(Color(.secondarySystemGroupedBackground))
            .clipShape(RoundedRectangle(cornerRadius: 12))
    }
    ```

    4. `isFormValid` (Zeile 642–649) anpassen:
    ```swift
    private var isFormValid: Bool {
        let hasValidServer = serverType == .cloud || !serverURL.isEmpty
        if selectedProvider == .umami {
            return hasValidServer && !username.isEmpty && !password.isEmpty
        } else {
            return hasValidServer && !apiKey.isEmpty
        }
    }
    ```

    5. In `addAccount()` (Zeile ~656) die URL-Normalisierung für Cloud-Auswahl anpassen:
    ```swift
    var normalizedURL = serverType == .cloud
        ? (selectedProvider == .umami ? "https://cloud.umami.is" : "https://plausible.io")
        : serverURL.trimmingCharacters(in: .whitespacesAndNewlines)
    // Bestehende while/if-Prefix-Logik nur für selfHosted relevant — sie ist harmlos für bereits vollständige URLs,
    // daher bestehende Normalisierungszeilen direkt nach dieser Zeile belassen.
    ```
    WICHTIG: Pitfall vermeiden — ProviderSelectionButton (wählt Umami/Plausible) und ServerTypeButton (wählt Cloud/Self-Hosted) sind zwei verschiedene Komponenten. Nicht verwechseln.
  </action>
  <verify>
    <automated>xcodebuild build -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'serverType: ServerType' InsightFlow/Views/Dashboard/DashboardView.swift` gibt genau einen Treffer zurück
    - `grep -n 'ServerTypeButton' InsightFlow/Views/Dashboard/DashboardView.swift` gibt mindestens einen Treffer zurück
    - `grep -n 'Image(systemName: "xmark")' InsightFlow/Views/Dashboard/DashboardView.swift` gibt mindestens einen Treffer in AddAccountView zurück
    - `grep -n 'ToolbarItem(placement: .cancellationAction)' InsightFlow/Views/Dashboard/DashboardView.swift` gibt einen Treffer zurück
    - Build kompiliert ohne Fehler
  </acceptance_criteria>
  <done>AddAccountView hat ServerType-Selektor (Cloud/Self-Hosted) und X-Button in der Toolbar. URL-Feld erscheint nur für Self-Hosted. isFormValid prüft serverType.</done>
</task>

<task type="auto">
  <name>Task 3: MODAL-01 — Admin-Sheet-Toolbar-Buttons auf Icons umstellen</name>
  <files>InsightFlow/Views/Admin/AdminSheets.swift</files>
  <read_first>
    InsightFlow/Views/Admin/AdminSheets.swift komplett (alle 527 Zeilen)
  </read_first>
  <action>
    In AdminSheets.swift alle 8 Sheets auf Icon-only Toolbar-Buttons umstellen.

    Regel (aus Research):
    - Sheets mit Cancel+Confirm: `xmark` (cancellationAction) + `checkmark` (confirmationAction)
    - Sheets mit nur Done: `checkmark` (confirmationAction) — kein xmark

    **CreateWebsiteSheet** (Zeile 44–57):
    ```swift
    // VORHER
    Button("button.cancel") { dismiss() }
    // ...
    Button("button.create") { Task { await viewModel.createWebsite(name: name, domain: domain); dismiss() } }

    // NACHHER
    Button { dismiss() } label: { Image(systemName: "xmark") }
    // ...
    Button { Task { await viewModel.createWebsite(name: name, domain: domain); dismiss() } } label: { Image(systemName: "checkmark") }
    ```

    **CreateTeamSheet** (Zeile 78–92):
    ```swift
    // VORHER
    Button("button.cancel") { dismiss() }
    Button("button.create") { Task { await viewModel.createTeam(name: name); await viewModel.loadTeams(); dismiss() } }

    // NACHHER
    Button { dismiss() } label: { Image(systemName: "xmark") }
    Button { Task { await viewModel.createTeam(name: name); await viewModel.loadTeams(); dismiss() } } label: { Image(systemName: "checkmark") }
    ```

    **CreateUserSheet** (Zeile 127–139):
    ```swift
    // VORHER
    Button("button.cancel") { dismiss() }
    Button("button.create") { Task { await viewModel.createUser(username: username, password: password, role: role); dismiss() } }

    // NACHHER
    Button { dismiss() } label: { Image(systemName: "xmark") }
    Button { Task { await viewModel.createUser(username: username, password: password, role: role); dismiss() } } label: { Image(systemName: "checkmark") }
    ```

    **PlausibleTrackingCodeSheet** (Zeile 193–196, nur Done):
    ```swift
    // VORHER
    Button("button.done") { dismiss() }

    // NACHHER
    Button { dismiss() } label: { Image(systemName: "checkmark") }
    ```

    **TrackingCodeSheet** (Zeile 253–256, nur Done):
    ```swift
    // VORHER
    Button("button.done") { dismiss() }

    // NACHHER
    Button { dismiss() } label: { Image(systemName: "checkmark") }
    ```

    **ShareLinkSheet** (Zeile 353–356, nur Done):
    ```swift
    // VORHER
    Button("button.done") { dismiss() }

    // NACHHER
    Button { dismiss() } label: { Image(systemName: "checkmark") }
    ```

    **EditWebsiteSheet** (Zeile 403–417):
    ```swift
    // VORHER
    Button("button.cancel") { dismiss() }
    Button("button.save") { Task { isUpdating = true; await viewModel.updateWebsite(website, name: name); isUpdating = false; dismiss() } }

    // NACHHER
    Button { dismiss() } label: { Image(systemName: "xmark") }
    Button { Task { isUpdating = true; await viewModel.updateWebsite(website, name: name); isUpdating = false; dismiss() } } label: { Image(systemName: "checkmark") }
    ```

    **TeamMemberSheet** (Zeile 510–513, nur Done):
    ```swift
    // VORHER
    Button("button.done") { dismiss() }

    // NACHHER
    Button { dismiss() } label: { Image(systemName: "checkmark") }
    ```

    Alle .disabled()-Modifier auf Confirm-Buttons beibehalten. Keine anderen Änderungen an den Sheets vornehmen.
  </action>
  <verify>
    <automated>xcodebuild test -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "(PASS|FAIL|error:|BUILD)" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'Button("button.cancel")' InsightFlow/Views/Admin/AdminSheets.swift` gibt null Treffer zurück
    - `grep -n 'Button("button.create")' InsightFlow/Views/Admin/AdminSheets.swift` gibt null Treffer zurück
    - `grep -n 'Button("button.save")' InsightFlow/Views/Admin/AdminSheets.swift` gibt null Treffer zurück
    - `grep -n 'Button("button.done")' InsightFlow/Views/Admin/AdminSheets.swift` gibt null Treffer zurück
    - `grep -c 'Image(systemName: "xmark")' InsightFlow/Views/Admin/AdminSheets.swift` ergibt 4 (CreateWebsite, CreateTeam, CreateUser, EditWebsite)
    - `grep -c 'Image(systemName: "checkmark")' InsightFlow/Views/Admin/AdminSheets.swift` ergibt mindestens 8 (je ein checkmark pro Sheet, EditWebsite/CreateWebsite/CreateTeam/CreateUser haben jeweils schon checkmark für Copy-Button — also nur Toolbar-checkmarks zählen)
    - Build und Test-Suite bestehen ohne Fehler
  </acceptance_criteria>
  <done>Alle 8 Admin-Sheets verwenden Icon-only Toolbar-Buttons. Kein Text-Label mehr in Toolbar-Buttons von AdminSheets.swift.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manueller Simulator-Check — alle 4 Requirements verifizieren</name>
  <files>n/a</files>
  <action>Keine Code-Änderungen — manuelle visuelle Verifikation der in Task 1–3 implementierten Fixes im iOS-Simulator.</action>
  <what-built>
    - ACCT-01: LoginView zeigt lokalisierten Self-Hosted-Text im Badge
    - ACCT-02: AddAccountView hat Cloud/Self-Hosted-Selektor mit Buttons
    - ACCT-03: AddAccountView hat X-Button in der Toolbar
    - MODAL-01: Alle Admin-Sheets zeigen nur Icons in der Toolbar
  </what-built>
  <how-to-verify>
    1. App im Simulator starten (iPhone 16, iOS 17)
    2. ACCT-01: Onboarding durchklicken bis zum Login-Screen. Self-Hosted wählen — Badge muss lokalisierten Text anzeigen (DE: "Selbst gehostet" oder ähnlich), NICHT den rohen Key "login.selfhosted.title"
    3. ACCT-02 + ACCT-03: Settings öffnen → "Account hinzufügen" antippen. Modal öffnet sich. Prüfen: (a) X-Button oben links in der Toolbar vorhanden, (b) Sektion Cloud/Self-Hosted mit zwei Auswahl-Buttons vorhanden, (c) URL-Feld erscheint nur nach Antippen von "Self-Hosted"
    4. MODAL-01: Admin-Bereich öffnen → eine beliebige neue Website erstellen. Sheet öffnet sich — Toolbar zeigt nur Icons (X links, Häkchen rechts), kein Text. Dasselbe für Team, Benutzer und Website-Bearbeiten prüfen.
  </how-to-verify>
  <verify>
    <automated>MISSING — UI-only verification, kein automatisierter Test möglich (kein XCUITest-Target im Projekt)</automated>
  </verify>
  <resume-signal>Alle 4 Punkte bestätigt: "approved". Oder einzelne Issues beschreiben.</resume-signal>
  <done>Alle 4 Requirements visuell im Simulator bestätigt.</done>
</task>

</tasks>

<verification>
Build-Verifikation nach jedem Task:
```
xcodebuild build -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5
```

Abschluss-Verifikation (Test-Suite):
```
xcodebuild test -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "(PASS|FAIL|error:|BUILD)" | tail -10
```

Grep-Checks nach Abschluss aller Tasks:
- `grep -n 'String(localized: "login.selfhosted.title")' InsightFlow/Views/Auth/LoginView.swift` → 1 Treffer
- `grep -n 'serverType: ServerType' InsightFlow/Views/Dashboard/DashboardView.swift` → 1 Treffer
- `grep -n 'Image(systemName: "xmark")' InsightFlow/Views/Dashboard/DashboardView.swift` → ≥1 Treffer
- `grep -c 'Button("button.cancel")' InsightFlow/Views/Admin/AdminSheets.swift` → 0 Treffer
- `grep -c 'Button("button.done")' InsightFlow/Views/Admin/AdminSheets.swift` → 0 Treffer
</verification>

<success_criteria>
1. LoginView Zeile 242: String(localized:) wird verwendet — kein roher String-Key
2. AddAccountView: @State private var serverType: ServerType = .cloud vorhanden
3. AddAccountView: ServerTypeButton-Komponenten erscheinen nach Provider-Auswahl
4. AddAccountView: ToolbarItem(placement: .cancellationAction) mit xmark-Icon vorhanden
5. AdminSheets.swift: Kein Button mit Text-Label "button.cancel", "button.create", "button.save", "button.done" in Toolbar-Positionen
6. AdminSheets.swift: Alle Toolbar-Buttons verwenden Image(systemName:) Labels
7. Build kompiliert fehlerfrei
8. Bestehende Unit-Tests bestehen weiterhin
</success_criteria>

<output>
Nach Abschluss `.planning/phases/17-modale-account-flow/17-01-SUMMARY.md` erstellen.
</output>
