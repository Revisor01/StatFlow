---
phase: 16-ui-layout-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - InsightFlow/Views/Detail/WebsiteDetailSupportingViews.swift
  - InsightFlow/Views/Settings/SettingsView.swift
  - InsightFlow/Resources/de.lproj/Localizable.strings
  - InsightFlow/Resources/en.lproj/Localizable.strings
autonomous: false
requirements: [DASH-01, SET-01, SET-02, NOTIF-01]

must_haves:
  truths:
    - "Alle 4 Dashboard-Kacheln (Sessions, Vergleich, Events, Reports) sind gleich hoch"
    - "Bei 'Analytics einrichten' und 'Analytics Glossar' erscheint nur ein Chevron statt zwei"
    - "Das Dove-Icon ist im logoutSection-Footer sichtbar"
    - "Die Benachrichtigungs-Beschreibungstexte sind klar und natürlich formuliert"
  artifacts:
    - path: "InsightFlow/Views/Detail/WebsiteDetailSupportingViews.swift"
      provides: "QuickActionCard mit maxHeight: .infinity und alignment: .topLeading"
      contains: "maxHeight: .infinity"
    - path: "InsightFlow/Views/Settings/SettingsView.swift"
      provides: "aboutSection ohne manuelle Chevrons bei NavigationLink-Zeilen; dove.fill ohne doppeltes foregroundStyle"
      contains: "SetupGuideView"
    - path: "InsightFlow/Resources/de.lproj/Localizable.strings"
      provides: "Überarbeitete Notification-Strings auf Deutsch"
      contains: "Datenquelle"
    - path: "InsightFlow/Resources/en.lproj/Localizable.strings"
      provides: "Überarbeitete Notification-Strings auf Englisch"
      contains: "Data source"
  key_links:
    - from: "QuickActionCard body"
      to: "LazyVGrid in WebsiteDetailView"
      via: ".frame(maxHeight: .infinity) sorgt für gleichmäßige Zeilenhöhe"
      pattern: "maxHeight: \\.infinity"
    - from: "aboutSection NavigationLink labels"
      to: "List disclosure indicator"
      via: "Keine manuelle chevron.right Image mehr im Label"
      pattern: "SetupGuideView|AnalyticsGlossaryView"
    - from: "logoutSection footer dove.fill"
      to: "sichtbares Icon"
      via: "foregroundStyle(.secondary) entfernt — erbt vom HStack"
      pattern: "dove\\.fill"
---

<objective>
Vier visuelle Inkonsistenzen in StatFlow beheben: gleich hohe Dashboard-Kacheln, einfache Chevrons bei NavigationLink-Rows, sichtbares Dove-Icon, klarere Benachrichtigungstexte.

Purpose: Die App sieht poliert und konsistent aus — bereit für den App Store.
Output: Vier chirurgische Edits in drei bestehenden Dateien (plus en-Strings). Keine neuen Dateien.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Bestehende Code-Stellen, die der Executor direkt bearbeitet. Kein Codebase-Scanning nötig. -->

QuickActionCard (InsightFlow/Views/Detail/WebsiteDetailSupportingViews.swift, Zeile 206-237):
```swift
struct QuickActionCard: View {
    let icon: String
    let title: String
    let subtitle: String
    let color: Color

    var body: some View {
        VStack(alignment: .leading, spacing: 8) {
            // ...icon + chevron HStack...
            Text(title)...
            Text(subtitle)...
        }
        .padding()
        .frame(maxWidth: .infinity, alignment: .leading)   // <-- DASH-01: alignment + maxHeight fehlt
        .background(Color(.secondarySystemGroupedBackground))
        .clipShape(RoundedRectangle(cornerRadius: 16))
    }
}
```

aboutSection NavigationLinks (SettingsView.swift, Zeilen 302-324):
```swift
// SetupGuideView link (Zeile 302):
NavigationLink {
    SetupGuideView()
} label: {
    HStack {
        Label(String(localized: "setupGuide.settings.link"), systemImage: "doc.text.magnifyingglass")
        Spacer()
        Image(systemName: "chevron.right")   // <-- SET-01: ENTFERNEN (List fügt auto hinzu)
            .font(.caption)
            .foregroundStyle(.tertiary)
    }
}

// AnalyticsGlossaryView link (Zeile 314):
NavigationLink {
    AnalyticsGlossaryView()
} label: {
    HStack {
        Label(String(localized: "glossary.settings.link"), systemImage: "book.closed")
        Spacer()
        Image(systemName: "chevron.right")   // <-- SET-01: ENTFERNEN (List fügt auto hinzu)
            .font(.caption)
            .foregroundStyle(.tertiary)
    }
}

// ACHTUNG: Button "Einführung anzeigen" (Zeile 290) behält seinen manuellen Chevron!
// Button fügt KEINEN automatischen Disclosure-Indicator hinzu.
```

logoutSection footer (SettingsView.swift, Zeile 394-407):
```swift
} footer: {
    VStack(spacing: 4) {
        Text("StatFlow v...")
        HStack(spacing: 4) {
            Text("Made with")
            Image(systemName: "dove.fill")
                .foregroundStyle(.secondary)   // <-- SET-02: ENTFERNEN — doppelt mit parent
            Text("in Hennstedt")
        }
    }
    ...
}
```

Aktuelle Notification-Strings de.lproj (Zeilen 210-220):
```
"settings.notifications.time" = "Uhrzeit";
"settings.notifications.stats" = "Statistiken";
"settings.notifications.stats.auto" = "Automatisch";
"settings.notifications.stats.auto.description" = "Vor 12 Uhr → Statistiken von gestern\nAb 12 Uhr → Statistiken von heute";
"settings.notifications.frequency" = "Häufigkeit";
"settings.notifications.footer %@" = "Erhalte tägliche oder wöchentliche Zusammenfassungen deiner Website-Statistiken um %@.";
```

Aktuelle Notification-Strings en.lproj (Zeilen 210-220):
```
"settings.notifications.time" = "Time";
"settings.notifications.stats" = "Statistics";
"settings.notifications.stats.auto" = "Automatic";
"settings.notifications.stats.auto.description" = "Before 12 PM → Yesterday's stats\nAfter 12 PM → Today's stats";
"settings.notifications.frequency" = "Frequency";
"settings.notifications.footer %@" = "Receive daily or weekly summaries of your website statistics at %@.";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DASH-01 — QuickActionCard gleiche Höhe</name>
  <files>InsightFlow/Views/Detail/WebsiteDetailSupportingViews.swift</files>
  <read_first>
    - InsightFlow/Views/Detail/WebsiteDetailSupportingViews.swift (Zeilen 206-237, QuickActionCard body)
    - InsightFlow/Views/Detail/WebsiteDetailSupportingViews.swift (Zeilen 170-182, falls QuickActionCard im LazyVGrid gewrappt ist)
  </read_first>
  <action>
    In `QuickActionCard.body` die `.frame`-Zeile anpassen:

    VORHER:
    ```swift
    .frame(maxWidth: .infinity, alignment: .leading)
    ```

    NACHHER:
    ```swift
    .frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .topLeading)
    ```

    Sicherstellen, dass `.background(...)` und `.clipShape(...)` NACH dem `.frame(...)` stehen — sonst wächst der Hintergrund nicht mit (Pitfall 1 aus RESEARCH.md). Die aktuelle Reihenfolge (padding → frame → background → clipShape) ist bereits korrekt.

    Keine anderen Änderungen an der Datei.
  </action>
  <verify>
    `xcodebuild build -project /Users/simonluthe/Documents/umami/InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16 Pro' 2>&1 | tail -5`
    Erwartetes Ergebnis: BUILD SUCCEEDED, keine Warnings zu QuickActionCard.
  </verify>
  <acceptance_criteria>
    `grep -n "maxHeight: .infinity" /Users/simonluthe/Documents/umami/InsightFlow/Views/Detail/WebsiteDetailSupportingViews.swift` gibt genau eine Zeile aus (im QuickActionCard body).
    `grep -n "alignment: .topLeading" /Users/simonluthe/Documents/umami/InsightFlow/Views/Detail/WebsiteDetailSupportingViews.swift` gibt genau eine Zeile aus.
  </acceptance_criteria>
  <done>QuickActionCard hat `.frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .topLeading)`. Alle vier Kacheln dehnen sich in einer LazyVGrid-Zeile auf gleiche Höhe aus.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SET-01 + SET-02 — Doppelte Chevrons entfernen, Dove-Icon reparieren</name>
  <files>InsightFlow/Views/Settings/SettingsView.swift</files>
  <read_first>
    - InsightFlow/Views/Settings/SettingsView.swift (Zeilen 288-330, aboutSection)
    - InsightFlow/Views/Settings/SettingsView.swift (Zeilen 383-410, logoutSection footer)
  </read_first>
  <action>
    **SET-01: Doppelte Chevrons entfernen**

    In `aboutSection` (Zeilen 302-324): Bei den ZWEI `NavigationLink`-Blöcken (SetupGuideView und AnalyticsGlossaryView) je das innere `HStack { Label(...) Spacer() Image(systemName: "chevron.right") ... }` vereinfachen zu nur `Label(...)`:

    VORHER (SetupGuideView, Zeile 302):
    ```swift
    NavigationLink {
        SetupGuideView()
    } label: {
        HStack {
            Label(String(localized: "setupGuide.settings.link"), systemImage: "doc.text.magnifyingglass")
            Spacer()
            Image(systemName: "chevron.right")
                .font(.caption)
                .foregroundStyle(.tertiary)
        }
    }
    ```

    NACHHER:
    ```swift
    NavigationLink {
        SetupGuideView()
    } label: {
        Label(String(localized: "setupGuide.settings.link"), systemImage: "doc.text.magnifyingglass")
    }
    ```

    Identisch für AnalyticsGlossaryView (Zeile 314). Der `Button { showOnboarding = true }` (Zeile 290) bleibt UNVERÄNDERT — er ist ein Button, kein NavigationLink, und braucht den manuellen Chevron.

    **SET-02: Dove-Icon sichtbar machen**

    Im `logoutSection` footer (Zeile 399): `.foregroundStyle(.secondary)` vom `Image(systemName: "dove.fill")` entfernen. Das Icon erbt dann die Farbe des parent `HStack` (kein explizites foregroundStyle), was es auf dem Footer-Hintergrund sichtbar macht.

    VORHER:
    ```swift
    Image(systemName: "dove.fill")
        .foregroundStyle(.secondary)
    ```

    NACHHER:
    ```swift
    Image(systemName: "dove.fill")
    ```

    Falls das Icon nach dem Build im Simulator immer noch nicht erscheint (Symbol nicht gefunden), als Fallback `bird.fill` verwenden (iOS 16+, sicher im Deployment Target iOS 18).
  </action>
  <verify>
    `xcodebuild build -project /Users/simonluthe/Documents/umami/InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16 Pro' 2>&1 | tail -5`
    Erwartetes Ergebnis: BUILD SUCCEEDED.
  </verify>
  <acceptance_criteria>
    `grep -c 'Image(systemName: "chevron.right")' /Users/simonluthe/Documents/umami/InsightFlow/Views/Settings/SettingsView.swift` gibt 1 aus (nur noch der Button-Row-Chevron bleibt).
    `grep -A1 'dove.fill' /Users/simonluthe/Documents/umami/InsightFlow/Views/Settings/SettingsView.swift` enthält KEINE Zeile mit `.foregroundStyle(.secondary)` direkt danach.
  </acceptance_criteria>
  <done>SetupGuideView- und AnalyticsGlossaryView-NavigationLinks haben kein manuelles Chevron mehr. Dove-Icon hat kein redundantes foregroundStyle(.secondary) mehr.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: NOTIF-01 — Benachrichtigungs-Strings überarbeiten</name>
  <files>InsightFlow/Resources/de.lproj/Localizable.strings, InsightFlow/Resources/en.lproj/Localizable.strings</files>
  <read_first>
    - InsightFlow/Resources/de.lproj/Localizable.strings (Zeilen 205-225, Notification-Bereich)
    - InsightFlow/Resources/en.lproj/Localizable.strings (Zeilen 205-225, Notification-Bereich)
  </read_first>
  <action>
    **de.lproj/Localizable.strings — folgende Keys ersetzen:**

    ```
    // VORHER:
    "settings.notifications.stats" = "Statistiken";
    "settings.notifications.stats.auto.description" = "Vor 12 Uhr → Statistiken von gestern\nAb 12 Uhr → Statistiken von heute";
    "settings.notifications.footer %@" = "Erhalte tägliche oder wöchentliche Zusammenfassungen deiner Website-Statistiken um %@.";

    // NACHHER:
    "settings.notifications.stats" = "Datenquelle";
    "settings.notifications.stats.auto.description" = "Vor 12 Uhr: Statistiken von gestern. Ab 12 Uhr: Statistiken von heute.";
    "settings.notifications.footer %@" = "Täglich oder wöchentlich erhältst du eine Zusammenfassung deiner Website-Statistiken um %@.";
    ```

    Keys `settings.notifications.time`, `settings.notifications.stats.today`, `settings.notifications.stats.yesterday`, `settings.notifications.stats.auto`, `settings.notifications.frequency` bleiben unverändert.

    **en.lproj/Localizable.strings — entsprechende Keys ersetzen:**

    ```
    // VORHER:
    "settings.notifications.stats" = "Statistics";
    "settings.notifications.stats.auto.description" = "Before 12 PM → Yesterday's stats\nAfter 12 PM → Today's stats";
    "settings.notifications.footer %@" = "Receive daily or weekly summaries of your website statistics at %@.";

    // NACHHER:
    "settings.notifications.stats" = "Data source";
    "settings.notifications.stats.auto.description" = "Before 12 PM: Yesterday's stats. After 12 PM: Today's stats.";
    "settings.notifications.footer %@" = "You'll receive a daily or weekly summary of your website statistics at %@.";
    ```

    Beide Dateien müssen gespeichert werden. Kein anderer String darf verändert werden.
  </action>
  <verify>
    `xcodebuild build -project /Users/simonluthe/Documents/umami/InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16 Pro' 2>&1 | tail -5`
    Erwartetes Ergebnis: BUILD SUCCEEDED.
  </verify>
  <acceptance_criteria>
    `grep '"settings.notifications.stats"' /Users/simonluthe/Documents/umami/InsightFlow/Resources/de.lproj/Localizable.strings` gibt `"settings.notifications.stats" = "Datenquelle";` aus.
    `grep '"settings.notifications.stats"' /Users/simonluthe/Documents/umami/InsightFlow/Resources/en.lproj/Localizable.strings` gibt `"settings.notifications.stats" = "Data source";` aus.
    `grep 'Pfeil\|→' /Users/simonluthe/Documents/umami/InsightFlow/Resources/de.lproj/Localizable.strings` gibt nichts aus (keine Pfeil-Notation mehr).
  </acceptance_criteria>
  <done>Drei Notification-String-Keys in beiden Locales überarbeitet. Keine Pfeil-Notation mehr, natürliche Satzstellung, "Datenquelle" statt "Statistiken" als Picker-Label.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visuelle Verifikation im Simulator</name>
  <what-built>
    Vier UI-Fixes: gleiche Kachelhöhe, einfache Chevrons, sichtbares Dove-Icon, überarbeitete Notification-Strings.
  </what-built>
  <how-to-verify>
    1. App im iPhone 16 Pro Simulator starten (oder `xcodebuild` build + Simulator öffnen).
    2. **DASH-01**: Zur Website-Detailansicht navigieren. Die vier Kacheln (Sessions, Vergleich, Events, Reports) müssen exakt gleich hoch sein — Events-Kachel darf nicht mehr überragen.
    3. **SET-01**: Settings öffnen → "Über"-Sektion. Bei "Analytics einrichten" und "Analytics Glossar" darf nur ein Chevron sichtbar sein (der automatische Disclosure-Indicator der List). Der Button "Einführung anzeigen" hat weiterhin einen Chevron — das ist korrekt.
    4. **SET-02**: Im Settings-Footer ganz unten muss "Made with 🕊 in Hennstedt" sichtbar sein (Dove-Icon erscheint).
    5. **NOTIF-01**: Settings → Benachrichtigungen. Der Picker-Label lautet "Datenquelle" (DE) bzw. "Data source" (EN). Der automatische Footer-Text hat keine Pfeil-Notation mehr.
  </how-to-verify>
  <action>Alle drei vorherigen Tasks sind abgeschlossen. Keine Code-Aktion nötig — diese Task veranlasst den User zur visuellen Inspektion im Simulator.</action>
  <verify>User bestätigt alle vier Fixes visuell im Simulator.</verify>
  <done>User hat "approved" getippt oder alle vier Fixes sind als korrekt bestätigt.</done>
  <resume-signal>Tippe "approved" wenn alle vier Punkte korrekt aussehen, oder beschreibe konkrete Probleme.</resume-signal>
</task>

</tasks>

<verification>
Build muss fehlerfrei durchlaufen:
`xcodebuild build -project /Users/simonluthe/Documents/umami/InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16 Pro' 2>&1 | grep -E "BUILD|error:" | tail -10`

Grep-Checks (alle müssen passen):
- `grep -c 'maxHeight: .infinity' InsightFlow/Views/Detail/WebsiteDetailSupportingViews.swift` → 1
- `grep -c 'Image(systemName: "chevron.right")' InsightFlow/Views/Settings/SettingsView.swift` → 1
- `grep '"settings.notifications.stats" =' InsightFlow/Resources/de.lproj/Localizable.strings` → enthält "Datenquelle"
</verification>

<success_criteria>
1. QuickActionCard hat `.frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .topLeading)` — alle vier Dashboard-Kacheln gleich hoch
2. `aboutSection` NavigationLinks (SetupGuideView, AnalyticsGlossaryView) haben nur noch `Label(...)` als label — kein manueller Chevron mehr
3. `dove.fill` Image hat kein `.foregroundStyle(.secondary)` mehr
4. `settings.notifications.stats` = "Datenquelle" (DE) / "Data source" (EN)
5. `settings.notifications.stats.auto.description` verwendet Doppelpunkt statt Pfeil
6. Build succeeds ohne neue Fehler oder Warnings
7. Visuelle Inspektion durch User bestätigt alle vier Fixes (Task 4 checkpoint)
</success_criteria>

<output>
Nach Abschluss `.planning/phases/16-ui-layout-fixes/16-01-SUMMARY.md` erstellen mit:
- Was geändert wurde (Dateien + konkrete Änderungen)
- Welche Fixes bestätigt wurden (DASH-01, SET-01, SET-02, NOTIF-01)
- Build-Status
</output>
