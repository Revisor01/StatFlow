---
phase: 13-critical-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: ["00"]
files_modified:
  - InsightFlow/Services/AnalyticsCacheService.swift
  - InsightFlow/App/InsightFlowApp.swift
  - InsightFlow/Views/Dashboard/DashboardView.swift
  - InsightFlowTests/AnalyticsCacheServiceTests.swift
  - InsightFlowTests/DashboardViewModelTests.swift
autonomous: true
requirements: [FIX-03, FIX-04]

must_haves:
  truths:
    - "Cache-Eintraege aelter als 7 Tage werden beim App-Start geloescht"
    - "Cache-Groesse wird beim App-Start auf maximal 100MB reduziert"
    - "Account-Wechsel zeigt Loading-Indikator statt Flash alter Daten"
    - "Published properties (websites, stats, sparklineData, activeVisitors) werden vor Cache-Load geleert"
  artifacts:
    - path: "InsightFlow/Services/AnalyticsCacheService.swift"
      provides: "clearStaleEntries + evictOldestEntries Methoden"
      contains: "func clearStaleEntries"
    - path: "InsightFlow/App/InsightFlowApp.swift"
      provides: "Cache-Cleanup Aufruf beim App-Start"
      contains: "clearStaleEntries"
    - path: "InsightFlow/Views/Dashboard/DashboardView.swift"
      provides: "clearFirst Parameter bei Account-Wechsel"
      contains: "clearFirst"
  key_links:
    - from: "InsightFlowApp.swift"
      to: "AnalyticsCacheService.clearStaleEntries"
      via: "Task.detached in init()"
      pattern: "Task\\.detached.*clearStaleEntries"
    - from: "DashboardView.swift"
      to: "DashboardViewModel.loadData(clearFirst:)"
      via: "onReceive(.accountDidChange)"
      pattern: "clearFirst:\\s*true"
---

<objective>
Cache Lifecycle Management (FIX-03) und Account-Wechsel Loading State (FIX-04) implementieren.

Purpose: Zwei unabhaengige Bugs fixen — Cache waechst endlos (kein Cleanup beim Start, kein LRU) und Account-Wechsel flasht alte Daten statt Loading-State zu zeigen.
Output: Erweiterte AnalyticsCacheService mit Stale/LRU-Cleanup, App-Start-Integration, und Flash-freier Account-Wechsel im Dashboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-critical-bug-fixes/13-RESEARCH.md

@InsightFlow/Services/AnalyticsCacheService.swift
@InsightFlow/Views/Dashboard/DashboardView.swift
@InsightFlow/App/InsightFlowApp.swift
@InsightFlowTests/AnalyticsCacheServiceTests.swift
@InsightFlowTests/DashboardViewModelTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cache clearStaleEntries + evictOldestEntries mit Tests</name>
  <files>InsightFlow/Services/AnalyticsCacheService.swift, InsightFlowTests/AnalyticsCacheServiceTests.swift</files>
  <read_first>
    - InsightFlow/Services/AnalyticsCacheService.swift (vollstaendige aktuelle Implementierung)
    - InsightFlowTests/AnalyticsCacheServiceTests.swift (bestehendes Test-Setup, Helpers, Patterns)
  </read_first>
  <behavior>
    - Test: clearStaleEntries loescht JSON-Dateien deren cachedAt aelter als 7 Tage ist
    - Test: clearStaleEntries behaelt Dateien die juenger als 7 Tage sind
    - Test: evictOldestEntries loescht aelteste Dateien bis Cache unter maxSize (100MB simuliert mit kleinen Dateien)
    - Test: evictOldestEntries tut nichts wenn Cache bereits unter Limit
    - Test: cacheSize() gibt korrekte Groesse nach eviction zurueck
  </behavior>
  <action>
    1. In AnalyticsCacheServiceTests.swift ZUERST Tests schreiben:

    ```swift
    // MARK: - Stale Entry Cleanup Tests

    func testClearStaleEntriesRemovesOldEntries() {
        // Speichere einen Eintrag mit cachedAt 8 Tage in der Vergangenheit
        // Nutze den internen save-Mechanismus, dann manipuliere die JSON-Datei manuell:
        // Schreibe direkt eine JSON-Datei in tempDir mit cachedAt 8 Tage alt
        let oldDate = Date().addingTimeInterval(-8 * 86400)
        let wrapper = "{\"data\":{\"id\":\"1\",\"name\":\"Old\",\"domain\":\"old.com\",\"shareId\":null,\"provider\":\"umami\"},\"cachedAt\":\"\(ISO8601DateFormatter().string(from: oldDate))\",\"expiresAt\":\"\(ISO8601DateFormatter().string(from: oldDate.addingTimeInterval(3600)))\"}"
        let fileURL = tempDir.appendingPathComponent("stale_test.json")
        try! wrapper.data(using: .utf8)!.write(to: fileURL)

        sut.clearStaleEntries(olderThan: 7)

        XCTAssertFalse(FileManager.default.fileExists(atPath: fileURL.path))
    }

    func testClearStaleEntriesKeepsRecentEntries() {
        // Speichere einen frischen Eintrag (cachedAt = jetzt)
        sut.saveWebsites([makeCachedWebsite()], accountId: "keep-test")
        sut.clearStaleEntries(olderThan: 7)

        let result = sut.loadWebsites(accountId: "keep-test")
        XCTAssertNotNil(result)
    }
    ```

    Fuer evictOldestEntries Tests: Mehrere kleine Dateien mit unterschiedlichen contentModificationDate schreiben, evictOldestEntries(maxSize:) mit sehr kleinem maxSize aufrufen, pruefen dass aelteste weg sind.

    2. Tests muessen FEHLSCHLAGEN (RED) — Methoden existieren noch nicht.

    3. In AnalyticsCacheService.swift nach dem bestehenden `clearExpiredCache()` (Zeile 196) zwei neue Methoden hinzufuegen:

    ```swift
    /// Loescht Cache-Eintraege aelter als N Tage (basierend auf cachedAt, nicht expiresAt)
    func clearStaleEntries(olderThan days: Int = 7) {
        guard let cacheDir = cacheDirectory,
              let files = try? FileManager.default.contentsOfDirectory(
                  at: cacheDir, includingPropertiesForKeys: nil) else { return }
        let cutoff = Date().addingTimeInterval(-Double(days) * 86400)
        let decoder = JSONDecoder()
        decoder.dateDecodingStrategy = .iso8601
        var deletedCount = 0
        for fileURL in files where fileURL.pathExtension == "json" {
            if let data = try? Data(contentsOf: fileURL),
               let metadata = try? decoder.decode(CacheMetadata.self, from: data),
               metadata.cachedAt < cutoff {
                try? FileManager.default.removeItem(at: fileURL)
                deletedCount += 1
            }
        }
        #if DEBUG
        if deletedCount > 0 {
            print("AnalyticsCacheService: Cleared \(deletedCount) stale entries (>\(days) days)")
        }
        #endif
    }

    /// Loescht aelteste Cache-Dateien bis Gesamtgroesse unter maxSize liegt
    func evictOldestEntries(maxSize: Int64) {
        guard let cacheDir = cacheDirectory,
              let files = try? FileManager.default.contentsOfDirectory(
                  at: cacheDir,
                  includingPropertiesForKeys: [.contentModificationDateKey, .fileSizeKey]) else { return }

        // Sortiere nach Aenderungsdatum (aelteste zuerst)
        let sorted = files
            .filter { $0.pathExtension == "json" }
            .compactMap { url -> (URL, Date, Int64)? in
                guard let values = try? url.resourceValues(forKeys: [.contentModificationDateKey, .fileSizeKey]),
                      let date = values.contentModificationDate,
                      let size = values.fileSize else { return nil }
                return (url, date, Int64(size))
            }
            .sorted { $0.1 < $1.1 }

        var currentSize = cacheSize()
        var deletedCount = 0
        for (url, _, _) in sorted {
            guard currentSize > maxSize else { break }
            if let size = try? url.resourceValues(forKeys: [.fileSizeKey]).fileSize {
                try? FileManager.default.removeItem(at: url)
                currentSize -= Int64(size)
                deletedCount += 1
            }
        }
        #if DEBUG
        if deletedCount > 0 {
            print("AnalyticsCacheService: Evicted \(deletedCount) entries (cache was over \(maxSize) bytes)")
        }
        #endif
    }
    ```

    WICHTIG: `CacheMetadata` ist bereits als `private struct` definiert (Zeile 258). Da `clearStaleEntries` darauf zugreift und es in der gleichen Datei liegt, funktioniert der Zugriff.

    4. Tests erneut ausfuehren — muessen BESTEHEN (GREEN).
  </action>
  <verify>
    <automated>xcodebuild test -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:InsightFlowTests/AnalyticsCacheServiceTests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - AnalyticsCacheService.swift enthaelt `func clearStaleEntries(olderThan days: Int = 7)`
    - AnalyticsCacheService.swift enthaelt `func evictOldestEntries(maxSize: Int64)`
    - clearStaleEntries prueft `metadata.cachedAt < cutoff` (nicht expiresAt)
    - evictOldestEntries sortiert nach contentModificationDateKey und loescht aelteste zuerst
    - AnalyticsCacheServiceTests.swift enthaelt mindestens `testClearStaleEntries` und `testEvictOldestEntries` Methoden
    - Alle AnalyticsCacheServiceTests bestehen
  </acceptance_criteria>
  <done>clearStaleEntries loescht Eintraege aelter als 7 Tage, evictOldestEntries reduziert Cache auf maxSize, beide mit gruenen Tests bewiesen</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: App-Start Cache-Cleanup + Account-Wechsel Loading State mit Tests</name>
  <files>InsightFlow/App/InsightFlowApp.swift, InsightFlow/Views/Dashboard/DashboardView.swift, InsightFlowTests/DashboardViewModelTests.swift</files>
  <read_first>
    - InsightFlow/App/InsightFlowApp.swift (aktuelle init() Methode — derzeit nur registerBackgroundTasks())
    - InsightFlow/Views/Dashboard/DashboardView.swift (onReceive accountDidChange Handler Zeile 207, DashboardViewModel.loadData Zeile 873)
    - InsightFlowTests/DashboardViewModelTests.swift (Wave-0-Stub)
  </read_first>
  <behavior>
    - Test: loadData mit clearFirst=true setzt websites auf leeres Array
    - Test: loadData mit clearFirst=false behaelt bestehende websites
    - Test: loadData mit clearFirst=true setzt stats, sparklineData, activeVisitors auf leere Dicts
  </behavior>
  <action>
    **Tests ZUERST in DashboardViewModelTests.swift schreiben:**

    ```swift
    import XCTest
    @testable import InsightFlow

    @MainActor
    class DashboardViewModelTests: XCTestCase {

        // MARK: - FIX-04: Account-Wechsel Loading State

        func testLoadDataWithClearFirstResetsWebsites() async {
            let viewModel = DashboardViewModel()
            // Simuliere vorhandene Daten (direkt setzen da @Published)
            viewModel.websites = [makeTestWebsite()]

            XCTAssertFalse(viewModel.websites.isEmpty, "Precondition: websites nicht leer")

            // loadData mit clearFirst — websites muss sofort leer sein
            // Wir starten loadData und pruefen den Zustand nach dem Clear
            // Da loadData async ist und Netzwerk-Calls macht, nutzen wir einen kurzen Delay
            let task = Task {
                await viewModel.loadData(dateRange: .day, clearFirst: true)
            }

            // Kurz warten damit clearFirst ausgefuehrt wird
            try? await Task.sleep(nanoseconds: 50_000_000) // 0.05s

            XCTAssertTrue(viewModel.websites.isEmpty, "websites muss nach clearFirst leer sein")
            task.cancel()
        }

        func testLoadDataWithoutClearFirstKeepsWebsites() async {
            let viewModel = DashboardViewModel()
            let testSite = makeTestWebsite()
            viewModel.websites = [testSite]

            let task = Task {
                await viewModel.loadData(dateRange: .day, clearFirst: false)
            }

            try? await Task.sleep(nanoseconds: 50_000_000)

            // websites sollte nicht geleert worden sein (Cache koennte ueberschreiben, aber nicht sofort)
            // Hier testen wir nur dass clearFirst=false NICHT explizit leert
            task.cancel()
        }

        // MARK: - Helper

        private func makeTestWebsite() -> Website {
            Website(id: "test", name: "Test", domain: "test.com", shareId: nil, provider: .umami)
        }
    }
    ```

    HINWEIS: Helper-Methode an tatsaechliche Website-Struct anpassen. DashboardViewModel() Init pruefen.

    Tests muessen FEHLSCHLAGEN (RED) — `clearFirst` Parameter existiert noch nicht.

    **Dann Implementation:**

    1. In InsightFlowApp.swift (PrivacyFlowApp struct) die init() Methode erweitern — nach `registerBackgroundTasks()` einen Background-Task fuer Cache-Cleanup hinzufuegen:

    ```swift
    init() {
        registerBackgroundTasks()
        // Cache-Cleanup im Hintergrund beim App-Start (FIX-03)
        Task.detached(priority: .background) {
            AnalyticsCacheService.shared.clearStaleEntries(olderThan: 7)
            let maxCacheSize: Int64 = 100 * 1024 * 1024 // 100MB
            if AnalyticsCacheService.shared.cacheSize() > maxCacheSize {
                AnalyticsCacheService.shared.evictOldestEntries(maxSize: maxCacheSize)
            }
        }
    }
    ```

    2. In DashboardView.swift den DashboardViewModel.loadData erweitern — neuen `clearFirst` Parameter hinzufuegen.

    In der DashboardViewModel Klasse (Zeile 747), die loadData Methode (Zeile 873) aendern:

    VON:
    ```swift
    func loadData(dateRange: DateRange) async {
        isLoading = true
        currentDateRange = dateRange
        isOffline = false
        loadWebsiteOrder()
        loadFromCache(dateRange: dateRange)
    ```

    ZU:
    ```swift
    func loadData(dateRange: DateRange, clearFirst: Bool = false) async {
        if clearFirst {
            websites = []
            stats = [:]
            sparklineData = [:]
            activeVisitors = [:]
        }
        isLoading = true
        currentDateRange = dateRange
        isOffline = false
        loadWebsiteOrder()
        loadFromCache(dateRange: dateRange)
    ```

    3. In DashboardView.swift den onReceive Handler (Zeile 207-214) aendern:

    VON:
    ```swift
    .onReceive(NotificationCenter.default.publisher(for: .accountDidChange)) { _ in
        if !showAllAccounts {
            Task {
                await viewModel.loadData(dateRange: selectedDateRange)
            }
        }
    }
    ```

    ZU:
    ```swift
    .onReceive(NotificationCenter.default.publisher(for: .accountDidChange)) { _ in
        if !showAllAccounts {
            Task {
                await viewModel.loadData(dateRange: selectedDateRange, clearFirst: true)
            }
        }
    }
    ```

    4. Tests erneut ausfuehren — muessen BESTEHEN (GREEN).
  </action>
  <verify>
    <automated>xcodebuild test -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:InsightFlowTests/DashboardViewModelTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - InsightFlowApp.swift init() enthaelt `Task.detached(priority: .background)` mit `clearStaleEntries(olderThan: 7)`
    - InsightFlowApp.swift init() enthaelt `evictOldestEntries(maxSize: 100 * 1024 * 1024)` innerhalb einer cacheSize()-Pruefung
    - DashboardViewModel.loadData hat Signatur `func loadData(dateRange: DateRange, clearFirst: Bool = false)`
    - DashboardViewModel.loadData setzt `websites = []`, `stats = [:]`, `sparklineData = [:]`, `activeVisitors = [:]` wenn clearFirst == true
    - onReceive(.accountDidChange) Handler ruft `loadData(dateRange: selectedDateRange, clearFirst: true)` auf
    - DashboardViewModelTests verifizieren clearFirst-Verhalten
    - Projekt kompiliert fehlerfrei
  </acceptance_criteria>
  <done>Cache wird beim App-Start bereinigt (Stale >7d + LRU >100MB). Account-Wechsel zeigt ProgressView statt Flash alter Daten weil Published properties geleert werden bevor Cache geladen wird. Tests bestaetigen clearFirst-Verhalten.</done>
</task>

</tasks>

<verification>
1. Build erfolgreich: `xcodebuild build -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16'`
2. Cache-Tests bestehen: `xcodebuild test -scheme InsightFlow -only-testing:InsightFlowTests/AnalyticsCacheServiceTests`
3. DashboardViewModel-Tests bestehen: `xcodebuild test -scheme InsightFlow -only-testing:InsightFlowTests/DashboardViewModelTests`
4. Grep-Checks:
   - `grep -n "clearStaleEntries" InsightFlow/Services/AnalyticsCacheService.swift` zeigt Methodendefinition
   - `grep -n "evictOldestEntries" InsightFlow/Services/AnalyticsCacheService.swift` zeigt Methodendefinition
   - `grep -n "clearFirst" InsightFlow/Views/Dashboard/DashboardView.swift` zeigt Parameter-Nutzung
   - `grep -n "clearStaleEntries" InsightFlow/App/InsightFlowApp.swift` zeigt App-Start-Aufruf
</verification>

<success_criteria>
- FIX-03: clearStaleEntries und evictOldestEntries existieren und sind getestet
- FIX-03: App-Start ruft beide Methoden auf einem Background-Thread auf
- FIX-04: Account-Wechsel leert Published properties VOR Cache-Load
- FIX-04: ProgressView wird sichtbar (da websites.isEmpty && isLoading)
- FIX-04: DashboardViewModelTests bestaetigen clearFirst-Verhalten
- Keine Regressions — bestehende Tests bestehen weiterhin
</success_criteria>

<output>
After completion, create `.planning/phases/13-critical-bug-fixes/13-01-SUMMARY.md`
</output>
