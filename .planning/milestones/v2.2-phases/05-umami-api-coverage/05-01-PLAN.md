---
phase: 05-umami-api-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - InsightFlow/Models/Stats.swift
  - InsightFlow/Models/Events.swift
  - InsightFlow/Models/Reports.swift
  - InsightFlow/Models/Share.swift
autonomous: true
requirements: [API-01]

must_haves:
  truths:
    - "Alle fehlenden Umami-API-Response-Modelle sind als Codable Structs definiert"
    - "Bestehende Modelle in Stats.swift sind um fehlende Endpunkt-Antworten erweitert"
    - "Neue Report-Typen (Funnel, UTM, Goal, Attribution, Performance, Breakdown, Revenue) haben Response-Modelle"
  artifacts:
    - path: "InsightFlow/Models/Events.swift"
      provides: "Event-Data und Events response models"
      contains: "struct EventDataResponse"
    - path: "InsightFlow/Models/Reports.swift"
      provides: "Report CRUD und spezialisierte Report-Response-Modelle"
      contains: "struct FunnelStep"
    - path: "InsightFlow/Models/Share.swift"
      provides: "Share-Page response models"
      contains: "struct SharePage"
    - path: "InsightFlow/Models/Stats.swift"
      provides: "Erweiterte Stats-Modelle (DateRange, ExpandedMetric, SessionStats, Weekly)"
      contains: "struct DateRangeResponse"
  key_links:
    - from: "InsightFlow/Models/Events.swift"
      to: "InsightFlow/Services/UmamiAPI.swift"
      via: "decoder.decode calls in Plan 02"
      pattern: "Codable.*Sendable"
    - from: "InsightFlow/Models/Reports.swift"
      to: "InsightFlow/Services/UmamiAPI.swift"
      via: "decoder.decode calls in Plan 03"
      pattern: "Codable.*Sendable"
---

<objective>
Response-Modelle fuer alle fehlenden Umami-API-Endpunkte erstellen.

Purpose: Bevor die eigentlichen API-Methoden implementiert werden, muessen alle Response-Modelle existieren. Das vermeidet Hin-und-Her zwischen Model- und API-Dateien.
Output: 3 neue Model-Dateien + Erweiterung von Stats.swift mit allen Codable Structs fuer die fehlenden ~50 Endpunkte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@InsightFlow/Models/Stats.swift
@InsightFlow/Models/Admin.swift
@InsightFlow/Models/Website.swift
@InsightFlow/Models/DateRange.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stats.swift erweitern + Events.swift erstellen</name>
  <files>InsightFlow/Models/Stats.swift, InsightFlow/Models/Events.swift</files>
  <read_first>InsightFlow/Models/Stats.swift, InsightFlow/Models/Admin.swift</read_first>
  <action>
**Stats.swift erweitern** — Folgende Structs am Ende der Datei (vor dem letzten Kommentar-Block oder nach RetentionRow) hinzufuegen:

1. `DateRangeResponse` fuer GET /api/websites/:id/daterange:
```swift
struct DateRangeResponse: Codable, Sendable {
    let startDate: String
    let endDate: String
}
```

2. `ExpandedMetricItem` fuer GET /api/websites/:id/metrics/expanded:
```swift
struct ExpandedMetricItem: Codable, Identifiable, Sendable {
    let name: String
    let pageviews: Int
    let visitors: Int
    let visits: Int
    let bounces: Int
    let totaltime: Int

    var id: String { name }

    var bounceRate: Double {
        guard visits > 0 else { return 0 }
        return Double(bounces) / Double(visits) * 100
    }

    var avgTime: TimeInterval {
        guard visits > 0 else { return 0 }
        return Double(totaltime) / Double(visits)
    }
}
```

3. `SessionStatsResponse` fuer GET /api/websites/:id/sessions/stats:
```swift
struct SessionStatsResponse: Codable, Sendable {
    let visitors: Int
    let visits: Int
    let pageviews: Int
    let bounces: Int
    let totaltime: Int
    let comparison: StatsComparison?
}
```

4. `WeeklySessionPoint` fuer GET /api/websites/:id/sessions/weekly:
```swift
struct WeeklySessionPoint: Codable, Identifiable, Sendable {
    let day: Int     // 0=Sunday .. 6=Saturday
    let hour: Int    // 0-23
    let count: Int

    var id: String { "\(day)-\(hour)" }
}
```

5. `SessionProperties` fuer GET /api/websites/:id/sessions/:id/properties:
```swift
struct SessionPropertyItem: Codable, Identifiable, Sendable {
    let propertyName: String
    let dataType: Int
    let value: String
    let total: Int

    var id: String { "\(propertyName)-\(value)" }
}
```

6. `SessionDataProperty` und `SessionDataValue` fuer session-data endpoints:
```swift
struct SessionDataProperty: Codable, Identifiable, Sendable {
    let propertyName: String
    let dataType: Int
    let total: Int

    var id: String { propertyName }
}

struct SessionDataValue: Codable, Identifiable, Sendable {
    let value: String
    let total: Int

    var id: String { value }
}
```

7. `EventsResponse` fuer GET /api/websites/:id/events:
```swift
struct EventsResponse: Codable, Sendable {
    let data: [EventDetail]
    let count: Int
    let page: Int
    let pageSize: Int
}

struct EventDetail: Codable, Identifiable, Sendable {
    let id: String
    let websiteId: String
    let sessionId: String
    let eventName: String?
    let urlPath: String?
    let createdAt: String

    var createdDate: Date {
        let formatter = ISO8601DateFormatter()
        formatter.formatOptions = [.withInternetDateTime, .withFractionalSeconds]
        return formatter.date(from: createdAt) ?? Date()
    }
}
```

8. `EventStatsResponse`:
```swift
struct EventStatsResponse: Codable, Sendable {
    let events: Int
    let properties: Int
    let records: Int
    let comparison: EventStatsComparison?
}

struct EventStatsComparison: Codable, Sendable {
    let events: Int
    let properties: Int
    let records: Int
}
```

**Events.swift erstellen** — Neue Datei mit allen Event-Data response models:

```swift
import Foundation

// MARK: - Event Data Models

struct EventDataResponse: Codable, Sendable {
    let data: [EventDataItem]
    let count: Int
    let page: Int
    let pageSize: Int
}

struct EventDataItem: Codable, Identifiable, Sendable {
    let id: String
    let websiteId: String
    let eventName: String
    let propertyName: String?
    let dataType: Int?
    let value: String?
    let createdAt: String
}

struct EventDataEvent: Codable, Identifiable, Sendable {
    let eventName: String
    let propertyName: String?
    let total: Int

    var id: String { "\(eventName)-\(propertyName ?? "")" }
}

struct EventDataField: Codable, Identifiable, Sendable {
    let propertyName: String
    let dataType: Int
    let total: Int

    var id: String { propertyName }
}

struct EventDataProperty: Codable, Identifiable, Sendable {
    let eventName: String
    let propertyName: String
    let total: Int

    var id: String { "\(eventName)-\(propertyName)" }
}

struct EventDataValue: Codable, Identifiable, Sendable {
    let value: String
    let total: Int

    var id: String { value }
}

struct EventDataStats: Codable, Sendable {
    let events: Int
    let properties: Int
    let records: Int
}
```

Alle Structs muessen `Codable, Sendable` sein (actor-basierte API). Identifiable wo sinnvoll fuer SwiftUI-Listen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "struct" InsightFlow/Models/Stats.swift` ergibt mindestens 18 (bestehende + neue)
- `grep -c "struct" InsightFlow/Models/Events.swift` ergibt mindestens 6
- `grep "DateRangeResponse" InsightFlow/Models/Stats.swift` findet einen Treffer
- `grep "EventDataResponse" InsightFlow/Models/Events.swift` findet einen Treffer
- `grep "ExpandedMetricItem" InsightFlow/Models/Stats.swift` findet einen Treffer
- `grep "WeeklySessionPoint" InsightFlow/Models/Stats.swift` findet einen Treffer
- Xcode Build erfolgreich (keine Compile-Fehler)
  </acceptance_criteria>
  <done>Alle Stats-, Session- und Event-Response-Modelle existieren als compilierbare Codable Structs</done>
</task>

<task type="auto">
  <name>Task 2: Reports.swift + Share.swift erstellen</name>
  <files>InsightFlow/Models/Reports.swift, InsightFlow/Models/Share.swift</files>
  <read_first>InsightFlow/Models/Admin.swift, InsightFlow/Models/Stats.swift</read_first>
  <action>
**Reports.swift erstellen** — Neue Datei mit Report-CRUD und spezialisierten Report-Response-Modellen:

```swift
import Foundation

// MARK: - Report CRUD Models

struct ReportListResponse: Codable, Sendable {
    let data: [Report]
    let count: Int
    let page: Int
    let pageSize: Int
}

struct Report: Codable, Identifiable, Sendable {
    let id: String
    let userId: String
    let websiteId: String
    let type: String
    let name: String
    let description: String?
    let parameters: String?  // JSON string
    let createdAt: String
    let updatedAt: String?
}

// MARK: - Funnel Report

struct FunnelStep: Codable, Identifiable, Sendable {
    let type: String       // "path" or "event"
    let value: String
    let visitors: Int
    let dropoff: Int

    var id: String { "\(type)-\(value)" }

    var dropoffRate: Double {
        guard visitors + dropoff > 0 else { return 0 }
        return Double(dropoff) / Double(visitors + dropoff) * 100
    }
}

// MARK: - UTM Report

struct UTMReportItem: Codable, Identifiable, Sendable {
    let source: String?
    let medium: String?
    let campaign: String?
    let content: String?
    let term: String?
    let visitors: Int

    var id: String { "\(source ?? "")-\(medium ?? "")-\(campaign ?? "")" }
}

// MARK: - Goal Report

struct GoalReportItem: Codable, Identifiable, Sendable {
    let type: String       // "path" or "event"
    let value: String
    let goal: Int
    let result: Int

    var id: String { "\(type)-\(value)" }

    var completionRate: Double {
        guard goal > 0 else { return 0 }
        return Double(result) / Double(goal) * 100
    }
}

// MARK: - Attribution Report

struct AttributionItem: Codable, Identifiable, Sendable {
    let source: String?
    let medium: String?
    let campaign: String?
    let channel: String?
    let visitors: Int

    var id: String { "\(source ?? "")-\(channel ?? "")" }
}

// MARK: - Performance Report (Core Web Vitals)

struct PerformanceItem: Codable, Identifiable, Sendable {
    let path: String?
    let lcp: Double?       // Largest Contentful Paint (ms)
    let inp: Double?       // Interaction to Next Paint (ms)
    let cls: Double?       // Cumulative Layout Shift
    let fcp: Double?       // First Contentful Paint (ms)
    let ttfb: Double?      // Time to First Byte (ms)

    var id: String { path ?? UUID().uuidString }
}

// MARK: - Breakdown Report

struct BreakdownItem: Codable, Identifiable, Sendable {
    let name: String
    let pageviews: Int?
    let visitors: Int?
    let visits: Int?
    let bounces: Int?
    let totaltime: Int?

    var id: String { name }
}

// MARK: - Revenue Report

struct RevenueItem: Codable, Identifiable, Sendable {
    let currency: String
    let total: Double
    let count: Int
    let uniqueCount: Int
    let comparison: RevenueComparison?

    var id: String { currency }
}

struct RevenueComparison: Codable, Sendable {
    let total: Double
    let count: Int
    let uniqueCount: Int
}
```

**Share.swift erstellen** — Neue Datei:

```swift
import Foundation

// MARK: - Share Page Models

struct SharePage: Codable, Identifiable, Sendable {
    let id: String
    let entityId: String
    let shareType: Int     // 1=website, 2=link, 3=pixel, 4=board
    let name: String
    let slug: String
    let parameters: String?  // JSON string
    let createdAt: String
    let updatedAt: String?
}

struct ShareListResponse: Codable, Sendable {
    let data: [SharePage]
    let count: Int
}

// MARK: - Me (Current User) Models

struct MeResponse: Codable, Sendable {
    let id: String
    let username: String
    let role: String
    let createdAt: String?
}
```

Alle Structs: `Codable, Sendable`, `Identifiable` wo fuer SwiftUI-Listen sinnvoll.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "struct" InsightFlow/Models/Reports.swift` ergibt mindestens 10
- `grep -c "struct" InsightFlow/Models/Share.swift` ergibt mindestens 3
- `grep "FunnelStep" InsightFlow/Models/Reports.swift` findet einen Treffer
- `grep "PerformanceItem" InsightFlow/Models/Reports.swift` findet einen Treffer
- `grep "SharePage" InsightFlow/Models/Share.swift` findet einen Treffer
- `grep "MeResponse" InsightFlow/Models/Share.swift` findet einen Treffer
- Xcode Build erfolgreich
  </acceptance_criteria>
  <done>Alle Report-, Share- und Me-Response-Modelle existieren als compilierbare Codable Structs</done>
</task>

</tasks>

<verification>
- Xcode Build laeuft durch ohne Fehler
- Alle neuen Model-Dateien sind im Xcode-Projekt referenziert
- Jeder geplante Endpunkt in Plan 02-04 hat ein passendes Response-Modell
</verification>

<success_criteria>
- 3 neue Model-Dateien erstellt (Events.swift, Reports.swift, Share.swift)
- Stats.swift um ~8 neue Structs erweitert
- Alle Structs sind Codable + Sendable
- Projekt kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/05-umami-api-coverage/05-01-SUMMARY.md`
</output>
