---
phase: 05-umami-api-coverage
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - InsightFlow/Services/UmamiAPI.swift
autonomous: true
requirements: [API-01]

must_haves:
  truths:
    - "UmamiAPI hat Methoden fuer alle 8 spezialisierten Report-Endpunkte (funnel, utm, goal, attribution, performance, breakdown, revenue)"
    - "UmamiAPI hat Methoden fuer Report-CRUD (list, create, get, update, delete)"
    - "Alle Report-Methoden verwenden POST mit websiteId, type, filters und parameters im Body"
  artifacts:
    - path: "InsightFlow/Services/UmamiAPI.swift"
      provides: "Report-API-Methoden"
      contains: "func getFunnelReport"
  key_links:
    - from: "InsightFlow/Services/UmamiAPI.swift"
      to: "InsightFlow/Models/Reports.swift"
      via: "decoder.decode(FunnelStep.self)"
      pattern: "FunnelStep"
---

<objective>
Alle Report-Endpunkte in UmamiAPI.swift implementieren: CRUD + 8 spezialisierte Report-Typen.

Purpose: Reports sind das analytische Herzstuck von Umami — Funnels, UTM-Tracking, Goals, Attribution, Core Web Vitals, Breakdown, Revenue.
Output: ~13 neue Methoden fuer Report-Endpunkte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-umami-api-coverage/05-01-SUMMARY.md

@InsightFlow/Services/UmamiAPI.swift
@InsightFlow/Models/Reports.swift
</context>

<interfaces>
<!-- Report models from Plan 01 -->
From InsightFlow/Models/Reports.swift:
- ReportListResponse { data: [Report], count, page, pageSize }
- Report { id, userId, websiteId, type, name, description, parameters, createdAt, updatedAt }
- FunnelStep { type, value, visitors, dropoff }
- UTMReportItem { source, medium, campaign, content, term, visitors }
- GoalReportItem { type, value, goal, result }
- AttributionItem { source, medium, campaign, channel, visitors }
- PerformanceItem { path, lcp, inp, cls, fcp, ttfb }
- BreakdownItem { name, pageviews, visitors, visits, bounces, totaltime }
- RevenueItem { currency, total, count, uniqueCount, comparison }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Report CRUD + spezialisierte Report-Methoden</name>
  <files>InsightFlow/Services/UmamiAPI.swift</files>
  <read_first>InsightFlow/Services/UmamiAPI.swift, InsightFlow/Models/Reports.swift</read_first>
  <action>
In UmamiAPI.swift die bestehende "// MARK: - Reports" Section erweitern. Bereits vorhanden: `getRetention` und `getJourneyReport`. Folgende Methoden NACH den bestehenden Report-Methoden einfuegen:

**Report CRUD:**

```swift
func getReports(websiteId: String, page: Int = 1, pageSize: Int = 20) async throws -> ReportListResponse {
    let data = try await request(
        endpoint: "api/reports",
        queryItems: [
            URLQueryItem(name: "websiteId", value: websiteId),
            URLQueryItem(name: "page", value: String(page)),
            URLQueryItem(name: "pageSize", value: String(pageSize))
        ]
    )
    return try decoder.decode(ReportListResponse.self, from: data)
}

func createReport(websiteId: String, type: String, name: String, description: String? = nil, parameters: [String: Any] = [:]) async throws -> Report {
    var body: [String: Any] = [
        "websiteId": websiteId,
        "type": type,
        "name": name
    ]
    if let description = description { body["description"] = description }
    if !parameters.isEmpty { body["parameters"] = parameters }

    let data = try await postRequest(endpoint: "api/reports", body: body)
    return try decoder.decode(Report.self, from: data)
}

func getReport(reportId: String) async throws -> Report {
    let data = try await request(endpoint: "api/reports/\(reportId)")
    return try decoder.decode(Report.self, from: data)
}

func updateReport(reportId: String, name: String? = nil, description: String? = nil, parameters: [String: Any]? = nil) async throws -> Report {
    var body: [String: Any] = [:]
    if let name = name { body["name"] = name }
    if let description = description { body["description"] = description }
    if let parameters = parameters { body["parameters"] = parameters }

    let data = try await postRequest(endpoint: "api/reports/\(reportId)", body: body)
    return try decoder.decode(Report.self, from: data)
}

func deleteReport(reportId: String) async throws {
    _ = try await deleteRequest(endpoint: "api/reports/\(reportId)")
}
```

**Spezialisierte Report-Endpunkte** — alle folgen gleichem Pattern wie bestehender `getRetention`:

```swift
func getFunnelReport(websiteId: String, dateRange: DateRange, steps: [[String: String]]) async throws -> [FunnelStep] {
    let formatter = DateFormatter()
    formatter.dateFormat = "yyyy-MM-dd"
    let dates = dateRange.dates

    let body: [String: Any] = [
        "websiteId": websiteId,
        "type": "funnel",
        "filters": [:],
        "parameters": [
            "startDate": formatter.string(from: dates.start),
            "endDate": formatter.string(from: dates.end),
            "steps": steps  // e.g. [["type": "path", "value": "/"], ["type": "path", "value": "/signup"]]
        ]
    ]

    let data = try await postRequest(endpoint: "api/reports/funnel", body: body)
    return try decoder.decode([FunnelStep].self, from: data)
}

func getUTMReport(websiteId: String, dateRange: DateRange) async throws -> [UTMReportItem] {
    let formatter = DateFormatter()
    formatter.dateFormat = "yyyy-MM-dd"
    let dates = dateRange.dates

    let body: [String: Any] = [
        "websiteId": websiteId,
        "type": "utm",
        "filters": [:],
        "parameters": [
            "startDate": formatter.string(from: dates.start),
            "endDate": formatter.string(from: dates.end)
        ]
    ]

    let data = try await postRequest(endpoint: "api/reports/utm", body: body)
    return try decoder.decode([UTMReportItem].self, from: data)
}

func getGoalReport(websiteId: String, dateRange: DateRange, goals: [[String: Any]]) async throws -> [GoalReportItem] {
    let formatter = DateFormatter()
    formatter.dateFormat = "yyyy-MM-dd"
    let dates = dateRange.dates

    let body: [String: Any] = [
        "websiteId": websiteId,
        "type": "goal",
        "filters": [:],
        "parameters": [
            "startDate": formatter.string(from: dates.start),
            "endDate": formatter.string(from: dates.end),
            "goals": goals  // e.g. [["type": "path", "value": "/thank-you", "goal": 100]]
        ]
    ]

    let data = try await postRequest(endpoint: "api/reports/goal", body: body)
    return try decoder.decode([GoalReportItem].self, from: data)
}

func getAttributionReport(websiteId: String, dateRange: DateRange) async throws -> [AttributionItem] {
    let formatter = DateFormatter()
    formatter.dateFormat = "yyyy-MM-dd"
    let dates = dateRange.dates

    let body: [String: Any] = [
        "websiteId": websiteId,
        "type": "attribution",
        "filters": [:],
        "parameters": [
            "startDate": formatter.string(from: dates.start),
            "endDate": formatter.string(from: dates.end)
        ]
    ]

    let data = try await postRequest(endpoint: "api/reports/attribution", body: body)
    return try decoder.decode([AttributionItem].self, from: data)
}

func getPerformanceReport(websiteId: String, dateRange: DateRange) async throws -> [PerformanceItem] {
    let formatter = DateFormatter()
    formatter.dateFormat = "yyyy-MM-dd"
    let dates = dateRange.dates

    let body: [String: Any] = [
        "websiteId": websiteId,
        "type": "performance",
        "filters": [:],
        "parameters": [
            "startDate": formatter.string(from: dates.start),
            "endDate": formatter.string(from: dates.end)
        ]
    ]

    let data = try await postRequest(endpoint: "api/reports/performance", body: body)
    return try decoder.decode([PerformanceItem].self, from: data)
}

func getBreakdownReport(websiteId: String, dateRange: DateRange, property: String) async throws -> [BreakdownItem] {
    let formatter = DateFormatter()
    formatter.dateFormat = "yyyy-MM-dd"
    let dates = dateRange.dates

    let body: [String: Any] = [
        "websiteId": websiteId,
        "type": "breakdown",
        "filters": [:],
        "parameters": [
            "startDate": formatter.string(from: dates.start),
            "endDate": formatter.string(from: dates.end),
            "property": property  // e.g. "browser", "os", "country"
        ]
    ]

    let data = try await postRequest(endpoint: "api/reports/breakdown", body: body)
    return try decoder.decode([BreakdownItem].self, from: data)
}

func getRevenueReport(websiteId: String, dateRange: DateRange) async throws -> [RevenueItem] {
    let formatter = DateFormatter()
    formatter.dateFormat = "yyyy-MM-dd"
    let dates = dateRange.dates

    let body: [String: Any] = [
        "websiteId": websiteId,
        "type": "revenue",
        "filters": [:],
        "parameters": [
            "startDate": formatter.string(from: dates.start),
            "endDate": formatter.string(from: dates.end)
        ]
    ]

    let data = try await postRequest(endpoint: "api/reports/revenue", body: body)
    return try decoder.decode([RevenueItem].self, from: data)
}
```

WICHTIG: Die bestehenden `getRetention` und `getJourneyReport` Methoden NICHT aendern — nur neue Methoden danach einfuegen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `grep "func getReports(" InsightFlow/Services/UmamiAPI.swift` findet einen Treffer
- `grep "func createReport(" InsightFlow/Services/UmamiAPI.swift` findet einen Treffer
- `grep "func getReport(" InsightFlow/Services/UmamiAPI.swift` findet einen Treffer
- `grep "func updateReport(" InsightFlow/Services/UmamiAPI.swift` findet einen Treffer
- `grep "func deleteReport(" InsightFlow/Services/UmamiAPI.swift` findet einen Treffer
- `grep "func getFunnelReport" InsightFlow/Services/UmamiAPI.swift` findet einen Treffer
- `grep "func getUTMReport" InsightFlow/Services/UmamiAPI.swift` findet einen Treffer
- `grep "func getGoalReport" InsightFlow/Services/UmamiAPI.swift` findet einen Treffer
- `grep "func getAttributionReport" InsightFlow/Services/UmamiAPI.swift` findet einen Treffer
- `grep "func getPerformanceReport" InsightFlow/Services/UmamiAPI.swift` findet einen Treffer
- `grep "func getBreakdownReport" InsightFlow/Services/UmamiAPI.swift` findet einen Treffer
- `grep "func getRevenueReport" InsightFlow/Services/UmamiAPI.swift` findet einen Treffer
- Xcode Build erfolgreich
  </acceptance_criteria>
  <done>Alle 12 Report-Methoden (5 CRUD + 7 spezialisierte) sind implementiert und kompilieren fehlerfrei</done>
</task>

</tasks>

<verification>
- Xcode Build laeuft durch
- Bestehende getRetention und getJourneyReport sind unveraendert
- Alle neuen Report-Methoden verwenden postRequest mit dem gleichen Body-Pattern
</verification>

<success_criteria>
- 12 neue Report-Methoden in UmamiAPI.swift
- Report CRUD vollstaendig (list, create, get, update, delete)
- Alle 7 fehlenden spezialisierten Reports implementiert (funnel, utm, goal, attribution, performance, breakdown, revenue)
- Projekt kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/05-umami-api-coverage/05-03-SUMMARY.md`
</output>
