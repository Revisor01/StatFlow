---
phase: 06-plausible-api-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - InsightFlow/Services/PlausibleAPI.swift
  - InsightFlow/Models/PlausibleGoal.swift
autonomous: true
requirements: [API-02]

must_haves:
  truths:
    - "PlausibleAPI kann alle Sites vom Server abrufen statt nur lokale Liste"
    - "PlausibleAPI kann Goals fuer eine Site auflisten, erstellen und loeschen"
    - "PlausibleAPI unterstuetzt Filter in v2 Query API Anfragen"
  artifacts:
    - path: "InsightFlow/Services/PlausibleAPI.swift"
      provides: "getSites(), getGoals(), createGoal(), deleteGoal(), PlausibleQueryFilter"
      contains: "func getSitesList"
    - path: "InsightFlow/Models/PlausibleGoal.swift"
      provides: "PlausibleGoal, PlausibleGoalType response models"
      contains: "struct PlausibleGoal"
  key_links:
    - from: "InsightFlow/Services/PlausibleAPI.swift"
      to: "/api/v1/sites"
      via: "GET request"
      pattern: "api/v1/sites"
    - from: "InsightFlow/Services/PlausibleAPI.swift"
      to: "/api/v1/sites/{domain}/goals"
      via: "GET/PUT/DELETE requests"
      pattern: "goals"
    - from: "InsightFlow/Services/PlausibleAPI.swift"
      to: "/api/v2/query"
      via: "filters parameter in POST body"
      pattern: "filters"
---

<objective>
Sites-Liste, Goals-API und Filter-Infrastruktur fuer PlausibleAPI

Purpose: Plausible CE bietet v1 REST-Endpunkte fuer Sites-Verwaltung und Goals, sowie Filter im v2 Query API. Aktuell fehlen alle drei komplett.
Output: getSitesList-Endpunkt, Goals CRUD, wiederverwendbare Filter-Infrastruktur fuer v2 Query
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@InsightFlow/Services/PlausibleAPI.swift
@InsightFlow/Services/AnalyticsProvider.swift
</context>

<interfaces>
<!-- Existing PlausibleAPI actor pattern — all new methods follow this -->

From InsightFlow/Services/PlausibleAPI.swift:
```swift
actor PlausibleAPI: AnalyticsProvider {
    // GET helper
    private func request(endpoint: String) async throws -> Data
    // POST helper
    private func postRequest(endpoint: String, body: [String: Any]) async throws -> Data
    // Existing models
    struct PlausibleSite: Codable, Identifiable { let domain: String; let timezone: String? }
    struct PlausibleSitesResponse: Codable { let sites: [PlausibleSite] }
}
```

Plausible CE v1 API contracts:
- GET /api/v1/sites -> { "data": [{ "domain": "...", "timezone": "..." }] }
- GET /api/v1/sites/{domain}/goals -> { "goals": [{ "id": N, "goal_type": "event|page", "event_name": "...", "page_path": "..." }] }
- PUT /api/v1/sites/{domain}/goals -> { "goal_type": "event|page", "event_name"|"page_path": "..." } -> Goal object
- DELETE /api/v1/sites/{domain}/goals/{id} -> 200

Plausible CE v2 Query API filter format:
- "filters": [["is", "visit:source", ["Google"]], ["contains", "event:page", ["/blog"]]]
- Operators: is, is_not, contains, does_not_contain, matches, matches_not
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Response-Modelle und Filter-Typen</name>
  <files>InsightFlow/Models/PlausibleGoal.swift</files>
  <read_first>
    - InsightFlow/Services/PlausibleAPI.swift (bestehende Response-Modelle am Ende der Datei)
    - InsightFlow/Models/Stats.swift (Muster fuer Codable Structs)
  </read_first>
  <action>
Neue Datei InsightFlow/Models/PlausibleGoal.swift erstellen mit:

1. PlausibleGoal struct (Codable, Sendable, Identifiable):
   - id: Int
   - goalType: String (CodingKey: "goal_type")
   - eventName: String? (CodingKey: "event_name")
   - pagePath: String? (CodingKey: "page_path")
   - displayName: String (computed — eventName ?? pagePath ?? "Unknown")

2. PlausibleGoalsResponse struct (Codable, Sendable):
   - goals: [PlausibleGoal]

3. PlausibleGoalType enum (String, Codable, Sendable):
   - case event = "event"
   - case page = "page"

4. PlausibleQueryFilter struct (Sendable) in gleicher Datei:
   - dimension: String (z.B. "visit:source", "event:page")
   - operator_: PlausibleFilterOperator (z.B. .is, .isNot, .contains)
   - values: [String]
   - func toQueryParam() -> [Any] — konvertiert zu ["is", "visit:source", ["Google"]] Array-Format

5. PlausibleFilterOperator enum (String, Sendable):
   - case is_ = "is"
   - case isNot = "is_not"
   - case contains = "contains"
   - case doesNotContain = "does_not_contain"
   - case matches = "matches"
   - case matchesNot = "matches_not"

WICHTIG: Alle Structs muessen Codable UND Sendable sein (Actor-API-Client Pattern). Verwende snake_case CodingKeys wie im bestehenden Code.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "struct PlausibleGoal" InsightFlow/Models/PlausibleGoal.swift
    - grep -q "struct PlausibleQueryFilter" InsightFlow/Models/PlausibleGoal.swift
    - grep -q "PlausibleFilterOperator" InsightFlow/Models/PlausibleGoal.swift
    - grep -q "Sendable" InsightFlow/Models/PlausibleGoal.swift
    - grep -q "func toQueryParam" InsightFlow/Models/PlausibleGoal.swift
  </acceptance_criteria>
  <done>PlausibleGoal, PlausibleGoalsResponse, PlausibleGoalType, PlausibleQueryFilter und PlausibleFilterOperator existieren, sind Codable+Sendable, Projekt kompiliert</done>
</task>

<task type="auto">
  <name>Task 2: Sites-Liste, Goals-API und Filter-Infrastruktur in PlausibleAPI</name>
  <files>InsightFlow/Services/PlausibleAPI.swift</files>
  <read_first>
    - InsightFlow/Services/PlausibleAPI.swift (komplette Datei — bestehende request/postRequest Helfer)
    - InsightFlow/Models/PlausibleGoal.swift (gerade erstellte Modelle)
  </read_first>
  <action>
In PlausibleAPI.swift folgende Methoden hinzufuegen:

1. **GET /api/v1/sites** — Sites-Liste vom Server:
```swift
func getSitesList() async throws -> [PlausibleSite] {
    let data = try await request(endpoint: "api/v1/sites")
    let response = try decoder.decode(PlausibleSitesResponse.self, from: data)
    return response.sites
}
```
Platzierung: Im "Site Management" MARK-Block, vor addSite().

2. **Goals API** — Neuen MARK-Block "Goals" nach "Shared Links" einfuegen:

```swift
// MARK: - Goals

func getGoals(domain: String) async throws -> [PlausibleGoal] {
    let encodedDomain = domain.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? domain
    let data = try await request(endpoint: "api/v1/sites/\(encodedDomain)/goals")
    let response = try decoder.decode(PlausibleGoalsResponse.self, from: data)
    return response.goals
}

func createGoal(domain: String, goalType: PlausibleGoalType, value: String) async throws -> PlausibleGoal {
    // PUT request mit JSON body: goal_type + event_name oder page_path
    // Verwendet bestehenden apiKey-Guard und URL-Bau-Pattern
    // Body: {"goal_type": "event", "event_name": "Signup"} oder {"goal_type": "page", "page_path": "/thank-you"}
}

func deleteGoal(domain: String, goalId: Int) async throws {
    // DELETE /api/v1/sites/{domain}/goals/{id}
    // Analog zu deleteSite() Pattern
}
```

3. **Filter-Infrastruktur** — Private Hilfsmethode fuer v2 Query:

```swift
/// Builds v2 query body with optional filters
private func buildQueryBody(
    siteId: String,
    metrics: [String],
    dateRange: DateRange,
    dimensions: [String] = [],
    filters: [PlausibleQueryFilter] = [],
    limit: Int? = nil
) -> [String: Any] {
    var body: [String: Any] = [
        "site_id": siteId,
        "metrics": metrics,
        "date_range": plausibleDateRange(for: dateRange)
    ]
    if !dimensions.isEmpty { body["dimensions"] = dimensions }
    if !filters.isEmpty { body["filters"] = filters.map { $0.toQueryParam() } }
    if let limit = limit { body["limit"] = limit }
    return body
}
```

4. **Bestehende getBreakdown() refactoren** um buildQueryBody zu nutzen (einfacher Refactor, gleiche Logik):
```swift
private func getBreakdown(websiteId: String, dateRange: DateRange, dimension: String, filters: [PlausibleQueryFilter] = []) async throws -> [AnalyticsMetricItem] {
    let body = buildQueryBody(siteId: websiteId, metrics: ["visitors"], dateRange: dateRange, dimensions: [dimension], filters: filters)
    // rest bleibt gleich
}
```

5. **createGoal und deleteGoal** brauchen eigene HTTP-Methoden (PUT/DELETE). Entweder:
   - Neue private Hilfsmethode `putRequest(endpoint:body:)` analog zu postRequest
   - Oder inline wie bei createSite/deleteSite (bevorzugt — konsistent mit bestehendem Code)

WICHTIG: Bestehende Public-API-Signaturen NICHT aendern (getBreakdown bekommt nur optionalen filters-Parameter). createGoal/deleteGoal folgen exakt dem Pattern von createSite/deleteSite.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "func getSitesList" InsightFlow/Services/PlausibleAPI.swift
    - grep -q "func getGoals" InsightFlow/Services/PlausibleAPI.swift
    - grep -q "func createGoal" InsightFlow/Services/PlausibleAPI.swift
    - grep -q "func deleteGoal" InsightFlow/Services/PlausibleAPI.swift
    - grep -q "func buildQueryBody" InsightFlow/Services/PlausibleAPI.swift
    - grep -q "PlausibleQueryFilter" InsightFlow/Services/PlausibleAPI.swift
  </acceptance_criteria>
  <done>getSitesList gibt Sites vom Server zurueck, Goals CRUD funktioniert, buildQueryBody akzeptiert Filter, bestehende getBreakdown nutzt buildQueryBody, Projekt kompiliert</done>
</task>

</tasks>

<verification>
- Projekt kompiliert ohne Fehler
- getSitesList, getGoals, createGoal, deleteGoal existieren als public Methoden
- buildQueryBody akzeptiert filters-Parameter
- Bestehende Methoden (getPages, getReferrers, etc.) funktionieren weiterhin (Signatur-kompatibel)
- PlausibleGoal und PlausibleQueryFilter Modelle sind Codable+Sendable
</verification>

<success_criteria>
- PlausibleAPI hat GET /api/v1/sites Endpunkt
- PlausibleAPI hat Goals CRUD (GET/PUT/DELETE)
- v2 Query API hat Filter-Infrastruktur (PlausibleQueryFilter + buildQueryBody)
- Alle bestehenden Methoden weiterhin funktional
- Projekt kompiliert
</success_criteria>

<output>
After completion, create `.planning/phases/06-plausible-api-coverage/06-01-SUMMARY.md`
</output>
