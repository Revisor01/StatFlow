---
phase: 06-plausible-api-coverage
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - InsightFlow/Services/PlausibleAPI.swift
autonomous: true
requirements: [API-02]

must_haves:
  truths:
    - "PlausibleAPI liefert Entry-Pages und Exit-Pages als eigene Breakdowns"
    - "PlausibleAPI liefert UTM-Dimensionen (Source, Medium, Campaign, Term, Content)"
    - "PlausibleAPI liefert Custom-Events ueber event:name Dimension"
    - "PlausibleAPI implementiert getPageTitles, getLanguages, getScreens, getEvents vom AnalyticsProvider-Protokoll"
  artifacts:
    - path: "InsightFlow/Services/PlausibleAPI.swift"
      provides: "Neue Breakdown-Methoden fuer alle fehlenden Dimensionen und AnalyticsProvider-Implementierungen"
      contains: "func getEntryPages"
  key_links:
    - from: "InsightFlow/Services/PlausibleAPI.swift"
      to: "/api/v2/query"
      via: "POST mit dimensions: visit:entry_page, visit:exit_page, visit:utm_source etc."
      pattern: "entry_page|exit_page|utm_source"
    - from: "InsightFlow/Services/PlausibleAPI.swift"
      to: "AnalyticsProvider protocol"
      via: "getEvents, getScreens, getLanguages, getPageTitles"
      pattern: "func getEvents.*dateRange"
---

<objective>
Fehlende Dimensionen und AnalyticsProvider-Methoden fuer PlausibleAPI

Purpose: PlausibleAPI nutzt aktuell nur 8 von ~18 verfuegbaren Plausible CE v2 Query Dimensionen. Entry/Exit-Pages, UTM-Parameter, Custom Events und Screen-Sizes fehlen. Ausserdem implementiert PlausibleAPI 4 AnalyticsProvider-Methoden nicht (nutzt leere Default-Implementierungen).
Output: Vollstaendige Plausible CE Dimensions-Abdeckung und AnalyticsProvider-Konformitaet
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@InsightFlow/Services/PlausibleAPI.swift
@InsightFlow/Services/AnalyticsProvider.swift
@.planning/phases/06-plausible-api-coverage/06-01-SUMMARY.md
</context>

<interfaces>
<!-- After Plan 01: PlausibleAPI has buildQueryBody and filter infrastructure -->

From InsightFlow/Services/PlausibleAPI.swift (after Plan 01):
```swift
// Available from Plan 01:
private func buildQueryBody(siteId: String, metrics: [String], dateRange: DateRange, dimensions: [String] = [], filters: [PlausibleQueryFilter] = [], limit: Int? = nil) -> [String: Any]
private func getBreakdown(websiteId: String, dateRange: DateRange, dimension: String, filters: [PlausibleQueryFilter] = []) async throws -> [AnalyticsMetricItem]
```

Plausible CE v2 Query API available dimensions:
- event:page (DONE), event:name (MISSING — custom events)
- visit:source (DONE as referrers), visit:referrer (MISSING — full referrer URL)
- visit:entry_page (MISSING), visit:exit_page (MISSING)
- visit:utm_source (MISSING), visit:utm_medium (MISSING), visit:utm_campaign (MISSING), visit:utm_term (MISSING), visit:utm_content (MISSING)
- visit:country (DONE), visit:region (DONE), visit:city (DONE)
- visit:device (DONE), visit:browser (DONE), visit:browser_version (MISSING), visit:os (DONE), visit:os_version (MISSING)
- visit:screen (MISSING — screen size like "Desktop", "Laptop", "Tablet", "Mobile")

AnalyticsProvider methods with empty default implementations:
```swift
func getPageTitles(websiteId: String, dateRange: DateRange) -> [AnalyticsMetricItem] { [] }
func getLanguages(websiteId: String, dateRange: DateRange) -> [AnalyticsMetricItem] { [] }
func getScreens(websiteId: String, dateRange: DateRange) -> [AnalyticsMetricItem] { [] }
func getEvents(websiteId: String, dateRange: DateRange) -> [AnalyticsMetricItem] { [] }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Entry/Exit-Pages, UTM-Dimensionen und fehlende Visit-Dimensionen</name>
  <files>InsightFlow/Services/PlausibleAPI.swift</files>
  <read_first>
    - InsightFlow/Services/PlausibleAPI.swift (vollstaendig — bestehende getBreakdown und buildQueryBody aus Plan 01)
    - InsightFlow/Services/AnalyticsProvider.swift (Protokoll-Anforderungen)
  </read_first>
  <action>
Neue public Methoden in PlausibleAPI.swift hinzufuegen, jeweils als Wrapper um getBreakdown():

1. **Entry/Exit Pages** — Neuen MARK-Block "Entry &amp; Exit Pages" nach dem Metrics-Block:
```swift
func getEntryPages(websiteId: String, dateRange: DateRange, filters: [PlausibleQueryFilter] = []) async throws -> [AnalyticsMetricItem] {
    return try await getBreakdown(websiteId: websiteId, dateRange: dateRange, dimension: "visit:entry_page", filters: filters)
}

func getExitPages(websiteId: String, dateRange: DateRange, filters: [PlausibleQueryFilter] = []) async throws -> [AnalyticsMetricItem] {
    return try await getBreakdown(websiteId: websiteId, dateRange: dateRange, dimension: "visit:exit_page", filters: filters)
}
```

2. **UTM Dimensionen** — Neuen MARK-Block "UTM Parameters":
```swift
func getUTMSources(websiteId: String, dateRange: DateRange, filters: [PlausibleQueryFilter] = []) async throws -> [AnalyticsMetricItem] {
    return try await getBreakdown(websiteId: websiteId, dateRange: dateRange, dimension: "visit:utm_source", filters: filters)
}

func getUTMMedia(websiteId: String, dateRange: DateRange, filters: [PlausibleQueryFilter] = []) async throws -> [AnalyticsMetricItem] {
    return try await getBreakdown(websiteId: websiteId, dateRange: dateRange, dimension: "visit:utm_medium", filters: filters)
}

func getUTMCampaigns(websiteId: String, dateRange: DateRange, filters: [PlausibleQueryFilter] = []) async throws -> [AnalyticsMetricItem] {
    return try await getBreakdown(websiteId: websiteId, dateRange: dateRange, dimension: "visit:utm_campaign", filters: filters)
}

func getUTMTerms(websiteId: String, dateRange: DateRange, filters: [PlausibleQueryFilter] = []) async throws -> [AnalyticsMetricItem] {
    return try await getBreakdown(websiteId: websiteId, dateRange: dateRange, dimension: "visit:utm_term", filters: filters)
}

func getUTMContent(websiteId: String, dateRange: DateRange, filters: [PlausibleQueryFilter] = []) async throws -> [AnalyticsMetricItem] {
    return try await getBreakdown(websiteId: websiteId, dateRange: dateRange, dimension: "visit:utm_content", filters: filters)
}
```

3. **Fehlende Visit-Dimensionen** — Im bestehenden Metrics-Block ergaenzen:
```swift
func getBrowserVersions(websiteId: String, dateRange: DateRange, filters: [PlausibleQueryFilter] = []) async throws -> [AnalyticsMetricItem] {
    return try await getBreakdown(websiteId: websiteId, dateRange: dateRange, dimension: "visit:browser_version", filters: filters)
}

func getOSVersions(websiteId: String, dateRange: DateRange, filters: [PlausibleQueryFilter] = []) async throws -> [AnalyticsMetricItem] {
    return try await getBreakdown(websiteId: websiteId, dateRange: dateRange, dimension: "visit:os_version", filters: filters)
}
```

Alle neuen Methoden folgen exakt dem Pattern der bestehenden getPages/getReferrers/etc. — nur die dimension-Strings unterscheiden sich.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "func getEntryPages" InsightFlow/Services/PlausibleAPI.swift
    - grep -q "func getExitPages" InsightFlow/Services/PlausibleAPI.swift
    - grep -q "func getUTMSources" InsightFlow/Services/PlausibleAPI.swift
    - grep -q "func getUTMMedia" InsightFlow/Services/PlausibleAPI.swift
    - grep -q "func getUTMCampaigns" InsightFlow/Services/PlausibleAPI.swift
    - grep -q "func getBrowserVersions" InsightFlow/Services/PlausibleAPI.swift
    - grep -q "func getOSVersions" InsightFlow/Services/PlausibleAPI.swift
    - grep -q "entry_page" InsightFlow/Services/PlausibleAPI.swift
    - grep -q "utm_source" InsightFlow/Services/PlausibleAPI.swift
  </acceptance_criteria>
  <done>Entry/Exit-Pages, 5 UTM-Dimensionen und Browser/OS-Versionen als Methoden verfuegbar, Projekt kompiliert</done>
</task>

<task type="auto">
  <name>Task 2: AnalyticsProvider-Methoden und Custom Events implementieren</name>
  <files>InsightFlow/Services/PlausibleAPI.swift</files>
  <read_first>
    - InsightFlow/Services/PlausibleAPI.swift (nach Task 1 — alle neuen Methoden)
    - InsightFlow/Services/AnalyticsProvider.swift (Default-Implementierungen die ueberschrieben werden sollen)
  </read_first>
  <action>
Die 4 AnalyticsProvider-Methoden implementieren, die aktuell leere Default-Implementierungen nutzen:

1. **getEvents** — Custom Events via event:name Dimension:
```swift
func getEvents(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem] {
    return try await getBreakdown(websiteId: websiteId, dateRange: dateRange, dimension: "event:name")
}
```

2. **getScreens** — Screen-Size via visit:screen Dimension:
```swift
func getScreens(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem] {
    return try await getBreakdown(websiteId: websiteId, dateRange: dateRange, dimension: "visit:screen")
}
```

3. **getPageTitles** — Plausible CE hat KEINE page-title Dimension. Explizit dokumentieren und leere Liste zurueckgeben (NICHT die Default-Extension nutzen — eigene Implementierung fuer Klarheit):
```swift
func getPageTitles(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem] {
    // Plausible CE does not track page titles (privacy-first design)
    return []
}
```

4. **getLanguages** — Plausible CE hat KEINE language Dimension. Gleiche Begruendung:
```swift
func getLanguages(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem] {
    // Plausible CE does not track browser language (privacy-first design)
    return []
}
```

5. **Zusaetzlich: getReferrerURLs** — Volle Referrer-URL (visit:referrer) vs. aggregierte Source (visit:source):
```swift
func getReferrerURLs(websiteId: String, dateRange: DateRange, filters: [PlausibleQueryFilter] = []) async throws -> [AnalyticsMetricItem] {
    return try await getBreakdown(websiteId: websiteId, dateRange: dateRange, dimension: "visit:referrer", filters: filters)
}
```

WICHTIG: getEvents und getScreens ueberschreiben die Default-Extension aus AnalyticsProvider — sie MUESSEN exakt die gleiche Signatur haben (websiteId: String, dateRange: DateRange) OHNE filters-Parameter. Die filters-freien Versionen sind die Protokoll-Implementierungen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "func getEvents.*dateRange.*AnalyticsMetricItem" InsightFlow/Services/PlausibleAPI.swift
    - grep -q "func getScreens.*dateRange.*AnalyticsMetricItem" InsightFlow/Services/PlausibleAPI.swift
    - grep -q "func getPageTitles" InsightFlow/Services/PlausibleAPI.swift
    - grep -q "func getLanguages" InsightFlow/Services/PlausibleAPI.swift
    - grep -q "event:name" InsightFlow/Services/PlausibleAPI.swift
    - grep -q "visit:screen" InsightFlow/Services/PlausibleAPI.swift
    - grep -q "func getReferrerURLs" InsightFlow/Services/PlausibleAPI.swift
  </acceptance_criteria>
  <done>Alle 4 AnalyticsProvider-Methoden haben eigene PlausibleAPI-Implementierungen (nicht mehr Default), Custom Events via event:name, Screens via visit:screen, getReferrerURLs fuer volle Referrer-URLs, Projekt kompiliert</done>
</task>

</tasks>

<verification>
- Projekt kompiliert ohne Fehler
- PlausibleAPI implementiert ALLE AnalyticsProvider-Protokoll-Methoden explizit (keine Default-Extension mehr noetig)
- Neue Dimensionen: entry_page, exit_page, utm_source, utm_medium, utm_campaign, utm_term, utm_content, browser_version, os_version, event:name, visit:screen, visit:referrer
- Bestehende Methoden unveraendert funktional
</verification>

<success_criteria>
- 12 neue Dimensions-Methoden in PlausibleAPI
- getEvents/getScreens/getPageTitles/getLanguages ueberschreiben Default-Implementierungen
- UTM-Parameter komplett abgedeckt (Source, Medium, Campaign, Term, Content)
- Entry/Exit-Pages verfuegbar
- Projekt kompiliert
</success_criteria>

<output>
After completion, create `.planning/phases/06-plausible-api-coverage/06-02-SUMMARY.md`
</output>
