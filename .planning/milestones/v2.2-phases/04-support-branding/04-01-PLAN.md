---
phase: 04-support-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - InsightFlow/Views/Settings/SupportView.swift
  - InsightFlow/Services/SupportManager.swift
  - InsightFlow/Views/Settings/SettingsView.swift
  - InsightFlow/Resources/de.lproj/Localizable.strings
  - InsightFlow/Resources/en.lproj/Localizable.strings
autonomous: false
requirements: [SUP-01, SUP-02]

must_haves:
  truths:
    - "SupportView zeigt Tip-Optionen ohne Emojis in cleanem Card-Design"
    - "SupportButton verwendet SF Symbols statt Emoji-Text"
    - "Settings-Footer zeigt 'Mit Liebe in Hennstedt gemacht' mit dezenter Untertitel-Zeile"
    - "Alle neuen/geaenderten Strings sind in DE und EN lokalisiert"
  artifacts:
    - path: "InsightFlow/Views/Settings/SupportView.swift"
      provides: "Redesigned SupportView und SupportButton ohne Emojis"
      contains: "systemName"
    - path: "InsightFlow/Services/SupportManager.swift"
      provides: "Product Extension mit SF Symbol statt Emoji"
      contains: "symbolName"
    - path: "InsightFlow/Views/Settings/SettingsView.swift"
      provides: "Footer mit Branding-Untertitel"
      contains: "settings.branding.subtitle"
    - path: "InsightFlow/Resources/de.lproj/Localizable.strings"
      provides: "Deutsche Lokalisierung fuer Branding-Untertitel"
      contains: "settings.branding.subtitle"
    - path: "InsightFlow/Resources/en.lproj/Localizable.strings"
      provides: "Englische Lokalisierung fuer Branding-Untertitel"
      contains: "settings.branding.subtitle"
  key_links:
    - from: "InsightFlow/Views/Settings/SupportView.swift"
      to: "InsightFlow/Services/SupportManager.swift"
      via: "Product.symbolName property statt Product.emoji"
      pattern: "product\\.symbolName"
    - from: "InsightFlow/Views/Settings/SettingsView.swift"
      to: "InsightFlow/Resources/de.lproj/Localizable.strings"
      via: "Lokalisierter Branding-Untertitel"
      pattern: "settings\\.branding\\.subtitle"
---

<objective>
Redesign der SupportView fuer cleanes Tip-Design ohne Emojis und Ergaenzung des Settings-Footers um einen dezenten Branding-Untertitel.

Purpose: Einheitliches, professionelles Support- und Branding-Erlebnis, das als Vorlage fuer ValetudiOS dient.
Output: Ueberarbeitete SupportView, aktualisierter SupportManager, erweiterter Settings-Footer, aktualisierte Lokalisierungen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-support-branding/04-CONTEXT.md

<interfaces>
<!-- Bestehende Interfaces die der Executor kennen muss -->

From InsightFlow/Services/SupportManager.swift:
```swift
@MainActor
class SupportManager: ObservableObject {
    static let shared = SupportManager()
    @Published var products: [Product] = []
    @Published var isLoading = false
    @Published var purchaseError: String?
    @Published var showThankYou = false
    func loadProducts() async
    func purchase(_ product: Product) async
    var shouldShowReminder: Bool
    func markReminderShown()
    func markAlreadySupported()
}

extension Product {
    var emoji: String       // WIRD ERSETZT durch symbolName
    var supportName: String // BLEIBT
}
```

From InsightFlow/Views/Settings/SupportView.swift:
```swift
struct SupportView: View           // WIRD REDESIGNED
struct SupportButton: View {       // WIRD REDESIGNED
    let product: Product
    let action: () -> Void
}
```

From InsightFlow/Views/Settings/SettingsView.swift (Zeilen 379-392):
```swift
// Aktueller Footer:
VStack(spacing: 4) {
    Text("PrivacyFlow v\(Bundle.main.infoDictionary?["CFBundleShortVersionString"] as? String ?? "1.0")")
    HStack(spacing: 4) {
        Text("Made with")
        Image(systemName: "heart.fill")
        Text("in Hennstedt")
    }
}
.font(.caption)
.foregroundStyle(.secondary)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SupportView Redesign und SupportManager Update (SUP-01)</name>
  <files>
    InsightFlow/Views/Settings/SupportView.swift,
    InsightFlow/Services/SupportManager.swift,
    InsightFlow/Resources/de.lproj/Localizable.strings,
    InsightFlow/Resources/en.lproj/Localizable.strings
  </files>
  <read_first>
    InsightFlow/Views/Settings/SupportView.swift,
    InsightFlow/Services/SupportManager.swift,
    InsightFlow/Resources/de.lproj/Localizable.strings,
    InsightFlow/Resources/en.lproj/Localizable.strings
  </read_first>
  <action>
    **SupportManager.swift — Product Extension aendern (per D: keine Emojis):**
    - Property `emoji: String` durch `symbolName: String` ersetzen
    - Mapping: small -> "cup.and.saucer.fill", medium -> "gift.fill", large -> "sparkles"
    - Default-Fall: "heart.fill"
    - Property `supportName` bleibt unveraendert

    **SupportView.swift — Komplettes Redesign (per D: clean, reduziert, keine Emojis):**

    SupportButton neu gestalten:
    - Links: SF Symbol in farbigem Circle (wie Settings-Support-Button-Pattern in SettingsView Zeile 192-206)
    - Circle-Farben per Tier: small -> .blue, medium -> .purple, large -> .orange
    - Neues computed property `tierColor` auf Product Extension hinzufuegen
    - Mitte: product.supportName (headline) + product.description (caption, secondary)
    - Rechts: displayPrice in Capsule (Akzentfarbe .accentColor statt .pink)
    - Kein Text(product.emoji) mehr — stattdessen Image(systemName: product.symbolName)
    - RoundedRectangle cornerRadius 12 (statt 16)
    - Padding und Spacing beibehalten

    SupportView Header:
    - Heart-Icon beibehalten (systemName: "heart.fill", .pink, size 50)
    - Header/Message Text bleibt (lokalisiert)
    - Footer Text bleibt

    **Lokalisation — bestehende support.* Keys beibehalten, keine neuen noetig fuer SUP-01**
    (Die bestehenden support.small/medium/large Keys bleiben, da supportName unveraendert bleibt)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "symbolName" InsightFlow/Services/SupportManager.swift (emoji property ersetzt)
    - grep -q "cup.and.saucer.fill" InsightFlow/Services/SupportManager.swift (small tier symbol)
    - grep -q "gift.fill" InsightFlow/Services/SupportManager.swift (medium tier symbol)
    - grep -q "sparkles" InsightFlow/Services/SupportManager.swift (large tier symbol)
    - grep -qv "product.emoji" InsightFlow/Views/Settings/SupportView.swift (keine emoji-Referenz mehr)
    - grep -q "product.symbolName" InsightFlow/Views/Settings/SupportView.swift (SF Symbol verwendet)
    - grep -q "Circle()" InsightFlow/Views/Settings/SupportView.swift (farbiger Circle fuer Icon)
    - Projekt kompiliert ohne Fehler
  </acceptance_criteria>
  <done>SupportView zeigt drei Tip-Optionen mit SF Symbols in farbigen Circles, ohne Emojis. SupportButton hat cleanes Card-Layout. Projekt kompiliert.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Branding-Untertitel im Settings-Footer (SUP-02)</name>
  <files>
    InsightFlow/Views/Settings/SettingsView.swift,
    InsightFlow/Resources/de.lproj/Localizable.strings,
    InsightFlow/Resources/en.lproj/Localizable.strings
  </files>
  <read_first>
    InsightFlow/Views/Settings/SettingsView.swift,
    InsightFlow/Resources/de.lproj/Localizable.strings,
    InsightFlow/Resources/en.lproj/Localizable.strings
  </read_first>
  <action>
    **SettingsView.swift — Footer erweitern (per D: dezenter Untertitel, kein Easter Egg, keine Animation):**

    Den bestehenden Footer-Block (Zeilen 379-392) erweitern. Nach der HStack "Made with heart in Hennstedt" und vor dem schliessenden `}` des VStack eine neue Text-Zeile einfuegen:

    ```swift
    Text("settings.branding.subtitle")
        .font(.caption2)
        .foregroundStyle(.tertiary)
    ```

    Der Untertitel verwendet .caption2 und .tertiary — deutlich dezenter als die Hennstedt-Zeile.

    **Lokalisierung — neue Keys hinzufuegen:**

    In de.lproj/Localizable.strings am Ende des Settings-Bereichs hinzufuegen:
    ```
    "settings.branding.subtitle" = "Ein Pastorenprojekt";
    ```

    In en.lproj/Localizable.strings am Ende des Settings-Bereichs hinzufuegen:
    ```
    "settings.branding.subtitle" = "A pastor's project";
    ```

    Der Ton ist warmherzig-informell per User-Entscheidung. "Ein Pastorenprojekt" ist dezent, nicht aufdringlich, und kommuniziert den Segens-Bezug ohne Easter Egg oder Animation.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "settings.branding.subtitle" InsightFlow/Views/Settings/SettingsView.swift (Untertitel-Key verwendet)
    - grep -q ".caption2" InsightFlow/Views/Settings/SettingsView.swift (dezentere Schriftgroesse)
    - grep -q ".tertiary" InsightFlow/Views/Settings/SettingsView.swift (dezentere Farbe)
    - grep -q '"settings.branding.subtitle" = "Ein Pastorenprojekt"' InsightFlow/Resources/de.lproj/Localizable.strings
    - grep -q '"settings.branding.subtitle"' InsightFlow/Resources/en.lproj/Localizable.strings
    - Projekt kompiliert ohne Fehler
  </acceptance_criteria>
  <done>Settings-Footer zeigt "Mit Liebe in Hennstedt gemacht" mit dezenter Untertitel-Zeile "Ein Pastorenprojekt" darunter. Beide Sprachen lokalisiert.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visuelle Pruefung Support-Redesign und Branding</name>
  <files>InsightFlow/Views/Settings/SupportView.swift, InsightFlow/Views/Settings/SettingsView.swift</files>
  <action>
    Visuelle Pruefung durch den User: App im Simulator starten, SupportView und Settings-Footer inspizieren.
  </action>
  <what-built>
    1. SupportView komplett redesigned: SF Symbols in farbigen Circles statt Emojis, cleanes Card-Layout
    2. Settings-Footer mit neuem Untertitel "Ein Pastorenprojekt" unter "Mit Liebe in Hennstedt gemacht"
  </what-built>
  <how-to-verify>
    1. App im Simulator starten
    2. Settings oeffnen, nach unten scrollen, Footer pruefen:
       - Version, "Made with heart in Hennstedt", darunter dezent "Ein Pastorenprojekt"
    3. "Entwicklung unterstuetzen" tippen, SupportView pruefen:
       - Drei Optionen mit SF Symbols (Tasse, Geschenk, Sparkles) in farbigen Circles
       - Keine Emojis sichtbar
       - Preis-Buttons rechts in Capsule
    4. Sprache auf Englisch wechseln, gleiche Pruefung
  </how-to-verify>
  <verify>User bestaetigt visuelles Ergebnis</verify>
  <done>User hat SupportView-Design und Branding-Footer visuell geprueft und genehmigt.</done>
  <resume-signal>Type "approved" or describe issues with the design</resume-signal>
</task>

</tasks>

<verification>
- Projekt kompiliert ohne Fehler und Warnungen
- Keine Emoji-Zeichen mehr in SupportView oder SupportManager
- Branding-Untertitel in beiden Sprachen vorhanden
- SupportButton verwendet SF Symbols mit farbigen Circles
</verification>

<success_criteria>
1. SupportView zeigt drei Tip-Optionen mit SF Symbols statt Emojis in cleanem Design
2. Settings-Footer zeigt Version + "Mit Liebe in Hennstedt gemacht" + "Ein Pastorenprojekt"
3. Alle Strings in DE und EN lokalisiert
4. Design dient als Vorlage fuer ValetudiOS (gleiche Struktur, kein shared Package)
</success_criteria>

<output>
After completion, create `.planning/phases/04-support-branding/04-01-SUMMARY.md`
</output>
