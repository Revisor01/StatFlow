---
phase: 04-architektur
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - InsightFlow/Services/AnalyticsProvider.swift
  - InsightFlow/Services/UmamiAPI.swift
  - InsightFlow/Views/Detail/WebsiteDetailViewModel.swift
autonomous: true
requirements:
  - ARCH-02

must_haves:
  truths:
    - "WebsiteDetailViewModel enthaelt kein if isPlausible mehr"
    - "Alle loadX()-Methoden rufen provider.methodName() auf statt direkter API-Referenzen"
    - "AnalyticsProvider-Protokoll deckt alle genutzten Methoden ab (inkl. regions, cities, pageTitles, etc.)"
    - "UmamiAPI implementiert die neuen Protokoll-Methoden"
    - "Plausible-only Methoden geben leere Arrays zurueck via Protocol Extension Default"
  artifacts:
    - path: "InsightFlow/Services/AnalyticsProvider.swift"
      provides: "Erweiteres AnalyticsProvider-Protokoll mit allen Metriken + Default-Implementierungen"
      contains: "func getRegions"
    - path: "InsightFlow/Services/UmamiAPI.swift"
      provides: "UmamiAPI mit neuen Protokoll-Methoden (getRegions, getCities, getPageTitles, etc.)"
      contains: "func getPageTitles"
    - path: "InsightFlow/Views/Detail/WebsiteDetailViewModel.swift"
      provides: "ViewModel ohne isPlausible-Branching, nur provider-Aufrufe"
      contains: "provider.getPages"
  key_links:
    - from: "InsightFlow/Views/Detail/WebsiteDetailViewModel.swift"
      to: "InsightFlow/Services/AnalyticsProvider.swift"
      via: "provider.methodName() Aufrufe"
      pattern: "provider\\.get"
    - from: "InsightFlow/Views/Detail/WebsiteDetailViewModel.swift"
      to: "InsightFlow/Services/AnalyticsProvider.swift"
      via: "AnalyticsManager.shared.currentProvider"
      pattern: "AnalyticsManager\\.shared\\.currentProvider"
---

<objective>
WebsiteDetailViewModel auf AnalyticsProvider-Protokoll umstellen (ARCH-02)

Purpose: Alle 15 if-isPlausible-Branches entfernen. Das ViewModel soll nur noch ueber das Protokoll mit dem aktiven Provider kommunizieren.
Output: Erweitertes AnalyticsProvider-Protokoll, UmamiAPI mit neuen Methoden, ViewModel ohne Branching
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-architektur/04-RESEARCH.md
@.planning/phases/04-architektur/04-01-SUMMARY.md

<interfaces>
From InsightFlow/Services/AnalyticsProvider.swift (aktuell, Zeile 130-156):
```swift
protocol AnalyticsProvider: Sendable {
    nonisolated var providerType: AnalyticsProviderType { get }
    nonisolated var serverURL: String { get }
    nonisolated var isAuthenticated: Bool { get }
    func authenticate(serverURL: String, credentials: AnalyticsCredentials) async throws
    func getAnalyticsWebsites() async throws -> [AnalyticsWebsite]
    func getAnalyticsStats(websiteId: String, dateRange: DateRange) async throws -> AnalyticsStats
    func getPageviewsData(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsChartPoint]
    func getVisitorsData(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsChartPoint]
    func getActiveVisitors(websiteId: String) async throws -> Int
    func getRealtimeData(websiteId: String) async throws -> AnalyticsRealtimeData
    func getPages(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem]
    func getReferrers(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem]
    func getCountries(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem]
    func getDevices(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem]
    func getBrowsers(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem]
    func getOS(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem]
}
```

From InsightFlow/Services/AnalyticsProvider.swift (Zeile 84):
```swift
func toWebsiteStats() -> WebsiteStats  // auf AnalyticsStats extension
```

From InsightFlow/Services/UmamiAPI.swift (Zeile 503):
```swift
func getMetrics(websiteId: String, dateRange: DateRange, type: MetricType, limit: Int = 10) async throws -> [MetricItem]
```

From InsightFlow/Views/Detail/WebsiteDetailViewModel.swift (aktuell):
- 15 `if isPlausible` Branches in loadStats, loadActiveVisitors, loadPageviews, loadTopPages, loadPageTitles, loadReferrers, loadCountries, loadRegions, loadCities, loadDevices, loadBrowsers, loadOperatingSystems, loadLanguages, loadScreens, loadEvents
- Plausible-Methoden geben `[AnalyticsMetricItem]` zurueck, werden zu `[MetricItem]` gemappt
- Umami `getMetrics` gibt direkt `[MetricItem]` zurueck
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AnalyticsProvider-Protokoll erweitern und UmamiAPI anpassen</name>
  <files>InsightFlow/Services/AnalyticsProvider.swift, InsightFlow/Services/UmamiAPI.swift</files>
  <read_first>InsightFlow/Services/AnalyticsProvider.swift, InsightFlow/Services/UmamiAPI.swift, InsightFlow/Views/Detail/WebsiteDetailViewModel.swift</read_first>
  <action>
**AnalyticsProvider.swift — Protokoll erweitern:**

1. Neue Methoden zum `AnalyticsProvider` protocol hinzufuegen (nach getOS):
   ```swift
   func getRegions(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem]
   func getCities(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem]
   func getPageTitles(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem]
   func getLanguages(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem]
   func getScreens(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem]
   func getEvents(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem]
   func getRealtimeTopPages(websiteId: String, limit: Int) async throws -> [AnalyticsMetricItem]
   func getRealtimeCountries(websiteId: String, limit: Int) async throws -> [AnalyticsMetricItem]
   func getRealtimePageviews(websiteId: String) async throws -> Int
   ```

2. Protocol Extension mit Default-Implementierungen fuer Provider die bestimmte Metriken nicht unterstuetzen:
   ```swift
   extension AnalyticsProvider {
       func getPageTitles(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem] { [] }
       func getLanguages(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem] { [] }
       func getScreens(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem] { [] }
       func getEvents(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem] { [] }
   }
   ```
   HINWEIS: getRegions, getCities, getRealtimeTopPages, getRealtimeCountries, getRealtimePageviews brauchen KEINE Default-Extension, da PlausibleAPI diese bereits implementiert hat.

3. **AnalyticsManager.isAuthenticated entfernen** (Zeile 181): Die `@Published var isAuthenticated = false` Property und alle Stellen in setProvider/logout die sie setzen. Wird von keiner View direkt gelesen (Research bestaetigt).

**UmamiAPI.swift — Neue Protokoll-Methoden implementieren:**

4. Neue Methoden hinzufuegen die intern `getMetrics()` aufrufen und `[MetricItem]` zu `[AnalyticsMetricItem]` konvertieren:
   ```swift
   func getRegions(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem] {
       let items = try await getMetrics(websiteId: websiteId, dateRange: dateRange, type: .region)
       return items.map { AnalyticsMetricItem(name: $0.x, value: $0.y) }
   }
   func getCities(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem] {
       let items = try await getMetrics(websiteId: websiteId, dateRange: dateRange, type: .city)
       return items.map { AnalyticsMetricItem(name: $0.x, value: $0.y) }
   }
   func getPageTitles(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem] {
       let items = try await getMetrics(websiteId: websiteId, dateRange: dateRange, type: .title, limit: 50)
       return items.map { AnalyticsMetricItem(name: $0.x, value: $0.y) }
   }
   func getLanguages(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem] {
       let items = try await getMetrics(websiteId: websiteId, dateRange: dateRange, type: .language)
       return items.map { AnalyticsMetricItem(name: $0.x, value: $0.y) }
   }
   func getScreens(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem] {
       let items = try await getMetrics(websiteId: websiteId, dateRange: dateRange, type: .screen)
       return items.map { AnalyticsMetricItem(name: $0.x, value: $0.y) }
   }
   func getEvents(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem] {
       let items = try await getMetrics(websiteId: websiteId, dateRange: dateRange, type: .event)
       return items.map { AnalyticsMetricItem(name: $0.x, value: $0.y) }
   }
   ```

   Fuer getPages, getReferrers, getCountries, getDevices, getBrowsers, getOS: Pruefen ob UmamiAPI diese bereits als Protokoll-konforme Methoden hat. Falls UmamiAPI nur `getMetrics(type:)` nutzt aber keine separaten getPages/etc., dann analog implementieren:
   ```swift
   func getPages(websiteId: String, dateRange: DateRange) async throws -> [AnalyticsMetricItem] {
       let items = try await getMetrics(websiteId: websiteId, dateRange: dateRange, type: .path, limit: 50)
       return items.map { AnalyticsMetricItem(name: $0.x, value: $0.y) }
   }
   ```
   Gleiches Muster fuer getReferrers (.referrer), getCountries (.country), getDevices (.device), getBrowsers (.browser), getOS (.os).

WICHTIG: `AnalyticsMetricItem` hat `name: String` und `value: Int`. `MetricItem` hat `x: String` und `y: Int`. Die Mapping-Richtung ist `MetricItem(x,y) -> AnalyticsMetricItem(name: x, value: y)`.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && grep "func getRegions\|func getCities\|func getPageTitles\|func getLanguages\|func getScreens\|func getEvents" InsightFlow/Services/AnalyticsProvider.swift | wc -l | grep -q "[4-9]" && grep "func getRegions\|func getCities\|func getPageTitles" InsightFlow/Services/UmamiAPI.swift | wc -l | grep -q "[3-9]" && echo "PASS: Protocol extended, UmamiAPI implements new methods"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "func getRegions" InsightFlow/Services/AnalyticsProvider.swift` findet mindestens 1 Treffer (Protokoll-Deklaration)
    - `grep "func getPageTitles" InsightFlow/Services/UmamiAPI.swift` findet 1 Treffer
    - `grep "func getLanguages" InsightFlow/Services/UmamiAPI.swift` findet 1 Treffer
    - `grep "func getScreens" InsightFlow/Services/UmamiAPI.swift` findet 1 Treffer
    - `grep "func getEvents" InsightFlow/Services/UmamiAPI.swift` findet 1 Treffer
    - `grep "isAuthenticated" InsightFlow/Services/AnalyticsProvider.swift` findet NUR die Protocol-Property (nonisolated var), NICHT @Published var in AnalyticsManager
  </acceptance_criteria>
  <done>AnalyticsProvider-Protokoll deckt alle Metriken ab. UmamiAPI hat neue Wrapper-Methoden. AnalyticsManager.isAuthenticated ist entfernt.</done>
</task>

<task type="auto">
  <name>Task 2: WebsiteDetailViewModel auf Protocol-Dispatch umstellen</name>
  <files>InsightFlow/Views/Detail/WebsiteDetailViewModel.swift</files>
  <read_first>InsightFlow/Views/Detail/WebsiteDetailViewModel.swift, InsightFlow/Services/AnalyticsProvider.swift (nach Task 1)</read_first>
  <action>
WebsiteDetailViewModel.swift komplett ueberarbeiten — alle 15 if-isPlausible-Branches entfernen:

1. **Private API-Referenzen entfernen** (Zeile 28-33):
   - ENTFERNEN: `private let umamiAPI = UmamiAPI.shared`
   - ENTFERNEN: `private let plausibleAPI = PlausibleAPI.shared`
   - ENTFERNEN: `private var isPlausible: Bool { ... }`

2. **Jede loadX()-Methode auf einheitliches Pattern umstellen:**

   Das Pattern fuer ALLE Methoden ist identisch:
   ```swift
   private func loadXxx(dateRange: DateRange) async {
       guard let provider = AnalyticsManager.shared.currentProvider else { return }
       do {
           let items = try await provider.getXxx(websiteId: websiteId, dateRange: dateRange)
           xxx = items.map { MetricItem(x: $0.name, y: $0.value) }
       } catch {
           #if DEBUG
           print("Failed to load xxx: \(error)")
           #endif
       }
   }
   ```

3. **Konkrete Umstellungen fuer jede Methode:**

   **loadStats** (Zeile 62-79):
   ```swift
   private func loadStats(dateRange: DateRange) async {
       guard let provider = AnalyticsManager.shared.currentProvider else { return }
       do {
           let analyticsStats = try await provider.getAnalyticsStats(websiteId: websiteId, dateRange: dateRange)
           stats = analyticsStats.toWebsiteStats()
       } catch {
           self.error = error.localizedDescription
       }
   }
   ```
   HINWEIS: `toWebsiteStats()` existiert bereits als Extension auf AnalyticsStats (AnalyticsProvider.swift Zeile 84).

   **loadActiveVisitors** (Zeile 81-93):
   ```swift
   private func loadActiveVisitors() async {
       guard let provider = AnalyticsManager.shared.currentProvider else { return }
       do {
           activeVisitors = try await provider.getActiveVisitors(websiteId: websiteId)
       } catch {
           #if DEBUG
           print("Failed to load active visitors: \(error)")
           #endif
       }
   }
   ```

   **loadPageviews** (Zeile 95-119):
   ```swift
   private func loadPageviews(dateRange: DateRange) async {
       guard let provider = AnalyticsManager.shared.currentProvider else { return }
       do {
           let formatter = ISO8601DateFormatter()
           let pageviewData = try await provider.getPageviewsData(websiteId: websiteId, dateRange: dateRange)
           let visitorData = try await provider.getVisitorsData(websiteId: websiteId, dateRange: dateRange)
           pageviewsData = fillMissingTimeSlots(
               data: pageviewData.map { TimeSeriesPoint(x: formatter.string(from: $0.date), y: $0.value) },
               dateRange: dateRange
           )
           sessionsData = fillMissingTimeSlots(
               data: visitorData.map { TimeSeriesPoint(x: formatter.string(from: $0.date), y: $0.value) },
               dateRange: dateRange
           )
       } catch {
           #if DEBUG
           print("Failed to load pageviews: \(error)")
           #endif
       }
   }
   ```

   **loadTopPages** (Zeile 187-200):
   ```swift
   private func loadTopPages(dateRange: DateRange) async {
       guard let provider = AnalyticsManager.shared.currentProvider else { return }
       do {
           let items = try await provider.getPages(websiteId: websiteId, dateRange: dateRange)
           topPages = items.map { MetricItem(x: $0.name, y: $0.value) }
       } catch {
           #if DEBUG
           print("Failed to load top pages: \(error)")
           #endif
       }
   }
   ```

   **loadPageTitles** — identisches Pattern mit `provider.getPageTitles(...)`, Ergebnis in `pageTitles`

   **loadReferrers** — `provider.getReferrers(...)`, Ergebnis in `referrers`

   **loadCountries** — `provider.getCountries(...)`, Ergebnis in `countries`

   **loadRegions** — `provider.getRegions(...)`, Ergebnis in `regions`

   **loadCities** — `provider.getCities(...)`, Ergebnis in `cities`

   **loadDevices** — `provider.getDevices(...)`, Ergebnis in `devices`

   **loadBrowsers** — `provider.getBrowsers(...)`, Ergebnis in `browsers`

   **loadOperatingSystems** — `provider.getOS(...)`, Ergebnis in `operatingSystems`

   **loadLanguages** — `provider.getLanguages(...)`, Ergebnis in `languages`

   **loadScreens** — `provider.getScreens(...)`, Ergebnis in `screens`

   **loadEvents** — `provider.getEvents(...)`, Ergebnis in `events`

4. **fillMissingTimeSlots bleibt unveraendert** — reine Hilfsmethode ohne Provider-Abhaengigkeit.

5. **loadData() bleibt strukturell unveraendert** — nur die aufgerufenen Methoden sind intern anders.

WICHTIG: Jede Methode die AnalyticsMetricItem zurueckgibt muss zu MetricItem(x: name, y: value) gemappt werden. Das ist der einzige Konvertierungsschritt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && test $(grep -c "isPlausible" InsightFlow/Views/Detail/WebsiteDetailViewModel.swift) -eq 0 && test $(grep -c "umamiAPI\." InsightFlow/Views/Detail/WebsiteDetailViewModel.swift) -eq 0 && test $(grep -c "plausibleAPI\." InsightFlow/Views/Detail/WebsiteDetailViewModel.swift) -eq 0 && echo "PASS: No isPlausible, no direct API references"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isPlausible" InsightFlow/Views/Detail/WebsiteDetailViewModel.swift` gibt 0 zurueck
    - `grep -c "umamiAPI" InsightFlow/Views/Detail/WebsiteDetailViewModel.swift` gibt 0 zurueck
    - `grep -c "plausibleAPI" InsightFlow/Views/Detail/WebsiteDetailViewModel.swift` gibt 0 zurueck
    - `grep "provider.getPages\|provider.getReferrers\|provider.getCountries" InsightFlow/Views/Detail/WebsiteDetailViewModel.swift` findet mindestens 3 Treffer
    - `grep "AnalyticsManager.shared.currentProvider" InsightFlow/Views/Detail/WebsiteDetailViewModel.swift` findet mindestens 1 Treffer
  </acceptance_criteria>
  <done>WebsiteDetailViewModel hat kein isPlausible-Branching mehr. Alle Methoden nutzen ausschliesslich das AnalyticsProvider-Protokoll via AnalyticsManager.shared.currentProvider.</done>
</task>

</tasks>

<verification>
```bash
# Kein isPlausible mehr im ViewModel
grep -c "isPlausible" InsightFlow/Views/Detail/WebsiteDetailViewModel.swift  # muss 0 sein

# Keine direkten API-Referenzen
grep -c "umamiAPI\|plausibleAPI" InsightFlow/Views/Detail/WebsiteDetailViewModel.swift  # muss 0 sein

# Protokoll hat neue Methoden
grep "func getRegions\|func getCities\|func getPageTitles" InsightFlow/Services/AnalyticsProvider.swift

# Build-Check
xcodebuild -scheme InsightFlow build -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5
```
</verification>

<success_criteria>
- WebsiteDetailViewModel enthaelt kein `if isPlausible` Branching
- Alle loadX()-Methoden nutzen provider.methodName() ueber das Protokoll
- AnalyticsProvider-Protokoll deckt alle Metriken ab
- UmamiAPI implementiert neue Wrapper-Methoden
- Build kompiliert clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-architektur/04-02-SUMMARY.md`
</output>
