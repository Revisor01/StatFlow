---
phase: 05-tests
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - InsightFlowTests/UmamiAPIParsingTests.swift
  - InsightFlowTests/PlausibleAPIParsingTests.swift
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "Umami JSON-Responses werden korrekt in WebsiteStatsResponse, WebsiteResponse, ActiveVisitorsResponse decodiert"
    - "Plausible JSON-Responses werden korrekt in PlausibleAPIResponse, PlausibleSitesResponse decodiert"
    - "PlausibleStatsResult parst metrics-Array korrekt in benannte Properties"
    - "Fehlerhafte JSON-Daten fuehren zu DecodingError, nicht zu Crashes"
  artifacts:
    - path: "InsightFlowTests/UmamiAPIParsingTests.swift"
      provides: "Umami response parsing tests mit statischen JSON-Daten"
      contains: "class UmamiAPIParsingTests"
    - path: "InsightFlowTests/PlausibleAPIParsingTests.swift"
      provides: "Plausible response parsing tests mit statischen JSON-Daten"
      contains: "class PlausibleAPIParsingTests"
  key_links:
    - from: "InsightFlowTests/UmamiAPIParsingTests.swift"
      to: "InsightFlow/Models/Stats.swift"
      via: "JSONDecoder().decode(WebsiteStatsResponse.self)"
      pattern: "decode.*WebsiteStatsResponse"
    - from: "InsightFlowTests/PlausibleAPIParsingTests.swift"
      to: "InsightFlow/Services/PlausibleAPI.swift"
      via: "JSONDecoder().decode(PlausibleAPIResponse.self)"
      pattern: "decode.*PlausibleAPIResponse"
---

<objective>
Unit Tests fuer JSON-Response-Parsing beider API-Clients (Umami + Plausible) mit statischen JSON-Daten. Kein Netzwerk, kein Actor-Zugriff — reine Decoding-Tests.

Purpose: API-Response-Parsing ist der kritischste Pfad fuer Datenintegritaet. Wenn sich Umami/Plausible API-Formate aendern, schlagen diese Tests sofort fehl.
Output: 2 Test-Klassen die alle Response-Typen beider APIs mit realistischen JSON-Fixtures testen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-tests/05-RESEARCH.md
@.planning/phases/05-tests/05-01-SUMMARY.md

<interfaces>
<!-- Umami Response Types (from InsightFlow/Models/Stats.swift) — all internal -->
```swift
struct WebsiteStatsResponse: Codable, Sendable {
    let pageviews: Int; let visitors: Int; let visits: Int; let bounces: Int; let totaltime: Int
    let comparison: StatsComparison
}
struct StatsComparison: Codable, Sendable {
    let pageviews: Int; let visitors: Int; let visits: Int; let bounces: Int; let totaltime: Int
}
struct WebsiteStats: Sendable {
    init(from response: WebsiteStatsResponse)  // computes change = value - comparison
    let pageviews: StatValue; let visitors: StatValue; let visits: StatValue; let bounces: StatValue; let totaltime: StatValue
}
struct StatValue: Codable, Sendable { let value: Int; let change: Int }
struct ActiveVisitorsResponse: Codable, Sendable { let visitors: Int; var count: Int { visitors } }
struct PageviewsData: Codable, Sendable { let pageviews: [TimeSeriesPoint]; let sessions: [TimeSeriesPoint] }
struct TimeSeriesPoint: Codable, Identifiable, Sendable { let x: String; let y: Int }
struct MetricItem: Codable, Identifiable, Sendable { let x: String; let y: Int }
struct SessionsResponse: Codable, Sendable { let data: [Session]; let count: Int; let page: Int; let pageSize: Int }
```

<!-- Umami Website Response (from InsightFlow/Models/Website.swift) -->
```swift
struct Website: Codable, Identifiable, Hashable, Sendable {
    let id: String; let name: String; let domain: String?; let shareId: String?; let teamId: String?; let resetAt: Date?; let createdAt: Date?
}
struct WebsiteResponse: Codable, Sendable { let data: [Website]?; let count: Int?; var websites: [Website] { data ?? [] } }
```

<!-- Plausible Response Types (from InsightFlow/Services/PlausibleAPI.swift) — all internal -->
```swift
struct PlausibleAPIResponse: Codable { let results: [PlausibleAPIResult] }
struct PlausibleAPIResult: Codable { let metrics: [Double]; let dimensions: [String] }
struct PlausibleStatsResult {
    var visitors: Int = 0; var pageviews: Int = 0; var visits: Int = 0; var bounceRate: Double = 0; var visitDuration: Int = 0
    init(from apiResult: PlausibleAPIResult)
}
struct PlausibleTimeseriesResult { let date: String; let value: Int; init(from apiResult: PlausibleAPIResult) }
struct PlausibleBreakdownResult { let dimension: String; let visitors: Int; init(from apiResult: PlausibleAPIResult) }
struct PlausibleSitesResponse: Codable { let sites: [PlausibleSite] }  // CodingKey: "data" -> sites
struct PlausibleSite: Codable, Identifiable { let domain: String; let timezone: String? }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Umami API Response Parsing Tests</name>
  <files>InsightFlowTests/UmamiAPIParsingTests.swift</files>
  <read_first>
    - InsightFlow/Models/Stats.swift (alle Response-Typen)
    - InsightFlow/Models/Website.swift (WebsiteResponse)
    - InsightFlow/Services/UmamiAPI.swift (Zeilen 280-330 fuer Parsing-Kontext)
  </read_first>
  <behavior>
    - testWebsiteStatsResponseDecoding: JSON mit pageviews=200, visitors=100, comparison.pageviews=180 -> WebsiteStatsResponse.pageviews == 200
    - testWebsiteStatsChangeCalculation: WebsiteStats(from: response) -> visitors.change == 100-90 == 10
    - testActiveVisitorsResponseDecoding: {"visitors": 42} -> count == 42
    - testWebsiteResponseDecoding: {"data": [{"id":"abc","name":"Test","domain":"test.com"}], "count":1} -> websites.count == 1, websites[0].domain == "test.com"
    - testWebsiteResponseEmptyData: {"data": null, "count": 0} -> websites == []
    - testPageviewsDataDecoding: JSON mit pageviews/sessions arrays -> korrekte TimeSeriesPoint-Werte
    - testMetricItemDecoding: [{"x":"Chrome","y":500}] -> name == "Chrome", value == 500
    - testSessionsResponseDecoding: JSON mit data/count/page/pageSize -> korrekte Session-Werte
    - testInvalidJSONThrowsDecodingError: ungueltiges JSON -> XCTAssertThrowsError mit DecodingError
    - testStatValueChangePercentage: StatValue(value: 110, change: 10) -> changePercentage == 100.0 (10/100*100)
  </behavior>
  <action>
1. **UmamiAPIParsingTests.swift erstellen** mit `@testable import InsightFlow`
2. Fuer jeden Test: statischen JSON-String als `Data` definieren, mit `JSONDecoder().decode()` parsen
3. Fuer WebsiteStats-Change-Test: WebsiteStatsResponse erstellen, dann `WebsiteStats(from:)` aufrufen und `.change` pruefen
4. Fuer Website-Response: JSONDecoder braucht `.iso8601` dateDecodingStrategy fuer createdAt/resetAt
5. Fuer Invalid-JSON-Test: `XCTAssertThrowsError(try JSONDecoder().decode(WebsiteStatsResponse.self, from: invalidData))`
6. StatValue.changePercentage testen: `StatValue(value: 110, change: 10)` -> `changePercentage` sollte `100.0` sein (10 / (110-10) * 100)

Alle JSON-Fixtures inline als String-Literals — keine separaten Fixture-Dateien.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild test -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:InsightFlowTests/UmamiAPIParsingTests 2>&1 | grep -E "Test Suite|passed|failed"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "func test" InsightFlowTests/UmamiAPIParsingTests.swift gibt mindestens 10 zurueck
    - grep -c "JSONDecoder" InsightFlowTests/UmamiAPIParsingTests.swift gibt mindestens 5 zurueck
    - grep -c "XCTAssert" InsightFlowTests/UmamiAPIParsingTests.swift gibt mindestens 10 zurueck
    - xcodebuild test output enthaelt "passed" und nicht "failed"
  </acceptance_criteria>
  <done>10 Umami-Parsing-Tests laufen gruen, alle Response-Typen sind abgedeckt</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Plausible API Response Parsing Tests</name>
  <files>InsightFlowTests/PlausibleAPIParsingTests.swift</files>
  <read_first>
    - InsightFlow/Services/PlausibleAPI.swift (Zeilen 620-720 fuer Response-Models)
  </read_first>
  <behavior>
    - testPlausibleAPIResponseDecoding: JSON mit results array -> results.count korrekt
    - testPlausibleStatsResultFromAPIResult: metrics=[100,200,150,45.5,120] -> visitors==100, pageviews==200, bounceRate==45.5
    - testPlausibleStatsResultEmptyMetrics: leeres metrics-Array -> alle Werte 0
    - testPlausibleStatsResultPartialMetrics: nur 2 metrics -> visitors+pageviews gesetzt, Rest 0
    - testPlausibleTimeseriesResult: dimensions=["2025-01-15"], metrics=[42] -> date=="2025-01-15", value==42
    - testPlausibleBreakdownResult: dimensions=["Chrome"], metrics=[500] -> dimension=="Chrome", visitors==500
    - testPlausibleSitesResponseDecoding: {"data": [{"domain":"example.com","timezone":"Europe/Berlin"}]} -> sites[0].domain == "example.com"
    - testPlausibleSitesResponseEmptyArray: {"data": []} -> sites.count == 0
    - testPlausibleAPIResultEmptyDimensions: dimensions leer -> TimeseriesResult.date == "", BreakdownResult.dimension == "Unknown"
    - testInvalidPlausibleJSONThrowsError: ungueltiges JSON -> DecodingError
  </behavior>
  <action>
1. **PlausibleAPIParsingTests.swift erstellen** mit `@testable import InsightFlow`
2. Fuer jeden Test: statischen JSON-String als `Data`, decode mit JSONDecoder
3. Fuer PlausibleStatsResult: erst PlausibleAPIResult decoden, dann `PlausibleStatsResult(from:)` aufrufen
4. Fuer PlausibleSitesResponse: CodingKey "data" -> "sites" beachten — JSON muss `"data"` Key haben
5. Edge cases: leere/partielle metrics-Arrays, leere dimensions

Alle JSON-Fixtures inline als String-Literals.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild test -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:InsightFlowTests/PlausibleAPIParsingTests 2>&1 | grep -E "Test Suite|passed|failed"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "func test" InsightFlowTests/PlausibleAPIParsingTests.swift gibt mindestens 10 zurueck
    - grep -c "PlausibleStatsResult" InsightFlowTests/PlausibleAPIParsingTests.swift gibt mindestens 3 zurueck
    - grep -c "XCTAssert" InsightFlowTests/PlausibleAPIParsingTests.swift gibt mindestens 10 zurueck
    - xcodebuild test output enthaelt "passed" und nicht "failed"
  </acceptance_criteria>
  <done>10 Plausible-Parsing-Tests laufen gruen, alle Response-Typen inkl. Edge Cases abgedeckt</done>
</task>

</tasks>

<verification>
```bash
# Beide Test-Klassen existieren
test -f InsightFlowTests/UmamiAPIParsingTests.swift && test -f InsightFlowTests/PlausibleAPIParsingTests.swift

# Alle Parsing-Tests laufen
xcodebuild test -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:InsightFlowTests/UmamiAPIParsingTests -only-testing:InsightFlowTests/PlausibleAPIParsingTests 2>&1 | grep -E "Test Suite|passed|failed"
```
</verification>

<success_criteria>
- UmamiAPIParsingTests: 10 Tests gruen (Stats, Websites, ActiveVisitors, Pageviews, Metrics, Sessions, Errors)
- PlausibleAPIParsingTests: 10 Tests gruen (APIResponse, StatsResult, Timeseries, Breakdown, Sites, Edge Cases)
- Kein Netzwerkzugriff in Tests — rein statische JSON-Daten
</success_criteria>

<output>
After completion, create `.planning/phases/05-tests/05-02-SUMMARY.md`
</output>
