---
phase: 05-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - InsightFlow.xcodeproj/project.pbxproj
  - InsightFlowTests/KeychainServiceTests.swift
  - InsightFlowTests/DateRangeTests.swift
  - InsightFlow/Services/AnalyticsCacheService.swift
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "xcodebuild kann das InsightFlowTests-Target bauen ohne Fehler"
    - "KeychainService save/load/delete round-trip funktioniert im Simulator"
    - "DateRange-Presets liefern korrekte Start/End-Invarianten"
    - "AnalyticsCacheService hat einen testbaren init mit cacheDirectoryOverride"
  artifacts:
    - path: "InsightFlowTests/KeychainServiceTests.swift"
      provides: "Keychain round-trip, account-scoping, delete tests"
      contains: "class KeychainServiceTests"
    - path: "InsightFlowTests/DateRangeTests.swift"
      provides: "DateRange preset, custom, unit logic tests"
      contains: "class DateRangeTests"
    - path: "InsightFlow.xcodeproj/project.pbxproj"
      provides: "InsightFlowTests target mit BUNDLE_LOADER und TEST_HOST"
      contains: "InsightFlowTests"
  key_links:
    - from: "InsightFlowTests/*"
      to: "InsightFlow module"
      via: "@testable import InsightFlow"
      pattern: "@testable import InsightFlow"
    - from: "InsightFlow.xcodeproj/project.pbxproj"
      to: "InsightFlowTests target"
      via: "PBXNativeTarget unit-test-bundle"
      pattern: "com.apple.product-type.unit-test-bundle"
---

<objective>
Test-Target in Xcode-Projekt anlegen, AnalyticsCacheService fuer Testbarkeit refactorn, und erste Unit Tests fuer KeychainService und DateRange schreiben.

Purpose: Ohne Test-Target kann kein einziger Test laufen. Dieses Plan legt die Infrastruktur und testet die zwei einfachsten Services (pure Logik / Keychain round-trip).
Output: Funktionierendes InsightFlowTests-Target mit 2 Test-Klassen und refactored CacheService init.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-tests/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From InsightFlow/Services/KeychainService.swift:
```swift
enum KeychainService: Sendable {
    enum Key: String, Sendable { case serverURL, token, username, providerType, serverType, apiKey, plausibleSiteId }
    enum CredentialType: String, CaseIterable { case token, apiKey }
    static func save(_ value: String, for key: Key) throws
    static func load(for key: Key) -> String?
    static func delete(for key: Key)
    static func deleteAll()
    static func saveCredential(_ value: String, type: CredentialType, accountId: String) throws
    static func loadCredential(type: CredentialType, accountId: String) -> String?
    static func deleteCredentials(for accountId: String)
}
```

From InsightFlow/Models/DateRange.swift:
```swift
enum DateRangePreset: String, CaseIterable, Identifiable, Sendable {
    case today, yesterday, thisWeek, last7Days, last30Days, thisMonth, lastMonth, thisYear, lastYear, custom
}
struct DateRange: Equatable, Sendable {
    let preset: DateRangePreset
    let customStart: Date?
    let customEnd: Date?
    var dates: (start: Date, end: Date)  // computed
    var unit: String  // "hour" | "day" | "month"
    static func custom(start: Date, end: Date) -> DateRange
}
```

From InsightFlow/Services/AnalyticsCacheService.swift:
```swift
final class AnalyticsCacheService: @unchecked Sendable {
    static let shared = AnalyticsCacheService()
    private var cacheDirectory: URL? { /* uses containerURL(forSecurityApplicationGroupIdentifier:) */ }
    private init() {}
    // Needs: init(cacheDirectoryOverride: URL) for testability
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test-Target in project.pbxproj anlegen und CacheService refactorn</name>
  <files>InsightFlow.xcodeproj/project.pbxproj, InsightFlowTests/DateRangeTests.swift, InsightFlow/Services/AnalyticsCacheService.swift</files>
  <read_first>
    - InsightFlow.xcodeproj/project.pbxproj (vollstaendig lesen — pbxproj-Struktur verstehen)
    - InsightFlow/Services/AnalyticsCacheService.swift
    - .planning/phases/05-tests/05-RESEARCH.md (Pattern 1 + Pattern 4)
  </read_first>
  <action>
1. **InsightFlowTests/ Verzeichnis anlegen** (mkdir -p InsightFlowTests)

2. **project.pbxproj bearbeiten** — neues Test-Target hinzufuegen:
   - Neue UUIDs generieren (12 hex chars, eindeutig im File) fuer: PBXGroup, PBXNativeTarget, PBXTargetDependency, PBXContainerItemProxy, 2x XCBuildConfiguration (Debug+Release), XCConfigurationList, PBXFileSystemSynchronizedRootGroup, PBXBuildFile (XCTest.framework)
   - `PBXFileSystemSynchronizedRootGroup` Eintrag fuer `InsightFlowTests/` (path = InsightFlowTests, name = InsightFlowTests)
   - `PBXNativeTarget` Eintrag:
     - name = "InsightFlowTests"
     - productType = "com.apple.product-type.unit-test-bundle"
     - buildPhases: Sources, Frameworks (mit XCTest.framework)
     - dependencies: PBXTargetDependency auf InsightFlow-Target
   - `PBXTargetDependency` + `PBXContainerItemProxy` auf das InsightFlow-Target
   - 2x `XCBuildConfiguration` (Debug + Release) mit:
     - BUNDLE_LOADER = "$(TEST_HOST)"
     - TEST_HOST = "$(BUILT_PRODUCTS_DIR)/InsightFlow.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/InsightFlow"
     - PRODUCT_BUNDLE_IDENTIFIER = de.godsapp.PrivacyFlow.InsightFlowTests
     - SWIFT_VERSION = 6.0
     - IPHONEOS_DEPLOYMENT_TARGET = 18.0
     - DEVELOPMENT_TEAM = J459G9CJT5
     - CODE_SIGN_STYLE = Automatic
     - GENERATE_INFOPLIST_FILE = YES
     - PRODUCT_NAME = "$(TARGET_NAME)"
     - TARGETED_DEVICE_FAMILY = "1"
   - `XCConfigurationList` fuer das neue Target
   - Target in `targets = (` Liste des PBXProject eintragen
   - `PBXFileSystemSynchronizedRootGroup` im `mainGroup` (PBXGroup) referenzieren

3. **AnalyticsCacheService.swift refactorn** — testbaren init hinzufuegen:
   - Neue Property: `private let overrideCacheDirectory: URL?`
   - Bestehenden `private init()` aendern zu: `private init() { self.overrideCacheDirectory = nil }`
   - Neuen init: `init(cacheDirectoryOverride: URL) { self.overrideCacheDirectory = cacheDirectoryOverride }`
   - `cacheDirectory` computed property erweitern: am Anfang `if let override = overrideCacheDirectory { return override }` vor der bestehenden App-Group-Logik
   - `shared` bleibt unveraendert (nutzt weiterhin den privaten init)

4. **DateRangeTests.swift als Platzhalter erstellen** (minimal, damit das Target etwas zum Kompilieren hat):
   ```swift
   import XCTest
   @testable import InsightFlow

   final class DateRangeTests: XCTestCase {
       func testPlaceholder() {
           XCTAssertTrue(true)
       }
   }
   ```

5. **Build verifizieren:**
   ```bash
   xcodebuild build-for-testing -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5
   ```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild build-for-testing -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "unit-test-bundle" InsightFlow.xcodeproj/project.pbxproj gibt mindestens 1 zurueck
    - grep -c "InsightFlowTests" InsightFlow.xcodeproj/project.pbxproj gibt mindestens 5 zurueck
    - grep -c "BUNDLE_LOADER" InsightFlow.xcodeproj/project.pbxproj gibt mindestens 2 zurueck
    - grep -c "overrideCacheDirectory" InsightFlow/Services/AnalyticsCacheService.swift gibt mindestens 2 zurueck
    - test -d InsightFlowTests (Verzeichnis existiert)
    - test -f InsightFlowTests/DateRangeTests.swift (Platzhalter-Test existiert)
  </acceptance_criteria>
  <done>InsightFlowTests-Target kompiliert erfolgreich, CacheService hat testbaren init, Build-for-testing laeuft ohne Fehler</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: KeychainService und DateRange Unit Tests schreiben</name>
  <files>InsightFlowTests/KeychainServiceTests.swift, InsightFlowTests/DateRangeTests.swift</files>
  <read_first>
    - InsightFlow/Services/KeychainService.swift
    - InsightFlow/Models/DateRange.swift
    - InsightFlowTests/DateRangeTests.swift (Platzhalter aus Task 1)
  </read_first>
  <behavior>
    KeychainServiceTests:
    - testSaveAndLoadCredential: saveCredential("test-token", .token, accountId) -> loadCredential(.token, accountId) == "test-token"
    - testDeleteCredentials: save -> delete -> load == nil
    - testAccountScopingIsolation: save fuer accountA -> load fuer accountB == nil
    - testOverwriteCredential: save "v1" -> save "v2" -> load == "v2"
    - testDeleteAllClearsLegacyKeys: save(for: .token) -> deleteAll() -> load(for: .token) == nil
    - testApiKeyCredentialType: saveCredential("key", .apiKey, id) -> loadCredential(.apiKey, id) == "key"

    DateRangeTests:
    - testTodayStartBeforeOrEqualEnd: .today.dates.start <= .today.dates.end
    - testTodayUnitIsHour: .today.unit == "hour"
    - testYesterdayUnitIsHour: .yesterday.unit == "hour"
    - testLast7DaysSpanIs6Days: Calendar diff == 6
    - testLast30DaysSpanIs29Days: Calendar diff == 29
    - testLast30DaysUnitIsDay: .last30Days.unit == "day"
    - testThisYearUnitIsMonth (wenn > 90 Tage): .thisYear.unit == "month" (wenn Jan-Dez > 90 Tage)
    - testCustomRangePreservesExactDates: custom(start, end).dates == (start, end)
    - testAllPresetsStartBeforeEnd: fuer alle Presets: start <= end
    - testThisWeekStartsOnMonday: weekday component von .thisWeek.dates.start == 2 (Monday)
  </behavior>
  <action>
1. **KeychainServiceTests.swift erstellen** mit:
   - `let testAccountId = "test-account-\(UUID().uuidString)"` als Property
   - `override func tearDown()`: `KeychainService.deleteCredentials(for: testAccountId)` + `KeychainService.deleteAll()` + `super.tearDown()`
   - Alle 6 Tests gemaess behavior-Block implementieren
   - Jeder Test nutzt eindeutige Account-IDs wo noetig (defer cleanup)
   - Falls `-34018 errSecMissingEntitlement` auftritt: Test mit `try XCTSkipIf` wrappen

2. **DateRangeTests.swift erweitern** (Platzhalter ersetzen):
   - Kein setUp/tearDown noetig (pure Logik)
   - Alle 10 Tests gemaess behavior-Block implementieren
   - Invarianten pruefen statt exakte Daten (zeitunabhaengig)
   - Fuer thisYear unit-Test: nur pruefen wenn aktuelles Datum > 90 Tage nach Jahresanfang

3. **Tests ausfuehren:**
   ```bash
   xcodebuild test -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:InsightFlowTests/KeychainServiceTests -only-testing:InsightFlowTests/DateRangeTests 2>&1 | grep -E "Test Case|passed|failed|error:"
   ```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild test -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:InsightFlowTests/KeychainServiceTests -only-testing:InsightFlowTests/DateRangeTests 2>&1 | grep -E "Test Suite|passed|failed"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "func test" InsightFlowTests/KeychainServiceTests.swift gibt mindestens 6 zurueck
    - grep -c "func test" InsightFlowTests/DateRangeTests.swift gibt mindestens 10 zurueck
    - grep -c "XCTAssert" InsightFlowTests/KeychainServiceTests.swift gibt mindestens 6 zurueck
    - grep -c "XCTAssert" InsightFlowTests/DateRangeTests.swift gibt mindestens 10 zurueck
    - xcodebuild test output enthaelt "passed" und nicht "failed"
  </acceptance_criteria>
  <done>16 Tests (6 Keychain + 10 DateRange) laufen gruen im Simulator</done>
</task>

</tasks>

<verification>
```bash
# Test-Target existiert
grep -c "unit-test-bundle" InsightFlow.xcodeproj/project.pbxproj

# Alle Tests laufen
xcodebuild test -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:InsightFlowTests 2>&1 | grep -E "Test Suite|passed|failed"

# CacheService refactored
grep "overrideCacheDirectory" InsightFlow/Services/AnalyticsCacheService.swift
```
</verification>

<success_criteria>
- InsightFlowTests-Target kompiliert und laeuft im Simulator
- KeychainService: 6 Tests gruen (save/load, delete, scoping, overwrite, deleteAll, apiKey)
- DateRange: 10 Tests gruen (presets, custom, unit, invarianten)
- AnalyticsCacheService hat testbaren init(cacheDirectoryOverride:)
</success_criteria>

<output>
After completion, create `.planning/phases/05-tests/05-01-SUMMARY.md`
</output>
