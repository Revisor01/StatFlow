---
phase: 05-tests
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - InsightFlowTests/AccountManagerTests.swift
  - InsightFlowTests/AnalyticsCacheServiceTests.swift
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "AccountManager kann Accounts hinzufuegen, entfernen und als aktiv setzen"
    - "AccountManager persistiert Accounts in UserDefaults und Credentials in Keychain"
    - "AnalyticsCacheService speichert und laedt Websites/Stats mit korrektem TTL"
    - "Abgelaufene Cache-Eintraege werden als isExpired markiert"
  artifacts:
    - path: "InsightFlowTests/AccountManagerTests.swift"
      provides: "AccountManager CRUD, Persistence, Active-Account Tests"
      contains: "class AccountManagerTests"
    - path: "InsightFlowTests/AnalyticsCacheServiceTests.swift"
      provides: "Cache save/load, TTL, clear Tests"
      contains: "class AnalyticsCacheServiceTests"
  key_links:
    - from: "InsightFlowTests/AccountManagerTests.swift"
      to: "InsightFlow/Services/AccountManager.swift"
      via: "AccountManager.shared"
      pattern: "AccountManager.shared"
    - from: "InsightFlowTests/AnalyticsCacheServiceTests.swift"
      to: "InsightFlow/Services/AnalyticsCacheService.swift"
      via: "AnalyticsCacheService(cacheDirectoryOverride:)"
      pattern: "cacheDirectoryOverride"
---

<objective>
Unit Tests fuer AccountManager (CRUD, Persistence, Credential-Handling) und AnalyticsCacheService (save/load, TTL-Expiry, clear) schreiben.

Purpose: AccountManager ist die zentrale Auth-Autoritaet (Phase 4). Cache-Service ermoeglicht Offline-Zugriff. Beide muessen zuverlaessig funktionieren.
Output: 2 Test-Klassen die Singleton-basierte Services mit proper State-Cleanup testen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-tests/05-RESEARCH.md
@.planning/phases/05-tests/05-01-SUMMARY.md

<interfaces>
<!-- AccountManager (from InsightFlow/Services/AccountManager.swift) — @MainActor -->
```swift
@MainActor
class AccountManager: ObservableObject {
    static let shared = AccountManager()
    private let accountsKey = "analytics_accounts"
    private let activeAccountKey = "active_account_id"
    @Published var accounts: [AnalyticsAccount] = []
    @Published var activeAccount: AnalyticsAccount?
    func addAccount(_ account: AnalyticsAccount)
    func removeAccount(_ account: AnalyticsAccount)
    func setActiveAccount(_ account: AnalyticsAccount)
    func clearActiveAccount()
    // private init() — Tests muessen .shared verwenden
}

struct AnalyticsAccount: Codable, Identifiable, Equatable {
    let id: UUID; let name: String; let serverURL: String
    let providerType: AnalyticsProviderType; let credentials: AccountCredentials
    var sites: [String]?; let createdAt: Date
}
struct AccountCredentials: Codable { let token: String?; let apiKey: String? }
enum AnalyticsProviderType: String, Codable, CaseIterable { case umami, plausible }
```

<!-- AnalyticsCacheService (refactored in Plan 01) -->
```swift
final class AnalyticsCacheService: @unchecked Sendable {
    static let shared = AnalyticsCacheService()
    init(cacheDirectoryOverride: URL)  // NEW from Plan 01
    func saveWebsites(_ websites: [CachedWebsite], accountId: String)
    func loadWebsites(accountId: String) -> CachedData<[CachedWebsite]>?
    func saveStats(_ stats: CachedStats, websiteId: String, dateRangeId: String)
    func loadStats(websiteId: String, dateRangeId: String) -> CachedData<CachedStats>?
    func clearAllCache()
    func clearExpiredCache()
    func clearCache(for websiteId: String)
    func clearCache(forAccount accountId: String)
}

struct CachedWebsite: Codable {
    let id: String; let name: String; let domain: String; let shareId: String?; let provider: String
    init(from website: AnalyticsWebsite)
}
struct CachedStats: Codable { let visitors: CachedStatValue; ... }
struct CachedStatValue: Codable { let value: Int; let change: Int }
struct CachedData<T> { let data: T; let cachedAt: Date; let isExpired: Bool }
struct AnalyticsWebsite: Identifiable, Codable {
    let id: String; let name: String; let domain: String; let shareId: String?
    let provider: AnalyticsProviderType
}
struct StatValue: Codable, Sendable { let value: Int; let change: Int }
struct AnalyticsStats { let visitors: StatValue; let pageviews: StatValue; let visits: StatValue; let bounces: StatValue; let totaltime: StatValue }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AccountManager Unit Tests</name>
  <files>InsightFlowTests/AccountManagerTests.swift</files>
  <read_first>
    - InsightFlow/Services/AccountManager.swift (vollstaendig)
    - InsightFlow/Services/KeychainService.swift (fuer Credential-Cleanup in tearDown)
  </read_first>
  <behavior>
    - testAddAccount: addAccount -> accounts.count == 1, accounts[0].name == expected
    - testAddDuplicateServerURLUpdatesExisting: add account1 mit URL "https://a.com" -> add account2 mit gleicher URL -> accounts.count == 1 (updated)
    - testRemoveAccount: add -> remove -> accounts.count == 0
    - testRemoveAccountClearsKeychain: add mit token -> remove -> KeychainService.loadCredential(.token, id) == nil
    - testSetActiveAccount: add -> setActiveAccount -> activeAccount?.id == account.id
    - testClearActiveAccount: add -> setActive -> clearActive -> activeAccount == nil
    - testAccountsPersistInUserDefaults: add -> UserDefaults.standard.data(forKey: "analytics_accounts") != nil
    - testActiveAccountIdPersistsInUserDefaults: add -> setActive -> UserDefaults.standard.string(forKey: "active_account_id") != nil
  </behavior>
  <action>
1. **AccountManagerTests.swift erstellen** mit `@testable import InsightFlow`
2. **Alle Testmethoden als `@MainActor func ... async`** deklarieren (AccountManager ist @MainActor)
3. **setUp():** `@MainActor override func setUp() async throws` — alle Accounts entfernen, UserDefaults keys clearen:
   ```swift
   let manager = AccountManager.shared
   for account in manager.accounts {
       manager.removeAccount(account)
   }
   UserDefaults.standard.removeObject(forKey: "analytics_accounts")
   UserDefaults.standard.removeObject(forKey: "active_account_id")
   ```
4. **tearDown():** gleiche Cleanup wie setUp (sicherheitshalber)
5. **Helper-Methode** fuer Test-Account-Erstellung:
   ```swift
   private func makeTestAccount(name: String = "Test", serverURL: String = "https://test.example.com", providerType: AnalyticsProviderType = .umami) -> AnalyticsAccount {
       AnalyticsAccount(name: name, serverURL: serverURL, providerType: providerType, credentials: AccountCredentials(token: "test-token", apiKey: nil))
   }
   ```
6. Alle 8 Tests gemaess behavior-Block implementieren
7. **Achtung:** AccountManager.shared ist ein Singleton — State-Cleanup ist kritisch. Jeder Test MUSS unabhaengig sein.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild test -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:InsightFlowTests/AccountManagerTests 2>&1 | grep -E "Test Suite|passed|failed"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "func test" InsightFlowTests/AccountManagerTests.swift gibt mindestens 8 zurueck
    - grep -c "@MainActor" InsightFlowTests/AccountManagerTests.swift gibt mindestens 1 zurueck
    - grep -c "AccountManager.shared" InsightFlowTests/AccountManagerTests.swift gibt mindestens 2 zurueck
    - grep -c "XCTAssert" InsightFlowTests/AccountManagerTests.swift gibt mindestens 8 zurueck
    - xcodebuild test output enthaelt "passed" und nicht "failed"
  </acceptance_criteria>
  <done>8 AccountManager-Tests laufen gruen, Singleton-State wird zwischen Tests korrekt bereinigt</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AnalyticsCacheService Unit Tests</name>
  <files>InsightFlowTests/AnalyticsCacheServiceTests.swift</files>
  <read_first>
    - InsightFlow/Services/AnalyticsCacheService.swift (vollstaendig — refactored init aus Plan 01)
    - InsightFlow/Services/AnalyticsProvider.swift (fuer AnalyticsWebsite, AnalyticsProviderType)
  </read_first>
  <behavior>
    - testSaveAndLoadWebsites: saveWebsites -> loadWebsites -> data.count == 1, data[0].domain == "test.com"
    - testLoadWebsitesNotExpired: sofort nach save -> isExpired == false
    - testSaveAndLoadStats: saveStats -> loadStats -> data.visitors.value == expected
    - testLoadNonExistentKeyReturnsNil: loadWebsites("nonexistent") == nil
    - testClearAllCache: save -> clearAll -> load == nil
    - testClearCacheForAccount: save fuer acc-1 und acc-2 -> clearCache(forAccount: "acc-1") -> load(acc-1) == nil, load(acc-2) != nil
    - testClearCacheForWebsite: saveStats fuer web-1 und web-2 -> clearCache(for: "web-1") -> load(web-1) == nil, load(web-2) != nil
    - testCacheSizeReturnsNonZeroAfterSave: save -> cacheSize() > 0
  </behavior>
  <action>
1. **AnalyticsCacheServiceTests.swift erstellen** mit `@testable import InsightFlow`
2. **setUp():** temporaeres Verzeichnis erstellen, `sut = AnalyticsCacheService(cacheDirectoryOverride: tempDir)`
   ```swift
   var tempDir: URL!
   var sut: AnalyticsCacheService!

   override func setUp() {
       super.setUp()
       tempDir = FileManager.default.temporaryDirectory.appendingPathComponent(UUID().uuidString)
       try! FileManager.default.createDirectory(at: tempDir, withIntermediateDirectories: true)
       sut = AnalyticsCacheService(cacheDirectoryOverride: tempDir)
   }
   ```
3. **tearDown():** `try? FileManager.default.removeItem(at: tempDir)`
4. **Helper fuer CachedWebsite:**
   ```swift
   private func makeCachedWebsite(id: String = "1", domain: String = "test.com") -> CachedWebsite {
       CachedWebsite(from: AnalyticsWebsite(id: id, name: "Test", domain: domain, shareId: nil, provider: .umami))
   }
   ```
5. **Helper fuer CachedStats:**
   ```swift
   private func makeCachedStats(visitors: Int = 100) -> CachedStats {
       CachedStats(from: AnalyticsStats(
           visitors: StatValue(value: visitors, change: 10),
           pageviews: StatValue(value: 200, change: 20),
           visits: StatValue(value: 150, change: 15),
           bounces: StatValue(value: 50, change: -5),
           totaltime: StatValue(value: 3000, change: 300)
       ))
   }
   ```
6. Alle 8 Tests gemaess behavior-Block implementieren
7. **Achtung:** AnalyticsWebsite init braucht `provider: AnalyticsProviderType` — pruefen ob der init-Signature so stimmt oder ob es `provider: .umami` ist
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild test -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:InsightFlowTests/AnalyticsCacheServiceTests 2>&1 | grep -E "Test Suite|passed|failed"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "func test" InsightFlowTests/AnalyticsCacheServiceTests.swift gibt mindestens 8 zurueck
    - grep -c "cacheDirectoryOverride" InsightFlowTests/AnalyticsCacheServiceTests.swift gibt mindestens 1 zurueck
    - grep -c "XCTAssert" InsightFlowTests/AnalyticsCacheServiceTests.swift gibt mindestens 8 zurueck
    - xcodebuild test output enthaelt "passed" und nicht "failed"
  </acceptance_criteria>
  <done>8 Cache-Tests laufen gruen mit temporaerem Verzeichnis, kein App-Group-Entitlement noetig</done>
</task>

</tasks>

<verification>
```bash
# Beide Test-Klassen existieren
test -f InsightFlowTests/AccountManagerTests.swift && test -f InsightFlowTests/AnalyticsCacheServiceTests.swift

# Alle Tests dieser Plan laufen
xcodebuild test -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:InsightFlowTests/AccountManagerTests -only-testing:InsightFlowTests/AnalyticsCacheServiceTests 2>&1 | grep -E "Test Suite|passed|failed"

# Gesamte Test-Suite (alle 6 Klassen) laeuft gruen
xcodebuild test -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:InsightFlowTests 2>&1 | grep -E "Test Suite|passed|failed"
```
</verification>

<success_criteria>
- AccountManagerTests: 8 Tests gruen (CRUD, Persistence, ActiveAccount, Keychain-Cleanup)
- AnalyticsCacheServiceTests: 8 Tests gruen (save/load, TTL, clear, cacheSize)
- Gesamte Test-Suite (alle 6 Klassen, ~52 Tests) laeuft gruen
</success_criteria>

<output>
After completion, create `.planning/phases/05-tests/05-03-SUMMARY.md`
</output>
