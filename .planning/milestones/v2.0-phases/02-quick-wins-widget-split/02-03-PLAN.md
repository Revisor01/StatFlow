---
phase: 02-quick-wins-widget-split
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - InsightFlow/Views/Detail/WebsiteDetailView.swift
  - InsightFlow/Views/Detail/WebsiteDetailChartSection.swift
  - InsightFlow/Views/Detail/WebsiteDetailMetricsSections.swift
  - InsightFlow/Views/Detail/WebsiteDetailSupportingViews.swift
  - InsightFlow/Views/Detail/CompareView.swift
  - InsightFlow/Views/Detail/CompareChartSection.swift
  - InsightFlow/Views/Detail/CompareViewModel.swift
  - InsightFlow/Views/Detail/CompareHeroCard.swift
  - InsightFlow/Views/Admin/AdminView.swift
  - InsightFlow/Views/Admin/AdminCards.swift
  - InsightFlow/Views/Admin/AdminSheets.swift
autonomous: true
requirements:
  - STRUC-02
  - STAB-03

must_haves:
  truths:
    - "WebsiteDetailView.swift ist unter 600 Zeilen"
    - "AdminView.swift ist unter 600 Zeilen"
    - "CompareView.swift ist unter 600 Zeilen"
    - "Alle extrahierten Views kompilieren fehlerfrei"
    - "Kein unwrapped print() in den 3 bearbeiteten Originaldateien"
  artifacts:
    - path: "InsightFlow/Views/Detail/WebsiteDetailView.swift"
      provides: "Reduzierte Hauptview"
      max_lines: 600
    - path: "InsightFlow/Views/Detail/WebsiteDetailChartSection.swift"
      provides: "Chart-Sektion mit @Binding fuer State"
      contains: "struct WebsiteDetailChartSection"
    - path: "InsightFlow/Views/Detail/WebsiteDetailMetricsSections.swift"
      provides: "Location, Tech, Language, Screen, Events Sektionen"
      contains: "struct WebsiteDetailMetricsSections"
    - path: "InsightFlow/Views/Detail/WebsiteDetailSupportingViews.swift"
      provides: "SectionHeader, DateRangeChip, StatCard etc."
      contains: "struct SectionHeader"
    - path: "InsightFlow/Views/Detail/CompareChartSection.swift"
      provides: "Comparison Chart Sektion"
      contains: "CompareChartSection"
    - path: "InsightFlow/Views/Detail/CompareViewModel.swift"
      provides: "CompareViewModel"
      contains: "class CompareViewModel"
    - path: "InsightFlow/Views/Detail/CompareHeroCard.swift"
      provides: "CompareHeroCard"
      contains: "struct CompareHeroCard"
    - path: "InsightFlow/Views/Admin/AdminCards.swift"
      provides: "WebsiteAdminCard, TeamCard, UserCard, PlausibleSiteAdminCard"
      contains: "struct WebsiteAdminCard"
    - path: "InsightFlow/Views/Admin/AdminSheets.swift"
      provides: "Alle Admin-Sheet-Views"
      contains: "struct CreateWebsiteSheet"
  key_links:
    - from: "InsightFlow/Views/Detail/WebsiteDetailView.swift"
      to: "InsightFlow/Views/Detail/WebsiteDetailChartSection.swift"
      via: "@Binding state properties"
      pattern: "WebsiteDetailChartSection"
    - from: "InsightFlow/Views/Detail/CompareView.swift"
      to: "InsightFlow/Views/Detail/CompareChartSection.swift"
      via: "Subview reference"
      pattern: "CompareChartSection"
    - from: "InsightFlow/Views/Admin/AdminView.swift"
      to: "InsightFlow/Views/Admin/AdminSheets.swift"
      via: "Sheet presentation"
      pattern: "AdminSheets\\|CreateWebsiteSheet"
    - from: "InsightFlow/Views/Admin/AdminView.swift"
      to: "InsightFlow/Views/Admin/AdminCards.swift"
      via: "Card-View references in Website/Team/User sections"
      pattern: "WebsiteAdminCard"
---

<objective>
Die drei groessten Views (WebsiteDetailView 1611Z, AdminView 1318Z, CompareView 1183Z) durch Extraktion von Subviews auf unter 600 Zeilen reduzieren. Gleichzeitig die 4 verbleibenden unwrapped print()-Calls in diesen Dateien wrappen.

Purpose: STRUC-02 — Views navigierbar und wartbar machen. Partial STAB-03 — letzte print()-Calls in Views wrappen.
Output: 8 neue Subview-Dateien, 3 reduzierte Hauptdateien.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-quick-wins-widget-split/02-RESEARCH.md
@InsightFlow/Views/Detail/WebsiteDetailView.swift
@InsightFlow/Views/Detail/CompareView.swift
@InsightFlow/Views/Admin/AdminView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: WebsiteDetailView und CompareView Subviews extrahieren</name>
  <files>
    InsightFlow/Views/Detail/WebsiteDetailView.swift,
    InsightFlow/Views/Detail/WebsiteDetailChartSection.swift,
    InsightFlow/Views/Detail/WebsiteDetailMetricsSections.swift,
    InsightFlow/Views/Detail/WebsiteDetailSupportingViews.swift,
    InsightFlow/Views/Detail/CompareView.swift,
    InsightFlow/Views/Detail/CompareChartSection.swift,
    InsightFlow/Views/Detail/CompareViewModel.swift,
    InsightFlow/Views/Detail/CompareHeroCard.swift
  </files>
  <read_first>
    InsightFlow/Views/Detail/WebsiteDetailView.swift,
    InsightFlow/Views/Detail/CompareView.swift,
    InsightFlow/Views/Detail/WebsiteDetailViewModel.swift
  </read_first>
  <action>
**WebsiteDetailView.swift (1611 → ~420 Zeilen):**

1. **WebsiteDetailChartSection.swift** erstellen (~420 Zeilen):
   - Extrahiere MARK "Main Chart" (Zeile 336-726): die gesamte Chart-Sektion
   - Als eigenstaendige `struct WebsiteDetailChartSection: View` mit `@Binding` fuer veraenderliche State-Properties:
     - `@Binding var selectedMetric` (Typ aus dem Original uebernehmen)
     - `@Binding var selectedChartPoint` (optional TimeSeriesPoint oder aehnlich)
     - `@Binding var selectedChartStyle`
     - Read-only Properties als `let` Parameter (z.B. `viewModel`, Daten-Arrays)
   - Im Parent (WebsiteDetailView): computed property durch Subview-Aufruf ersetzen
   - Imports: `import SwiftUI`, `import Charts` (falls verwendet)

2. **WebsiteDetailMetricsSections.swift** erstellen (~290 Zeilen):
   - Extrahiere MARK "Location Section" (Zeile 922-1004), "Tech Section" (1005-1101), "Language & Screen Section" (1102-1151), "Events Section" (1152-1178)
   - Als Container-Struct oder einzelne Structs — je nachdem ob sie gemeinsamen State teilen
   - Alle benoetigten ViewModel-Properties als Parameter uebergeben
   - Imports: `import SwiftUI`

3. **WebsiteDetailSupportingViews.swift** erstellen (~360 Zeilen):
   - Extrahiere MARK "Supporting Views" (ab Zeile 1280 bis Ende): SectionHeader, DateRangeChip, QuickActionCard, StatCard und weitere Supporting Views
   - Diese sind bereits eigenstaendige Structs — einfach in neue Datei verschieben
   - Imports: `import SwiftUI`

4. In **WebsiteDetailView.swift**: Die extrahierten Abschnitte entfernen und durch Subview-Aufrufe ersetzen. Die 0 unwrapped prints hier — keine STAB-03-Arbeit noetig (bereits alle gewrappt laut Audit).

**CompareView.swift (1183 → ~370 Zeilen):**

5. **CompareChartSection.swift** erstellen (~620 Zeilen):
   - Extrahiere MARK "Comparison Chart" (Zeile 377-965): der groesste einzelne Block
   - Als eigenstaendige View-Struct mit Parametern fuer die benoetigten Daten/State
   - Imports: `import SwiftUI`, `import Charts` (falls verwendet)

6. **CompareViewModel.swift** erstellen (~120 Zeilen):
   - Extrahiere MARK "ViewModel" (Zeile 1066-Ende)
   - ObservableObject-Klasse, eigenstaendig
   - Imports: `import SwiftUI`, `import Foundation`

7. **CompareHeroCard.swift** erstellen (~110 Zeilen):
   - Extrahiere MARK "Compare Hero Card" (Zeile 966-1065)
   - Eigenstaendige View-Struct
   - Imports: `import SwiftUI`

8. In **CompareView.swift**: Extrahierte Abschnitte entfernen, durch Referenzen ersetzen. Die 2 unwrapped print()-Calls mit `#if DEBUG` wrappen.

WICHTIG bei allen Extraktionen:
- @State/@StateObject Properties die in extrahierten Views geschrieben werden → @Binding im Subview
- @State Properties die nur gelesen werden → let Parameter
- @ObservedObject/@EnvironmentObject → in Subview als Parameter oder eigenes @ObservedObject
- Computed properties die auf self.xyz zugreifen muessen Parameter werden
- Alle Typen (enums, structs) die in extrahierten Code referenziert werden muessen im selben Module sichtbar sein (kein Problem — gleiches Target)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && WDV=$(wc -l < InsightFlow/Views/Detail/WebsiteDetailView.swift) && CV=$(wc -l < InsightFlow/Views/Detail/CompareView.swift) && test "$WDV" -lt 600 && test "$CV" -lt 600 && test -f InsightFlow/Views/Detail/WebsiteDetailChartSection.swift && test -f InsightFlow/Views/Detail/WebsiteDetailMetricsSections.swift && test -f InsightFlow/Views/Detail/WebsiteDetailSupportingViews.swift && test -f InsightFlow/Views/Detail/CompareChartSection.swift && test -f InsightFlow/Views/Detail/CompareViewModel.swift && test -f InsightFlow/Views/Detail/CompareHeroCard.swift && echo "PASS: WDV=$WDV CV=$CV"</automated>
  </verify>
  <acceptance_criteria>
    - WebsiteDetailView.swift unter 600 Zeilen (Ziel ~420)
    - CompareView.swift unter 600 Zeilen (Ziel ~370)
    - WebsiteDetailChartSection.swift existiert mit `struct WebsiteDetailChartSection: View`
    - WebsiteDetailMetricsSections.swift existiert mit Location/Tech/Language/Events Sections
    - WebsiteDetailSupportingViews.swift existiert mit SectionHeader, DateRangeChip etc.
    - CompareChartSection.swift existiert mit Comparison Chart Logic
    - CompareViewModel.swift existiert mit CompareViewModel class
    - CompareHeroCard.swift existiert mit CompareHeroCard struct
    - CompareView.swift: 0 unwrapped print()-Calls
  </acceptance_criteria>
  <done>WebsiteDetailView und CompareView sind unter 600 Zeilen. 6 neue Subview-Dateien erstellt. print()-Calls in CompareView gewrappt.</done>
</task>

<task type="auto">
  <name>Task 2: AdminView Subviews extrahieren und Build verifizieren</name>
  <files>
    InsightFlow/Views/Admin/AdminView.swift,
    InsightFlow/Views/Admin/AdminCards.swift,
    InsightFlow/Views/Admin/AdminSheets.swift
  </files>
  <read_first>
    InsightFlow/Views/Admin/AdminView.swift
  </read_first>
  <action>
**AdminView.swift (1318 → ~350 Zeilen):**

1. **AdminCards.swift** erstellen (~320 Zeilen):
   - Extrahiere MARK "Website Admin Card" (Zeile 247-352)
   - Extrahiere MARK "Team Card" (Zeile 353-437)
   - Extrahiere MARK "User Card" (Zeile 438-484)
   - Extrahiere MARK "Plausible Site Admin Card" (Zeile 627-682)
   - Alle Card-Structs sind bereits eigenstaendige View-Typen — einfach verschieben
   - Imports: `import SwiftUI`

2. **AdminSheets.swift** erstellen (~560 Zeilen):
   - Extrahiere MARK "Create Sheets" (Zeile 485-626)
   - Extrahiere MARK "Plausible Tracking Code Sheet" (Zeile 683-739)
   - Extrahiere MARK "Tracking Code Sheet (Umami)" (Zeile 740-799)
   - Extrahiere MARK "Share Link Sheet" (Zeile 800-899)
   - Extrahiere MARK "Edit Website Sheet" (Zeile 1124-1184)
   - Extrahiere MARK "Team Member Sheet" (Zeile 1185-1290)
   - Alle Sheet-Structs sind bereits eigenstaendige View-Typen mit @ObservedObject viewModel — einfach verschieben
   - Imports: `import SwiftUI`

3. In **AdminView.swift** verbleiben:
   - AdminView struct mit body, Sections (Websites/Teams/Users)
   - MARK "Admin Section Enum" (Zeile 225-246)
   - MARK "ViewModel" (Zeile 900-1123) inkl. Umami/Plausible Methoden
   - MARK "ViewModel Extensions" (Zeile 1291-1303)
   - MARK "User Extension for localized role" (Zeile 1304-Ende)
   - Die 2 unwrapped print()-Calls mit `#if DEBUG` wrappen

WICHTIG:
- Sheet-Views referenzieren `AdminViewModel` via @ObservedObject — das funktioniert weiterhin da gleiches Target
- Card-Views haben eigene @State/@Binding — keine Aenderung noetig
- Sheets die `@Environment(\.dismiss)` nutzen behalten das unveraendert

Nach der Extraktion: Build-Check fuer das gesamte Projekt:
```
xcodebuild build -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16'
```

Bei Compiler-Fehlern: fehlende Imports oder Parameter-Mismatches sofort fixen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && AV=$(wc -l < InsightFlow/Views/Admin/AdminView.swift) && test "$AV" -lt 600 && test -f InsightFlow/Views/Admin/AdminCards.swift && test -f InsightFlow/Views/Admin/AdminSheets.swift && grep -q "struct WebsiteAdminCard" InsightFlow/Views/Admin/AdminCards.swift && grep -q "Sheet" InsightFlow/Views/Admin/AdminSheets.swift && echo "PASS: AV=$AV"</automated>
  </verify>
  <acceptance_criteria>
    - AdminView.swift unter 600 Zeilen (Ziel ~350-400)
    - AdminCards.swift existiert mit WebsiteAdminCard, TeamCard, UserCard, PlausibleSiteAdminCard
    - AdminSheets.swift existiert mit allen Sheet-Views (Create, TrackingCode, ShareLink, Edit, TeamMember)
    - AdminView.swift: 0 unwrapped print()-Calls
    - `xcodebuild build -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16'` kompiliert ohne Fehler
  </acceptance_criteria>
  <done>AdminView ist unter 600 Zeilen. 2 neue Dateien (AdminCards, AdminSheets) erstellt. print()-Calls gewrappt. Gesamtprojekt kompiliert fehlerfrei.</done>
</task>

</tasks>

<verification>
1. `wc -l InsightFlow/Views/Detail/WebsiteDetailView.swift` — unter 600
2. `wc -l InsightFlow/Views/Admin/AdminView.swift` — unter 600
3. `wc -l InsightFlow/Views/Detail/CompareView.swift` — unter 600
4. `ls InsightFlow/Views/Detail/` — 6 neue Dateien vorhanden
5. `ls InsightFlow/Views/Admin/` — 2 neue Dateien vorhanden
6. `xcodebuild build -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16'` — kompiliert
7. Kein unwrapped print() in AdminView.swift oder CompareView.swift
</verification>

<success_criteria>
- Alle 3 Views unter 600 Zeilen (Success Criterion aus ROADMAP)
- 8 neue Subview-Dateien in korrekten Verzeichnissen
- App kompiliert fehlerfrei
- 4 print()-Calls in AdminView (2) und CompareView (2) sind in #if DEBUG gewrappt
</success_criteria>

<output>
After completion, create `.planning/phases/02-quick-wins-widget-split/02-03-SUMMARY.md`
</output>
