---
phase: 02-quick-wins-widget-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - InsightFlowWidget/InsightFlowWidget.swift
  - InsightFlowWidget/Models/WidgetModels.swift
  - InsightFlowWidget/Models/WidgetTimeRange.swift
  - InsightFlowWidget/Storage/WidgetStorage.swift
  - InsightFlowWidget/Cache/WidgetCache.swift
  - InsightFlowWidget/Intents/WidgetIntents.swift
  - InsightFlowWidget/Networking/WidgetNetworking.swift
  - InsightFlowWidget/Views/WidgetChartViews.swift
  - InsightFlowWidget/Views/WidgetSizeViews.swift
autonomous: true
requirements:
  - STRUC-01
  - STAB-03

must_haves:
  truths:
    - "InsightFlowWidget.swift ist auf unter 100 Zeilen reduziert"
    - "Widget-Extension kompiliert fehlerfrei nach dem Split"
    - "Alle Widget-Groessen (small, medium) zeigen Daten korrekt"
    - "widgetLog() gibt in Release-Builds nichts aus"
  artifacts:
    - path: "InsightFlowWidget/InsightFlowWidget.swift"
      provides: "Widget-Entry-Point (nur PrivacyFlowWidget + PrivacyFlowWidgetEntryView)"
      max_lines: 100
    - path: "InsightFlowWidget/Models/WidgetModels.swift"
      provides: "WidgetProviderType, WidgetAccount, WidgetData, StatsEntry"
      contains: "struct WidgetAccount"
    - path: "InsightFlowWidget/Storage/WidgetStorage.swift"
      provides: "WidgetAccountsStorage, WidgetCredentials"
      contains: "class WidgetAccountsStorage"
    - path: "InsightFlowWidget/Networking/WidgetNetworking.swift"
      provides: "Provider timeline logic, fetchStats, fetchUmamiStats, fetchPlausibleStats"
      contains: "struct Provider"
    - path: "InsightFlowWidget/Views/WidgetChartViews.swift"
      provides: "BarChartView, LineChartView"
      contains: "struct BarChartView"
    - path: "InsightFlowWidget/Views/WidgetSizeViews.swift"
      provides: "SmallWidgetView, MediumWidgetView"
      contains: "struct SmallWidgetView"
    - path: "InsightFlowWidget/Intents/WidgetIntents.swift"
      provides: "AccountEntity, WebsiteEntity, ConfigureWidgetIntent"
      contains: "struct AccountEntity"
    - path: "InsightFlowWidget/Cache/WidgetCache.swift"
      provides: "WidgetCache"
      contains: "struct WidgetCache"
    - path: "InsightFlowWidget/Models/WidgetTimeRange.swift"
      provides: "WidgetTimeRange, WidgetChartStyle"
      contains: "enum WidgetTimeRange"
  key_links:
    - from: "InsightFlowWidget/InsightFlowWidget.swift"
      to: "InsightFlowWidget/Networking/WidgetNetworking.swift"
      via: "Provider reference in PrivacyFlowWidget"
      pattern: "Provider"
    - from: "InsightFlowWidget/Networking/WidgetNetworking.swift"
      to: "InsightFlowWidget/Storage/WidgetStorage.swift"
      via: "WidgetAccountsStorage usage in Provider"
      pattern: "WidgetAccountsStorage"
    - from: "InsightFlowWidget/Intents/WidgetIntents.swift"
      to: "InsightFlowWidget/Models/WidgetModels.swift"
      via: "WidgetAccount type usage"
      pattern: "WidgetAccount"
---

<objective>
Den 2034-Zeilen Widget-Monolith InsightFlowWidget.swift in 9 separate Dateien aufteilen, organisiert nach Verantwortlichkeit (Models, Storage, Cache, Intents, Networking, Views). Gleichzeitig widgetLog() mit #if DEBUG absichern.

Purpose: STRUC-01 — Widget-Code navigierbar und wartbar machen. Partial STAB-03 — widgetLog print in Release unterdruecken.
Output: 9 Dateien in InsightFlowWidget/ mit Subdirectories, Hauptdatei unter 100 Zeilen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-quick-wins-widget-split/02-RESEARCH.md
@InsightFlowWidget/InsightFlowWidget.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widget Models, Storage, Cache und TimeRange extrahieren</name>
  <files>
    InsightFlowWidget/Models/WidgetModels.swift,
    InsightFlowWidget/Models/WidgetTimeRange.swift,
    InsightFlowWidget/Storage/WidgetStorage.swift,
    InsightFlowWidget/Cache/WidgetCache.swift
  </files>
  <read_first>
    InsightFlowWidget/InsightFlowWidget.swift
  </read_first>
  <action>
Aus InsightFlowWidget.swift die folgenden MARK-Sektionen in neue Dateien extrahieren. Jede Datei bekommt eigene Imports. WICHTIG: Alle Dateien muessen Swift 5.0-kompatibel sein — keine Sendable-Annotationen, keine strict concurrency Patterns.

1. **InsightFlowWidget/Models/WidgetModels.swift** (Verzeichnis Models/ erstellen):
   - Zeile 11-16: `// MARK: - Debug` — widgetLog-Funktion. HIER `#if DEBUG` um den print()-Call wrappen:
     ```swift
     private func widgetLog(_ message: String) {
         #if DEBUG
         print("[Widget] \(message)")
         #endif
     }
     ```
   - Zeile 17-23: `// MARK: - Credentials` — WidgetProviderType enum
   - Zeile 24-177: `// MARK: - Widget Account` — WidgetAccount struct (OHNE WidgetAccountsStorage, die kommt in Storage)
   - Zeile 711-784: `// MARK: - Widget Data` — WidgetData struct
   - Zeile 785-792: `// MARK: - Timeline Entry` — StatsEntry struct
   - Imports: `import WidgetKit`, `import SwiftUI`, `import Foundation`

2. **InsightFlowWidget/Models/WidgetTimeRange.swift**:
   - Zeile 276-317: `// MARK: - Time Range Enum` — WidgetTimeRange enum
   - Zeile 543-555: `// MARK: - Chart Style Enum` — WidgetChartStyle enum
   - Imports: `import SwiftUI`

3. **InsightFlowWidget/Storage/WidgetStorage.swift** (Verzeichnis Storage/ erstellen):
   - Zeile 24 ff: Die `WidgetAccountsStorage` class (mit den Encryption Helpers ab Zeile 178) — diese Klasse liegt innerhalb des Widget Account MARK-Blocks
   - Die `WidgetCredentials` struct (unterhalb von WidgetAccountsStorage im selben Block)
   - ACHTUNG: WidgetAccountsStorage wurde in Phase 1 modifiziert fuer encrypted storage. Den gesamten Code 1:1 uebernehmen.
   - Imports: `import Foundation`, `import CryptoKit`, `import WidgetKit`

4. **InsightFlowWidget/Cache/WidgetCache.swift** (Verzeichnis Cache/ erstellen):
   - Zeile 602-710: `// MARK: - Widget Cache` — WidgetCache struct
   - Imports: `import Foundation`

Beim Extrahieren: Code 1:1 kopieren, keine Logik-Aenderungen. Nur widgetLog bekommt das #if DEBUG Wrapping. Alle Typen muessen gleiche Sichtbarkeit (internal) behalten. Die `widgetLog`-Funktion in WidgetModels.swift muss fuer alle anderen Widget-Dateien sichtbar sein (kein `private` auf File-Level, sondern package-internal).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && test -f InsightFlowWidget/Models/WidgetModels.swift && test -f InsightFlowWidget/Models/WidgetTimeRange.swift && test -f InsightFlowWidget/Storage/WidgetStorage.swift && test -f InsightFlowWidget/Cache/WidgetCache.swift && grep -q "struct WidgetAccount" InsightFlowWidget/Models/WidgetModels.swift && grep -q "#if DEBUG" InsightFlowWidget/Models/WidgetModels.swift && grep -q "class WidgetAccountsStorage" InsightFlowWidget/Storage/WidgetStorage.swift && grep -q "struct WidgetCache" InsightFlowWidget/Cache/WidgetCache.swift && grep -q "enum WidgetTimeRange" InsightFlowWidget/Models/WidgetTimeRange.swift && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - WidgetModels.swift enthaelt `struct WidgetAccount`, `struct WidgetData`, `struct StatsEntry`, `enum WidgetProviderType`
    - WidgetModels.swift enthaelt widgetLog mit `#if DEBUG` um den print-Call
    - WidgetTimeRange.swift enthaelt `enum WidgetTimeRange` und `enum WidgetChartStyle`
    - WidgetStorage.swift enthaelt `class WidgetAccountsStorage` mit Encryption Helpers und `struct WidgetCredentials`
    - WidgetCache.swift enthaelt `struct WidgetCache`
    - Keine Datei enthaelt `@unchecked Sendable` oder andere Swift 6.0-only Patterns
    - Alle Dateien haben korrekte Imports (kein CryptoKit in Models, kein AppIntents in Storage)
  </acceptance_criteria>
  <done>4 neue Dateien in Models/, Storage/, Cache/ mit allen extrahierten Typen. widgetLog ist DEBUG-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Widget Intents, Networking, Views extrahieren und Hauptdatei aufraeumen</name>
  <files>
    InsightFlowWidget/Intents/WidgetIntents.swift,
    InsightFlowWidget/Networking/WidgetNetworking.swift,
    InsightFlowWidget/Views/WidgetChartViews.swift,
    InsightFlowWidget/Views/WidgetSizeViews.swift,
    InsightFlowWidget/InsightFlowWidget.swift
  </files>
  <read_first>
    InsightFlowWidget/InsightFlowWidget.swift,
    InsightFlowWidget/Models/WidgetModels.swift,
    InsightFlowWidget/Storage/WidgetStorage.swift
  </read_first>
  <action>
Restliche MARK-Sektionen aus InsightFlowWidget.swift extrahieren und die Hauptdatei auf den Entry-Point reduzieren. WICHTIG: Swift 5.0-kompatibel, alle benoetigten Imports pro Datei.

5. **InsightFlowWidget/Intents/WidgetIntents.swift** (Verzeichnis Intents/ erstellen):
   - Zeile 318-386: `// MARK: - Account Entity` — AccountEntity struct
   - Zeile 387-542: `// MARK: - Website Entity` — WebsiteEntity, WebsiteQuery
   - Zeile 556-601: `// MARK: - Website Options Provider`, `// MARK: - Widget Intent` — FilteredWebsiteOptionsProvider, WebsiteOptionsProvider, ConfigureWidgetIntent
   - Imports: `import WidgetKit`, `import SwiftUI`, `import AppIntents`

6. **InsightFlowWidget/Networking/WidgetNetworking.swift** (Verzeichnis Networking/ erstellen):
   - Zeile 793-1413: `// MARK: - Provider` inkl. `// MARK: - Umami Stats` und `// MARK: - Plausible Stats`
   - Das ist der groesste Block (~620 Zeilen) mit Provider struct, getTimeline(), fetchStats(), fetchUmamiStats(), fetchPlausibleStats()
   - Imports: `import WidgetKit`, `import SwiftUI`, `import Foundation`

7. **InsightFlowWidget/Views/WidgetChartViews.swift** (Verzeichnis Views/ erstellen):
   - Zeile 1414-1758: `// MARK: - Bar Chart View` und `// MARK: - Line Chart View`
   - Imports: `import SwiftUI`, `import WidgetKit`

8. **InsightFlowWidget/Views/WidgetSizeViews.swift**:
   - Zeile 1759-2001: `// MARK: - Widget Views`, `// MARK: - Small Widget`, `// MARK: - Medium Widget`
   - Enthaelt PrivacyFlowWidgetEntryView, SmallWidgetView, MediumWidgetView
   - Imports: `import SwiftUI`, `import WidgetKit`

9. **InsightFlowWidget/InsightFlowWidget.swift** — auf Entry-Point reduzieren:
   - Behalte NUR: den `import`-Header, `PrivacyFlowWidget` struct (mit `kind`, `body`) und den `#Preview`-Block am Ende (Zeile 2002-2034)
   - ACHTUNG: PrivacyFlowWidgetEntryView wandert nach WidgetSizeViews.swift — die Referenz in PrivacyFlowWidget bleibt bestehen, da sie im selben Target liegt
   - Entferne ALLEN anderen Code aus dieser Datei
   - Ergebnis: ~35-60 Zeilen

Nach dem Split: `xcodebuild build` fuer das Widget-Target ausfuehren um Kompilierung zu pruefen. Bei fehlenden Imports sofort fixen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && test -f InsightFlowWidget/Intents/WidgetIntents.swift && test -f InsightFlowWidget/Networking/WidgetNetworking.swift && test -f InsightFlowWidget/Views/WidgetChartViews.swift && test -f InsightFlowWidget/Views/WidgetSizeViews.swift && LINES=$(wc -l < InsightFlowWidget/InsightFlowWidget.swift) && test "$LINES" -lt 100 && grep -q "struct Provider" InsightFlowWidget/Networking/WidgetNetworking.swift && grep -q "struct BarChartView" InsightFlowWidget/Views/WidgetChartViews.swift && grep -q "struct SmallWidgetView" InsightFlowWidget/Views/WidgetSizeViews.swift && grep -q "struct AccountEntity" InsightFlowWidget/Intents/WidgetIntents.swift && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - InsightFlowWidget.swift ist unter 100 Zeilen und enthaelt NUR PrivacyFlowWidget + #Preview
    - WidgetIntents.swift enthaelt `struct AccountEntity`, `struct WebsiteEntity`, `struct ConfigureWidgetIntent`
    - WidgetNetworking.swift enthaelt `struct Provider` mit getTimeline, fetchStats, fetchUmamiStats, fetchPlausibleStats
    - WidgetChartViews.swift enthaelt `struct BarChartView` und `struct LineChartView`
    - WidgetSizeViews.swift enthaelt `struct SmallWidgetView`, `struct MediumWidgetView`, `PrivacyFlowWidgetEntryView`
    - `xcodebuild build -project InsightFlow.xcodeproj -scheme InsightFlowWidgetExtension -destination 'platform=iOS Simulator,name=iPhone 16'` kompiliert ohne Fehler
    - Widget-Kind-String `PrivacyFlowWidget` ist unveraendert
  </acceptance_criteria>
  <done>Widget-Monolith ist in 9 Dateien aufgeteilt. InsightFlowWidget.swift unter 100 Zeilen. Widget-Extension kompiliert fehlerfrei.</done>
</task>

</tasks>

<verification>
1. `wc -l InsightFlowWidget/InsightFlowWidget.swift` — unter 100 Zeilen
2. `ls InsightFlowWidget/Models/ InsightFlowWidget/Storage/ InsightFlowWidget/Cache/ InsightFlowWidget/Intents/ InsightFlowWidget/Networking/ InsightFlowWidget/Views/` — alle Subdirectories und Dateien vorhanden
3. `grep -r "widgetLog" InsightFlowWidget/` — Funktion existiert und hat `#if DEBUG`
4. `xcodebuild build -project InsightFlow.xcodeproj -scheme InsightFlowWidgetExtension -destination 'platform=iOS Simulator,name=iPhone 16'` — kompiliert fehlerfrei
5. Kein `@unchecked Sendable` in Widget-Dateien
</verification>

<success_criteria>
- InsightFlowWidget.swift unter 100 Zeilen (aktuell 2034)
- 8 neue Dateien in logischen Subdirectories
- Widget-Extension kompiliert fehlerfrei
- widgetLog gibt in Release nichts aus (#if DEBUG)
- Keine Verhaltensaenderung — Widget zeigt identische Daten
</success_criteria>

<output>
After completion, create `.planning/phases/02-quick-wins-widget-split/02-01-SUMMARY.md`
</output>
