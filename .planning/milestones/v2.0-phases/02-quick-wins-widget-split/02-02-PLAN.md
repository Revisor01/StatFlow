---
phase: 02-quick-wins-widget-split
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - InsightFlow/Services/AccountManager.swift
  - InsightFlow/Views/SessionsView.swift
  - InsightFlow/Views/DashboardView.swift
  - InsightFlow/Views/RealtimeView.swift
  - InsightFlow/Services/NotificationManager.swift
  - InsightFlow/Services/PlausibleAPI.swift
  - InsightFlow/InsightFlowApp.swift
  - InsightFlow/Views/WebsiteCard.swift
  - InsightFlow/Views/AddUmamiSiteView.swift
  - InsightFlow/Views/AddPlausibleSiteView.swift
  - InsightFlow/Views/RetentionView.swift
  - InsightFlow/Views/InsightsView.swift
  - InsightFlow/Services/SupportManager.swift
  - InsightFlow/Services/UmamiAPI.swift
autonomous: true
requirements:
  - STAB-03

must_haves:
  truths:
    - "Kein unwrapped print()-Call in den 14 bearbeiteten Dateien"
    - "Bestehende Logik ist unveraendert — nur print() ist in #if DEBUG gewrappt"
    - "App kompiliert fehlerfrei nach dem Wrapping"
  artifacts:
    - path: "InsightFlow/Services/AccountManager.swift"
      provides: "7 print-Calls gewrappt"
      contains: "#if DEBUG"
    - path: "InsightFlow/Views/SessionsView.swift"
      provides: "4 print-Calls gewrappt"
      contains: "#if DEBUG"
    - path: "InsightFlow/Views/RealtimeView.swift"
      provides: "3 print-Calls gewrappt"
      contains: "#if DEBUG"
  key_links: []
---

<objective>
Alle 31 verbleibenden unwrapped print()-Statements in 14 Main-App-Dateien mit #if DEBUG wrappen. (Der 32. unwrapped Call — widgetLog — wird in Plan 01 behandelt.)

Purpose: STAB-03 — Release-Builds enthalten keine Debug-Ausgaben mehr.
Output: 14 Dateien mit ausschliesslich DEBUG-gewrappten print()-Calls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-quick-wins-widget-split/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Print-Statements in High-Volume-Dateien wrappen (AccountManager, SessionsView, RealtimeView, DashboardView)</name>
  <files>
    InsightFlow/Services/AccountManager.swift,
    InsightFlow/Views/SessionsView.swift,
    InsightFlow/Views/RealtimeView.swift,
    InsightFlow/Views/DashboardView.swift
  </files>
  <read_first>
    InsightFlow/Services/AccountManager.swift,
    InsightFlow/Views/SessionsView.swift,
    InsightFlow/Views/RealtimeView.swift,
    InsightFlow/Views/DashboardView.swift
  </read_first>
  <action>
In jeder Datei jeden `print(...)` Call der NICHT bereits in einem `#if DEBUG` Block steht mit `#if DEBUG` / `#endif` umschliessen.

Pattern (immer anwenden):
```swift
// Vorher:
print("some debug message: \(variable)")

// Nachher:
#if DEBUG
print("some debug message: \(variable)")
#endif
```

WICHTIG:
- NUR den print()-Call wrappen, NICHT die umgebende Logik (catch, if, etc.)
- Bereits gewrappte print()-Calls (AccountManager hat 2 davon) NICHT doppelt wrappen
- Keine andere Code-Aenderung — rein mechanisches Wrapping

Erwartete Counts pro Datei:
- **AccountManager.swift**: 7 unwrapped (2 bereits wrapped — NICHT anfassen)
- **SessionsView.swift**: 4 unwrapped
- **RealtimeView.swift**: 3 unwrapped
- **DashboardView.swift**: 1 unwrapped (3 bereits wrapped)

Nach dem Wrapping: Kurzer Kompilierungscheck.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && for f in InsightFlow/Services/AccountManager.swift InsightFlow/Views/SessionsView.swift InsightFlow/Views/RealtimeView.swift InsightFlow/Views/DashboardView.swift; do UNWRAPPED=$(python3 -c "
import re
with open('$f') as fh:
    lines = fh.readlines()
count = 0
in_debug = False
for line in lines:
    stripped = line.strip()
    if stripped == '#if DEBUG':
        in_debug = True
    elif stripped == '#endif':
        in_debug = False
    elif 'print(' in stripped and not in_debug and not stripped.startswith('//'):
        count += 1
print(count)
"); echo "$f: $UNWRAPPED unwrapped"; done</automated>
  </verify>
  <acceptance_criteria>
    - AccountManager.swift: 0 unwrapped print()-Calls (9 total, alle in #if DEBUG)
    - SessionsView.swift: 0 unwrapped print()-Calls
    - RealtimeView.swift: 0 unwrapped print()-Calls
    - DashboardView.swift: 0 unwrapped print()-Calls (4 total, alle in #if DEBUG)
    - Keine Logik-Aenderung ausser dem Wrapping
  </acceptance_criteria>
  <done>15 print-Calls in 4 High-Volume-Dateien sind in #if DEBUG gewrappt. 0 unwrapped Calls verbleiben in diesen Dateien.</done>
</task>

<task type="auto">
  <name>Task 2: Print-Statements in restlichen 10 Dateien wrappen</name>
  <files>
    InsightFlow/Services/NotificationManager.swift,
    InsightFlow/Services/PlausibleAPI.swift,
    InsightFlow/InsightFlowApp.swift,
    InsightFlow/Views/WebsiteCard.swift,
    InsightFlow/Views/AddUmamiSiteView.swift,
    InsightFlow/Views/AddPlausibleSiteView.swift,
    InsightFlow/Views/RetentionView.swift,
    InsightFlow/Views/InsightsView.swift,
    InsightFlow/Services/SupportManager.swift,
    InsightFlow/Services/UmamiAPI.swift
  </files>
  <read_first>
    InsightFlow/Services/NotificationManager.swift,
    InsightFlow/Services/PlausibleAPI.swift,
    InsightFlow/InsightFlowApp.swift,
    InsightFlow/Views/WebsiteCard.swift,
    InsightFlow/Views/AddUmamiSiteView.swift,
    InsightFlow/Views/AddPlausibleSiteView.swift,
    InsightFlow/Views/RetentionView.swift,
    InsightFlow/Views/InsightsView.swift,
    InsightFlow/Services/SupportManager.swift,
    InsightFlow/Services/UmamiAPI.swift
  </read_first>
  <action>
Identisches Pattern wie Task 1: Jeden unwrapped `print(...)` Call mit `#if DEBUG` / `#endif` umschliessen.

Erwartete Counts pro Datei (je 1-2 Calls):
- **NotificationManager.swift**: 2 unwrapped
- **PlausibleAPI.swift**: 2 unwrapped
- **InsightFlowApp.swift**: 1 unwrapped
- **WebsiteCard.swift**: 1 unwrapped
- **AddUmamiSiteView.swift**: 1 unwrapped
- **AddPlausibleSiteView.swift**: 1 unwrapped
- **RetentionView.swift**: 1 unwrapped
- **InsightsView.swift**: 1 unwrapped
- **SupportManager.swift**: 1 unwrapped
- **UmamiAPI.swift**: 1 unwrapped

HINWEIS: Pfade pruefen — manche Views koennten in Subdirectories liegen (z.B. InsightFlow/Views/Detail/). Die read_first-Dateien zeigen die tatsaechlichen Pfade. Falls ein Pfad nicht stimmt, mit `find` den korrekten Pfad ermitteln.

Nach dem Wrapping: `xcodebuild build -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16'` ausfuehren.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && python3 -c "
import os, re

# Find all .swift files in InsightFlow/ (not Widget)
unwrapped_total = 0
for root, dirs, files in os.walk('InsightFlow'):
    for f in files:
        if f.endswith('.swift'):
            path = os.path.join(root, f)
            with open(path) as fh:
                lines = fh.readlines()
            in_debug = False
            for line in lines:
                stripped = line.strip()
                if stripped == '#if DEBUG':
                    in_debug = True
                elif stripped == '#endif':
                    in_debug = False
                elif 'print(' in stripped and not in_debug and not stripped.startswith('//'):
                    unwrapped_total += 1
                    print(f'UNWRAPPED: {path}: {stripped}')
print(f'Total unwrapped in InsightFlow/: {unwrapped_total}')
" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - 0 unwrapped print()-Calls in allen 14 Main-App-Dateien
    - Gesamt-Scan von InsightFlow/ zeigt 0 unwrapped print()-Calls
    - `xcodebuild build` fuer InsightFlow-Scheme kompiliert ohne Fehler
    - Keine Logik-Aenderung — nur #if DEBUG Wrapping
  </acceptance_criteria>
  <done>Alle 31 unwrapped print()-Calls in der Main App sind in #if DEBUG gewrappt. Release-Build enthaelt keine Debug-Ausgaben.</done>
</task>

</tasks>

<verification>
1. Python-Script zaehlt 0 unwrapped print()-Calls in InsightFlow/
2. `xcodebuild build -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16'` — kompiliert fehlerfrei
3. Stichprobe: `grep -B2 -A2 "print(" InsightFlow/Services/AccountManager.swift` zeigt alle Calls in #if DEBUG
</verification>

<success_criteria>
- 0 unwrapped print()-Calls in allen 14 bearbeiteten Main-App-Dateien
- App kompiliert fehlerfrei
- Keine Verhaltensaenderung ausser fehlende print-Ausgabe in Release-Builds
</success_criteria>

<output>
After completion, create `.planning/phases/02-quick-wins-widget-split/02-02-SUMMARY.md`
</output>
