---
phase: 03-stabilitaet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - InsightFlow/Services/PlausibleAPI.swift
  - InsightFlow/Services/UmamiAPI.swift
  - InsightFlowWidget/Networking/WidgetNetworking.swift
autonomous: true
requirements: [STAB-01]

must_haves:
  truths:
    - "Kein Force Unwrap auf URL(string:) oder URLComponents in PlausibleAPI.swift"
    - "Kein Force Unwrap auf URLComponents in UmamiAPI.swift"
    - "Kein Force Unwrap auf URLComponents oder calendar.date in WidgetNetworking.swift"
    - "API-Methoden werfen spezifische Fehler statt abzustuerzen"
    - "Widget gibt .error()-Zustand zurueck statt abzustuerzen"
  artifacts:
    - path: "InsightFlow/Services/PlausibleAPI.swift"
      provides: "Force-Unwrap-freie URL-Konstruktion"
      contains: "guard let url"
    - path: "InsightFlow/Services/UmamiAPI.swift"
      provides: "Force-Unwrap-freie URLComponents-Konstruktion"
      contains: "guard var components"
    - path: "InsightFlowWidget/Networking/WidgetNetworking.swift"
      provides: "Force-Unwrap-freie URL- und Calendar-Operationen"
      contains: "guard var statsURL"
  key_links:
    - from: "InsightFlow/Services/PlausibleAPI.swift"
      to: "PlausibleError.invalidResponse"
      via: "throw bei URL-Konstruktionsfehler"
      pattern: "throw PlausibleError\\.invalidResponse"
    - from: "InsightFlow/Services/UmamiAPI.swift"
      to: "APIError.invalidURL"
      via: "throw bei URLComponents-Fehler"
      pattern: "throw APIError\\.invalidURL"
    - from: "InsightFlowWidget/Networking/WidgetNetworking.swift"
      to: ".error(String(localized:))"
      via: "return .error bei URL-Konstruktionsfehler"
      pattern: "return \\.error.*invalidURL"
---

<objective>
Alle Force Unwraps in Networking-Code durch safe unwrapping ersetzen — guard let mit Error-Propagation in API-Clients, guard let mit .error()-Fallback im Widget.

Purpose: Verhindert Abstuerze wenn serverURL aus dem Keychain eine ungueltige URL enthaelt oder URLComponents nil zurueckgibt.
Output: Drei Dateien ohne Force-Unwrap-Operatoren auf URL/URLComponents/Calendar-Typen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-stabilitaet/03-RESEARCH.md

@InsightFlow/Services/PlausibleAPI.swift
@InsightFlow/Services/UmamiAPI.swift
@InsightFlowWidget/Networking/WidgetNetworking.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Force Unwraps in PlausibleAPI.swift und UmamiAPI.swift eliminieren</name>
  <files>InsightFlow/Services/PlausibleAPI.swift, InsightFlow/Services/UmamiAPI.swift</files>
  <read_first>
    - InsightFlow/Services/PlausibleAPI.swift (Zeilen 295-310, 440-520, 550-590 — alle Force-Unwrap-Stellen)
    - InsightFlow/Services/UmamiAPI.swift (Zeilen 520-540 — URLComponents Force-Unwrap)
  </read_first>
  <action>
PlausibleAPI.swift — 7 Force Unwraps ersetzen:

1. Zeile 302: `URLComponents(url: baseURL.appendingPathComponent("api/v1/stats/realtime/visitors"), resolvingAgainstBaseURL: false)!`
   Ersetzen durch:
   ```swift
   guard var components = URLComponents(url: baseURL.appendingPathComponent("api/v1/stats/realtime/visitors"), resolvingAgainstBaseURL: false) else {
       throw PlausibleError.invalidResponse
   }
   ```

2. Zeile 305: `components.url!`
   Ersetzen durch:
   ```swift
   guard let url = components.url else {
       throw PlausibleError.invalidResponse
   }
   var request = URLRequest(url: url)
   ```

3. Zeile 447: `URL(string: "\(serverURL)/api/v1/sites")!`
   Ersetzen durch:
   ```swift
   guard let url = URL(string: "\(serverURL)/api/v1/sites") else {
       throw PlausibleError.invalidResponse
   }
   ```

4. Zeile 482: `URL(string: "\(serverURL)/api/v1/sites/\(encodedDomain)")!`
   Gleiches guard-let-Pattern mit `throw PlausibleError.invalidResponse`.

5. Zeile 516: `URL(string: "\(serverURL)/api/v1/sites/\(encodedDomain)/shared-links")!`
   Gleiches guard-let-Pattern mit `throw PlausibleError.invalidResponse`.

6. Zeile 557: `URL(string: "\(serverURL)/\(endpoint)")!` (private `request`-Methode)
   Gleiches guard-let-Pattern mit `throw PlausibleError.invalidResponse`.

7. Zeile 583: `URL(string: "\(serverURL)/\(endpoint)")!` (private `postRequest`-Methode)
   Gleiches guard-let-Pattern mit `throw PlausibleError.invalidResponse`.

UmamiAPI.swift — 1 Force Unwrap ersetzen:

1. Zeile 528: `URLComponents(url: baseURL.appendingPathComponent(endpoint), resolvingAgainstBaseURL: true)!`
   Ersetzen durch:
   ```swift
   guard var components = URLComponents(url: baseURL.appendingPathComponent(endpoint), resolvingAgainstBaseURL: true) else {
       throw APIError.invalidURL
   }
   ```

WICHTIG: Nur die Force-Unwrap-Operatoren aendern. Keine andere Logik anfassen. Bestehende Error-Enums (`PlausibleError.invalidResponse`, `APIError.invalidURL`) verwenden — keine neuen Error-Cases einfuehren.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && grep -n '!' InsightFlow/Services/PlausibleAPI.swift | grep -E 'URL\(string:|URLComponents\(|\.url!' | wc -l | xargs test 0 -eq && grep -n '!' InsightFlow/Services/UmamiAPI.swift | grep -E 'URLComponents\(' | wc -l | xargs test 0 -eq && echo "PASS: Keine Force Unwraps in API-Clients"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'guard let url = URL(string:' InsightFlow/Services/PlausibleAPI.swift liefert mindestens 5
    - grep -c 'guard var components = URLComponents' InsightFlow/Services/PlausibleAPI.swift liefert mindestens 1
    - grep -c 'throw PlausibleError.invalidResponse' InsightFlow/Services/PlausibleAPI.swift ist hoeher als vorher
    - grep -c 'guard var components = URLComponents' InsightFlow/Services/UmamiAPI.swift liefert mindestens 1
    - grep 'URL(string:.*)\!' InsightFlow/Services/PlausibleAPI.swift liefert 0 Treffer
    - grep 'URLComponents(.*)\!' InsightFlow/Services/UmamiAPI.swift liefert 0 Treffer
  </acceptance_criteria>
  <done>Alle 7 Force Unwraps in PlausibleAPI.swift und 1 Force Unwrap in UmamiAPI.swift sind durch guard-let mit spezifischem Error-Throw ersetzt. Keine neuen Error-Cases eingefuehrt.</done>
</task>

<task type="auto">
  <name>Task 2: Force Unwraps in WidgetNetworking.swift eliminieren</name>
  <files>InsightFlowWidget/Networking/WidgetNetworking.swift</files>
  <read_first>
    - InsightFlowWidget/Networking/WidgetNetworking.swift (vollstaendig lesen — 19 Force-Unwrap-Stellen verteilt ueber die gesamte Datei)
  </read_first>
  <action>
WidgetNetworking.swift — 19 Force Unwraps in zwei Kategorien ersetzen:

**Kategorie A: URLComponents Force Unwraps (6 Stellen) — guard let + return .error**

1. Zeile 200: `URLComponents(url: baseURL.appendingPathComponent("api/websites/\(website.id)/stats"), resolvingAgainstBaseURL: false)!`
   Ersetzen durch:
   ```swift
   guard var statsURL = URLComponents(url: baseURL.appendingPathComponent("api/websites/\(website.id)/stats"), resolvingAgainstBaseURL: false) else {
       return .error(String(localized: "widget.error.invalidURL"))
   }
   ```

2. Zeile 206: `statsURL.url!` — nach den queryItems:
   ```swift
   guard let statsURLBuilt = statsURL.url else {
       return .error(String(localized: "widget.error.invalidURL"))
   }
   var statsReq = URLRequest(url: statsURLBuilt)
   ```

3. Zeile 251: `URLComponents(url: baseURL.appendingPathComponent("api/websites/\(website.id)/pageviews"), resolvingAgainstBaseURL: false)!`
   Gleiches guard-let-Pattern mit `return .error(String(localized: "widget.error.invalidURL"))`.

4. Zeile 258: `pvURL.url!`
   Gleiches guard-let-Pattern fuer `.url`.

5. Zeile 595: `URLComponents(url: baseURL.appendingPathComponent("api/v1/stats/realtime/visitors"), resolvingAgainstBaseURL: false)!`
   Gleiches guard-let-Pattern.

6. Zeile 598: `realtimeComponents.url!`
   Gleiches guard-let-Pattern fuer `.url`.

**Kategorie B: calendar.date(byAdding:)! Force Unwraps (13 Stellen) — ?? Fallback**

Alle `calendar.date(byAdding:)!` durch `?? now` bzw. `?? today` ersetzen (je nach Kontext-Variable):

- Zeile 37: `Calendar.current.date(byAdding: .minute, value: 15, to: now)!` → `?? now.addingTimeInterval(15 * 60)`
- Zeile 182: `calendar.date(byAdding: .day, value: -1, to: now)!` → `?? now`
- Zeile 186: `calendar.date(byAdding: .day, value: -6, to: calendar.startOfDay(for: now))!` → `?? now`
- Zeile 189: `calendar.date(byAdding: .day, value: -29, to: calendar.startOfDay(for: now))!` → `?? now`
- Zeile 293: `calendar.date(byAdding: .day, value: -1, to: today)!` → `?? today`
- Zeile 380: `calendar.date(byAdding: .day, value: -1, to: today)!` → `?? today`
- Zeile 384: `calendar.date(byAdding: .day, value: -1, to: today)!` → `?? today`
- Zeile 388: `calendar.date(byAdding: .day, value: -2, to: today)!` → `?? today`
- Zeile 394: `calendar.date(byAdding: .day, value: -13, to: today)!` → `?? today`
- Zeile 395: `calendar.date(byAdding: .day, value: -7, to: today)!` → `?? today`
- Zeile 400: `calendar.date(byAdding: .day, value: -59, to: today)!` → `?? today`
- Zeile 401: `calendar.date(byAdding: .day, value: -30, to: today)!` → `?? today`
- Zeile 534: `calendar.date(byAdding: .day, value: -1, to: today)!` → `?? today`

WICHTIG: Widget-Methoden sind NICHT throws — daher `return .error(...)` fuer URL-Fehler, `?? fallback` fuer Calendar. Keine andere Logik aendern. Bestehende Localization-Keys (`widget.error.invalidURL`) verwenden.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && count=$(grep -c '!' InsightFlowWidget/Networking/WidgetNetworking.swift | head -1); url_unwraps=$(grep '!' InsightFlowWidget/Networking/WidgetNetworking.swift | grep -E 'URLComponents\(|\.url!|calendar\.date.*\)!' | wc -l | tr -d ' '); test "$url_unwraps" -eq 0 && echo "PASS: Keine Force Unwraps auf URL/Calendar in WidgetNetworking" || echo "FAIL: $url_unwraps Force Unwraps verbleiben"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'URLComponents(.*)\!' InsightFlowWidget/Networking/WidgetNetworking.swift liefert 0 Treffer
    - grep '\.url!' InsightFlowWidget/Networking/WidgetNetworking.swift liefert 0 Treffer
    - grep 'calendar.date(.*)\!' InsightFlowWidget/Networking/WidgetNetworking.swift liefert 0 Treffer
    - grep 'Calendar.current.date(.*)\!' InsightFlowWidget/Networking/WidgetNetworking.swift liefert 0 Treffer
    - grep -c 'guard var statsURL = URLComponents' InsightFlowWidget/Networking/WidgetNetworking.swift liefert mindestens 1
    - grep -c '?? now' InsightFlowWidget/Networking/WidgetNetworking.swift liefert mindestens 4
    - grep -c 'widget.error.invalidURL' InsightFlowWidget/Networking/WidgetNetworking.swift ist hoeher als vorher
  </acceptance_criteria>
  <done>Alle 19 Force Unwraps in WidgetNetworking.swift sind eliminiert — URLComponents mit guard-let und .error()-Fallback, Calendar mit ?? Fallback-Date. Kein Force-Unwrap-Operator mehr auf URL-, URLComponents- oder Calendar-Typen.</done>
</task>

</tasks>

<verification>
Gesamte Verifikation fuer STAB-01:
1. `grep -rn '!' InsightFlow/Services/PlausibleAPI.swift InsightFlow/Services/UmamiAPI.swift InsightFlowWidget/Networking/WidgetNetworking.swift | grep -E 'URL\(string:|URLComponents\(|\.url!|calendar\.date.*\)!' | wc -l` muss 0 sein
2. Xcode Build (`xcodebuild build`) kompiliert ohne Fehler
</verification>

<success_criteria>
- Null Force Unwraps auf URL(string:), URLComponents(), .url, und calendar.date(byAdding:) in allen drei Dateien
- API-Clients werfen spezifische Fehler (PlausibleError.invalidResponse, APIError.invalidURL) statt abzustuerzen
- Widget gibt .error()-Zustand zurueck statt abzustuerzen
- Alle bestehenden Funktionssignaturen unveraendert
</success_criteria>

<output>
After completion, create `.planning/phases/03-stabilitaet/03-01-SUMMARY.md`
</output>
