---
phase: 01-security-hardening
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - InsightFlow/Services/AccountManager.swift
  - InsightFlow/Services/SharedCredentials.swift
  - InsightFlowWidget/InsightFlowWidget.swift
autonomous: true
requirements: [SEC-02, SEC-03]
must_haves:
  truths:
    - "widget_accounts werden verschluesselt in den App Group Container geschrieben"
    - "Das Widget kann verschluesselte widget_accounts lesen und entschluesseln"
    - "Legacy widget_accounts.json wird als Fallback unterstuetzt und nach Migration geloescht"
    - "Widget-Logs enthalten keine Token-Praefixe oder Credential-Fragmente mehr"
  artifacts:
    - path: "InsightFlow/Services/SharedCredentials.swift"
      provides: "saveWidgetAccounts und loadWidgetAccounts Methoden"
      contains: "widget_accounts.encrypted"
    - path: "InsightFlow/Services/AccountManager.swift"
      provides: "Verschluesselte Widget-Sync via SharedCredentials"
      contains: "SharedCredentials.saveWidgetAccounts"
    - path: "InsightFlowWidget/InsightFlowWidget.swift"
      provides: "Entschluesselung der widget_accounts und bereinigte Logs"
      contains: "widget_accounts.encrypted"
  key_links:
    - from: "AccountManager.syncAccountsToWidget()"
      to: "SharedCredentials.saveWidgetAccounts()"
      via: "Verschluesselter Datei-Write in App Group"
      pattern: "SharedCredentials\\.saveWidgetAccounts"
    - from: "WidgetAccountsStorage.loadAccounts()"
      to: "AES.GCM.open()"
      via: "Entschluesselung mit widget_credentials.key"
      pattern: "widget_accounts\\.encrypted"
    - from: "WidgetAccountsStorage.loadAccounts() log"
      to: "widgetLog ohne Token"
      via: "Bereinigter Log-String ohne acc.token"
      pattern: 'widgetLog\("  - \\(acc\.name\)'
---

<objective>
Widget-Account-Daten verschluesselt speichern und Token-Logging entfernen.

Purpose: widget_accounts.json liegt aktuell als Plaintext im App Group Container und enthaelt Tokens. Muss mit AES-GCM verschluesselt werden (identisches Pattern wie widget_credentials.encrypted). Token-Logging im Widget muss vollstaendig entfernt werden.
Output: Verschluesselte widget_accounts.encrypted Datei, Widget-seitige Entschluesselung, keine Token in Logs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-hardening/01-CONTEXT.md
@.planning/phases/01-security-hardening/01-RESEARCH.md
@.planning/phases/01-security-hardening/01-01-SUMMARY.md

@InsightFlow/Services/SharedCredentials.swift
@InsightFlow/Services/AccountManager.swift
@InsightFlowWidget/InsightFlowWidget.swift

<interfaces>
<!-- Bestehende SharedCredentials-Encryption-API -->

From InsightFlow/Services/SharedCredentials.swift:
```swift
enum SharedCredentials {
    private static let appGroupID = "group.de.godsapp.PrivacyFlow"
    private static let keyFileName = "widget_credentials.key"

    // Encryption-Key wird automatisch geladen/generiert
    private static var encryptionKey: SymmetricKey { ... }
    private static func encrypt(_ data: Data) throws -> Data  // AES.GCM.seal
    private static func decrypt(_ data: Data) throws -> Data  // AES.GCM.open

    private static var containerURL: URL? {
        FileManager.default.containerURL(forSecurityApplicationGroupIdentifier: appGroupID)
    }
}
```

From InsightFlowWidget/InsightFlowWidget.swift (Widget-seitige Entschluesselung bereits vorhanden fuer widget_credentials):
```swift
struct WidgetCredentials {
    private static let appGroupID = "group.de.godsapp.PrivacyFlow"
    private static let keyFileName = "widget_credentials.key"

    private static func loadEncryptionKey() -> SymmetricKey? { ... }
    // Widget hat KEINE Key-Generierung, nur Lesen
    // AES.GCM.open() fuer Entschluesselung
}

struct WidgetAccount: Codable, Hashable {
    let id: String
    let name: String
    let serverURL: String
    private let providerTypeRaw: String
    let token: String
    let sites: [String]?
}

struct WidgetAccountsStorage {
    private static let appGroupID = "group.de.godsapp.PrivacyFlow"
    private static let fileName = "widget_accounts.json"  // wird zu .encrypted
    static func loadAccounts() -> [WidgetAccount]
}
```

From InsightFlow/Services/AccountManager.swift (nach Plan 01):
```swift
// syncAccountsToWidget() schreibt aktuell Plaintext:
// let data = try JSONEncoder().encode(widgetAccounts)
// try data.write(to: fileURL)
// -> muss durch SharedCredentials.saveWidgetAccounts() ersetzt werden
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SharedCredentials um Widget-Account-Verschluesselung erweitern und syncAccountsToWidget() umstellen</name>
  <files>InsightFlow/Services/SharedCredentials.swift, InsightFlow/Services/AccountManager.swift</files>
  <read_first>
    - InsightFlow/Services/SharedCredentials.swift (bestehende encrypt/decrypt API, containerURL, encryptionKey Logik)
    - InsightFlow/Services/AccountManager.swift (syncAccountsToWidget() Methode — nach Plan 01 bereits modifiziert, WidgetAccount struct innerhalb der Methode)
  </read_first>
  <action>
    **A) SharedCredentials.swift — Zwei neue statische Methoden hinzufuegen:**

    Neuen `// MARK: - Widget Accounts (Encrypted)` Abschnitt nach dem bestehenden `delete()` und vor der schliessenden `}`:

    ```swift
    // MARK: - Widget Accounts (Encrypted)

    private static let widgetAccountsFileName = "widget_accounts.encrypted"
    private static let legacyWidgetAccountsFileName = "widget_accounts.json"

    /// Speichert Widget-Accounts verschluesselt im App Group Container
    static func saveWidgetAccounts(_ accountsData: Data) -> Bool {
        guard let url = containerURL?.appendingPathComponent(widgetAccountsFileName) else {
            return false
        }
        do {
            let encrypted = try encrypt(accountsData)
            try encrypted.write(to: url, options: [.atomic, .completeFileProtection])
            // Legacy-Datei loeschen falls vorhanden
            if let legacyURL = containerURL?.appendingPathComponent(legacyWidgetAccountsFileName) {
                try? FileManager.default.removeItem(at: legacyURL)
            }
            return true
        } catch {
            #if DEBUG
            print("SharedCredentials: Failed to save widget accounts - \(error)")
            #endif
            return false
        }
    }

    /// Laedt und entschluesselt Widget-Accounts (fuer App-seitigen Zugriff)
    static func loadWidgetAccounts() -> Data? {
        guard let url = containerURL?.appendingPathComponent(widgetAccountsFileName),
              FileManager.default.fileExists(atPath: url.path) else {
            // Fallback auf Legacy-Plaintext
            guard let legacyURL = containerURL?.appendingPathComponent(legacyWidgetAccountsFileName),
                  FileManager.default.fileExists(atPath: legacyURL.path) else {
                return nil
            }
            return try? Data(contentsOf: legacyURL)
        }
        do {
            let encrypted = try Data(contentsOf: url)
            return try decrypt(encrypted)
        } catch {
            #if DEBUG
            print("SharedCredentials: Failed to load widget accounts - \(error)")
            #endif
            return nil
        }
    }
    ```

    **B) AccountManager.swift — syncAccountsToWidget() aendern:**

    Die bestehende `syncAccountsToWidget()` Methode umstellen. Statt direkt `data.write(to: fileURL)` aufzurufen, `SharedCredentials.saveWidgetAccounts()` verwenden.

    Geaenderte Methode (den WidgetAccount-Struct innerhalb der Methode beibehalten):
    ```swift
    private func syncAccountsToWidget() {
        // Convert accounts to widget format using Codable struct
        struct WidgetAccount: Codable {
            let id: String
            let name: String
            let serverURL: String
            let providerType: String
            let token: String
            let sites: [String]?
        }

        let widgetAccounts = accounts.map { account -> WidgetAccount in
            let token: String
            switch account.providerType {
            case .umami:
                token = account.credentials.token ?? ""
            case .plausible:
                token = account.credentials.apiKey ?? ""
            }

            return WidgetAccount(
                id: account.id.uuidString,
                name: account.name,
                serverURL: account.serverURL,
                providerType: account.providerType.rawValue,
                token: token,
                sites: account.sites
            )
        }

        do {
            let data = try JSONEncoder().encode(widgetAccounts)
            if SharedCredentials.saveWidgetAccounts(data) {
                #if DEBUG
                print("AccountManager: synced \(widgetAccounts.count) accounts to widget (encrypted)")
                #endif
            }
        } catch {
            #if DEBUG
            print("Failed to encode widget accounts: \(error)")
            #endif
        }
    }
    ```

    WICHTIG: Die alte print-Zeile `print("AccountManager: synced \(widgetAccounts.count) accounts to widget, sites: \(widgetAccounts.map { $0.sites ?? [] })")` ersetzen durch die `#if DEBUG` Version. Keine Tokens oder Sites in Production-Logs.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && grep -c "widget_accounts.encrypted" InsightFlow/Services/SharedCredentials.swift && grep -c "saveWidgetAccounts" InsightFlow/Services/SharedCredentials.swift && grep -c "loadWidgetAccounts" InsightFlow/Services/SharedCredentials.swift && grep "SharedCredentials.saveWidgetAccounts" InsightFlow/Services/AccountManager.swift | head -1 && grep -c "widget_accounts.json" InsightFlow/Services/AccountManager.swift</automated>
  </verify>
  <acceptance_criteria>
    - SharedCredentials.swift enthaelt `private static let widgetAccountsFileName = "widget_accounts.encrypted"`
    - SharedCredentials.swift enthaelt `static func saveWidgetAccounts(_ accountsData: Data) -> Bool`
    - SharedCredentials.swift enthaelt `static func loadWidgetAccounts() -> Data?`
    - saveWidgetAccounts ruft `try encrypt(accountsData)` und `.write(to: url` auf
    - saveWidgetAccounts loescht Legacy-Datei `widget_accounts.json` nach dem Schreiben
    - loadWidgetAccounts hat Fallback auf `widget_accounts.json` wenn `.encrypted` nicht existiert
    - AccountManager.syncAccountsToWidget() ruft `SharedCredentials.saveWidgetAccounts(data)` auf
    - AccountManager.syncAccountsToWidget() schreibt NICHT mehr direkt `data.write(to: fileURL)`
    - AccountManager.syncAccountsToWidget() enthaelt keine ungeschuetzten print-Statements (nur `#if DEBUG`)
    - `grep -c "widget_accounts.json" InsightFlow/Services/AccountManager.swift` ergibt 0
    - `xcodebuild build` kompiliert ohne Fehler
  </acceptance_criteria>
  <done>Widget-Accounts werden verschluesselt als widget_accounts.encrypted gespeichert. Legacy-Plaintext wird automatisch geloescht.</done>
</task>

<task type="auto">
  <name>Task 2: Widget-seitige Entschluesselung und Token-Log-Entfernung</name>
  <files>InsightFlowWidget/InsightFlowWidget.swift</files>
  <read_first>
    - InsightFlowWidget/InsightFlowWidget.swift (Zeilen 67-131 fuer WidgetAccountsStorage, Zeilen 133-162 fuer WidgetCredentials mit loadEncryptionKey und Decrypt-Logik, Zeile 98 fuer Token-Log)
  </read_first>
  <action>
    **A) WidgetAccountsStorage.loadAccounts() — verschluesselte Datei lesen mit Legacy-Fallback:**

    Die Methode komplett ueberarbeiten. Das Pattern von `WidgetCredentials.load()` folgen (die bereits AES-GCM Entschluesselung implementiert):

    ```swift
    struct WidgetAccountsStorage {
        private static let appGroupID = "group.de.godsapp.PrivacyFlow"
        private static let fileName = "widget_accounts.encrypted"
        private static let legacyFileName = "widget_accounts.json"
        private static let keyFileName = "widget_credentials.key"

        private static var containerURL: URL? {
            FileManager.default.containerURL(forSecurityApplicationGroupIdentifier: appGroupID)
        }

        private static func loadEncryptionKey() -> SymmetricKey? {
            guard let url = containerURL?.appendingPathComponent(keyFileName),
                  FileManager.default.fileExists(atPath: url.path),
                  let keyData = try? Data(contentsOf: url),
                  keyData.count == 32 else {
                return nil
            }
            return SymmetricKey(data: keyData)
        }

        static func loadAccounts() -> [WidgetAccount] {
            guard let containerURL = containerURL else {
                widgetLog("loadAccounts: no container URL")
                return []
            }

            // Versuche verschluesselte Datei zuerst
            let encryptedURL = containerURL.appendingPathComponent(fileName)
            if FileManager.default.fileExists(atPath: encryptedURL.path) {
                do {
                    guard let key = loadEncryptionKey() else {
                        widgetLog("loadAccounts: no encryption key, trying legacy")
                        return loadLegacyAccounts(containerURL: containerURL)
                    }
                    let encryptedData = try Data(contentsOf: encryptedURL)
                    let sealedBox = try AES.GCM.SealedBox(combined: encryptedData)
                    let jsonData = try AES.GCM.open(sealedBox, using: key)
                    let accounts = try JSONDecoder().decode([WidgetAccount].self, from: jsonData)
                    widgetLog("loadAccounts: loaded \(accounts.count) accounts (encrypted)")
                    for acc in accounts {
                        widgetLog("  - \(acc.name): provider=\(acc.providerType), sites=\(acc.sites ?? [])")
                    }
                    return accounts
                } catch {
                    widgetLog("loadAccounts: decrypt error: \(error), trying legacy")
                    return loadLegacyAccounts(containerURL: containerURL)
                }
            }

            // Fallback auf Legacy-Plaintext
            return loadLegacyAccounts(containerURL: containerURL)
        }

        /// Legacy-Plaintext laden (fuer Uebergangszeit nach Update)
        private static func loadLegacyAccounts(containerURL: URL) -> [WidgetAccount] {
            let legacyURL = containerURL.appendingPathComponent(legacyFileName)
            guard FileManager.default.fileExists(atPath: legacyURL.path) else {
                widgetLog("loadAccounts: no legacy file, trying legacy credentials")
                if let legacyCreds = WidgetCredentials.load() {
                    widgetLog("loadAccounts: using legacy creds, provider=\(legacyCreds.providerType)")
                    return [WidgetAccount(
                        id: "legacy",
                        name: "",
                        serverURL: legacyCreds.serverURL,
                        providerType: legacyCreds.providerType,
                        token: legacyCreds.token,
                        sites: legacyCreds.sites
                    )]
                }
                return []
            }
            do {
                let data = try Data(contentsOf: legacyURL)
                let accounts = try JSONDecoder().decode([WidgetAccount].self, from: data)
                widgetLog("loadAccounts: loaded \(accounts.count) accounts (legacy plaintext)")
                for acc in accounts {
                    widgetLog("  - \(acc.name): provider=\(acc.providerType), sites=\(acc.sites ?? [])")
                }
                return accounts
            } catch {
                widgetLog("loadAccounts: legacy decode error: \(error)")
                if let legacyCreds = WidgetCredentials.load() {
                    return [WidgetAccount(
                        id: "legacy",
                        name: "",
                        serverURL: legacyCreds.serverURL,
                        providerType: legacyCreds.providerType,
                        token: legacyCreds.token,
                        sites: legacyCreds.sites
                    )]
                }
                return []
            }
        }
    ```

    WICHTIG: `import CryptoKit` muss am Anfang der Datei stehen. Pruefen ob bereits vorhanden (WidgetCredentials nutzt es schon) — wenn ja, nicht doppelt importieren.

    **B) Token-Log-Entfernung (SEC-03):**

    Zeile 98 (im alten Code) enthielt:
    ```swift
    widgetLog("  - \(acc.name): provider=\(acc.providerType), sites=\(acc.sites ?? []), token=\(acc.token.prefix(10))...")
    ```

    Diese Zeile existiert nach dem Umbau in Task A nicht mehr (die for-Schleifen in loadAccounts sind neu geschrieben). Aber sicherstellen, dass KEIN `acc.token` mehr in IRGENDEINEM widgetLog-Aufruf vorkommt. Die neuen Log-Zeilen verwenden:
    ```swift
    widgetLog("  - \(acc.name): provider=\(acc.providerType), sites=\(acc.sites ?? [])")
    ```

    Ausserdem: `saveAccounts()` in WidgetAccountsStorage beibehalten (falls vorhanden, Zeile 119-130) — diese wird ggf. vom Widget selbst aufgerufen. Aber die Plaintext-Datei darf NICHT mehr fuer neue Writes genutzt werden. Falls `saveAccounts()` existiert, muss sie ebenfalls verschluesselt schreiben ODER entfernt werden (Widget sollte ohnehin nur lesen, nie schreiben).

    Pruefen ob `WidgetAccountsStorage.saveAccounts()` aufgerufen wird. Falls nicht: entfernen. Falls ja: auf verschluesselte Variante umstellen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && grep -c "widget_accounts.encrypted" InsightFlowWidget/InsightFlowWidget.swift && grep -c "AES.GCM" InsightFlowWidget/InsightFlowWidget.swift && grep -c "token.prefix" InsightFlowWidget/InsightFlowWidget.swift && grep -c "acc.token" InsightFlowWidget/InsightFlowWidget.swift</automated>
  </verify>
  <acceptance_criteria>
    - InsightFlowWidget.swift enthaelt `private static let fileName = "widget_accounts.encrypted"`
    - InsightFlowWidget.swift: WidgetAccountsStorage.loadAccounts() enthaelt `AES.GCM.SealedBox(combined:` und `AES.GCM.open(sealedBox, using: key)`
    - InsightFlowWidget.swift: loadEncryptionKey() ist in WidgetAccountsStorage vorhanden (liest `widget_credentials.key`)
    - InsightFlowWidget.swift: Legacy-Fallback auf `widget_accounts.json` ist vorhanden (loadLegacyAccounts Methode)
    - `grep -c "token.prefix" InsightFlowWidget/InsightFlowWidget.swift` ergibt 0
    - `grep -c "acc.token" InsightFlowWidget/InsightFlowWidget.swift` ergibt 0 (kein Token in Logs)
    - Alle widgetLog-Aufrufe fuer Accounts enthalten nur `acc.name`, `acc.providerType`, `acc.sites` — KEIN `acc.token`
    - `import CryptoKit` steht am Anfang der Datei (nicht doppelt)
    - `xcodebuild build` kompiliert ohne Fehler
  </acceptance_criteria>
  <done>Widget liest verschluesselte widget_accounts.encrypted mit AES-GCM und faellt auf Legacy-Plaintext zurueck. Keine Token-Informationen in Widget-Logs.</done>
</task>

</tasks>

<verification>
Nach Abschluss beider Tasks:
1. `grep -r "widget_accounts.encrypted" InsightFlow/ InsightFlowWidget/` — verschluesselte Datei in App und Widget referenziert
2. `grep -r "token.prefix\|acc\.token" InsightFlowWidget/` — kein Treffer (Token-Logging entfernt)
3. `grep "SharedCredentials.saveWidgetAccounts" InsightFlow/Services/AccountManager.swift` — verschluesselter Write
4. `grep "AES.GCM" InsightFlowWidget/InsightFlowWidget.swift` — Widget entschluesselt
5. Xcode-Build kompiliert erfolgreich
</verification>

<success_criteria>
- Widget-Accounts werden als AES-GCM-verschluesselte widget_accounts.encrypted gespeichert (SEC-02)
- Widget liest verschluesselte Datei mit demselben Schluessel (widget_credentials.key) wie widget_credentials.encrypted
- Legacy widget_accounts.json wird bei erstem verschluesselten Write geloescht
- Widget hat Fallback auf Plaintext-Legacy fuer Uebergangszeit nach Update
- Kein `token.prefix()` oder `acc.token` in Widget-Logs (SEC-03)
- Token-Log-Zeile 98 ist vollstaendig entfernt, nicht nur kommentiert oder in DEBUG gewrappt
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-hardening/01-02-SUMMARY.md`
</output>
