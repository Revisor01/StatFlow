---
phase: 19-architektur-robustheit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - InsightFlow/Extensions/Error+Network.swift
  - InsightFlow/Views/Reports/ReportsViewModel.swift
  - InsightFlow/Views/Events/EventsViewModel.swift
  - InsightFlow/Views/Sessions/SessionsView.swift
  - InsightFlow/Views/Detail/WebsiteDetailViewModel.swift
  - InsightFlow/Views/Dashboard/DashboardView.swift
  - InsightFlow/Views/Admin/AdminView.swift
  - InsightFlow/Views/Events/EventsView.swift
  - InsightFlow/Views/Reports/ReportsHubView.swift
autonomous: true
requirements: [TASK-02, REFACTOR-01, REFACTOR-05]

must_haves:
  truths:
    - "isNetworkError logic exists in exactly one place (Error+Network.swift extension)"
    - "All 5 ViewModels use error.isNetworkError instead of inline URLError checks"
    - "AdminView Teams and Users sections use VStack instead of LazyVStack"
    - "EventsView uses VStack instead of LazyVStack"
    - "ReportsHubView uses VStack instead of LazyVStack"
    - "TASK-02 is documented as pre-completed from Phase 1 Plan 03"
  artifacts:
    - path: "InsightFlow/Extensions/Error+Network.swift"
      provides: "Shared isNetworkError computed property on Error"
      contains: "extension Error"
    - path: "InsightFlow/Views/Admin/AdminView.swift"
      provides: "VStack for Teams and Users sections"
    - path: "InsightFlow/Views/Events/EventsView.swift"
      provides: "VStack instead of LazyVStack for event list with conditional stats header"
    - path: "InsightFlow/Views/Reports/ReportsHubView.swift"
      provides: "VStack instead of LazyVStack for offline banner + grid"
  key_links:
    - from: "InsightFlow/Views/Reports/ReportsViewModel.swift"
      to: "InsightFlow/Extensions/Error+Network.swift"
      via: "error.isNetworkError in catch block"
      pattern: "error\\.isNetworkError"
    - from: "InsightFlow/Views/Dashboard/DashboardView.swift"
      to: "InsightFlow/Extensions/Error+Network.swift"
      via: "error.isNetworkError in catch block"
      pattern: "error\\.isNetworkError"
---

<objective>
Deduplicate network error detection into a single Error extension, convert 4 inappropriate LazyVStack usages to VStack, and document TASK-02 as pre-completed.

Purpose: Eliminate copy-paste error handling (REFACTOR-01), prevent stale rendering bugs from LazyVStack with conditional content (REFACTOR-05), and close out TASK-02.
Output: Error+Network.swift extension, 5 simplified catch blocks, 4 VStack conversions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v2.7-ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-architektur-robustheit/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract isNetworkError into Error extension and deduplicate all 5 catch blocks (REFACTOR-01)</name>
  <files>
    InsightFlow/Extensions/Error+Network.swift,
    InsightFlow/Views/Reports/ReportsViewModel.swift,
    InsightFlow/Views/Events/EventsViewModel.swift,
    InsightFlow/Views/Sessions/SessionsView.swift,
    InsightFlow/Views/Detail/WebsiteDetailViewModel.swift,
    InsightFlow/Views/Dashboard/DashboardView.swift
  </files>
  <read_first>
    InsightFlow/Views/Reports/ReportsViewModel.swift,
    InsightFlow/Views/Events/EventsViewModel.swift,
    InsightFlow/Views/Sessions/SessionsView.swift,
    InsightFlow/Views/Detail/WebsiteDetailViewModel.swift,
    InsightFlow/Views/Dashboard/DashboardView.swift
  </read_first>
  <action>
1. Create `InsightFlow/Extensions/Error+Network.swift` with:
```swift
import Foundation

extension Error {
    /// Checks if this error indicates a network connectivity problem.
    /// Centralizes network error detection — add new URLError codes here only.
    var isNetworkError: Bool {
        guard let urlError = self as? URLError else { return false }
        return [
            .notConnectedToInternet,
            .networkConnectionLost,
            .timedOut,
            .cannotFindHost,
            .cannotConnectToHost
        ].contains(urlError.code)
    }
}
```

2. Add the new file to the Xcode project (InsightFlow target) by adding it to the project.pbxproj. Use the same group/folder pattern as the existing `View+Extensions.swift` in the Extensions group.

3. Replace the duplicated isNetworkError blocks in all 5 files. Each file has a pattern like:
```swift
let isNetworkError = (error as? URLError)?.code == .notConnectedToInternet ||
                     (error as? URLError)?.code == .networkConnectionLost ||
                     (error as? URLError)?.code == .timedOut ||
                     (error as? URLError)?.code == .cannotFindHost ||
                     (error as? URLError)?.code == .cannotConnectToHost
if isNetworkError {
```
Replace with:
```swift
if error.isNetworkError {
```
Remove the `let isNetworkError = ...` lines entirely.

Exact locations from research:
- ReportsViewModel.swift lines ~63-67
- EventsViewModel.swift lines ~49-53
- SessionsView.swift lines ~522-526
- WebsiteDetailViewModel.swift lines ~91-95
- DashboardView.swift lines ~975-979
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && grep -r "URLError)?.code == .notConnectedToInternet" InsightFlow/ | wc -l | tr -d ' ' | grep -q "^0$" && echo "PASS: No inline URLError checks remain" || echo "FAIL: Inline URLError checks still exist"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "var isNetworkError: Bool" InsightFlow/Extensions/Error+Network.swift` finds exactly 1 match
    - `grep -r "URLError)?.code == .notConnectedToInternet" InsightFlow/` finds 0 matches
    - `grep -r "error.isNetworkError" InsightFlow/Views/` finds exactly 5 matches (one per ViewModel)
    - `xcodebuild -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5` shows BUILD SUCCEEDED
  </acceptance_criteria>
  <done>Error+Network.swift contains the single isNetworkError extension. All 5 ViewModels use error.isNetworkError. No inline URLError code checks remain. Project builds successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Convert 4 LazyVStack usages to VStack (REFACTOR-05) and document TASK-02 completion</name>
  <files>
    InsightFlow/Views/Admin/AdminView.swift,
    InsightFlow/Views/Events/EventsView.swift,
    InsightFlow/Views/Reports/ReportsHubView.swift
  </files>
  <read_first>
    InsightFlow/Views/Admin/AdminView.swift,
    InsightFlow/Views/Events/EventsView.swift,
    InsightFlow/Views/Reports/ReportsHubView.swift
  </read_first>
  <action>
1. **AdminView.swift** — Change LazyVStack to VStack at 2 locations:
   - Line ~173: Teams section `LazyVStack` -> `VStack` (teams list is always small, typically < 10)
   - Line ~213: Users section `LazyVStack` -> `VStack` (users list is always small, typically < 20)
   - Keep the LazyVStack at lines ~111 and ~134 (website cards — can grow large)

2. **EventsView.swift** — Change LazyVStack to VStack at line ~41:
   - The LazyVStack contains `if let stats = viewModel.eventStats` conditional content inside
   - Change `LazyVStack(spacing: 0)` to `VStack(spacing: 0)` (preserve spacing parameter)
   - This prevents stale rendering when eventStats changes while content is off-screen

3. **ReportsHubView.swift** — Change LazyVStack to VStack at line ~36:
   - Contains `if viewModel.isOffline` conditional content inside
   - Change `LazyVStack` to `VStack` (preserve any spacing/alignment parameters)
   - Only ~6 items total, no performance concern

4. Add a brief comment above each remaining LazyVStack in AdminView (lines ~111, ~134) and other files where LazyVStack is intentionally kept:
   ```swift
   // LazyVStack: intentional — homogeneous ForEach, list can grow large
   ```

5. **TASK-02 documentation**: TASK-02 (Account-Switch ohne globalen Singleton-State) was fully resolved in Phase 1 Plan 03 via `configureProviderForAccount()`. All remaining `setActiveAccount` calls are legitimate user-initiated actions. No code changes needed — this is documented here for requirement traceability.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && echo "=== AdminView LazyVStack count ===" && grep -c "LazyVStack" InsightFlow/Views/Admin/AdminView.swift && echo "=== EventsView LazyVStack count ===" && grep -c "LazyVStack" InsightFlow/Views/Events/EventsView.swift && echo "=== ReportsHubView LazyVStack count ===" && grep -c "LazyVStack" InsightFlow/Views/Reports/ReportsHubView.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LazyVStack" InsightFlow/Views/Admin/AdminView.swift` returns 2 (websites + sites kept)
    - `grep -c "LazyVStack" InsightFlow/Views/Events/EventsView.swift` returns 0
    - `grep -c "LazyVStack" InsightFlow/Views/Reports/ReportsHubView.swift` returns 0
    - `grep -c "VStack" InsightFlow/Views/Events/EventsView.swift` returns at least 1
    - `grep -c "VStack" InsightFlow/Views/Reports/ReportsHubView.swift` returns at least 1
    - `xcodebuild -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5` shows BUILD SUCCEEDED
  </acceptance_criteria>
  <done>4 LazyVStack usages converted to VStack (AdminView teams+users, EventsView, ReportsHubView). 4 LazyVStack usages intentionally kept with documentation comments. Project builds. TASK-02 documented as pre-completed.</done>
</task>

</tasks>

<verification>
1. `grep -r "URLError)?.code ==" InsightFlow/` returns 0 matches — no inline network error checks
2. `grep -r "error.isNetworkError" InsightFlow/` returns 5 matches — all ViewModels use extension
3. Total LazyVStack count: `grep -r "LazyVStack" InsightFlow/Views/` returns exactly 4 (DashboardView 1, AdminView 2, SessionsView 1)
4. Xcode build succeeds with no warnings related to changed files
</verification>

<success_criteria>
- Single source of truth for network error detection in Error+Network.swift
- Zero duplicated isNetworkError blocks across the codebase
- All conditional-content LazyVStacks converted to VStack
- All intentionally-kept LazyVStacks documented with comments
- Project compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/19-architektur-robustheit/19-01-SUMMARY.md`
</output>
