---
phase: 20-code-organisation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - InsightFlow/Views/Dashboard/DashboardView.swift
  - InsightFlow/Views/Dashboard/DashboardViewModel.swift
  - InsightFlow/Views/Sessions/SessionsView.swift
  - InsightFlow/Views/Sessions/SessionsViewModel.swift
  - InsightFlow/Views/Realtime/RealtimeView.swift
  - InsightFlow/Views/Realtime/RealtimeViewModel.swift
  - InsightFlow/Views/Settings/SettingsView.swift
  - InsightFlow/Views/Settings/SettingsViewModel.swift
  - InsightFlow/Views/Admin/AdminView.swift
  - InsightFlow/Views/Admin/AdminViewModel.swift
  - InsightFlow/Views/Reports/RetentionViewModel.swift
  - InsightFlow/Views/Reports/RetentionView.swift
  - InsightFlow/Views/Reports/ComparisonViewModel.swift
  - InsightFlow/Views/Reports/InsightsView.swift
  - InsightFlow/Views/Reports/PagesViewModel.swift
  - InsightFlow/Views/Reports/PagesView.swift
autonomous: true
requirements: [REFACTOR-03]

must_haves:
  truths:
    - "Every ViewModel lives in its own dedicated file, not embedded in a View file"
    - "All 8 View files have their ViewModel code removed and only contain View code"
    - "Project builds successfully with zero errors after extraction"
    - "No file exceeds 500 LOC except DashboardView.swift (~795) and SettingsView.swift (~591) which are pure View files"
  artifacts:
    - path: "InsightFlow/Views/Dashboard/DashboardViewModel.swift"
      provides: "DashboardViewModel class (~410 LOC)"
      contains: "class DashboardViewModel"
    - path: "InsightFlow/Views/Sessions/SessionsViewModel.swift"
      provides: "SessionsViewModel, SessionDetailViewModel, JourneyViewModel (~245 LOC)"
      contains: "class SessionsViewModel"
    - path: "InsightFlow/Views/Realtime/RealtimeViewModel.swift"
      provides: "RealtimeViewModel, LiveEventDetailViewModel (~363 LOC)"
      contains: "class RealtimeViewModel"
    - path: "InsightFlow/Views/Settings/SettingsViewModel.swift"
      provides: "SettingsViewModel (~162 LOC)"
      contains: "class SettingsViewModel"
    - path: "InsightFlow/Views/Admin/AdminViewModel.swift"
      provides: "AdminViewModel (~271 LOC)"
      contains: "class AdminViewModel"
    - path: "InsightFlow/Views/Reports/RetentionViewModel.swift"
      provides: "RetentionViewModel (~89 LOC)"
      contains: "class RetentionViewModel"
    - path: "InsightFlow/Views/Reports/ComparisonViewModel.swift"
      provides: "ComparisonViewModel (~72 LOC)"
      contains: "class ComparisonViewModel"
    - path: "InsightFlow/Views/Reports/PagesViewModel.swift"
      provides: "PagesViewModel (~149 LOC)"
      contains: "class PagesViewModel"
  key_links:
    - from: "InsightFlow/Views/Dashboard/DashboardView.swift"
      to: "InsightFlow/Views/Dashboard/DashboardViewModel.swift"
      via: "@StateObject var viewModel"
      pattern: "@StateObject.*DashboardViewModel"
    - from: "InsightFlow/Views/Sessions/SessionsView.swift"
      to: "InsightFlow/Views/Sessions/SessionsViewModel.swift"
      via: "@StateObject var viewModel"
      pattern: "@StateObject.*SessionsViewModel"
---

<objective>
Extract all 8 embedded ViewModels from their View files into dedicated ViewModel files.

Purpose: REFACTOR-03 requires each ViewModel in its own file for testability, maintainability, and the 500 LOC target. This is the structural foundation that Plan 02 (DI) builds on.

Output: 8 new ViewModel files created, 8 View files trimmed to View-only code, project compiles clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v2.7-ROADMAP.md
@.planning/milestones/v2.7-REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-code-organisation/20-RESEARCH.md

Key facts:
- Xcode uses folder-based project structure (Xcode 16+). New .swift files in existing directories are auto-discovered. NO pbxproj edits needed.
- Build command: `xcodebuild -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build`
- Each extracted ViewModel needs `import Foundation` at minimum, plus `import SwiftUI` if it uses `@Published` or SwiftUI types, plus `import Combine` if needed.
- Do NOT change any logic during extraction — pure move operation.
- The `@StateObject var viewModel = SomeViewModel()` in View files must remain unchanged since init signatures don't change.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract ViewModels from large files (Dashboard, Sessions, Realtime, Settings)</name>
  <read_first>
    - InsightFlow/Views/Dashboard/DashboardView.swift (lines 796-1205 = DashboardViewModel)
    - InsightFlow/Views/Sessions/SessionsView.swift (lines 480-724 = SessionsViewModel + SessionDetailViewModel + JourneyViewModel)
    - InsightFlow/Views/Realtime/RealtimeView.swift (lines 306-668 = RealtimeViewModel + LiveEventDetailViewModel)
    - InsightFlow/Views/Settings/SettingsView.swift (lines 592-753 = SettingsViewModel)
  </read_first>
  <files>
    InsightFlow/Views/Dashboard/DashboardView.swift
    InsightFlow/Views/Dashboard/DashboardViewModel.swift (NEW)
    InsightFlow/Views/Sessions/SessionsView.swift
    InsightFlow/Views/Sessions/SessionsViewModel.swift (NEW)
    InsightFlow/Views/Realtime/RealtimeView.swift
    InsightFlow/Views/Realtime/RealtimeViewModel.swift (NEW)
    InsightFlow/Views/Settings/SettingsView.swift
    InsightFlow/Views/Settings/SettingsViewModel.swift (NEW)
  </files>
  <action>
    For each of the 4 source files, perform these steps:

    1. **Read the full file** to identify exact ViewModel class boundaries (class declaration through closing brace).

    2. **Create the new ViewModel file** with:
       - `import Foundation` (always)
       - `import SwiftUI` (if ViewModel uses @Published, Color, or other SwiftUI types)
       - `import os` (if file has print statements — these will be migrated in Plan 03)
       - Copy the complete ViewModel class(es) verbatim — no logic changes
       - Ensure any helper types/enums used only by the ViewModel are also moved

    3. **Remove the ViewModel code from the View file:**
       - Delete the ViewModel class(es) from the View file
       - Keep all View structs, extensions, and PreviewProvider
       - Ensure the View file's imports are still sufficient for its remaining code

    4. **Verify each pair compiles** before moving to the next file.

    Specific extractions:
    - **DashboardView.swift** → DashboardViewModel.swift: Move `class DashboardViewModel: ObservableObject` (starts ~line 796). Check for any nested types/enums inside the class.
    - **SessionsView.swift** → SessionsViewModel.swift: Move `class SessionsViewModel`, `class SessionDetailViewModel`, AND `class JourneyViewModel` (starts ~line 480). All three go into one file.
    - **RealtimeView.swift** → RealtimeViewModel.swift: Move `class RealtimeViewModel` AND `class LiveEventDetailViewModel` (starts ~line 306). Both go into one file.
    - **SettingsView.swift** → SettingsViewModel.swift: Move `class SettingsViewModel` (starts ~line 592).

    IMPORTANT: Do NOT modify any logic, method signatures, property declarations, or access control. This is a pure extraction — cut from View file, paste into ViewModel file with correct imports.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "class DashboardViewModel" InsightFlow/Views/Dashboard/DashboardViewModel.swift` returns the new file
    - `grep -c "class DashboardViewModel" InsightFlow/Views/Dashboard/DashboardView.swift` returns 0
    - `grep -l "class SessionsViewModel" InsightFlow/Views/Sessions/SessionsViewModel.swift` returns the new file
    - `grep -c "class SessionsViewModel" InsightFlow/Views/Sessions/SessionsView.swift` returns 0
    - `grep -l "class RealtimeViewModel" InsightFlow/Views/Realtime/RealtimeViewModel.swift` returns the new file
    - `grep -c "class RealtimeViewModel" InsightFlow/Views/Realtime/RealtimeView.swift` returns 0
    - `grep -l "class SettingsViewModel" InsightFlow/Views/Settings/SettingsViewModel.swift` returns the new file
    - `grep -c "class SettingsViewModel" InsightFlow/Views/Settings/SettingsView.swift` returns 0
    - Build succeeds: `** BUILD SUCCEEDED **`
  </acceptance_criteria>
  <done>4 new ViewModel files created (DashboardViewModel, SessionsViewModel, RealtimeViewModel, SettingsViewModel), 4 View files trimmed, project builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extract ViewModels from smaller files (Admin, Retention, Insights/Comparison, Pages)</name>
  <read_first>
    - InsightFlow/Views/Admin/AdminView.swift (lines 252-522 = AdminViewModel)
    - InsightFlow/Views/Reports/RetentionView.swift (lines 363-451 = RetentionViewModel)
    - InsightFlow/Views/Reports/InsightsView.swift (lines 287-358 = ComparisonViewModel)
    - InsightFlow/Views/Reports/PagesView.swift (lines 142-290 = PagesViewModel)
  </read_first>
  <files>
    InsightFlow/Views/Admin/AdminView.swift
    InsightFlow/Views/Admin/AdminViewModel.swift (NEW)
    InsightFlow/Views/Reports/RetentionView.swift
    InsightFlow/Views/Reports/RetentionViewModel.swift (NEW)
    InsightFlow/Views/Reports/InsightsView.swift
    InsightFlow/Views/Reports/ComparisonViewModel.swift (NEW)
    InsightFlow/Views/Reports/PagesView.swift
    InsightFlow/Views/Reports/PagesViewModel.swift (NEW)
  </files>
  <action>
    Same extraction pattern as Task 1 for the remaining 4 files:

    - **AdminView.swift** → AdminViewModel.swift: Move `class AdminViewModel` (starts ~line 252, ~271 LOC).
    - **RetentionView.swift** → RetentionViewModel.swift: Move `class RetentionViewModel` (starts ~line 363, ~89 LOC).
    - **InsightsView.swift** → ComparisonViewModel.swift: Move `class ComparisonViewModel` (starts ~line 287, ~72 LOC).
    - **PagesView.swift** → PagesViewModel.swift: Move `class PagesViewModel` (starts ~line 142, ~149 LOC).

    Same rules: correct imports, no logic changes, verbatim move. Each new file gets `import Foundation` and `import SwiftUI` (if @Published used).

    After all 4 extractions, run a full build to confirm everything compiles.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "class AdminViewModel" InsightFlow/Views/Admin/AdminViewModel.swift` returns the new file
    - `grep -c "class AdminViewModel" InsightFlow/Views/Admin/AdminView.swift` returns 0
    - `grep -l "class RetentionViewModel" InsightFlow/Views/Reports/RetentionViewModel.swift` returns the new file
    - `grep -c "class RetentionViewModel" InsightFlow/Views/Reports/RetentionView.swift` returns 0
    - `grep -l "class ComparisonViewModel" InsightFlow/Views/Reports/ComparisonViewModel.swift` returns the new file
    - `grep -c "class ComparisonViewModel" InsightFlow/Views/Reports/InsightsView.swift` returns 0
    - `grep -l "class PagesViewModel" InsightFlow/Views/Reports/PagesViewModel.swift` returns the new file
    - `grep -c "class PagesViewModel" InsightFlow/Views/Reports/PagesView.swift` returns 0
    - Build succeeds: `** BUILD SUCCEEDED **`
    - `xcodebuild -project InsightFlow.xcodeproj -scheme InsightFlow test -destination 'platform=iOS Simulator,name=iPhone 17 Pro' 2>&1 | grep -E "TEST.*PASSED|TEST.*FAILED"` shows tests pass
  </acceptance_criteria>
  <done>All 8 ViewModel extractions complete. 8 new files created, 8 View files contain only View code. Full build and existing tests pass.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `xcodebuild build` succeeds with zero errors
2. `xcodebuild test` — existing tests pass (DashboardViewModelTests, KeychainServiceTests, etc.)
3. No ViewModel class definitions remain in any View file:
   `grep -rn "class.*ViewModel.*ObservableObject" InsightFlow/Views/` should only show results in *ViewModel.swift files, not in *View.swift files
4. Each new ViewModel file exists and contains the expected class
</verification>

<success_criteria>
- 8 new ViewModel files created in correct directories
- 8 View files trimmed to View-only code
- No file > 500 LOC except DashboardView.swift (~795, pure View) and SettingsView.swift (~591, pure View)
- Project builds and all existing tests pass
- No logic changes — pure extraction
</success_criteria>

<output>
After completion, create `.planning/phases/20-code-organisation/20-01-SUMMARY.md`
</output>
