---
phase: 20-code-organisation
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - InsightFlow/Views/Dashboard/DashboardViewModel.swift
  - InsightFlow/Views/Sessions/SessionsViewModel.swift
  - InsightFlow/Views/Realtime/RealtimeViewModel.swift
  - InsightFlow/Views/Settings/SettingsViewModel.swift
  - InsightFlow/Views/Admin/AdminViewModel.swift
  - InsightFlow/Views/Reports/RetentionViewModel.swift
  - InsightFlow/Views/Reports/ComparisonViewModel.swift
  - InsightFlow/Views/Reports/PagesViewModel.swift
  - InsightFlow/Views/Detail/WebsiteDetailViewModel.swift
  - InsightFlow/Views/Detail/CompareViewModel.swift
  - InsightFlow/Views/Events/EventsViewModel.swift
  - InsightFlow/Views/Reports/ReportsViewModel.swift
  - InsightFlow/Views/Auth/LoginViewModel.swift
  - InsightFlow/Services/KeychainService.swift
  - InsightFlowTests/KeychainServiceTests.swift
autonomous: true
requirements: [REFACTOR-04, SEC-01]

must_haves:
  truths:
    - "Every ViewModel accepts API dependencies via init parameters with default values"
    - "Existing code continues to work unchanged because defaults are .shared"
    - "Tests can inject mock providers without touching production code"
    - "KeychainService never crashes on non-UTF-8 input — throws error instead"
  artifacts:
    - path: "InsightFlow/Views/Dashboard/DashboardViewModel.swift"
      provides: "DI-enabled DashboardViewModel"
      contains: "init(umamiAPI: UmamiAPI = .shared"
    - path: "InsightFlow/Views/Sessions/SessionsViewModel.swift"
      provides: "DI-enabled SessionsViewModel"
      contains: "init(websiteId: String, api: UmamiAPI = .shared"
    - path: "InsightFlow/Services/KeychainService.swift"
      provides: "Safe KeychainService without force unwraps"
      contains: "case encodingFailed"
  key_links:
    - from: "All ViewModels"
      to: "UmamiAPI/PlausibleAPI"
      via: "init parameter with default"
      pattern: "init\\(.*API.*=.*\\.shared"
    - from: "KeychainService.swift"
      to: "KeychainError"
      via: "guard let + throw"
      pattern: "guard let data.*else.*throw.*encodingFailed"
---

<objective>
Add Dependency Injection to all ViewModels and fix force unwraps in KeychainService.

Purpose: REFACTOR-04 makes ViewModels testable by injecting API dependencies via init params with defaults. SEC-01 eliminates crash risk from force unwraps on UTF-8 encoding.

Output: All 13 ViewModels accept injectable dependencies, KeychainService uses guard-let instead of force unwrap, one new test for encoding error case.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v2.7-ROADMAP.md
@.planning/milestones/v2.7-REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-code-organisation/20-RESEARCH.md
@.planning/phases/20-code-organisation/20-01-SUMMARY.md

Key DI patterns from research:

**Single-provider VMs** (only UmamiAPI): SessionsViewModel, SessionDetailViewModel, JourneyViewModel, LiveEventDetailViewModel, RetentionViewModel, PagesViewModel, ComparisonViewModel, EventsViewModel, ReportsViewModel
```swift
init(websiteId: String, api: UmamiAPI = .shared) {
    self.websiteId = websiteId
    self.api = api
}
```

**Dual-provider VMs** (UmamiAPI + PlausibleAPI): DashboardViewModel, RealtimeViewModel, SettingsViewModel, AdminViewModel, CompareViewModel
```swift
init(umamiAPI: UmamiAPI = .shared, plausibleAPI: PlausibleAPI = .shared) {
    self.umamiAPI = umamiAPI
    self.plausibleAPI = plausibleAPI
}
```

**LoginViewModel**: Direct inline calls — convert to stored properties with defaults.

Build command: `xcodebuild -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build`
Test command: `xcodebuild -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DI to all 13 ViewModels</name>
  <read_first>
    - InsightFlow/Views/Dashboard/DashboardViewModel.swift
    - InsightFlow/Views/Sessions/SessionsViewModel.swift
    - InsightFlow/Views/Realtime/RealtimeViewModel.swift
    - InsightFlow/Views/Settings/SettingsViewModel.swift
    - InsightFlow/Views/Admin/AdminViewModel.swift
    - InsightFlow/Views/Reports/RetentionViewModel.swift
    - InsightFlow/Views/Reports/ComparisonViewModel.swift
    - InsightFlow/Views/Reports/PagesViewModel.swift
    - InsightFlow/Views/Detail/WebsiteDetailViewModel.swift
    - InsightFlow/Views/Detail/CompareViewModel.swift
    - InsightFlow/Views/Events/EventsViewModel.swift
    - InsightFlow/Views/Reports/ReportsViewModel.swift
    - InsightFlow/Views/Auth/LoginViewModel.swift
  </read_first>
  <files>
    InsightFlow/Views/Dashboard/DashboardViewModel.swift
    InsightFlow/Views/Sessions/SessionsViewModel.swift
    InsightFlow/Views/Realtime/RealtimeViewModel.swift
    InsightFlow/Views/Settings/SettingsViewModel.swift
    InsightFlow/Views/Admin/AdminViewModel.swift
    InsightFlow/Views/Reports/RetentionViewModel.swift
    InsightFlow/Views/Reports/ComparisonViewModel.swift
    InsightFlow/Views/Reports/PagesViewModel.swift
    InsightFlow/Views/Detail/WebsiteDetailViewModel.swift
    InsightFlow/Views/Detail/CompareViewModel.swift
    InsightFlow/Views/Events/EventsViewModel.swift
    InsightFlow/Views/Reports/ReportsViewModel.swift
    InsightFlow/Views/Auth/LoginViewModel.swift
  </files>
  <action>
    For each ViewModel, apply this transformation:

    1. **Find the current API property declarations** — look for patterns like:
       - `private let api = UmamiAPI.shared`
       - `private let umamiAPI = UmamiAPI.shared`
       - `private let plausibleAPI = PlausibleAPI.shared`
       - Inline calls like `UmamiAPI.shared.someMethod()`

    2. **Convert to init-injected properties:**
       - Change `private let api = UmamiAPI.shared` to `private let api: UmamiAPI`
       - Add/update `init()` to accept the parameter with default: `init(..., api: UmamiAPI = .shared)`
       - Assign in init: `self.api = api`

    3. **For dual-provider VMs**, inject both:
       ```swift
       init(..., umamiAPI: UmamiAPI = .shared, plausibleAPI: PlausibleAPI = .shared) {
           self.umamiAPI = umamiAPI
           self.plausibleAPI = plausibleAPI
       }
       ```

    4. **For DashboardViewModel**, also inject cache if it references `AnalyticsCacheService.shared`:
       ```swift
       init(umamiAPI: UmamiAPI = .shared, plausibleAPI: PlausibleAPI = .shared, cache: AnalyticsCacheService = .shared) {
       ```

    5. **For LoginViewModel**, convert inline `UmamiAPI.shared.login()` calls to stored property references.

    6. **Preserve existing init parameters** (like `websiteId: String`) — add API params AFTER existing params.

    7. **Do NOT change method bodies** — only property declarations and init signatures. All existing call sites use no-arg or existing-arg init, so default values keep them working.

    Build after every 3-4 files to catch issues early.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "= UmamiAPI.shared" InsightFlow/Views/` returns 0 results (no more direct assignment)
    - `grep -rn "= PlausibleAPI.shared" InsightFlow/Views/` returns 0 results
    - `grep -rn "UmamiAPI = .shared" InsightFlow/Views/` shows init params in all relevant ViewModels
    - Build succeeds: `** BUILD SUCCEEDED **`
  </acceptance_criteria>
  <done>All 13 ViewModels accept API dependencies via init parameters with default values. No direct singleton assignment remains in any ViewModel.</done>
</task>

<task type="auto">
  <name>Task 2: Fix force unwraps in KeychainService (SEC-01)</name>
  <read_first>
    - InsightFlow/Services/KeychainService.swift (full file, especially lines 18 and 89)
    - InsightFlowTests/KeychainServiceTests.swift
  </read_first>
  <files>
    InsightFlow/Services/KeychainService.swift
    InsightFlowTests/KeychainServiceTests.swift
  </files>
  <action>
    1. **In KeychainService.swift**, find the `KeychainError` enum and add:
       ```swift
       case encodingFailed
       ```
       And in `errorDescription`:
       ```swift
       case .encodingFailed:
           return "Wert konnte nicht als UTF-8 kodiert werden"
       ```

    2. **Line ~18** (in `save(_:for:)` method): Replace
       ```swift
       let data = value.data(using: .utf8)!
       ```
       with
       ```swift
       guard let data = value.data(using: .utf8) else {
           throw KeychainError.encodingFailed
       }
       ```

    3. **Line ~89** (in `saveCredential(_:type:accountId:)` method): Same replacement.

    4. **Verify no other force unwraps remain:**
       `grep -n "!" InsightFlow/Services/KeychainService.swift` — only `!` should be in type names like `String!` if any, not force unwrap operators.

    5. **Add one test** in KeychainServiceTests.swift — test that the error case is defined (since Swift String.data(using: .utf8) almost never returns nil for valid Swift strings, we test the error type exists):
       ```swift
       func testKeychainErrorEncodingFailed() {
           let error = KeychainError.encodingFailed
           XCTAssertNotNil(error.errorDescription)
           XCTAssertTrue(error.errorDescription!.contains("UTF-8"))
       }
       ```

    6. Run tests to confirm all pass.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test 2>&1 | grep -E "Test Suite.*passed|Test Suite.*failed|BUILD"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\.utf8)!" InsightFlow/Services/KeychainService.swift` returns 0 results (no force unwraps on UTF-8)
    - `grep -n "encodingFailed" InsightFlow/Services/KeychainService.swift` returns at least 3 matches (enum case + 2 throw sites)
    - `grep -n "guard let data" InsightFlow/Services/KeychainService.swift` returns 2 matches
    - All tests pass including the new encoding error test
  </acceptance_criteria>
  <done>KeychainService uses guard-let instead of force unwrap at both locations. New KeychainError.encodingFailed case added. Test coverage for the error case exists. Zero force unwraps remain in the file.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `xcodebuild build` succeeds
2. `xcodebuild test` — all tests pass (existing + new KeychainError test)
3. `grep -rn "= UmamiAPI.shared\|= PlausibleAPI.shared" InsightFlow/Views/` returns 0 results
4. `grep -n "\.utf8)!" InsightFlow/Services/KeychainService.swift` returns 0 results
</verification>

<success_criteria>
- All 13 ViewModels have injectable API dependencies with default .shared values
- Existing call sites unchanged (backward compatible via defaults)
- KeychainService has zero force unwraps
- KeychainError.encodingFailed case exists with German error description
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-code-organisation/20-02-SUMMARY.md`
</output>
