---
phase: 08-events-reports-screens
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - InsightFlow/Views/Detail/WebsiteDetailView.swift
autonomous: false
requirements: [SCREEN-01, SCREEN-02]

must_haves:
  truths:
    - "Quick Actions Section zeigt Events und Reports Karten bei Umami-Websites"
    - "Events und Reports Karten sind NUR bei Umami-Provider sichtbar (nicht bei Plausible)"
    - "Tap auf Events-Karte oeffnet EventsView"
    - "Tap auf Reports-Karte oeffnet ReportsHubView"
    - "Bestehende Quick Actions (Sessions, Compare) bleiben unveraendert"
  artifacts:
    - path: "InsightFlow/Views/Detail/WebsiteDetailView.swift"
      provides: "Erweiterte Quick Actions mit Events + Reports"
      contains: "EventsView"
  key_links:
    - from: "InsightFlow/Views/Detail/WebsiteDetailView.swift"
      to: "InsightFlow/Views/Events/EventsView.swift"
      via: "NavigationLink in quickActionsSection"
      pattern: "EventsView\\(website:"
    - from: "InsightFlow/Views/Detail/WebsiteDetailView.swift"
      to: "InsightFlow/Views/Reports/ReportsHubView.swift"
      via: "NavigationLink in quickActionsSection"
      pattern: "ReportsHubView\\(website:"
---

<objective>
Integration der Events- und Reports-Screens in die bestehende WebsiteDetailView ueber erweiterte Quick Actions.

Purpose: SCREEN-01 + SCREEN-02 — Nutzer koennen Events und Reports direkt aus der Website-Detailansicht erreichen.
Output: Erweiterte quickActionsSection in WebsiteDetailView.swift
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-events-reports-screens/08-CONTEXT.md
@.planning/phases/08-events-reports-screens/08-01-SUMMARY.md
@.planning/phases/08-events-reports-screens/08-02-SUMMARY.md

@InsightFlow/Views/Detail/WebsiteDetailView.swift (quickActionsSection modifizieren)
@InsightFlow/Views/Detail/WebsiteDetailSupportingViews.swift (QuickActionCard)

<interfaces>
<!-- Von Plan 01 erstellt -->
From InsightFlow/Views/Events/EventsView.swift:
```swift
struct EventsView: View {
    let website: Website
    // ... init mit website Parameter
}
```

<!-- Von Plan 02 erstellt -->
From InsightFlow/Views/Reports/ReportsHubView.swift:
```swift
struct ReportsHubView: View {
    let website: Website
    // ... init mit website Parameter
}
```

From InsightFlow/Views/Detail/WebsiteDetailView.swift (bestehend, quickActionsSection):
```swift
// Umami-Branch: HStack mit Sessions + Compare (2 Karten)
// Plausible-Branch: Nur Compare (1 Karte)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quick Actions um Events und Reports erweitern</name>
  <files>InsightFlow/Views/Detail/WebsiteDetailView.swift, InsightFlow/Resources/en.lproj/Localizable.strings, InsightFlow/Resources/de.lproj/Localizable.strings</files>
  <read_first>
    - InsightFlow/Views/Detail/WebsiteDetailView.swift (quickActionsSection — Zeile 334-379, das ist der zu aendernde Bereich)
    - InsightFlow/Views/Detail/WebsiteDetailSupportingViews.swift (QuickActionCard Signatur)
    - InsightFlow/Views/Events/EventsView.swift (Signatur pruefen)
    - InsightFlow/Views/Reports/ReportsHubView.swift (Signatur pruefen)
    - InsightFlow/Resources/en.lproj/Localizable.strings
    - InsightFlow/Resources/de.lproj/Localizable.strings
  </read_first>
  <action>
Aendere die `quickActionsSection` in WebsiteDetailView.swift:

**Umami-Branch** (else-Zweig ab Zeile 351):
Ersetze den bestehenden HStack mit 2 Karten durch ein Layout mit 4 Karten.
Da 4 Karten nicht in eine HStack passen, verwende ein LazyVGrid mit 2 Spalten:

```swift
} else {
    LazyVGrid(columns: [GridItem(.flexible()), GridItem(.flexible())], spacing: 12) {
        // Sessions (bestehend)
        NavigationLink {
            SessionsView(website: website)
        } label: {
            QuickActionCard(
                icon: "person.2.fill",
                title: String(localized: "sessions.tab.sessions"),
                subtitle: String(localized: "journeys.userJourney"),
                color: .purple
            )
        }
        .buttonStyle(.plain)

        // Compare (bestehend)
        NavigationLink {
            CompareView(website: website)
        } label: {
            QuickActionCard(
                icon: "arrow.left.arrow.right",
                title: String(localized: "compare.title"),
                subtitle: String(localized: "compare.type"),
                color: .orange
            )
        }
        .buttonStyle(.plain)

        // Events (NEU)
        NavigationLink {
            EventsView(website: website)
        } label: {
            QuickActionCard(
                icon: "bolt.fill",
                title: String(localized: "events.quickAction"),
                subtitle: String(localized: "events.quickAction.subtitle"),
                color: .cyan
            )
        }
        .buttonStyle(.plain)

        // Reports (NEU)
        NavigationLink {
            ReportsHubView(website: website)
        } label: {
            QuickActionCard(
                icon: "chart.pie.fill",
                title: String(localized: "reports.quickAction"),
                subtitle: String(localized: "reports.quickAction.subtitle"),
                color: .indigo
            )
        }
        .buttonStyle(.plain)
    }
}
```

**Plausible-Branch** bleibt unveraendert (nur Compare).

**Localization Keys** (an BEIDE .strings-Dateien anhaengen):
```
// Quick Actions
"events.quickAction" = "Events"; / "Events"
"events.quickAction.subtitle" = "Custom events"; / "Benutzerdefinierte Events"
"reports.quickAction" = "Reports"; / "Reports"
"reports.quickAction.subtitle" = "Analytics reports"; / "Analyse-Reports"
```

WICHTIG: Nur den Umami else-Zweig der quickActionsSection aendern. Alle anderen Teile von WebsiteDetailView.swift bleiben exakt gleich.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/umami && xcodebuild build -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - quickActionsSection hat LazyVGrid mit 4 Karten im Umami-Branch
    - EventsView(website:) und ReportsHubView(website:) sind als NavigationLink-Ziele eingebunden
    - Plausible-Branch ist unveraendert (nur Compare)
    - Localization Keys fuer Quick Actions vorhanden
    - Build kompiliert ohne Fehler
  </acceptance_criteria>
  <done>Quick Actions zeigen 4 Karten (Sessions, Compare, Events, Reports) fuer Umami-Websites</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visuelle Verifikation der neuen Screens</name>
  <files>n/a</files>
  <action>
Visuelle Verifikation durch den Nutzer. Events-Screen (Liste + Detail) und Reports-Hub (4 Karten + Detail-Views) integriert in WebsiteDetailView Quick Actions.

Pruefschritte:
1. App starten auf Simulator oder Geraet
2. Eine Umami-Website oeffnen — Quick Actions Section pruefen: 4 Karten sichtbar (Sessions, Compare, Events, Reports)
3. "Events" antippen — Events-Liste mit DateRange-Picker pruefen
4. Ein Event antippen — Event-Detail mit Properties pruefen
5. Zurueck — "Reports" antippen — Reports-Hub mit 4 Karten pruefen
6. "UTM-Kampagnen" antippen — UTM-Report pruefen (oder Empty State)
7. "Attribution" antippen — Attribution-Report pruefen (oder Empty State)
8. Plausible-Website oeffnen — Events und Reports Karten sollten NICHT sichtbar sein
  </action>
  <verify>Nutzer tippt "approved" oder beschreibt Probleme</verify>
  <done>Nutzer hat alle Screens visuell geprueft und bestaetigt</done>
</task>

</tasks>

<verification>
- Build kompiliert: `xcodebuild build -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' -quiet`
- grep "EventsView" WebsiteDetailView.swift findet NavigationLink
- grep "ReportsHubView" WebsiteDetailView.swift findet NavigationLink
- Quick Actions: 4 Karten bei Umami, 1 Karte bei Plausible
</verification>

<success_criteria>
- Events und Reports sind von WebsiteDetailView aus erreichbar
- Nur bei Umami-Provider sichtbar
- Bestehende Quick Actions (Sessions, Compare) funktionieren weiterhin
- Visuelle Qualitaet der neuen Karten passt zum bestehenden Design
</success_criteria>

<output>
After completion, create `.planning/phases/08-events-reports-screens/08-03-SUMMARY.md`
</output>
