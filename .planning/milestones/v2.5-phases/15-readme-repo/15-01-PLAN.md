---
phase: 15-readme-repo
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - README-01

must_haves:
  truths:
    - "README zeigt StatFlow als App-Name (nicht PrivacyFlow oder InsightFlow)"
    - "README enthält vollständige Feature-Liste mit allen 9 Features"
    - "README enthält Architektur-Überblick mit MVVM-Layern"
    - "README enthält Screenshots-Platzhalter-Sektion"
    - "README enthält Setup-Anleitung mit Repository-Klonen und Xcodeproj"
    - "Alle Badge-URLs verweisen auf Revisor01/StatFlow"
    - "Datenschutzerklärung ist korrekt auf StatFlow umbenannt"
  artifacts:
    - path: "README.md"
      provides: "StatFlow Landing Page"
      contains: "StatFlow"
  key_links:
    - from: "README.md"
      to: "https://github.com/Revisor01/StatFlow"
      via: "Badge URLs im Header"
      pattern: "Revisor01/StatFlow"
---

<objective>
README.md als öffentliche Landing Page für StatFlow neu schreiben — vollständige StatFlow-Umbenennung, Feature-Liste, Architektur-Überblick, Screenshots-Platzhalter und Setup-Anleitung.

Purpose: Das Repo ist öffentlich und die README ist die erste Kontaktfläche. Aktuell steht noch "PrivacyFlow" überall und fehlt Architektur-Kontext.
Output: README.md mit StatFlow-Branding, vollständiger Feature-Liste, MVVM-Architektur-Übersicht, Screenshots-Sektion und Selbst-Kompilier-Anleitung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md

<interfaces>
<!-- Relevante Architektur-Fakten aus der Codebase für README-Inhalte -->

App-Name: StatFlow (Bundle ID: de.godsapp.statflow)
Alte Namen: PrivacyFlow, InsightFlow — NICHT mehr verwenden

Architektur-Layer (aus ARCHITECTURE.md):
- Presentation: SwiftUI Views + ViewModels (InsightFlow/Views/)
- Service: UmamiAPI, PlausibleAPI, AccountManager, AnalyticsManager (InsightFlow/Services/)
- Model: Codable structs (InsightFlow/Models/)
- Infrastructure: KeychainService, AppDelegate (InsightFlow/App/)

Tech Stack (aus STACK.md):
- Swift 5.10+, SwiftUI, iOS 18.0+
- Keine Third-Party-Dependencies
- WidgetKit, Keychain, UserNotifications, BackgroundTasks
- Actor-based concurrency (UmamiAPI, PlausibleAPI als Actor)

Features (aus aktuellem README, alle beibehalten):
1. Multi-Account-Unterstützung
2. Echtzeit-Dashboard
3. Detaillierte Analysen (Pages, Referrer, Geo, Geräte, Browser)
4. Periodenvergleich
5. Home-Screen-Widgets
6. Push-Benachrichtigungen
7. Offline-Modus
8. Dark Mode
9. Lokalisierung (DE + EN)

Provider-Support:
- Umami: REST API, alle Features inkl. Sessions, Journeys, Share-Links
- Plausible: Stats API v2, Dashboard + Diagramme + Metriken

GitHub-Repo nach Rename: Revisor01/StatFlow
Xcodeproj: StatFlow.xcodeproj (nach Rename in v2.4)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.md als StatFlow Landing Page neu schreiben</name>
  <files>README.md</files>
  <read_first>README.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/STACK.md</read_first>
  <action>
README.md vollständig neu schreiben mit StatFlow-Branding und erweitertem Inhalt.

**Struktur:**

1. Header mit App-Icon-Platzhalter + `<h1 align="center">StatFlow</h1>`
2. Untertitel: "Native iOS-App für Umami und Plausible Analytics."
3. Badges (alle URLs auf Revisor01/StatFlow):
   - Platform iOS 18+
   - Swift 5.10
   - Xcode 16+
   - License (GitHub Badge)
   - Version (GitHub Tag Badge)

4. **Features** — alle 9 Features mit Emoji-Icons:
   - Multi-Account-Unterstützung
   - Echtzeit-Dashboard (Besucher, Seitenaufrufe, Absprungrate, Sitzungsdauer)
   - Detaillierte Analysen (Top-Seiten, Referrer, Geografie, Geräte, Browser)
   - Periodenvergleich (Woche, Monat, Jahr)
   - Home-Screen-Widgets
   - Push-Benachrichtigungen (täglich/wöchentlich)
   - Offline-Modus (gecachte Daten)
   - Dark Mode
   - Lokalisierung (Deutsch + Englisch)

5. **Unterstützte Anbieter** — Tabelle mit Umami und Plausible

6. **Architektur** — kurzer Überblick:
   - MVVM-Pattern mit SwiftUI
   - 4 Layer: Presentation (Views/ViewModels), Service (API-Clients, Manager), Model (Codable structs), Infrastructure (Keychain, AppDelegate)
   - Unified AnalyticsProvider-Protokoll abstrahiert Umami und Plausible
   - Actor-based Concurrency: UmamiAPI + PlausibleAPI als Swift Actors
   - Kein Third-Party-Dependencies — ausschließlich Apple Frameworks
   - Kurzes ASCII-Diagramm der Datenfluss-Architektur

7. **Screenshots** — Sektion mit Platzhalter-Text:
   ```
   *Screenshots werden nach dem ersten App Store Release ergänzt.*
   ```
   Unterabschnitte vorbereiten: Dashboard, Website-Detail, Widgets, Einstellungen

8. **Voraussetzungen**:
   - iOS 18.0+
   - Umami Self-Hosted oder Cloud ODER Plausible Self-Hosted oder Cloud

9. **Installation**:
   - App Store: "Demnächst verfügbar"
   - Selbst kompilieren:
     1. Repository klonen: `git clone https://github.com/Revisor01/StatFlow.git`
     2. `StatFlow.xcodeproj` in Xcode 16+ öffnen (NICHT mehr PrivacyFlow.xcodeproj)
     3. Bundle ID in Signing-Einstellungen anpassen (eigene Team-ID)
     4. Auf Gerät oder Simulator bauen

10. **Konfiguration**:
    1. App starten
    2. Analytics-Konto mit Server-URL und API-Zugangsdaten hinzufügen
    3. Websites auswählen
    4. Optional: Widgets auf Home Screen hinzufügen, Benachrichtigungen aktivieren

11. **Mitwirken**: Pull Requests willkommen

12. **Lizenz**: GNU GPL v3.0

13. **Danksagung**: Umami Analytics, Plausible Analytics

14. **Hinweis**: Inoffizielle Companion-App, nicht mit Umami oder Plausible verbunden

15. **Datenschutzerklärung** — MUSS aktualisiert werden:
    - Alle Vorkommen von "PrivacyFlow" durch "StatFlow" ersetzen
    - Stand auf aktuelles Datum aktualisieren: März 2026
    - Umlaute und Sonderzeichen korrekt (keine "ue", "oe" etc.)

WICHTIG: app-icon.png Referenz beibehalten (Datei existiert im Repo).
  </action>
  <verify>
    <automated>grep -c "StatFlow" README.md && grep -c "PrivacyFlow" README.md | grep "^0$" || (echo "FAIL: PrivacyFlow noch vorhanden" && false)</automated>
  </verify>
  <acceptance_criteria>
    - grep "StatFlow" README.md findet mindestens 10 Treffer
    - grep "PrivacyFlow" README.md findet 0 Treffer
    - grep "InsightFlow" README.md findet 0 Treffer
    - grep "Architektur" README.md gibt einen Treffer zurück
    - grep "Screenshots" README.md gibt einen Treffer zurück
    - grep "Revisor01/StatFlow" README.md findet Treffer in Badge-URLs
    - grep "StatFlow.xcodeproj" README.md findet einen Treffer
  </acceptance_criteria>
  <done>README.md ist vollständig auf StatFlow umgestellt, enthält alle 9 Features, Architektur-Überblick, Screenshots-Platzhalter, Setup-Anleitung und korrekte Badge-URLs.</done>
</task>

</tasks>

<verification>
grep "StatFlow" README.md
grep -c "PrivacyFlow" README.md  # muss 0 sein
grep "Architektur" README.md
grep "Screenshots" README.md
grep "StatFlow.xcodeproj" README.md
</verification>

<success_criteria>
- README.md enthält "StatFlow" als App-Name durchgehend
- Kein Vorkommen von "PrivacyFlow" oder "InsightFlow"
- Feature-Liste mit allen 9 Features vorhanden
- Architektur-Überblick mit MVVM-Layern und Provider-Abstraktion vorhanden
- Screenshots-Platzhalter-Sektion vorhanden
- Setup-Anleitung mit git clone URL und StatFlow.xcodeproj vorhanden
- Badge-URLs verweisen auf Revisor01/StatFlow
</success_criteria>

<output>
Nach Abschluss `.planning/phases/15-readme-repo/15-01-SUMMARY.md` erstellen.
</output>
