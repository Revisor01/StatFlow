---
phase: 13-critical-bug-fixes
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - InsightFlowTests/WebsiteDetailViewModelTests.swift
  - InsightFlowTests/DashboardViewModelTests.swift
autonomous: true
requirements: [FIX-02, FIX-04]

must_haves:
  truths:
    - "Test-Stubs fuer WebsiteDetailViewModel und DashboardViewModel existieren und kompilieren"
    - "Nyquist-Compliance: Jeder Code-produzierende Task in Phase 13 hat eine Testdatei als Ziel"
  artifacts:
    - path: "InsightFlowTests/WebsiteDetailViewModelTests.swift"
      provides: "Test-Stub fuer FIX-02 Task-Cancellation"
      contains: "class WebsiteDetailViewModelTests"
    - path: "InsightFlowTests/DashboardViewModelTests.swift"
      provides: "Test-Stub fuer FIX-04 clearFirst-Behavior"
      contains: "class DashboardViewModelTests"
  key_links:
    - from: "InsightFlowTests/WebsiteDetailViewModelTests.swift"
      to: "InsightFlow/Views/Detail/WebsiteDetailViewModel.swift"
      via: "@testable import InsightFlow"
      pattern: "@testable import InsightFlow"
    - from: "InsightFlowTests/DashboardViewModelTests.swift"
      to: "InsightFlow/Views/Dashboard/DashboardView.swift"
      via: "@testable import InsightFlow"
      pattern: "@testable import InsightFlow"
---

<objective>
Wave-0 Test-Stubs fuer Phase 13 erstellen — Nyquist-Compliance sicherstellen.

Purpose: Plans 13-01 und 13-02 brauchen Testdateien die noch nicht existieren. Ohne Wave 0 koennen TDD-Tasks nicht RED starten und Verify-Schritte haben kein Test-Target.
Output: Zwei neue leere XCTest-Dateien mit Grundstruktur, die in Wave 1 mit echten Tests befuellt werden.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-critical-bug-fixes/13-RESEARCH.md

@InsightFlowTests/AccountManagerTests.swift (Pattern-Referenz fuer Setup/Teardown)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test-Stubs WebsiteDetailViewModelTests + DashboardViewModelTests erstellen</name>
  <files>InsightFlowTests/WebsiteDetailViewModelTests.swift, InsightFlowTests/DashboardViewModelTests.swift</files>
  <read_first>
    - InsightFlowTests/AccountManagerTests.swift (Pattern: Import, @MainActor, setUp/tearDown Struktur)
  </read_first>
  <action>
    Zwei neue XCTest-Dateien erstellen die dem bestehenden Pattern folgen.

    1. `InsightFlowTests/WebsiteDetailViewModelTests.swift`:

    ```swift
    import XCTest
    @testable import InsightFlow

    @MainActor
    class WebsiteDetailViewModelTests: XCTestCase {

        // MARK: - FIX-02: Task Cancellation

        // Tests werden in 13-02 Plan implementiert
    }
    ```

    2. `InsightFlowTests/DashboardViewModelTests.swift`:

    ```swift
    import XCTest
    @testable import InsightFlow

    @MainActor
    class DashboardViewModelTests: XCTestCase {

        // MARK: - FIX-04: Account-Wechsel Loading State

        // Tests werden in 13-01 Plan implementiert
    }
    ```

    3. Beide Dateien zum Xcode-Projekt hinzufuegen:
    Pruefe ob das Projekt ein InsightFlowTests-Target hat und fuege die Dateien ueber `ruby` oder manuelles Einfuegen in die pbxproj hinzu. Alternativ: `xcodebuild` erkennt Swift-Dateien im Test-Verzeichnis automatisch wenn sie im Target-Verzeichnis liegen und das Target einen Glob verwendet.

    WICHTIG: Pruefe zuerst mit `grep -r "WebsiteDetailViewModelTests" InsightFlow.xcodeproj/` ob Xcode die Dateien automatisch erkennt. Falls nicht, muessen sie manuell in die .pbxproj eingetragen werden — nutze dafuer das bestehende Pattern von AccountManagerTests.swift als Vorlage.
  </action>
  <verify>
    <automated>xcodebuild build-for-testing -project InsightFlow.xcodeproj -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -15</automated>
  </verify>
  <done>Beide Test-Stubs existieren, kompilieren im Test-Target, und sind bereit fuer TDD RED-Phase in Plans 13-01 und 13-02</done>
</task>

</tasks>

<verification>
1. Dateien existieren: `ls InsightFlowTests/WebsiteDetailViewModelTests.swift InsightFlowTests/DashboardViewModelTests.swift`
2. Build-for-testing erfolgreich: `xcodebuild build-for-testing -scheme InsightFlow -destination 'platform=iOS Simulator,name=iPhone 16'`
3. Test-Target erkennt Dateien: `xcodebuild test -scheme InsightFlow -only-testing:InsightFlowTests/WebsiteDetailViewModelTests -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -10`
</verification>

<success_criteria>
- WebsiteDetailViewModelTests.swift und DashboardViewModelTests.swift existieren in InsightFlowTests/
- Beide Dateien kompilieren im InsightFlowTests Target
- Nyquist Wave 0 ist erfuellt — alle Plans in Wave 1 haben existierende Testdateien
</success_criteria>

<output>
After completion, create `.planning/phases/13-critical-bug-fixes/13-00-SUMMARY.md`
</output>
